const JournalRegister=({terms,list,filtered,totals,viewMode,query,setQuery,status,setStatus,journalType,setJournalType,fromDate,setFromDate,toDate,setToDate,creator,setCreator,onCreate,onDetail,onEdit,onAction,onImport})=>{const exportRegister=()=>{const rows=[['Voucher number','Date','Journal type','Source / reference','Debit total','Credit total','Status','Created by'],...filtered.map(j=>{const t=totals(j);return [j.number,j.date,typeLabel(j.type,viewMode),j.reference||'',t.debit/100,t.credit/100,j.status,j.createdBy]})];downloadReport(csvReport(rows),'wayvida-journals.csv','text/csv;charset=utf-8')};const creators=['All creators',...new Set(list.map(j=>j.createdBy).filter(Boolean))];const activeFilterCount=[status!=='All statuses',journalType!=='All',creator!=='All creators',!!fromDate,!!toDate].filter(Boolean).length;const menuRun=handler=>event=>{event.currentTarget.closest('details')?.removeAttribute('open');handler()};return <section className="je-page"><div className="je-heading registerHead"><div className="registerHeadText"><h2>{terms.journalEntries} <span className="je-heading-count">({list.length})</span></h2><p>{viewMode==='business'?JOURNAL_VIEW_LABELS.business.subtitle:'Manage manual entries. Daily transactions are recorded automatically.'}</p></div><div className="je-tools"><label><IconSearch size={17}/><input value={query} onChange={e=>setQuery(e.target.value)} placeholder="Search voucher, source or referenceâ€¦"/></label><details className="je-filters"><summary aria-label="Open filters"><IconFilter size={17}/>Filters{activeFilterCount>0&&<span>{activeFilterCount}</span>}</summary><div><label>Status<select aria-label="Journal status" value={status} onChange={e=>setStatus(e.target.value)}>{['All statuses','Draft','Pending Approval','Approved','Rejected','Posted','Reversed'].map(x=><option key={x}>{x}</option>)}</select></label><label>{viewMode==='business'?'Entry purpose':'Journal type'}<select value={journalType} onChange={e=>setJournalType(e.target.value)}>{purposeFilters(viewMode).map(([value,label])=><option key={value} value={value}>{label}</option>)}</select></label><label>Created by<select value={creator} onChange={e=>setCreator(e.target.value)}>{creators.map(value=><option key={value}>{value}</option>)}</select></label><label>Date range<select aria-label="Filter by date range" value={matchDateRange(fromDate,toDate)} onChange={e=>{const range=resolveDateRange(e.target.value);setFromDate(range.from);setToDate(range.to)}}>{DATE_RANGE_PRESETS.map(([value,label])=><option key={value} value={value}>{label}</option>)}{matchDateRange(fromDate,toDate)==='custom'&&<option value="custom">Custom range</option>}</select></label><label>From date<input type="date" value={fromDate} onChange={e=>setFromDate(e.target.value)}/></label><label>To date<input type="date" value={toDate} onChange={e=>setToDate(e.target.value)}/></label><button type="button" onClick={()=>{setStatus('All statuses');setJournalType('All');setCreator('All creators');setFromDate('');setToDate('')}}>Clear filters</button></div></details></div><div className="je-create-callout"><div className="registerSplit"><button type="button" className="primary registerSplitMain" onClick={onCreate}><IconPlus size={17}/>{viewMode==='business'?'Create Adjustment':'Create manual journal'}</button><details className="registerSplitMore"><summary aria-label="More journal actions" title="More journal actions"><IconChevronDown size={16}/></summary><div><button type="button" onClick={menuRun(onImport)}><IconUpload size={16}/>Import</button><button type="button" onClick={exportRegister}><IconDownload size={16}/>Export</button></div></details></div></div></div><div className="je-card unified"><div className="je-table-wrap"><table><thead><tr>{['Voucher number','Date',viewMode==='business'?'Entry purpose':'Journal type','Source / reference','Debit total','Credit total','Status','Created by','Actions'].map(x=><th key={x}>{x}</th>)}</tr></thead><tbody>{filtered.map(j=>{const t=totals(j);return <tr key={j.id}><td><button className="je-link" onClick={()=>onDetail(j)}>{j.number}</button></td><td>{fmtDate(j.date)}</td><td>{typeLabel(j.type,viewMode)}</td><td>{j.reference||'â€”'}</td><td>{money(t.debit)}</td><td>{money(t.credit)}</td><td><Status value={viewMode==='business'?businessStatus(j.status):j.status}/></td><td>{j.createdBy}</td><td><div className="je-row-actions"><details><summary aria-label={'More actions for '+j.number}><IconDots size={17}/></summary><div>{canEditJournal(j)&&<button type="button" onClick={menuRun(()=>onEdit(j))}><IconEdit size={16}/>Edit</button>}<button type="button" disabled={j.status!=='Pending Approval'} onClick={menuRun(()=>onAction(j,'Approved'))}><IconCheck size={16}/>Approve</button><button type="button" disabled={j.status!=='Pending Approval'} onClick={menuRun(()=>onAction(j,'Rejected'))}><IconTrash size={16}/>Reject</button><button type="button" disabled={j.status!=='Approved'} onClick={menuRun(()=>onAction(j,'Posted'))}><IconSend size={16}/>Post</button><button type="button" disabled={j.status!=='Posted'} onClick={menuRun(()=>onAction(j,'Reverse'))}><IconRefresh size={16}/>Reverse entry</button><button type="button" onClick={menuRun(()=>onAction(j,'Duplicate'))}><IconCopy size={16}/>Duplicate</button>{j.status==='Draft'&&<button type="button" onClick={menuRun(()=>onAction(j,'Delete'))}><IconTrash size={16}/>Delete draft</button>}{isReversalJournal(j)&&<button type="button" onClick={menuRun(()=>onAction(j,'Delete'))}><IconTrash size={16}/>Delete reversal</button>}</div></details></div></td></tr>})}{!filtered.length&&<tr className="emptyStateRow"><td colSpan={9} className="emptyStateCell"><EmptyState variant="journal" title="No journals found" description="Adjust the filters or create a manual journal." actionLabel="Create Journal" onAction={onCreate}/></td></tr>}</tbody></table></div></div></section>};
