# Prototype Instructions

## AI knowledge transfer

Start with `docs/README.md` for the verified project knowledge base, `docs/WAYVIDA-BOOKS-HANDBOOK.md` for a standalone transfer document, `docs/AI-HANDOFF-PROMPT.md` for a reusable bootstrap prompt, and `docs/project-manifest.json` for the machine-readable inventory. Source and tests remain the authority when prose differs.

### Mandatory feature checkpoint

Every completed feature change must update the knowledge-transfer package before the work is considered complete:

1. Update the relevant modular document in `docs/` with actual behavior, status, dependencies, storage, constraints, and source references.
2. Update `docs/project-manifest.json` when modules, navigation, components, services, persistence keys, tests, dependencies, constraints, or status changed.
3. Update `docs/WAYVIDA-BOOKS-HANDBOOK.md` when essential product, architecture, accounting, security, developer, or operational knowledge changed.
4. Record actual validation and unresolved failures in `docs/TESTING-AND-QA.md`; never claim unexecuted coverage.
5. Refresh `Last verified` in every materially changed knowledge document.
6. Run `npm run docs:check`, relevant tests, and the production build before reporting completion.

### Permanent cross-AI handover rule

This repository must remain transferable to ChatGPT, another Codex account, Gemini, Lovable, Google AI Studio, Antigravity, Claude, or another engineering assistant without access to prior conversation history.

For **every material project change**—including UI, terminology, workflow, domain behavior, accounting rules, persistence, navigation, tests, dependencies, architecture, configuration, known limitations, or accepted product decisions—the same change must also:

1. Update `docs/TRANSFER-TO-ANOTHER-AI.md` when the current checkpoint, continuation instructions, required files, or known limitations changed.
2. Update the relevant modular document in `docs/` and `docs/WAYVIDA-BOOKS-HANDBOOK.md` when the knowledge is essential to safe continuation.
3. Update `docs/project-manifest.json` whenever components, services, modules, storage keys, commands, dependencies, tests, constraints, or implementation status changed.
4. Record the exact validation performed and any unresolved failure in `docs/TESTING-AND-QA.md`.
5. Update `docs/AI-HANDOFF-PROMPT.md` when a new invariant, mandatory reading item, or continuation warning is introduced.
6. Preserve concise decision context: what changed, why, affected files/contracts, compatibility impact, and what remains unfinished.

Do **not** copy raw chat transcripts into the repository. Do not record credentials, API keys, tokens, private browser data, personal information, or unrelated machine paths. Summarize durable requirements and decisions in provider-neutral language. Source code and tests remain authoritative when documentation and prose disagree.

A task is not complete until its implementation, tests, and transfer documentation describe the same current state.

Documentation-only wording fixes need not rewrite unrelated documents. A feature is incomplete while its documentation status or interfaces are stale.

Payment Receipts (renamed from Customer Receipts on 2026-09-21; the route stays Payments Received) are independent financial transactions under Sales. Do not auto-create them on invoice creation. Keep draft/approval/posting, separate allocations, advance liability and immutable reversal history. Preserve legacy payments without reposting; all report balances must include native receipt allocations. The local approval-role selector is simulation only, never authenticated security.

Receipts, Invoices, Credit Notes and Sales Orders share customer-facing print previews with customizable templates. Open previews only on request. Keep template preferences separate from transaction data; never recalculate amounts or alter posting when changing layouts. Printouts retain lifecycle status and distinguish sales orders and unposted documents from posted transactions.

Document previews use a polished, original layout inspired by the supplied invoice PDF: clear document-type identity, structured business/customer metadata, numbered items, totals in figures and Indian amount words, notes and signature. Do not copy sample business/customer data or its withholding adjustment into actual transactions.

Open the customer document only on an explicit Preview action, not automatically after saving. The preview toolbar contains Print, Share, Customize and Close. Customize opens a right-side drawer with template selection and collapsible branding/footer sections; keep it closed initially. Show the supplied Wayvida logo in preview/print by default, preserving custom uploaded logos. Share currently sends a text summary, not a PDF file.

Keep document preview embedded in the dashboard content area. Preserve the visible, usable app sidebar and header; do not render the preview as a fullscreen body portal or lock dashboard navigation. Hide dashboard chrome only for printing.

Day Book lives under Reports and is strictly read-only. Use existing Posted journals for official totals; keep unposted documents separate. Dimension filters retain complete vouchers. Never invent missing company, approval or audit metadata.

Product branding: Wayvida Books. Use the supplied logo at public/wayvida-logo.png in the application brand area.

Typography preference: use Instrument Sans throughout the prototype. All visible text must be at least 12px, including helper text, table data, chart labels, and mobile controls.

Chart of Accounts uses one persistent master with Business View by default and an Accounting View. Keep guided creation, account codes and existing journal references compatible. Business labels must retain Owner’s Funds (Equity) and distinguish income/expenses from cash flow. Never convert old sample balances into real opening journals. Account creation itself must not post journals. Create Account and Edit Account pick the Account Group with the same searchable combobox the Journal lines account cell uses (`src/AccountGroupPicker.jsx` over `src/account-group-search.js`); do not regress that field to a native select, and keep the `type::purpose` key it writes compatible with `applyGroup`. The drawer wraps every field in a `label`, so a picker mounted there must never nest a label inside its own popup and must cancel the default action of clicks inside its root, or a click on an option row re-fires the trigger button through the label and reopens the list.
The Chart of Accounts page scrolls as one page: the account-grid wrapper is plain (`overflow:visible`, no `max-height`), so `.app>main` is the only scroller and the account list grows to its full height. The row header (`div.am-toolbar.am-filter-toolbar`, `position:sticky;top:0;z-index:9`) pins to that scroll port and slides away while the reader scrolls down, returning on the first upward scroll or at the top, so the sticky cells offset by the measured bar height (`--am-coa-bar-h`, published by `AccountWorkspace`, 65px fallback) plus `--am-coa-head-h:40px`: `thead th` is `sticky;top:var(--am-coa-bar-h);z-index:6`, dropping to `top:0` under `.am-coa-page.am-coa-bar-hidden`. The grid is ONE FLAT TABLE since 2026-09-21: the account-type accordion, `AccountGroup`, `openGroups` / `openList` / `toggleGroup` and every `.am-group-row` / `.am-group-toggle` rule are deleted, the body is `typeGroups.flatMap(group=>group.rows.map(row=>({...row,type:group.name})))`, the type that used to head a section is the **Account Type** column between the code and the group, and every row ends with `.am-row-actions`: a visible `button.am-row-edit` (36px, `1px solid #d0d5dd`, 7px radius) beside `details.am-more`, whose duplicate `Edit Account` entry is gone. Do not restore the accordion, its three-section cap or its chip and count-pill styling, and do not put the type help back on a query for `.am-group-toggle` - it lives on `.am-account-type` as `title` and `aria-description` now. The account name is the register link (`.am-tree-name>button:last-child`, `border:0`, transparent, `#1849a9` 13px/600), keeping its sub-account chevron beside it. Below 700px the wrapper returns `overflow-x:auto` and the sticky cells go static, because the table is wider than the viewport there. Keep the pinned rows below the row action menu (`z-index:80`), keep the Import / Export overflow menu (`.am-coa-more`) at `z-index:80`, and keep the sticky cells opaque fills and the inset shadows that stand in for the collapsed border they lose. Never restore the wrapper `max-height` or `overscroll-behavior:contain`.
The account details of the Chart of Accounts page open in a right-side popup rather than a second column beside the grid. Clicking an account, or arriving with the `wayvida-open-account` sessionStorage handoff, renders `div.am-create-drawer-layer.am-detail-layer` holding `button.am-create-drawer-backdrop` and a full-height `aside.am-create-drawer.am-detail-drawer` (`role="dialog"`, `aria-modal="true"`, `aria-label="Account details"`, `width:min(560px,100vw)`, header title `Account details` with `{account.name} · {account.code}`, `am-detail-body` as the only scroll container, `am-detail-footer` pinned), and the backdrop, the header close button and `Escape` all clear the selection while the body scroll lock mirrors `src/EnterpriseAccountForm.jsx`. Do not restore the inline panel: keep `am-layout` unmodified and keep the two-column `.am-layout.has-detail` rules, the `.am-detail{position:sticky;top:82px}` container and their media-query overrides deleted, while `.am-detail-top` stays in `src/account-workspace.css` because the Day Book transaction detail header still uses it.
`Budgets` is one flat destination in its own sidebar section in `src/Navigation.jsx` (`{label:'Other',children:[leaf('Budgets'),leaf('Period Lock'),leaf('Inventory Adjustments')]}`), which since 2026-09-25 is a NESTED group inside the `Settings` section rather than a section of its own after Accounting - Accounting still holds only `leaf('Chart of Accounts')` and `leaf('Journal','Journal Entries')`, and the top-level `Items` leaf now follows it (`{...leaf('Items'),icon:IconPackage}`), with the old `Inventory` group deleted - decided 2026-09-24 on the user instruction "Budget and Period Lock make a separate section module and remove from Accounting"; Period Lock moved with it: the Budget sub-group and the separate Budget Reports and Budget Settings screens are deleted, so never restore them - the `BudgetReports` / `BudgetSettings` components, their two `page` routes in `src/BudgetWorkspace.jsx`, their now-unused `IconFilter` / `BUDGET_PERMISSIONS` / `emptyBudgetState` imports and the `.budgetReportFilters` / `.budgetReportGrid` / `.budgetReportCard` / `.budgetReportTitle` / `.budgetVarianceColumns` / `.budgetPerformance` / `.budgetProgress` / `.budgetPerformanceMeta` / `.budgetSettingsGrid` / `.budgetPermissionGrid` / `.budgetSwitch` rules in `src/budget-workspace.css` are gone, the plan Overview alert hint now reads `Alerts follow the saved alert thresholds.` because it no longer has a Settings screen to point at, and `src/App.jsx` routes the page with `a==='Budgets'` alone since the two extra page names can never be active. Keep the `Budgets` page name unchanged so the routing list, the welcome-block exclusion and the `BudgetWorkspace` remount key stay valid. The sidebar leaf is the only budget navigation: `src/BudgetWorkspace.jsx` must not render a `BudgetTabs` / `nav.budgetWorkspaceTabs` section row on the register, and must not thread `page` / `onNavigate` into `BudgetList`; do not reintroduce that tab strip, its `BUDGET_TABS` label list or its styling in `src/budget-workspace.css`. Inside the Budgets destination the register is the shared row-action table pattern: columns `Budget Name`, `Financial Year`, `Budget Period` and `Actions`, where every row's `Actions` cell is one read-only `View Details` button beside one `details.budgetKebab` 3-dot menu holding `View budget` first, then `Edit`, `Duplicate` and the destructive `Delete` (class `budgetKebabDanger`). Keep that kebab matched to the canonical Period Lock / Journal Entries / Chart of Accounts geometry (32px square trigger, `1px solid #d9e1ec`, 7px radius, open `border-color:#3478f6;color:#245fd9`, menu `right:0;top:calc(100% + 6px)`, `min-width:196px`, `padding:6px`, `1px solid #e1e6ed`, 9px radius, `min-height:36px` rows), keep `.budgetListCard{overflow:visible}` with the first and last header-cell radii so the card never clips the menu, and keep exactly one page-level `pointerdown` / `Escape` dismissal effect in `BudgetList`, closing `.budgetKebab[open]` and `.budgetFiltersMore[open]` together. The register heading and its toolbar are one merged row at the top of the register card, exactly as on the Journal Entries and Vendors registers: `div.budgetV3Head.registerHead` is sticky, opaque and bordered, carries the `10px 10px 0 0` top radius, and holds `div.registerHeadText` (the title with its `span.budgetHeadingCount` and the description, on the Journal Entries 24px / 12px sizes), then `div.budgetRegisterBar` holding the search box (`label.budgetFilterSearch`) directly beside one `details.budgetFiltersMore` Filters disclosure (`summary` labelled `Open filters` carrying the 17px `IconFilter` funnel, `div.budgetFiltersPanel` holding Financial year, Budget type, Budget period, Scope and Status, a `Clear filters` row and one count badge off the single `activeFilterCount` memo), then `div.budgetHeadActions`. `table.budgetTable` follows in `section.budgetListCard`, which gives up its top border and top corners and takes `0 0 10px 10px`, so the heading row and the table are one card. Never put `select.budgetFilterYear` back on the row - it is a panel filter now, and `src/budget-workspace.css` scopes its row sizing to `.budgetRegisterBar>.budgetFilterYear` so the panel field keeps its own height. Copy the register chrome from the Period Lock register (`.pc-filters`, `.pc-filter-search`, `.pc-filters-more`, `.pc-filters-panel`) instead of inventing new values. Since 2026-09-21 the register screen also sits on the SAME content container as the Journal Entries and Chart of Accounts pages: `.app main>.budgetV3:not(.budgetWizard):not(.budgetDetailV3)` is `max-width:100%;margin:0;padding:0`, so the shell main padding is the only gutter and the card starts at the same x, the same width and the same top as those two pages (measured at 1440: page, head and card all at x=302, w=1091, y=127, seam 0), while the wizard and the detail keep the `padding:14px 18px 28px` their full-bleed heads are built on. Its grid reuses the Chart of Accounts cell tokens rather than the Period Lock ones: a `12px 14px` header on `#f7f9fc` in `12px/600 #5b6b81`, `11px 14px` body cells at 13px with `border-top:1px solid #edf1f6` and a `#f8fbff` row hover, which puts the header row at the same 42px height as that grid; keep the row hover and the right-aligned last column (`.budgetActionsCell{white-space:nowrap;text-align:right}` with `.budgetActionsCell .budgetRowActions{justify-content:flex-end}`), and keep one split button in `div.budgetHeadActions` beside the row: `div.registerSplit` holds `button.primary.registerSplitMain` labelled `Create Budget` beside `details.registerSplitMore`, whose menu holds `Import` (the hidden `input[type=file]`, which sets `setImportPayload(parsed)` so the parsed file actually reaches the wizard it opens) and `Export` (the filtered register as CSV through `budgetListCSV`), each wrapped in `menuRun` so the menu closes as it runs, with `.registerSplitMore[open]` in the page-level dismissal selector. The register renders no status card row: `section.budgetMetrics` and its `.budgetMetrics` / `.metric` rules are deleted, so the page heading leads straight into the register card, and the `budgetMetrics(...)` export in `src/budget-store.js` is engine-only. Never put Total Budgets, Active Budgets, Total Planned Income, Total Planned Expense or Expected Profit back above the card. Never restore the full-width filter row inside the card, the deleted `budgetRegisterHead` / `budgetFilterMore` / `budgetSearch` classes, or the header-cell radius workaround the toolbar row made unnecessary. `View Details` reopens the existing budget detail on its `Budget vs Actual` tab through the `detailTab` state, the `initialTab` prop and the `key={budget.id+'/'+detailTab}` remount. Do not restore the Budget Type, Scope, Total Amount, Status, Created By or Last Updated columns or the six icon-only row buttons; export and archive stay on the budget detail header, and no storage key, status workflow or budget calculation changes. Create Budget is one three-step page, `BudgetWizard` in `src/BudgetWorkspace.jsx`: `Budget Details`, `Account Selection`, `Budget Allocation`. Its head is the page header, exactly like the Create Journal Entry page: `div.budgetV3Head.budgetWizardHead` is a full-bleed white bar spanning the whole content area, flush under the application header, and carries one square 40px `button.budgetHeadBack` (`1px solid #d7e0eb`, 8px radius, hover `#eef4ff` / `#bcd0f5` / `#2463d4`, `aria-label="Back to budgets"`, `IconArrowLeft` at 19px) beside `div.budgetHeadText`, which holds a 16px/650 `span.budgetHeadTitle` (`Create Budget`, or `Edit Budget` with a `span.budgetHeadHint` naming the budget); there is no descriptive subtitle and no step chip. Reach that width the journal way instead of pinning a pixel width: `.app main:has(>.budgetV3.budgetWizard)` gives up main's padding and `max-width`, `.app main>.budgetV3.budgetWizard` gives up the page padding, the head rule sets `width:100%`, `max-width:none`, `min-height:56px`, a `0 0 16px` margin so the step card clears the bar, `padding:8px 24px`, `border-radius:0` with both side borders removed and no shadow, and `.app main>.budgetV3.budgetWizard>:not(.budgetV3Head)` hands the `clamp(16px,2.2vw,32px)` gutter back to every other child - so never reintroduce a fixed 190px tile, a `.companySelect`-matched width, or a card border and radius on that head. The `Step <n> of 3` chip is not in that head: it lives in the stepper card, where `nav.budgetSteps` is a `minmax(0,1fr) auto` grid whose second column is `span.budgetStepCount` (`justify-self:end`, a neutral `#eef2f7` pill). The three steps render as a numbered progress stepper - `nav.budgetSteps > ol` with one `li` per step - not an underline tab row: the current step is `class="active"` with `aria-current="step"`, a finished step is `class="complete"` and shows `IconCheck` in place of its number, a later step is `class="upcoming"` and `disabled` so it cannot be jumped to, the 2px `li:not(:last-child)::after` connector states its own progress - `var(--budget-blue)` behind a `li.complete` step, a half `linear-gradient(90deg,var(--budget-blue) 0,var(--budget-blue) 50%,#e1e6ed 50%,#e1e6ed 100%)` behind the `li.active` step so the step in progress reads as half travelled, and the neutral `#e8edf3` behind a `li.upcoming` step - and clicking an earlier step still walks back. No step name may be cut: `li` is `flex:1 1 170px`, the connector paints behind every label (`li:not(:last-child)::after` at `z-index:0`, each `button` at `z-index:1` with a `#fff` background and `padding:0 12px 0 0`), and `button span` is nowrap with no `text-overflow:ellipsis` and no `overflow:hidden`, while the sub-760px stacked form returns to `white-space:normal`. Never restore the underline styling, the `button.budgetBack` text back link on either budget screen (both use the arrow-led `button.budgetHeadBack`), the `Set the budget scope, choose the accounts and plan the amounts.` subtitle, or the tab-era rules `.budgetSteps{overflow:auto}` and `.budgetSteps button{min-width:155px}`. The wizard footer `footer.budgetWizardActions` is one static right-aligned row - `justify-content:flex-end` with no `margin-right:auto` split - so `Cancel` / `Back` sit immediately left of the one primary action and are never pushed across the page. On that step the read-only `div.budgetDuration` prints the dates the chosen `Financial year` and `Budget period` imply and nothing else: `p.budgetDurationRange` reads `01 Apr 2026 → 31 Mar 2027` and `p.budgetDurationMeta` reads `12 months · Monthly`, both from the pure `budgetDateRange()` / `budgetDurationLabel()` in `src/budget-store.js`, with `p.budgetDurationHint` saying `Dates are based on the selected financial year.`, so a standard period renders no date field at all. The `Budget period` select offers exactly the five `BUDGET_PERIODS` values `Monthly`, `Quarterly`, `Half-Yearly`, `Yearly` and `Custom`, and only `Custom` renders the two `input type="date"` fields (bounded by `min` / `max` from `financialYearRange()`, written to `form.startDate` / `form.endDate`) plus the inline `p.budgetDurationError` message from `budgetDateError()` - `Custom budget dates must fall within FY 2026-27.` - which `next()` refuses to advance past. The details step is one three-column field grid (`div.budgetSetupFields` at `grid-template-columns:repeat(3,minmax(0,1fr))`), and exactly five fields: row one leads with the budget name and gives the other two columns to the `budgetScopeRow`, so the Organisation and Branch pickers sit beside the name, and row two holds the financial year, the budget period and the read-only `div.budgetDuration` in that order. Never reintroduce a wrapper of its own around the duration and the scope, never move the duration ahead of the year and the period, and never restore a Description field here: `div.budgetSetupFields.budgetSetupNote` and the `budgetFieldWide` span rule are deleted, while `form.description` still seeds from `initial.description||''` so editing a stored budget never drops the description it already holds (the detail panel shows it when it is set). Keep the duration a quiet `#f7f9fc` read-only field with no chevron, keep its `min-height:40px` / `8px 10px` padding / 7px radius / `#d9e1eb` border matched to the two pickers beside it with its value styled through `.budgetSetupFields .budgetDurationRange`, keep the two custom date fields stacked in the duration column with `min-width:0` / `width:100%` so a narrow column can never squeeze them, let the scope row and the duration each take the whole width once the grid folds to two columns below 1100px with the two custom dates side by side there, and make every field a single column below 700px. The step carries no visible `Budget scope` label (`span.budgetScopeLabel` and its rule are deleted), and it states its width once - as the `span.budgetScopeSummary` chip in the `budgetSelectHead` - because the second `p.budgetScopeHint` line (`Planning for <organisation> · <branch>.`, or the empty-state sentence) and its rules are deleted; the `scope` value is still built from `scopePickerState()` and still travels with `save({...form,scope})`, so never drop the value, only the duplicate line. `budgetTiming()` is the one place that resolves the year, the period and the range, so `createBudget` and `updateBudget` can never store dates that disagree with the stored period - which is also why the wizard seeds `startDate` / `endDate` back from `initial` when editing - and `periodLabels(period,financialYear)` reads a Yearly plan as the financial year itself and a Custom plan as `Custom Period` rather than a second `Total` column beside the Total column. Its `budgetSelectHead` states the plan width as one `span.budgetScopeSummary` chip built by `scopeSummary(form)` (for example `2 Organisations · 3 Branches`), so the scope is readable without opening the picker; never drop the dates, the note or the chip, and never print raw organisation or branch ids there. The `Budget type` field is removed from that step, so `form.type` keeps its `'Profit & Loss Budget'` default for the stored budget only - the account step no longer filters by it and lists every active posting account. The scope row renders the shared `OrganisationBranchScope` in its `multiple` checkbox-list form over the scope universe `getScopeOrganisations()`, never over the narrowed working context: a plan may cover any organisation the header Change / Customize drawer can offer, so the control must never be left with nothing to choose - a one-organisation, one-branch working context used to hide both selectors and leave the scope unpickable. Read the scope from `scopePickerState()`, open it seeded from the working context through the one `wizardScopeSeed(initial)` helper (a saved budget keeps the `companyIds` / `branchIds` stored on its `scope`), keep those refs on the form and on the stored scope so editing reopens the same scope, re-read the universe when `wayvida-working-context-change`, `wayvida-organization-change` or `storage` fires and prune selections with `normaliseScope()`, render the multi-select option rows from the pure `scopePickerRows()` / `toggleScopePicker()` in the same module instead of rebuilding the lists in the page, keep `.budgetWizard .budgetSetup{overflow:visible}` so the open checkbox list is not clipped by the card the way the journal lines card may not clip the account combobox, and never reintroduce `BUDGET_SCOPES`, a hardcoded branch list or a manual scope-type select on that page, because the register scope filter still reads `BUDGET_SCOPES` - and a `budgetTemplateSwitch` (`input[type=checkbox][role=switch]`) that reveals `budgetTemplateCards` only while it is on and resets the template to `Custom Budget` when switched off. Step two renders `AccountSelectGrid`, one `.budgetAccountGrid` two-column table of every active posting account in the Chart of Accounts rather than only the accounts of one budget type, grouped under its accounting type in the shared `ACCOUNT_TYPE_ORDER` order from `src/terminology.jsx` (Assets, Liabilities, Equity, Income, Expenses, then an `Other` bucket for an unknown type), each section row named by `accountTypeLabel(type,business)` and carrying its count (`n of m selected`, or `m accounts`), and one labelled checkbox per account in a `budgetAccountCell` that prints the account name with its code - and its account group when it has one - beneath it, so the step reads as the Chart of Accounts itself; the separate Account Type column is gone, so never re-narrow this step through `BUDGET_TYPES` / `TYPES[form.type]` (`BUDGET_TYPES` stays only for the register Budget type filter), and it keeps no account-type tabs. The step's `budgetSelectionTools` row is search plus a `select.budgetTypeFilter` built from `ACCOUNT_TYPE_ORDER` and narrowed to the types actually on screen, then `Select all` and `Clear all` beside a `span.budgetSelectedCount` reading `n accounts selected`, and every accounting type row is a `button.budgetGroupCell` carrying `aria-expanded` that folds that type's accounts away so the list stays short; keep that filter wired into the same `visible` list the search narrows, and never rename the clear control back to `Clear selection`. Every cell in that grid stays a real table cell - never set a `display` other than `table-cell` on a `td`/`th`; put the flex row in the `button.budgetGroupCell` or the account `label` instead, because a flex `td` leaves the table layout algorithm and collapses to the 56px choose column, which is what printed the type name one letter per line - and the grid keeps its accessibility contract: an `aria-label` on the table, `th scope="col"` headers, a `th scope="colgroup" colSpan={2}` for each accounting type, an 18px checkbox with a `:focus-visible` ring, a `tbody tr:focus-within` tint, and each account cell a `label htmlFor` bound to that checkbox's `budget-account-<code>` id; The detail screen's `Accounts` tab renders `BudgetAccountsTable`, a filtered table of Account, Account Type, Budget, Actual, Variance and Variance % over the same `computeVarianceRows` rows; `AccountTree` is deleted, so never restore it, or a second tree renderer, for that tab. Step three allocates the chosen accounts over the allocation grid - the accounts grouped under `BUDGET_ALLOCATION_BANDS` (Profit & Loss first, then Balance Sheet), one sticky `td.budgetStickyCol` account column and a foldable `tr.budgetAllocGroup` per accounting type - whose shortcut row is Copy Previous Year, Distribute Equally, Use Average Spending, a `label.budgetGrowthField` Growth % input ahead of Apply Growth %, a `label.budgetImportButton` hiding an `input type=file` that reads a CSV through `parseBudgetCSV`, and a `button.budgetClearAll`; it closes with a `section.budgetTotals` statement-totals block: a `div.budgetTotalsGrid` table sharing the allocation grid period columns plus one row total, over the fixed statement lines `Total Revenue`, `Total Expenses`, `Net Profit / Loss`, `Profit Margin`, `Total Assets`, `Total Liabilities`, `Total Equity` and `Total Liabilities & Equity`. Every figure is computed through the shared `src/budget-statements.js` layer, which `BudgetWizard` calls as `budgetTotalRows(form.accounts,form.allocations,form.period)`, so a total can never drift from the allocations it summarises, and none of it is stored: the Profit and Loss line and the Liabilities & Equity line carry the same figure because the planned result rolls into equity, exactly as the standard budget sheets show. Keep the derived lines on `#f1f5fb`, the balance check on the 5% amber tint (`#fff6ec` / `#8a4b09`) while the sides disagree and on `#f1faf5` / `#19734c` once they agree, each with its one-line sentence, a negative figure on `#b42318`, every figure at 12px or larger, the table inside `div.budgetTotalsGrid` so a narrow viewport scrolls the totals rather than the page, and the whole block inside `section.budgetAllocation` so it is never a new direct child of `.budgetV3.budgetWizard`; never put the four deleted `budgetReviewTotals` chips (Total income budget, Total expense budget, Expected profit, Profit margin) back beside the grid, because the totals section is the only statement summary. Step three saves with `Create Budget` (`Save Changes` when editing); do not restore the separate `Review Budget` step, the `budgetPurposeCards` type grid, `Save Draft` or `Submit for Approval` there. The approval lifecycle is advanced from `BudgetDetail` through one mode-aware status action (`Submit for approval`, `Approve budget`, `Activate budget`, `Mark complete`) wired to `setBudgetStatus` and the engine `FLOW`, and an edit must keep the stored status instead of resetting it to `Draft`. The budget detail screen is the statement view and `src/budget-statements.js` is the shared statement layer that feeds it: `BUDGET_STATEMENTS` are `Profit and Loss`, `Balance Sheet` and `Cash Flow Statement`, `budgetStatementRows(statement,accounts,allocations,period)` returns the ordered `{kind,key,label,values,total,className,note,account}` rows whose `kind` is `section|type|group|account|groupTotal|typeTotal|grand|check`, it groups the planned accounts under their chart of accounts group (`account.group||account.type`) inside each accounting type and closes every group, every type and the statement itself with its own total row, and `budgetTotalRows` / `budgetPeriodTotals` / `budgetStatementRows` / `budgetBalanceCheck` are the only four places the planned amounts are ever totalled - `BudgetWizard` and `BudgetStatementView` both call into this module, so never reintroduce a page-private `planned` / `periodSums` / `sumValues` / `marginOf` / `totalsRows` chain in `src/BudgetWorkspace.jsx` and never restore the deleted `AllocationTable` or the flat `Budget Details` tab it rendered. The Profit and Loss block reads Revenue then Expenses and closes on `Net Profit / Loss`; the Balance Sheet reads Assets, Liabilities and Equity, closes on `Total Liabilities & Equity` and then prints its `kind:'check'` row `Balance Difference`; the Cash Flow reads Operating, Investing and Financing and closes on `Net Cash Flow`, signing an expense or an asset purchase negative because it moves cash out. The detail screen is one flat tab row defined as `const BUDGET_DETAIL_TABS=[['Overview','Overview'],['Profit and Loss','Profit & Loss'],['Balance Sheet','Balance Sheet'],['Cash Flow Statement','Cash Flow'],['Budget vs Actual','Budget vs Actual'],['Accounts','Accounts'],['Activity History','Activity History'],['Revisions','Revisions']]` with `const BUDGET_DETAIL_DEFAULT='Overview'`, which the `detailTab` state and the detail `back()` reset both use, so the detail opens on the overview, every statement stays one click away, and the register `View Details` button still opens `Budget vs Actual`; render that row from the pairs with `BUDGET_DETAIL_TABS.map(([key,label])=>...)` in a single `.budgetDetailV3>.budgetAccountTabs` run of eight tabs (never a boxed two-row grid, never a nested grouping) and never restore the older `[...BUDGET_STATEMENTS,'Forecast',...]` list or the first-statement default. `BudgetStatementView` renders those rows into the existing `div.budgetAllocationGrid` shell - `section` / `type` / `group` rows as full-width `th scope="rowgroup" colSpan={labels+2}` bands, account rows printing the name over its code in the accounting view, total rows as `th scope="row"` labels beside right-aligned figures, and the `budgetEmpty` sentence instead of a broken table when nothing is planned - styled by the appended `.budgetStatement` block in `src/budget-workspace.css` (bands `#f4f7fc` / `#fbfcfe` / `#536176`, totals `#f8fafd`, derived `#f1f5fb`, the `check` row `#fff6ec` on `#8a4b09`, negative `#b42318`, 12px floor). `View budget` is the register kebab's first entry, ahead of `Edit`, `Duplicate` and the destructive `Delete`, and opens the detail on the overview. `budgetBalanceCheck(accounts,allocations,period)` returns `{assets,liabilities,equity,profit,liabilitiesAndEquity,difference,balanced}` and is the one accounting relationship a plan is held to: the allocation step closes with a `section.budgetBalanceCheck` carrying a `balanced` / `unbalanced` state class that prints Total Assets, Total Liabilities + Equity and Balance Difference and reads `Budget is balanced` or `Budget requires adjustment` (naming whichever side is larger), and the `kind:'check'` statement row reads the same difference so the two screens can never disagree. That difference is reported only - never post it to a budget account, never store it, never give it a text input, and never bring the deleted `Budget Mismatch Adjustment` budget line or the `.budgetTotalsGrid tr.budgetTotalsMismatch` rules back. The `Overview` tab is the first block of the detail body and owns the four plan figures (`Revenue Budget`, `Expense Budget`, `Expected Profit / Loss`, `Budget Utilization`) inside `div.budgetSummaryCards`, followed by the read-only `BudgetDetailsPanel`, the `budgetPanel budgetHealth` bars and the `budgetPanel budgetAlertsPanel`; the plan figures are not in the page head. The detail page head is the full-bleed arrow-led back head the Create Budget page uses: `div.budgetV3Head.budgetDetailHead` holds one square `button.budgetHeadBack` (40px, `1px solid #d7e0eb`, 8px radius, `aria-label="Back to budgets"`), the `Budget Details` `span.budgetHeadTitle` and a `Back to budgets` `span.budgetHeadHint`, sits flush beneath the working-context organisation and branch bar at the header width through `.app main:has(>.budgetV3.budgetDetailV3){max-width:none;padding:0}` plus the `:not(.budgetV3Head)` `clamp(16px,2.2vw,32px)` gutter, and never renders the old inline `button.budgetBack` text link, whose rule is deleted. Below it `div.budgetDetailIdentityRow` pairs the name with its lifecycle badge in `div.budgetDetailName` above the `span.budgetDetailMeta` line (`financialYear · period · scopeSummary · type`) and right-aligns `div.budgetDetailActions`: one `button.primary` carrying the next allowed status from `statusAction`, then one `details.budgetDetailMore` More actions disclosure whose `summary.budgetDetailMoreBtn` is a labelled 38px control with the horizontal `IconDots` and a rotating caret, holding Edit, Duplicate, Export, Archive and the destructive Delete (`class="budgetKebabDanger"`, wired to the detail screen's `remove` prop, which archives the plan and returns to the register). The menu closes through `menuRun(...)` and the detail screen's own pointerdown / Escape effect over `.budgetDetailMore[open]`. Keep the status beside the name rather than inside an action button, never restore the separate Edit / Duplicate / Export / Archive button row, and let no stray text node sit between the identity row and the tab row. `Budget vs Actual` prints Account, Budget, Actual, Variance, Variance % and Achievement % and takes its colour from `varianceDirection(type,variance)` in `src/budget-store.js` (income: a positive variance is `favourable`; expenses: a negative variance is `favourable`; balance-sheet types and zero are `neutral`), so a lower actual expense reads green and a higher one reads red, and the mixed `<tfoot>` total is `neutral` rather than pretending to a direction.

Account creation has five steps: category, basic details, automatic setup, optional collapsed advanced settings, impact preview. Only name and description belong in basic details. Keep advanced GST/TDS, dimensions, control settings and opening posting accessible in Accounting View; a local view preference must not be represented as authenticated permission enforcement.

Sales order and invoice creation use the same inline page/card form style. Item rows use an item selector without a separate editable description field; retain the item-name snapshot in stored documents.

Invoice creation hides Income Account (mapped automatically), uses searchable item selection, and a single Tax control exposing CGST, SGST, IGST and Cess. Keep compatible tax components validated against place of supply.

Invoice details use the standard document-detail hierarchy: a separate Back to Invoices breadcrumb, document identity and lifecycle badge, one visually dominant next action, compact secondary actions, four summary metrics, accessible tabs, and a balanced information-plus-amount overview. Preserve all invoice fields, items, payments, accounting entries and audit data while avoiding large empty regions or competing button emphasis.

Sales orders reuse the Invoice creation fields and components, but must never post accounting. Generate a linked invoice draft with the order data; accounting starts only on invoice posting.

Credit Notes share the existing tax/journal ledger. Issuing reduces net customer receivables once; application only allocates that credit to invoices. Never delete posted credit history: cancellation reverses journals and voids applications. Inventory postings require a real inventory ledger, not the item master.

Manual Journal Entry creation keeps the posting gate on the posted values alone: balanced totals inside an open accounting period. Do not require an approved status before `Post journal` on that page, and do not restore the removed Cancel button, the `Approve this journal before posting.` hint or the three-dot More actions menu there; keep the footer to the save action plus `Post journal`. A Draft journal is submitted for approval from the journal detail screen (`Submit for approval` then `Approve` then `Post`), and Duplicate and Reverse entry stay on the register rows and the detail screen. Keep the balance status (`Debit equals credit`, or `Difference: <amount>`) stacked directly beneath the Total Debit and Total Credit amounts in the same right-aligned footer row. The Journal lines Organisation and Branch columns are conditional on the working context: hide Organisation when only one organisation is in scope, hide Branch as well when that organisation has a single branch, keep Branch only for one organisation with several branches, and keep both for several organisations. Filter the Branch dropdown to the selected organisation, reset a line's branch when its organisation changes, and state the implicit choice in the Journal lines subtitle rather than adding a second selector.

Organisation and branch selection is one control, not one per page. Render `OrganisationBranchScope` from `src/OrganisationBranchScope.jsx`, whose every decision comes from the pure `scopePickerState()` helper in `src/organisation-scope.js`; do not hand-roll another organisation/branch picker or copy its visibility or filtering logic into a page. The available organisation list is read through `src/organisation-context.js` (`getAccessibleOrganizations()`), shared by Create Journal Entry and Period Lock - never re-add a private `readOrgList` / `getAccessibleOrganizations` copy. That module owns both lists and the one `scopedOrganisations(companyKey,branchKey,companyFallback,branchFallback)` derivation behind them, and neither list may be re-derived in a page: `getAccessibleOrganizations()` is the narrowed working context the journal lines and Period Lock stay on, and `getScopeOrganisations()` is the universe a deliberate scope is chosen from - the same list the header Change / Customize drawer offers, which now reads it from there too instead of deriving it inline - so Create Budget plans over `getScopeOrganisations()` while Journal Lines keep the working context. Never add a third reader, a second universe or a page-local copy of either. The control's `multiple` checkbox-list form is opt in - only Create Budget asks for it, while the Create Journal Entry journal lines and the Period Lock lock drawer keep their one-choice selects - and both forms read the same rows: `scopePickerRows(state)` builds the `{organisationRows,branchRows}` option lists (each led by an `All organisations` / `All branches` row with ref `''`, ticked while nothing is chosen) and `toggleScopePicker(state,row)` returns the next `{companyIds,branchIds}`, so never add a `multi` flag to `getScopeVisibility()` or `scopePickerState()` and never hand-roll those lists in a page. The Period Lock Create Lock / Close drawer uses the picker with selectors; a period lock may cover a whole organisation, so the Branch list keeps `All branches` as its first option and a whole-organisation lock is never silently narrowed to one branch. The request unlock drawer renders the same control with `showFields={false}` because `requestPeriodUnlock` derives a request's scope from the locked period; do not add a selectable branch there without also adding an enforcement rule for temporary unlocks.

The Period Lock register row is merged, exactly like the Journal Entries, Chart of Accounts and Budgets registers: `div.pc-register-bar.registerHead` is the top of the register card (sticky, white, `padding:12px 16px`, the `10px 10px 0 0` radius and a hairline beneath it, with `section.pc-panel` giving up its top border and taking `0 0 10px 10px`), and it carries `div.pc-register-heading.pc-title-block.registerHeadText` (the `title` and `note` the page passes as props, rendered as an `h2` at the Journal Entries 24px/700 and its 12px note), then the attention pill and the split action. The grid controls are NOT on that row: `div.pc-register-tabs` sits between the toolbar row and the table heading row (`padding:10px 16px` and a `#eef1f6` bottom rule in `src/period-closing.css`) and holds the `Locked Periods` / `Requests` switch on the left and the search box directly beside the `Filters` disclosure on the right (`margin-left:auto`), so the switch reads as the grid sub-navigation and its filters stay beside it. The switch keeps its own `{onView&&showRequests&&…}` guard INSIDE the row, so it disappears with the approval policy while the search and Filters remain. The page no longer renders `header.pc-heading`: `PeriodClosing` passes `title={t.periodClosing}` (never a hardcoded title), `note="Lock accounting periods and control when postings stop."`, `actions` and `merged` to `LockRegister`, so the heading and the toolbar are one row. The `.pc-focused>.pc-heading` rules in `src/period-closing-focus.css` are dead; do not restore that element. The `Before you close` attention pill stays first in that action group (hidden while every check passes). Do not put the read-only locking policy tag back beside the title: the policy is read inside Period Lock Settings. `Before you close` is a compact, read-only attention pill (`details.pc-attention-pop` > `summary.pc-attention-pill`) in the heading action group: an amber warning icon plus the count as text (`3 checks`, never a bare number), an accessible `Review <n> checks that need attention` label, no trailing chevron, and one computed tone (neutral grey for one or two soft checks, the 5% orange tint for three or more, red when a check actually blocks the close). It is never dismissible - it disappears only when every check passes, and `All checks passed` prints inside the popover, never on the page. Clicking it opens `div.pc-attention-pop-menu`, read-only: the `Before you close` head with `<n> checks need attention · <m> entries affected`, then the failing checks only, then `Review all`, which opens the same pre-close review drawer that `panel==='before'` renders, whose body runs from the entries-affected count and the category chips straight into the checklist. One `attention` memo drives the pill, its tone, the popover summary and the count so they can never disagree, the count region stays `aria-live="polite"`, and the pill carries no badge. The heading row is sticky inside the `.pc` page scroller and the popover is a `details`, so the count stays visible while the register scrolls and the row returns its height when it closes. Do not restore the full-width strip, its duplicated drawer heading, the `The system verifies the following before <period> can be closed.` sentence or the `pc-ready-pill` readiness badge. The register toolbar opens with the `Locked Periods` / `Requests` toggle on the left; do not print a `Locked periods` / `Unlock requests` heading or an `Every lock for <organisation> in the selected financial year.` sentence there (no caller passes a title or note any more, so that heading block is not rendered anywhere), and let the `Period` select offer only the periods the register can show. What the lock list may show follows the locking mode through `lockRegisterRows`: automatic locking lists only locks whose period has already ended, manual locking lists only periods whose lock was created here (an audit entry with `Lock period created` or `Period closed`), and locking off hides nothing. The filtered register is the only lock list, because the lock history drawer and its `Audit log / History` menu entry were removed. Its toolbar is one compact row (`div.pc-register-bar`): the toggle on the left and, on the right, the search box and one `Filters` disclosure, with Period and Status inside that `Filters` panel ahead of Financial Year, Organisation, Branch, Closed By and Closed Type, so the visible row consumes as little space as possible. The current accounting period opens from the first More actions entry as a 520px right-side popup (`pc-current-drawer`) that renders the real `CurrentPeriodPanel`, not as a page card. Unlock requests are decided from the `Requests` half of the register, and the table never states a status. A pending request carries the same two controls as every other row in its last cell: one read-only `View` button (`button.pc-request-view`, opening the request details popup) and then that row's 3-dot menu, whose `Approve` renders only for a decidable pending request and whose `Cancel` stays requester-only. `Reject` is gone, so an unlock request is approved or withdrawn - `decideUnlockRequest` still accepts `'Rejected'` for stored data, but no control produces it. The register renders every filtered request in one pass: there is no pager, no `REQUEST_PAGE_SIZE` and no page state, and the Branch filter applies to the requests half exactly as it does to the lock list. Because the acting role is the first role the policy allows (`allowedRoles[0]`, `Admin` by default) and it is also the role that raises requests, a request it raised itself is decided as the next authorised approver resolved through `unlockApproverFor(requestedBy, settings)` - and the row never restates that approver, because the action group is the whole Actions cell - while `Another approver must review your own request.` stays only for the case where the policy leaves no other approver. Do not restore the page subtitle, the composite period/scope line, the locked-periods status line, the inline missing-periods row, the Create missing periods action, the Activity History block in the current-period card, the Transactions / Audit log / Close with review link row, a second close button, the lock history drawer or its `Audit log / History` entry, or the inline read-only locking policy tag beside the title. `Create Lock / Close` is mode aware: manual locking asks for a period, a date and an optional reason and locks every module, while automatic locking renders the saved schedule inputs (Frequency, Effective from, Lock after period end, Lock at, Notify before locking) and resolves the next unlocked period through `autoTarget()`. Period Lock takes its heading label from the shared terminology map (`t.periodClosing`), so the accountant view reads `Period Lock` and the business owner view reads `Financial Lock`; never reintroduce a hardcoded `Close Books` title. The action group ends in one split button, exactly like the Journal Entries and Budgets registers: `div.registerSplit` holds `button.primary.registerSplitMain` labelled `Create Lock / Close` beside `details.registerSplitMore`, whose 38px caret opens one menu holding `Current period` (`menuRun(openCurrent)`, `title={currentMeta}`), `Period lock settings` (`menuRun(openSettings)`) and `Export` (`menuRun(onExport)`, the filtered register through `exportLockRegister` and the Day Book CSV writer). The old standalone `More actions` disclosure is gone: never restore `details.pc-more`, its `.pc-more*` rules or its two entries as a second control, and keep `.registerSplitMore[open]` in the page-level dismissal selector beside `.pc-kebab[open]`, `.pc-filters-more[open]` and `.pc-attention-pop[open]`. The left-over `.pc-more` rules in `src/period-closing.css` are dead and may be deleted when someone next touches that sheet; `.pc-more-badge` is still the filter badge and must stay. Period Lock Settings keeps a one-line lead and one headingless settings card holding exactly two switch rows: `Enable automatic locking` (only ever `Automatic` <-> `Manual`) and `Require approval to unlock`, separated by a `pls-rule` divider, around the five schedule fields and the read-only generated schedule. Each switch row is `label.pls-switch` with a visually hidden `input[type=checkbox][role=switch]` and an `<i>` toggle track. Do not restore the `Period locking` card, the separate `Unlock approval` card, the collapsed `More policy settings` disclosure, the unlock-duration or scope fields, the allowed-roles list or the enforcement-coverage list: those values keep their engine defaults, and `lockingMode: 'Off'` stays writable only from Settings -> Accounting -> Period control, which is why a stored `Off` renders the master switch disabled with a note instead of a second off switch here. Keep `lockingMode` the single switch for the whole product: Settings -> Accounting -> Period control maps `Off` <-> `Manual` on that stored value and `Automatic period closing` maps `Automatic` <-> `Manual`, both through `normalisePeriodSettings`/`writePeriodSettings`, and its Closing frequency and Close after days edit `schedule.frequency` and `schedule.lockAfterDays`. Never drive the derived `autoLockPreviousMonth` mirror directly; switching automatic locking off means manual locking. Period Lock drawers are portalled into `document.body`. Wrap each one in `<div className="pc pc-portal-scope">` and give it explicit right-side geometry rather than relying on inherited shell rules. The Period Locking Settings drawer is the exception: keep it outside the `.pc` wrapper so the page base-control rules cannot reach the panel, and keep the `.pls header` reset in `period-lock-settings.css` because `.pls-card>header` otherwise inherits the fixed 58px application top bar and the card titles overlap: scrim `z-index:1390`, drawer `z-index:1400` with `position:fixed` and `right:0`, `width:min(<n>px,96vw)`, a fixed header and footer, and `pc-drawer-body` as the only scroll container. Keep one gutter per nesting level, keep `.pc-heading` on the Journal Entries heading grid, keep the `.pc-more-menu` dropdown anchored inside `.pc-more` and the `.pc-filters-panel` dropdown anchored inside `.pc-filters-more`, with the heading grid collapsing to one column below 1000px, and keep card header resets so the legacy fixed top-bar `header` rule cannot leak into nested cards. Never place a page control row inside a `<header>` unless you neutralise the app-bar element rules in `src/styles.css` (`header{position:fixed;height:58px}`, `header label{height:38px;background:#f6f8fb;padding:0 12px}`, `header>button{height:38px}`, and below 1050px `header>button:not(.primary):not(.hamb){display:none}`); that is why the register toolbar is a `div.pc-register-bar`, and why every drawer and modal close button needs an explicit `display:inline-flex!important` or it disappears on viewports narrower than 1050px. The Requests half of the locked-periods register is table-only: the columns Period, one combined Organisation & Branch cell (the organisation name with its branch scope beneath it, resolved from the period the request belongs to), Reason, Requested, Requested By, Status and Actions, rendered in the shared `pc-table-fit` / `pc-request-table` shell, so the register never scrolls sideways and the row menu is never clipped, with the whole action set in the last cell - one read-only `View` button and one `details.pc-kebab.pc-request-kebab` 3-dot menu holding `Approve`, `Cancel`, `View Details` and the destructive `Delete` (class `pc-kebab-danger`, offered to the requester or an approver and wired to the engine's `deleteUnlockRequest`), where both View entries open the `RequestDetailsDrawer` single-request popup (`aria-label="Unlock request details"`, `pc-request-facts` details list, `width:min(460px,96vw)`). Never restore the `Cards` / `List` switch or its `div.pc-request-view-bar`, the `article.pc-request-card` rendering, the `pc-request-none` placeholder, a `Reject` action, a pager or a status chip inside the Actions cell. The Status column keeps `pc-badge` (`Requested` while pending, then `Approved`, `Cancelled` or `Rejected`), because the status is a register fact rather than a row action.
Row action menus are one shared control. The Period Lock kebab (`details.pc-kebab` > `summary` holding a horizontal `IconDots`, plus `.pc-kebab-menu`) is the canonical 3-dot row action, and the Journal Entries register (`.je-row-actions`) and the Chart of Accounts table (`.am-table .am-more`) must keep the same geometry: a 32px square trigger with a `1px solid #d9e1ec` border, a 7px radius, a `#fff` background and a `#475467` icon; an open state of `border-color:#3478f6;color:#245fd9`; and a right-aligned menu anchored at `top:calc(100% + 6px)` with `min-width:196px`, `padding:6px`, a `1px solid #e1e6ed` border, a 9px radius and `box-shadow:0 12px 30px #1018281f`, whose rows are `min-height:36px` with `gap:9px`, `padding:0 10px`, a 6px radius, a transparent background, `#344054` labels and a `#f4f7ff` / `#245fd9` hover. Keep the horizontal `IconDots` at 17px there, keep every entry a `type="button"`, close the menu through `menuRun(handler)` when an action runs, and keep one page-level `pointerdown` / `Escape` dismissal effect in `JournalEntriesPro` and `AccountWorkspace` beside the Period Lock one. Never reintroduce `IconDotsVertical` or the old caret arrow, and do not restyle the Chart of Accounts header Templates menu or the destructive `je-edit-journal` / `am-danger` treatments.

The header account chip owns the display-vocabulary switch. `details.profileMenu` in `src/App.jsx` opens a panel listing `Business owner` (Admin) and `Head of Accountant` (Staff) with `aria-pressed`, calling the existing `setMode` from `src/terminology.jsx`; do not re-add a `View As:` control to the header action row or inject one from `src/Navigation.jsx`, and keep `TERMINOLOGY`, routes and accounting untouched. Create and Edit Account stay a right-side drawer (`src/EnterpriseAccountForm.jsx` with `src/account-create-drawer.css`) that carries its own header and close button, not a centred card and not a full page.

Run the local server yourself and open the preview in the browser available to this environment. Do not give the user server-start instructions when you can run it.

Before making substantial visual changes, use the Product Design plugin's `get-context` skill when the visual source is unclear or no longer matches the current goal. When the user gives durable prototype-specific design feedback, preferences, or decisions, record them in `AGENTS.md`.

When implementing from a selected generated mock, treat that image as the source of truth for layout, component anatomy, density, spacing, color, typography, visible content, and hierarchy.

Build app UI in `src/`. Keep `.openai/hosting.json`, `worker/index.js`, `scripts/prepare-sites-build.mjs`, and `tests/sites-worker.test.mjs` intact so the same local prototype can be handed to Sites. Before a Sites handoff, run `npm run build` and `npm run test:sites`; the build must leave `dist/client/index.html`, `dist/server/index.js`, and `dist/.openai/hosting.json`.

Local Vite defaults to port 4001. `npm run dev` and `npm run preview` set `server.port` / `preview.port` to 4001 with `strictPort` in `vite.config.mjs` and the matching npm scripts, so they fail instead of moving to 5173. Keep `allowedHosts: true` on both the dev and preview servers so a VM IP or hostname is not blocked (do not restore `allowedHosts: ["terminal.local"]`). VM hosting uses Docker Compose on host port 4002. `docker compose up --build -d` (or `npm run docker:up`) builds the Vite client and serves `dist/client` from nginx on host port 4002, overridable with `WAYVIDA_PORT`. nginx must 404 missing `/assets/` files instead of rewriting them to `index.html`, or the browser refuses the module and the UI stays blank. Keep `Dockerfile`, `docker-compose.yml`, `docker/nginx.conf`, and `.dockerignore`. This is static prototype hosting only: browser `localStorage` stays in the client, and the container is not a production accounting backend. Do not replace the Sites worker path with this image.
The Sales invoices register is one merged register row, exactly as Vendors, Journal Entries, Budgets, Period Lock, Items, Inventory Adjustments, Customers and Sales Orders are: `src/InvoiceWorkspace.jsx` renders `div.ivHeading.registerHead` holding `div.registerHeadText` (the `Sales invoices` title and the unchanged description), then `div.ivTools`, then `div.ivActions`, and `div.ivCard.ivRegisterCard` opens AFTER that row and holds only `div.ivScroll` and `table.ivInvoiceTable` - the toolbar must not be moved back inside the card, because it was inside it before and the row never merged until it left. The card gives up its top border and top corners for `0 0 10px 10px` while the row takes the card border and the `10px 10px 0 0` radius, so there is no seam. The Status select is a field of `div.soFiltersPanel` now, not a toolbar control: it keeps its `INVOICE_STATUSES` options and its accessible label, the panel `Clear filters` row calls `clearInvoiceFilters()` and `setFilter("")` together, and `activeInvoiceFilters` counts it with `+(filter?1:0)`, so the badge and the reset always cover the same ground. The add action is the shared split button over the page `create()` - `button.primary.registerSplitMain` beside `details.registerSplitMore` - and its chevron menu holds `Account mapping` (`setConfig({...db.config})`) alone - `Use demo example`, the register `Export` and the `exportRegister` helper with its `csvReport` / `downloadReport` import were removed on request, so do not restore them; the demo example is still reachable by choosing the customer on the create page, and the register itself is unchanged. Never add an Import to that menu either, because the invoice engine has no import contract and a second write path would bypass the create form validation the page depends on. Keep the shared `heading(title, sub, actions)` helper in that file: Payments received, General Ledger, Trial Balance and Financial Statements still render through it, and only the `Sales invoices` call was rewritten. Two rules in `src/register-head.css` are load-bearing for this shell: `.registerHead.ivHeading .registerHeadText h2` / `p` restate the Journal Entries 24px/700 `#172033` title and 12px `#667085` description, because the invoice and vendor shells otherwise take a 23px `.invoiceWorkspace h2`, and `@media(max-width:760px){.registerHead:has(.soFiltersMore[open]),.registerHead:has(.customerFilters[open]){position:static}}` gives the row up while one of those filter panels opens in the flow below the narrow fold - without it an open panel made the sticky row taller than the viewport it pins to, which measured 602px on Vendors at 540x800. The same sheet keeps the funnel itself unshrinkable, through `.registerHead details:has(>summary[aria-label="Open filters"]){flex:0 0 auto}` and `.registerHead summary[aria-label="Open filters"]>svg{flex:0 0 auto;width:17px;height:17px}`: every register renders the one `IconFilter`, but flex shrinking reached the glyph inside the disclosure and left it 8.5px wide on the invoices register and 5.3px on the sales orders register against 17px on Customers, Journal Entries and Chart of Accounts, which reads as a different icon. `src/sales-orders.css` `.soFiltersMore{position:relative;flex:none;margin-left:auto}` is the matching root-cause fix - it was the only filter disclosure in the app without `flex:none`. `tests/invoice-detail-ui.test.mjs` pins the merged row, the split menu, the export wiring and the panel move; the migrated set is now Vendors, Journal Entries, Budgets, Period Lock, Items, Inventory Adjustments, Customers, Sales Orders, Invoices, Credit Notes and Payment Receipts, with Purchase Orders, Goods Receipts, Purchase Bills, Debit Notes, Payments Made and Purchase Reports still to come. The same module now carries the global table-page scroll behaviour, so no page writes its own scroll logic: it hides the merged row once the reader passes HIDE_AFTER=48 and the row own height, returns it on the first upward scroll or at the very top, and clears the hidden state on focusin so a page action reached by keyboard is never left off screen. It marks the page grid data-sticky-head - the first table in the scroll port with a thead that is not inside a form, dialog, disclosure, detail sheet or document preview - and src/register-head.css pins [data-sticky-head] thead th at top:var(--register-head-h,0px) with z-index:6, an inset 0 -1px 0 #e6ecf6 stand-in for the collapsed border and a .22s top transition, while the module republishes --register-head-h as 0px once the row has slid away so the column heading takes its place. Never declare a fill in that rule: every shell paints its th opaquely, and a sticky cell that is not opaque shows the rows passing under it. Because a sticky element positions against its nearest scrolling ancestor, the module releases a scroll-container wrapper to overflow:visible through stickyHeadWrap, but only while the table fits it horizontally and re-decided on resize - the sales orders table is min-width:920px, so it pins its heading at 1440 and keeps its sideways scrolling at 900 and 760. Keep the Chart of Accounts page own --am-coa-bar-h rules, because they are more specific and already behind its tested invariant. tests/sticky-table-head.test.mjs pins the whole contract. The register heading also states the register count, in the parentheses Journal Entries, Items, Budgets and Period Lock already use: `<h2>Name <span className="registerHeadCount">({n})</span></h2>`, styled once at `.registerHead .registerHeadText .registerHeadCount` on the title tokens (24px/700 #172033) so it reads as part of the title. Customers, Sales orders, Sales invoices, Credit Notes and Payment Receipts all open that way now; count the register and not the rows on screen, so the heading does not move while the reader filters, keep the numbers unpadded, and never put a record count back in a register toolbar. tests/register-heading-count.test.mjs pins the convention. The register date filters are shared too: src/date-range-filter.js exports DATE_RANGE_PRESETS (All dates, Today, This week, This month, This year), resolveDateRange(key,today) and matchDateRange(from,to,today), and Sales Orders, Invoices, Credit Notes, Payment Receipts and Journal Entries each render one label>select aria-label="Filter by date range" before their own date fields. The two dates stay the only filter state - a range simply writes them - so no page filtering, count badge or Clear filters row changed, and the control value is derived from matchDateRange so it names a period only while the two dates are exactly that period, with a Custom range entry for anything else. Each range is the whole calendar period, not the part of it that has happened, and weeks run Monday to Sunday. Payment Receipts had no date filtering before this: its from and to live in RECEIPT_FILTER_DEFAULTS, the register filters on them, and its badge requires a truthy value before its startsWith('All ') test or the two empty dates count as active.

Sales document create pages are ONE STACKED COLUMN, top to bottom, with no second column anywhere. Create Sales Order and the New sales invoice page both render the shared `src/SalesDocumentFields.jsx`, whose blocks are the Order/Invoice details card, the Customer card, the lines card and the Billing summary card, in that order, each the full width of the page.

The details card leads and its header row is FOUR fields on one row (`.soFieldRow4`): the document ID, the document date, the Reference number and the Payment terms; the scope pair (`.soFieldRow2`) holds Organisation and Branch on the row beneath. The user asked for exactly this order on 2026-09-25. The ID field is editable and states the number the save will take as its PLACEHOLDER (`placeholder={autoNumber||"Automatic"}`), fed by `autoNumber`: the host pages pass `nextSalesOrderNumber(orders)` from `src/sales-order-service.js` and `nextInvoiceNumber(db.invoices)` from `src/invoice-engine.js`, which are the same functions `saveSalesOrder` and `command("save")` now call, so the shown number and the assigned number can never be two implementations. Do not put the number in `value` for a new document: a value taken at form-open can collide with another operator, where a placeholder cannot.

Every field on the page is the Record Transaction control the journal create page uses: `function Field({label,icon:Icon,hint,children})` renders `label.soField` over `span.soControl`, with an 18px `stroke={1.8}` icon inside the control and the input or select transparent within it. The marks are IconHash for the document ID, IconCalendar for the date, IconFileText for the reference, IconClock for the payment terms, IconBuildingBank for the organisation, IconSitemap for the branch, IconUser for the customer and IconMapPin for the place of supply. The tokens are copied into `src/sales-orders.css` under `.soCreatePage` rather than borrowed from `record-transaction.css`, so each page sheet stays self-contained.

The customer field is a PLAIN SEARCH BOX, not the shared `SearchSelect` combobox: `CustomerSearch` in the same file owns an input with `role="combobox"`, a `soCustomerResults` listbox under it, ArrowUp/ArrowDown/Enter/Escape and an outside-pointer dismissal, and it filters the customer list by name, code, phone and GSTIN. Two rules keep it usable, both learned the hard way on 2026-09-25: **focus must clear the query and re-open the list** (`onFocus={()=>{setOpen(true);setQuery("");setActive(0)}}`), and **choosing must release the caret** (`inputRef.current.blur()` in `pick`). Without both, the box keeps the committed name on screen with the input still focused, so the next search types appends to it instead of starting over. The root is a `div`, never a `label`, because an option row inside a label re-fires the field it labels.

The chosen customer is read in a profile card: `div.soCustomerProfile` is a `52px` profile column beside the facts, `span.soCustomerAvatar` carries the customer initials from `customerInitials()`, and `dl.soCustomerFacts` is TWO columns with the billing address spanning a full row through `.soCustomerFactWide{grid-column:1/-1}` and Phone and GSTIN side by side beneath it. The address prints through `addressLines(which)` as separate lines - street, city and state, PIN and country - because the document already stores it structured and one flattened string is not an address. The rest of the master data stays behind `details.soCustomerMore` (View customer details), closed by default.

Place of supply is NOT on the details card any more: it is rendered by the two host pages inside their Additional details block, through the exported `PlaceOfSupplyField` from `src/SalesDocumentFields.jsx`, which recomputes its own source line and reset. `details.soMoreCard` therefore carries `open={!form.place}` on both pages, so a required field is never stranded inside a closed disclosure where the browser cannot focus it to report an error. The shipment, delivery and salesperson fields were REMOVED from the create page at the user request on 2026-09-25; the Sales Orders register still exposes an Expected shipment date filter over `order.shipment`, which nothing can now set - a known leftover, remove it or restore the field rather than leaving both.

The Billing summary is the last card. `section.ivCard.soBillingSummary` holds `div.soSummaryHead` with the heading, then `div.soSummaryMoney` - a right-aligned column whose FIRST child is `span.soCurrencyChip` reading `INR ₹` and whose second is the totals panel - and then `p.soPostingNote`. The chip sits directly above the Subtotal row at the user request, not beside the heading. The lines card heading carries `span.soItemsCount` with the live line count. Above the whole page, the shared `.soCreatePage .soControl` tokens, the `.soFieldRow4`/`.soFieldRow2` folds and the customer search rules live at the end of `src/sales-orders.css`.

The sales line grid is SIX columns with SEVEN cells: Item, Quantity, Rate ₹ (with `small.ivPriceType` under it), Discount, Tax, Amount and the remove action. There is no Unit column any more - 2026-09-25 moved the unit inside the quantity cell, `div.ivQtyCell` (flex, `gap:6px`, the count at `flex:1 1 0;min-width:32px`, the unit at `flex:0 1 auto` as a 12px/600 `#6b7789` read-out on an order and `input.soUnitInput{flex:0 1 52px}` on an invoice) - so the header array is exactly `["Item","Quantity","Rate ₹","Discount","Tax","Amount",""]`. Column widths are declared once on `.soCreatePage .ivLineTable th:nth-child(n)`: 26/13/12/15/20/10 per cent for the six data columns and `width:46px` for the seventh, because a percentage action column narrower than its own padding clips the 32px remove button. The cell padding is `.soCreatePage .ivLineTable th,.soCreatePage .ivLineTable td{padding:10px 8px!important;height:auto!important;overflow:hidden}` and the `!important` is load-bearing: `src/ui-system.css` ends with a global table block that pins every cell to `padding:16px 20px !important;height:64px`, which cost this grid 40px a cell and truncated the tax and amount columns. Any density change to a line table has to out-rank that block, and any change to the column count has to restate every `nth-child` width.

`+ Add item` is `details.soAddItem` > `summary.soAddItemMain` (the one primary action on the section, `#3478f6`) > `div.soAddItemMenu`, and since 2026-09-25 it offers exactly TWO entries: **Add item row** - `addLine()`, which appends one `blankLine()` whose single item box searches the master and names an ad hoc line - and, only when the host page passes `onCreateItem`, **Create new item**. The separate `Add an ad hoc item` entry and the `addLine(shape)` argument are gone: a line never has to be declared a different kind to be named, because the item box itself offers the typed text as an ad hoc item. `input.soAdHocName` is deleted, and an ad-hoc line still prints `small.soAdHocNote` and is refused by `calculate()` until a price type is chosen, which is that function existing rule and not a new one. Create new item sets `ITEM_CREATE_KEY` (`wayvida-open-item-create`, exported by `src/quick-create.js`) plus `wayvida-return-to` and navigates to Items, and `src/Items.jsx` reads that key once on mount and clears it before calling `start()`, so the create form opens on exactly the arrival that asked for it; both host pages stash the draft in `DRAFT_KEY` first, as the customer-master detour already did. The menu closes through `closeAddItemMenus()` from the same component effect that closes `.soFactEdit`, on an outside pointer and on Escape, so `.soAddItem[open]` must stay in that effect pointerdown selector.

Additional details is one labelled block per job: `section.soMoreGroup` > `h4.soMoreGroupTitle` (12px/650, `#344054`, uppercase, `.04em`) over Shipment, Notes & terms, History (only for a saved document) and Documents, divided by `.soMoreGroup+.soMoreGroup{padding-top:16px;border-top:1px solid #eef1f6}` rather than full-width rules. The line tax states ONE rate: the `TaxSelection` collapsed summary in `src/InvoiceLineControls.jsx` is `GST <cgst+sgst+igst>%` with `Cess <n>%` appended when there is one, or `No tax`, while the CGST/SGST/IGST/Cess breakdown stays inside the menu where each field is labelled as it is entered. Do not remove the component fields from the form or from the stored line.

The invoice engine enforces duty at the command boundary, exactly as the journal and receipt modules do. `command(state, action, payload, ctx)` takes a context: when `ctx.role` is supplied, `invoiceAllowed(role, action)` gates it (Sales Executive and Accountant may save/submit/send; Finance Manager and Admin may also post, cancel and set a recurring schedule), **posting requires `status === "Pending Approval"`**, and the invoice must carry its own `organizationId` and `branchId` to be saved or posted. The app always passes `{role}` from the View as switch (`InvoiceWorkspace` derives it from `useTerminology`). The role-less call deliberately keeps the engine's earlier contract for tests and importers - it is a compatibility shim, never an authentication boundary; do not let a page call `command` without a role. The Review action buttons follow the same table: "Approve & post" renders only for a Pending Approval invoice whose viewer may post, and `p.ivPermissionNote` names who can when they may not, so a missing action is never silent.

An invoice is dated when it is raised, never when the order was taken. `convertSalesOrder(state, order, ctx)` sets `date = today()` and `dueDate = today + order.terms`, because dating an invoice back to the order puts revenue in the wrong period and a period that has since closed would block the posting later. It also refuses anything but a **Confirmed** order - invoicing a draft would turn an unapproved quantity and price into a legal document - and it forwards `ctx` to `command`.

The customer tax identity is FROZEN on the document: `customerGstin`, `customerGstTreatment` and `customerState` are written by `applyCustomerSnapshot` and `chooseCustomer` in `src/SalesDocumentFields.jsx`, the create page reports the frozen GSTIN (`documentGstin`) ahead of the live master and names the difference when they disagree, and `src/DocumentPreview.jsx` already printed `GSTIN: {partyGstin}` from `doc.customerGstin` - so before this the printed invoice carried no recipient GSTIN at all. Editing a customer must never rewrite the tax identity of an invoice that has already been issued.

The Sales Orders register no longer offers a shipment filter, the create page no longer records `shipment`/`delivery`/`salesperson`, and the new-order form no longer seeds them; do not put any of them back without also restoring the fields. Cancelling an order is blocked while it has a linked invoice (Delete already was), converting or completing it requires **Confirmed**, and `trail(order, action, from)` records the acting role plus `fromStatus`/`toStatus` rather than the order's creator.

The General Ledger and its Trial Balance read the real journal store and nothing else: the fabricated sample ledger and the hardcoded sample account list were deleted on 2026-09-25, so an empty store renders the component's own empty state, and the Branch and Cost-centre filter options are derived from the loaded rows. A report an accountant reads must never invent balances. Real journal lines carry a branch **id**, which is why the old hardcoded `["All Branches","Kochi","Bangalore"]` list matched nothing.

Still open from the 2026-09-25 sales audit, with the reason: inventory relief and cost of sales on a sale (stock only ever rises, and no COGS is posted, so the Balance Sheet and Profit & Loss are incomplete for goods); separate Output CGST/SGST/IGST/Cess accounts, which are still one account because `settings-store.js` maps all four components to a single `outputGst`; reverse charge and sales-side TDS/TCS; a draft that can be saved incomplete (`calculate` still runs on save); a delivery/fulfilment document and stock reservation; returning an ad hoc line to stock; a per-organisation GST registration; field-level audit change tracking; and the meaning of a completed order, which is a status only.

The chosen customer reads in ONE compact strip, never a stacked card. `div.soCustomerProfile` on both create pages is `display:flex;align-items:center;gap:14px;max-height:80px;margin-top:0;overflow:hidden` - a 44px `span.soCustomerAvatar`, then `div.soCustomerLines` holding two lines: `<strong class="soCustomerName">` and `<span class="soCustomerAddress">`. It was a two-row grid until 2026-09-25 and made the Customer card about 486px tall for three short facts, which pushed the Order items card off the first screen; after the actions row joined the strip row on 2026-09-25 the strip measures 66px and the whole Customer card 178px at 1440px. Keep the 80px ceiling. The `margin-top` must stay `0`: `div.soCustomerRef` is a grid since 2026-09-25 and a grid does not collapse a first child top margin the way the old block flow did, so leaving the old 10px pushed the strip below the pen and the eye it shares its row with.

The address is built by `addressLine(which)` in `src/SalesDocumentFields.jsx`, which prints each part once: a stored address can repeat itself - the customer seed ends up with `city:"India"` beside `country:"India"` - and on a single line the repeat reads as a mistake in the address. The full value is kept in the `title` because the line ellipsises. `.soCustomerAddress` is deliberately `color:#475467;font-size:12.5px;font-weight:400`, with `b{font-weight:600;color:#344054}` on its `Billing Address:` label, so the address reads as a caption under the 14px/650 `.soCustomerName` instead of printing at the customer own weight - that weight contrast IS the hierarchy, so do not raise it back to the name weight or the two lines merge into one block of bold text. The address is the ONE part allowed to give way: `.soCustomerLines{min-width:0}` lets it ellipsise while the strip keeps its single line and its 80px ceiling at every width; measured at 480px the strip is 321px beside the 76px actions and nothing overflows. The full address stays one tap away inside the eye-button disclosure. Do not lift the cap.

The sales create page order is **Customer first, then the details card**, switched back on 2026-09-25 at the user request: the customer card leads and the Order/Invoice details card follows, then the lines, then the Billing summary. The customer card names itself **once** - `<h3>Customer *</h3>` carries the label AND the required mark, and the search field below it has no visible label of its own (it keeps `aria-label="Search customers"` and its placeholder). Do not put a second "Customer *" on the field.

The details card scope row is THREE fields - Organisation, Branch, **Place of supply** - through `.soFieldRow3`, and `PlaceOfSupplyField` is rendered by `src/SalesDocumentFields.jsx` itself, not by the host pages. Place of supply moved out of Additional details on 2026-09-25 so the tax decision sits beside the branch that decides it, and because `calculate()` refuses to total without it; `details.soMoreCard` therefore no longer carries `open={!form.place}` and stays collapsed. Additional details now holds Terms, History and Documents only.

The Billing summary closes the page the way an invoice does: `div.soSummaryGrid` is `minmax(0,50fr) minmax(0,35fr)` with `justify-content:space-between`, so the operator panels (Customer note, Terms, Documents) take 50 parts and the figures 35 - the proportion the user asked for on 2026-09-25 - with the Customer note textarea leading `div.soSummaryLeft` and `soSummaryRight` holding the heading, the `INR` chip, the totals and the posting note; it folds to one column below 900px. Measured at 1440px the grid is 1093px split 631/442 with a 20px gap. The note is the document own `form.notes`; it lives here and nowhere else, so do not put a second "Customer notes" control back into Additional details - two inputs bound to one field is a bug, not a layout choice.

The `+ Add item` control is the LIGHT primary variant: `border:1px solid #bcd0f5;background:#eef4ff;color:#245fd9`, darkening to `#e2ecff`/`#1d4fbb` on hover. It is deliberately not the solid fill, which belongs to the page one primary action - Save and confirm.

The customer card ends in the actions that act on the customer, and they are ICON-ONLY: `button.soCustomerMoreToggle` is a 34x34 `aria-label="View customer details"` eye button and `details.soFactEdit > summary` is a matching 34x34 `aria-label="Edit address"` pencil, so the row no longer spends 182px of the card on the words "View customer details". Both carry their label in `aria-label`/`title` only - do not put the text back inside the buttons.

The customer strip on the sales create pages holds TWO lines and nothing else: `<strong class="soCustomerName">` for the customer, then `<span class="soCustomerAddress">` reading **Billing Address:** followed by the whole address. The separate `.soCustomerFacts` strip (Billing/Phone/GSTIN) was replaced on 2026-09-25 at the user request; the rest of the record is behind the eye-button disclosure. The strip keeps the same 80px ceiling - measured at 66px - so the card height does not change.

`addressLine(which)` in `src/SalesDocumentFields.jsx` is the only address formatter: it prints `line1, line2, city, state, pin` DE-DUPLICATED and drops any part equal to the country, then appends the country once. A stored record can hold the country in its `city` field - the customer seed does - and without that rule the address reads "Kochi, India, Kerala, India". Use it wherever a document prints an address on one line.

The two actions that act on the customer share ONE row WITH the strip, at the card trailing edge: `div.soCustomerRef` is `display:grid;grid-template-columns:minmax(0,1fr) auto;align-items:center;gap:10px` holding `div.soCustomerProfile` in the first column and `div.soCustomerActions` (flex, `justify-content:flex-end`, `margin-top:0`) in the second, so the two are vertically centred against each other. Every OTHER child of that grid - `div.soCustomerMoreBody`, `p.soCreditNotice` and the empty state - spans both columns through `grid-column:1/-1`; without it the disclosure body rendered only 1007px of the 1093px card. `div.soCustomerActions` must keep `margin-top:0`: on its own line under the strip it carried `margin-top:8px` and cost the card 42px of dead height (220px to 178px). The disclosure is a BUTTON with `aria-expanded`, not a native `<details>`, because a `<details>` cannot put its summary and its body in two different places and the summary had to sit beside the pencil. Its body is `div.soCustomerMoreBody`, rendered only while `customerMore` is true.

There is NO Additional details card on either sales create page. Its panels are passed into `src/SalesDocumentFields.jsx` as CHILDREN and rendered in the Billing summary left column, under the Customer note: the host pages supply the Terms group, the order history and the Documents drop zone. `soMoreCard`, `soMoreTitle`, `soMoreBody` and `soMoreHint` are deleted - do not reintroduce the card, and do not put a second Customer note or Terms control back into the host page, because `form.notes` and `form.termsAndConditions` each have exactly one control.

The Billing summary is the page footer in the invoice arrangement: `div.soSummaryGrid` is `minmax(0,50fr) minmax(0,35fr)` with `div.soSummaryLeft` (Customer note, then `{children}`) and `div.soSummaryRight` (the Billing summary heading, the `INR` chip, the totals and the posting note) - so the heading sits directly above the figures it names rather than above the whole block. The `INR` chip is right-aligned to the column edge while the heading starts at the column start, both on the same 1093px measure.

A sales create page can send the operator to the Item master AND to the Customer master. Each detour uses one sessionStorage marker - `ITEM_CREATE_KEY` (`wayvida-open-item-create`) and `CUSTOMER_CREATE_KEY` (`wayvida-open-customer-create`), both exported from `src/quick-create.js` - and both stash the draft under the page own `DRAFT_KEY` and set `wayvida-return-to` before navigating, so the document comes back after the master is saved. The customer marker is new on 2026-09-25: `src/Customers.jsx` reads it once in a second mount effect, clears it, and calls `start()` with no row, which opens a blank form carrying the next code from `nextCustomerCode(rows)`. An edit marker wins if both were somehow set. The button that fires it is `button.soCustomerCreate` - a 40x40 `aria-label="Create customer"` `IconPlus` - at the end of `div.soCustomerRow`, which is now `display:flex;align-items:center;gap:10px` with `.soCustomerRow>.soFieldSearch{flex:0 1 640px}`, so it reads as attached to the search box at every width.

The customer card names the customer TYPE and the NET RECEIVABLE. `span.soCustomerType` is a pill beside `strong.soCustomerName` inside `div.soCustomerHead` (a flex row) and it is ALWAYS rendered; its text comes from `customerTypeOf(row)` exported by `src/Customers.jsx`, which is the ONE rule the customer master already applied inside `normaliseCustomer` and which treats anything that is not `Individual` as `Business`. Do not go back to printing the raw `selectedCustomer.type`: that left the chip blank for a stored row that predates the field and made the create page disagree with the master, which shows Business for that same record. `div.soCustomerMeta` is a right-aligned two-line stat inside `div.soCustomerProfile` - the label `Net receivable`, then `money(creditOutstanding)`. `creditOutstanding` is the same paise figure the credit-limit warning already used, so the stat and the warning can never disagree, and BOTH hosts must pass it: `src/SalesOrders.jsx` and `src/InvoiceWorkspace.jsx` each read `customerCreditSummary(state,customerId).outstanding`, which is why the invoice page no longer falls back to a default zero. Below 620px `div.soCustomerMeta` is hidden, because a phone-width strip cannot hold the avatar, the name, its type, the address, the receivable and the two actions on one line, and the name is what identifies the customer. Both the type pill and the stat label are 12px: the twelve pixel floor is a repo invariant, so never size them at 11.5px again.

Every order/invoice LINE now carries ONE item box that answers both questions a line asks. `ItemSearch` in `src/InvoiceLineControls.jsx` takes `onAdHoc` and `onType` beside `onSelect`, builds ONE option list - `const rows=[...matches.map(item=>({kind:'item',item})),...(onAdHoc&&typed?[{kind:'adHoc',text:typed}]:[])]` - and `pick` sends an `adHoc` row to `onAdHoc(row.text)` and a master row to `onSelect(row.item)`. ArrowUp/ArrowDown/Enter walk that combined list through `const move=d=>setActive(a=>Math.max(0,Math.min(a+d,rows.length-1)))`, so the active row is clamped and an empty result set cannot run the index past the end. The typed text is offered as `Use "<text>" as an ad hoc item` in a `span.ivAdHocOption` (primary colour, styled in `src/invoice-workspace.css` beside the other `.ivItemResults` rules because both pages share it). `onType` is wired only when the line is already ad hoc, where the box edits the stored `description`; a master line keeps its description until a pick. The separate `input.soAdHocName` and the second kind of row it needed are DELETED from both `src/SalesDocumentFields.jsx` and `src/sales-orders.css` - do not put a second item-name field back on the line, and do not make an ad-hoc line a different kind of row. Choosing the ad-hoc row resets the line with `{...blankLine(),adHoc:true,description:text}`, the same shape `addLine()` then the ad-hoc offer produces, so no `item`, `income`, `unit` or `priceTaxMode` inherited from a master record can survive onto a line that no longer has one.

The Terms group on both create pages has NO heading. It is `<section class="soMoreGroup"><div class="ivFields soFieldRow soFieldRow1"><label>Terms & conditions` with the redundant `h4.soMoreGroupTitle` reading "Terms" removed on 2026-09-25, because the field already labels itself in exactly those words. The Documents and History groups keep their `soMoreGroupTitle soUploadLabel` heading, which has no inner label of its own.

The Billing summary heading and the currency chip share ONE row: `div.soSummaryHead` is `display:flex;align-items:center;justify-content:space-between` and holds the `h3` and `span.soCurrencyChip` together, so the chip is no longer a second line above the totals. `div.soSummaryMoney` keeps only the totals, and `.soSummaryMoney>p{margin:0}` removes the stray 14px paragraph margin the empty-state hint used to add.
A scripted edit whose replacement string contains a bare carriage return leaves a `\r\r\n` in the file, and git then classifies the whole file as `-text` and prints an every-line binary diff - the real change disappears into a whole-file rewrite, which is a review hazard. One such pair was found and removed from `src/Customers.jsx` on 2026-09-25. After any scripted edit, check that `git ls-files --eol <file>` reports `w/crlf` or `w/lf` rather than `w/-text`, and that `git diff --numstat <file>` reports the real line count rather than the whole file; if it does not, scan the file for `\r\r\n` and for any `\r` not followed by `\n`. Files reported `w/mixed` are legitimate here and still diff as text.

The HSN/SAC code the Item master holds is carried onto the document line and printed. `itemDefaults(item)` in `src/SalesDocumentFields.jsx` now returns `hsnSac:item.hsnSac||''` and `itemType:item.type||'Goods'` beside the fields it already copied, and the line states it under the box that chose it as `<small className="soHsnValue">{(l.itemType==='Service'?'SAC':'HSN')+' '+l.hsnSac}</small>` - rendered only when the line has a code, so an ad-hoc line shows no empty label. Goods print an HSN and a service prints an SAC, which is why the type travels with the code rather than being looked up later: `src/DocumentPreview.jsx` only has the document, not the Item master. The printed sheet therefore carries an `HSN / SAC` column between the item and its quantity, printing an em dash when a line has none. That column changed the print table from seven columns to eight, and `.dpItemsTable` widths in `src/document-preview.css` are POSITIONAL (`th:nth-child(n)`), so every one of them was restated and the right-align rule moved from `nth-child(n+4)` to `nth-child(n+5)` to keep the qty-and-unit cell left-aligned and the money cells right-aligned. Add a column again and you must redo all of them. A receipt prints allocations, not items, so it has no HSN column - do not invent one.

The Number and Currency Format is ONE setting with ONE formatter. `src/number-format.js` reads and writes the preference through `src/settings-store.js` (keys `numberFormat`, `currencySymbol`, `decimalPlaces`, `roundOffQuantity`, `roundOffRate` under `organization`), exports `formatRupees(value,override)` for rupee values, `formatMinor(minor,override)` for the engine stored paise, `formatQuantity` / `formatRate` for the two round-off switches, and `useNumberFormat()` for the re-render subscription. Every money helper in the app delegates to it - `src/invoice-engine.js` (`money`), `Customers.jsx`, `GeneralLedgerPro.jsx`, `SalesOrderActions.jsx`, `SalesOrders.jsx`, `Vendors.jsx`, `simple-journal-transaction.js`, `journal-templates.js`, `inventory-adjustments.js` and the file-local `money` inside `src/Items.jsx` - so `en-IN`/`₹`/two decimals is no longer hardcoded anywhere. `src/NumberCurrencyFormatDialog.jsx` is the `NumberFormatControl` mounted in BOTH create heads (`<NumberFormatControl/>` inside `.soCreateHead`): a 40px `button.soHeadSettings` in the LIGHT primary variant, because the one solid primary fill on the page belongs to Save, opening a `.nfLayer` fixed layer whose card, backdrop and close button copy the address editor tokens. Its label rules are scoped `.nfLayer .nfFormat` / `.nfLayer .nfDigits label` etc. on purpose: the create pages carry `.invoiceWorkspace`, whose own `label{display:flex;flex-direction:column}` outranks a bare single-class selector, which is what stacked the radios on the first attempt. Round-off moves quantities and rates only - never a posted amount.

`src/Items.jsx` owns a `money` helper that was chained onto the end of its `visible` line as `),money=value=>Number(value).toLocaleString('en-IN',{style:'currency',currency:'INR'});` - a hardcoded rupee literal that ignored the setting entirely, which is why the Items register kept printing `₹850.00` while every other surface followed the Number and Currency Format. It now delegates to `formatRupees(value,numberFormat)` with the hook beside it, and item prices are RUPEES, not paise, so never hand them to `formatMinor`. That file also had a `useRef` declared BELOW its `if` detail-page early return, which threw `Rendered fewer hooks than expected` the moment an item detail was opened; all three hooks now sit above the return, and a regression test asserts the hook lines stay above it. Check that ordering whenever a hook is added to a component with an early return.
The Sales invoices register is EIGHT columns and its rows end in an eye. `table.ivInvoiceTable` reads Invoice, Customer, **Status**, **Invoice amount**, Payment status, **Organisation**, **Branch**, Actions, at 13/16/11/12/12/16/11/9 percent - the widths are positional, so changing a column means restating all eight, and the credit-note register overrides all eight for its own table. There is NO Order ID column: the order reference is read on the invoice detail page, where the stats row states `Order number and status` and the details list states `Reference`, so `orderNumberOf(i)` survives only as the register's Order ID FILTER. The lifecycle status sits AHEAD of the amount, which is the order the user asked for on 2026-09-25 - do not swap them back. Organisation and Branch are cells of their own rather than one merged cell, and the invoice number cell carries `{i.date}` alone: the raw stored status used to sit under it, which would have printed `Draft` beside a Status column reading `Pending`. The Status cell is `StatusPill status={invoiceDisplayStatus(i)} tone={invoiceDisplayTone(i)}` from `src/invoice-register-display.js`, the one display rule: Cancelled wins first, then `posted` means Published, then Pending Approval means Pending, and anything else is Draft. It is presentation only and never rewrites a stored status - `Paid`, `Sent`, `Partially Paid` and `Overdue` all read as Published because posting is what puts a document in the ledger, and a cancelled invoice that had posted is still Cancelled. The amount cell keeps the outstanding balance as a small line only while `0 < outstanding < total`. The row action is `button.ivIconButton`, a 36x36 square holding `IconEye` with `aria-label={'View '+i.number}`, matching the row-menu trigger beside it; do not put a `View` text button back.

The Sales orders register row is two icons, and the EYE opens that order's preview. `button.soRowIconButton` (36x36, `aria-label`/`title` reading `Preview <number>`) calls `setPrintDoc(x)`, which renders `DocumentPreview kind="Sales Order"` - the only detail screen a sales order has - for EVERY order, whether or not it has an invoice; never restore the disabled `View details` text button that opened the invoice instead, or the `.soPreviewButton` rule. The invoice hand-off moved into the row menu, where it belongs: `SalesOrderActions` takes `onOpenInvoice`, its invoice entry is `disabled={!order.invoice&&order.status!=='Confirmed'}`, and it opens the invoice when one exists and converts only when it does not - so `invoiceIdOf(order)` (which resolves `order.invoice.id` first and then the invoice store by `sourceOrder`, exactly as `convertSalesOrder` does) and the `wayvida-open-invoice` handshake both stay in use rather than being deleted. Editing stays in that menu.

The invoice detail and create actions agree on which verb is primary. On the detail screen a Draft offers `button` + `IconEdit` + `Edit` (text AND icon - a bare word reads as a link in that action row) beside `button.primary` `Submit for approval`, and the pending step keeps its own `Approve & post` primary. On the create page `div.soBarActions` carries three verbs - `Cancel`, `Save draft`, `Submit for approval` - through ONE saver, `saveInvoice(status)`, which stamps the organisation and branch the way a sales order does. `Save draft` is the form's submit button and takes the primary fill ONLY when `invoiceAllowed(role,'submit')` is false, so the page always has exactly one primary action; `Submit for approval` is `type="button"` and primary when the role may submit. If the save succeeds and the SUBMIT is refused, the form stays open bound to the saved record (`setForm({...form,id:saved.id,number:saved.number})`) so a retry edits that invoice instead of creating a second one, and the engine error stays on screen. The create page never posts: posting lives on the detail screen, for the role that holds the duty.

The Sales orders register is EIGHT columns: Order (the SO number over its order date, through `fmtDate`), Customer, **Organisation**, **Branch**, Amount, Status, Payment status and Actions - at 12/15/17/12/11/10/11/12 percent. Organisation and Branch are cells of their own in that order, matching the invoices register, and they replace the single `Branch & Organisation` cell that printed the branch over the organisation. **There is no Created by column**, which was removed on the user request of 2026-09-25. `soOrdersTable` widths are positional, so any column change means restating all eight - and `src/sales-orders.css` used to carry TWO identical runs of those eight rules, the first entirely overridden by the second; the dead run was deleted, so keep exactly one. Created by survives as a register FILTER, the same choice made for the invoices Order ID: removing a column is not the same as removing the ability to search by it. Status and Payment status still render the shared `StatusPill` through `ORDER_TONES` and `PAYMENT_TONES`, and every row still ends in the 36x36 `button.soRowIconButton` eye that opens the order preview plus the row action menu.

The sidebar puts Items between Accounting and Sales, and dissolves the Inventory group. `src/Navigation.jsx` declares `{...leaf('Items'),icon:IconPackage}` as a TOP-LEVEL destination directly after Accounting and before Sales, and the old `{label:'Inventory',icon:IconPackage,children:[leaf('Items'),leaf('Inventory Adjustments')]}` is deleted - do not rebuild it. Inventory Adjustments moved into the `Other` group, which is now NESTED INSIDE `Settings` rather than a top-level section: `{label:'Settings',icon:IconSettings,children:[leaf('Settings Center'),leaf('Company'),leaf('Branch','Branches'),{label:'Other',children:[leaf('Budgets'),leaf('Period Lock'),leaf('Inventory Adjustments')]}]}`. `IconFolders` was only that section icon and is no longer imported. The renderer is recursive, so a nested group needs no new code - and no routed page name changed, so `Inventory Adjustments` still resolves through `src/App.jsx`; only where the sidebar lists it moved.

The item detail states the lifecycle on the name line, the form keeps no inventory accordion, and the scope field labels itself once. In `src/Items.jsx` the badge is `<div className="itemDetailNameRow"><h2>{selected.name}</h2><span className={'itemStatus '+(selected.active===false?'disabled':'active')}>` - it left `div.itemDetailActions`, and `.itemDetailNameRow{display:flex;align-items:center;gap:10px;min-width:0}` in `src/items.css` is what keeps it beside the name. The detail no longer carries Track inventory or Opening stock facts, and the orphaned `Inventory & Stock` card (Inventory asset / Cost of goods sold) is deleted. On the form, `itemScopeField` no longer repeats "Organisations and branches *" above the control, which labels itself `Item availability`; the Track inventory section and the two accounts it alone required are gone, and `changeType` must NOT set `trackInventory` - with the control removed, doing so would fail validation against accounts nobody can choose; `openingStockDetails` and its `Opening stock` disclosure are deleted; and `details.itemAdvanced` became a plain `div.itemAdvanced`, so Tax settings and the Item image are both visible without opening anything. The stored `trackInventory`, `openingQuantity`, `openingRate` and the inventory/cogs accounts stay in the data model, and the `isNew&&form.trackInventory&&openingValue(form)>0` branch in the save path is kept as a guard the form can no longer reach.
