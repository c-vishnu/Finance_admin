{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "metadata": {
    "product": "Wayvida Books",
    "package": "finance-erp-prototype",
    "version": "0.0.0",
    "lastVerified": "2026-09-15",
    "purpose": "Portable implementation and product-architecture inventory for engineers and AI tools"
  },
  "runtime": {
    "language": "JavaScript",
    "moduleSystem": "ES modules",
    "framework": "React 19.2.0",
    "bundler": "Vite 6.4.2",
    "entryPoints": [
      "index.html",
      "src/main.jsx",
      "src/App.jsx"
    ],
    "commands": {
      "development": "npm run dev",
      "productionBuild": "npm run build",
      "preview": "npm run preview",
      "sitesTest": "npm run test:sites",
      "allTests": "node --test tests/*.test.mjs",
      "documentationCheck": "npm run docs:check"
    },
    "dependencies": [
      "react",
      "react-dom",
      "@tabler/icons-react",
      "recharts",
      "vite",
      "@vitejs/plugin-react"
    ],
    "finalUiCompatibilityLayer": "src/ui-quality-polish.css"
  },
  "modules": [
    {
      "id": "dashboard",
      "label": "Dashboard",
      "component": "src/App.jsx",
      "status": "Prototype only",
      "accountingImpact": "Read-only summary"
    },
    {
      "id": "chart-of-accounts",
      "label": "Chart of Accounts",
      "component": "src/AccountWorkspace.jsx",
      "services": [
        "src/account-master.js",
        "src/account-store.js",
        "src/account-settings.js",
        "src/account-templates.js",
        "src/AccountGroupPicker.jsx",
        "src/account-group-search.js"
      ],
      "status": "Prototype only",
      "accountingImpact": "Master account structure; the list grid resolves Account Group only at normal cell weight; saved non-type groups win and type-equal legacy/seed groups map to operational groups; the Create Account / Edit Account drawer picks the Account Group through the shared searchable combobox (src/AccountGroupPicker.jsx over the DOM-free src/account-group-search.js) instead of a native select with optgroups, writing the same type::purpose key through applyGroup; selected working-context organisations are stored additively in organizationIds for scope presentation while organizationId remains compatible; no posting rule changes Row actions moved behind the shared horizontal three-dot menu: the Period Closing kebab (details.pc-kebab > summary holding IconDots, plus .pc-kebab-menu) is the canonical control, so the am-more summary now renders IconDots size 17 with a #d9e1ec!important border and a 7px radius, the open state is border-color:#3478f6!important;color:#245fd9;background:#fff instead of the #edf3ff fill, and the menu is min-width:196px!important with gap:0, border:1px solid #e1e6ed!important, border-radius:9px!important and box-shadow:0 12px 30px #1018281f!important, its rows using padding:0 10px, background:transparent, color #344054 and the #f4f7ff / #245fd9 hover while .am-danger keeps its red label. Every entry is now type=\"button\" and closes its own menu through the new menuRun(handler) helper, and one AccountWorkspace useEffect registers pointerdown and Escape listeners over .am-table .am-more[open]. Account create/edit/delete logic, start(), run(), storage and posting rules are unchanged. The Create Account / Edit Account surface is a right-side drawer instead of a centred 44vw card: src/EnterpriseAccountForm.jsx renders a div.am-create-drawer-header above the form carrying the Create Account / Edit Account title, a one-line description and a close button (aria-label=\"Close create account\", the same close handler as the backdrop) in place of the in-body .am-create-drawer-page-title, and src/account-create-drawer.css anchors the layer with position:fixed;inset:0;z-index:1200;display:flex;align-items:stretch;justify-content:flex-end while the aside is pinned top:0;right:0;bottom:0;left:auto with width:min(560px,100vw), height:100%, border-left:1px solid #dfe6ef, border-radius:0 and box-shadow:-18px 0 48px rgba(16,24,40,.15), relaxed to min(88vw,560px) under 900px and 100vw under 640px; the five 11px helper sizes in that file were raised to 12px. Field order, validation, applyGroup, MultiSelect, save logic and the shared create/edit surface are unchanged. Organisation/branch scope: changeAccount in src/account-master.js gained an organisation-aware validateScope check (with demoOrganizations passed from src/AccountWorkspace.jsx), so a branch that belongs to another organisation is rejected by the reducer as well as hidden in the picker. The account grid keeps its own scroll: the filter row pins to the top of the shell scroll port and the grid wrapper is bounded so the column header row and the account-group accordion row stick inside it, with filters, grouping, the account tree, colSpan arithmetic, the scope columns, the empty state and the Create/Edit Account drawer unchanged and no posting rule or stored field involved. Account details open as a right-side popup instead of an inline second column: src/AccountWorkspace.jsx renders div.am-create-drawer-layer.am-detail-layer holding button.am-create-drawer-backdrop and a full-height aside.am-create-drawer.am-detail-drawer (role=\"dialog\", aria-modal=\"true\", aria-label=\"Account details\") whose own header states Account details with {account.name} · {account.code}, over the unchanged am-detail-body and am-detail-footer; the backdrop, the header close button and Escape all clear selected, and the row click, the register 3-dot entry and the wayvida-open-account handoff keep opening the same detail. am-layout lost the has-detail modifier and src/account-workspace.css dropped the two-column .am-layout.has-detail grid rules, the .am-detail{position:sticky;top:82px} container and their media-query overrides, so the grid keeps the full page width, while .am-detail-top stays for the Day Book transaction detail header. The new commented CSS block sets the panel to min(560px,100vw), makes am-detail-body the only scroll container and pins am-detail-footer, and declares no font size, so the 12px floor and the Create Account drawer shell are preserved. No storage, validation, posting or account-record change.",
      "styles": [
        "src/account-group-picker.css"
      ]
    },
    {
      "id": "terminology-layer",
      "label": "Business Owner / Accountant terminology",
      "component": "src/terminology.jsx",
      "consumers": [
        "src/main.jsx",
        "src/Navigation.jsx",
        "src/AccountWorkspace.jsx",
        "src/App.jsx",
        "src/BudgetWorkspace.jsx"
      ],
      "status": "Implemented prototype",
      "accountingImpact": "Display labels only; no route, stored accounting value, journal, ledger, report, or workflow changes; the switch itself now lives in the header account menu (details.profileMenu in src/App.jsx) with type=\"button\" rows for Business owner (Admin) and Head of Accountant (Staff) carrying aria-pressed, while the old View As: control, the src/Navigation.jsx .terminologySwitch injection and the unused TerminologySwitch export were removed; it also owns the one accounting-type vocabulary - ACCOUNT_TYPE_ORDER (Assets, Liabilities, Equity, Income, Expenses) and accountTypeLabel(type,business) - so the Create Budget account step and the Chart of Accounts page name an accounting type from the same map instead of each keeping a private label list"
    },
    {
      "id": "shared-date-picker",
      "label": "Wayvida shared date picker",
      "component": "src/WayvidaDatePicker.jsx",
      "consumers": [
        "src/main.jsx",
        "all input[type=date] fields"
      ],
      "status": "Implemented prototype",
      "accountingImpact": "Presentation delegate only; retains ISO date values and dispatches normal input/change events to existing validation and posting workflows"
    },
    {
      "id": "journal-entries",
      "label": "Journal Entries",
      "component": "src/JournalEntriesPro.jsx",
      "status": "Prototype only",
      "accountingImpact": "Displays manual journals only, with business/accountant terminology and controlled approval, posting and reversal; automatic entries remain available through ledgers and reports. The create page is a compact task-oriented workflow with one mandatory metadata row (date, journal type, optional reference, plain single-line reason), no posting-context card because organisation and branch are edited per line between the account and debit columns, though only when the working context needs them (one accessible organisation hides the Organisation column, and a single branch under it hides Branch as well, so the grid renders seven, six or five columns), an inline locked-period hint inside the date field, mutually exclusive debit/credit inputs, right-aligned Total Debit and Total Credit under the grid with the balance status (Debit equals credit / Difference) reading directly beneath them inside the same block as the only posting indicator (the four conditions stay in postingChecks() as the gate), one Attach files button at the left of the tools row on the same left edge as the lines grid beside the Add Journal Line button that pairs with Use Template, a collapsed Additional details section holding only read-only approval comments and rendered only when they exist, and Cancel / Save draft / Post journal. Posting stays gated behind an approved status, balanced totals and an open period, and templates and account preferences stay outside the journal and ledger stores Register row actions moved behind the shared horizontal three-dot menu: the summary now renders IconDots size 17 (IconDotsVertical left the @tabler/icons-react import) without the title tooltip, every menu button carries type=\"button\" and closes its own menu through the new menuRun(handler) helper, and one JournalEntriesPro useEffect registers pointerdown and Escape listeners over .je-row-actions details[open] and .je-detail-more[open]. journal-entries.css lines 8 and 9 now carry the canonical .pc-kebab geometry - a 32px trigger with 1px solid #d9e1ec and a 7px radius, details[open]>summary{border-color:#3478f6;color:#245fd9} with the old summary:hover fill and details>div:before caret removed, and an absolutely positioned menu at top:calc(100% + 6px) with min-width:196px, padding:6px, 1px solid #e1e6ed, border-radius:9px and box-shadow:0 12px 30px #1018281f whose rows are min-height:36px with gap:9px, padding:0 10px, a 6px radius, transparent background, #344054 labels and the #f4f7ff / #245fd9 hover (disabled rows stay hidden by journal-period-fix.css). No journal validation, storage key, record field or posting rule changed. The page now reads its available organisation list through the shared src/organisation-context.js (getAccessibleOrganizations / getCurrentOrganizationContext) instead of holding private copies, so the journal lines and the Period Closing picker resolve the same working scope; expressions and behaviour are unchanged.",
      "components": [
        "src/JournalAccountPicker.jsx"
      ],
      "services": [
        "src/journal-templates.js",
        "src/invoice-engine.js",
        "src/period-locking.js"
      ],
      "styles": [
        "src/journal-entries.css",
        "src/journal-form.css"
      ]
    },
    {
      "id": "transaction-register",
      "label": "Transaction Register",
      "component": "src/TransactionRegister.jsx",
      "services": [
        "src/invoice-engine.js",
        "src/purchase-service.js",
        "src/operations-store.js"
      ],
      "styles": [
        "src/transaction-register.css"
      ],
      "status": "Implemented prototype",
      "accountingImpact": "Read-only cross-module projection separating business status from Not Posted, Posted and Reversed accounting status, with journal and ledger drill-down"
    },
    {
      "id": "general-ledger",
      "label": "General Ledger",
      "component": "src/GeneralLedgerPro.jsx",
      "services": [
        "src/invoice-engine.js"
      ],
      "status": "Prototype only",
      "accountingImpact": "Accountant view derived from posted journal lines with balance, dimension, reconciliation and source traceability"
    },
    {
      "id": "trial-balance",
      "label": "Trial Balance",
      "component": "src/App.jsx",
      "status": "Prototype only",
      "accountingImpact": "Derived report"
    },
    {
      "id": "day-book",
      "label": "Day Book",
      "component": "src/DayBook.jsx",
      "service": "src/daybook.js",
      "status": "Prototype only",
      "accountingImpact": "Read-only chronological journal view"
    },
    {
      "id": "period-closing",
      "label": "Period Closing",
      "component": "src/PeriodClosing.jsx",
      "service": "src/period-locking.js",
      "styles": [
        "src/period-closing.css",
        "src/period-closing-focus.css",
        "src/period-closing-details.css"
      ],
      "status": "Partially implemented",
      "accountingImpact": "2026-09-14: The Create Lock / Close drawer renders the shared OrganisationBranchScope picker over the available working context (scopeOrganisations from src/organisation-context.js), writing companyIds / branchIds into the existing lock form and therefore into createLockWindow; the popup header carries only the mode label and the Create Lock / Close title - the scope chip and the schedule paragraph were removed on the user instruction of 2026-09-14, together with the lockScope constant that fed them, so getScopeVisibility no longer appears on this page - and the picker emits only on a user change so a whole-organisation lock is never silently narrowed. The request unlock drawer renders the same control with showFields={false}, stating Unlocking for: <organisation> and the locked branch scope; the scope of an unlock request stays derived from the locked period by requestPeriodUnlock, so it is presented rather than selected (a branch-narrowed request would need a new temporary-unlock enforcement rule). No storage key, close, lock, audit or enforcement rule changed. Previous checkpoint, superseded in part: 2026-09-14: Period Closing consumes the shared organisation/branch scope module. src/PeriodClosing.jsx now validates a lock creation with validateScope({organisations:demoOrganizations,companyIds:lockForm.companyIds,branchIds:lockForm.branchIds,requireBranch:false}) in place of the bare empty-list check, so an unknown organisation code (UNKNOWN_ORGANISATION) or a branch belonging to another organisation (BRANCH_NOT_IN_ORGANISATION) is rejected rather than silently dropped, and the Create Lock drawer states its resolved scope in a new .pc-lock-scope chip built from getScopeVisibility({organisations:[organisation],organisationIds:[organisation.id],label:'Locking for'}) (src/period-closing.css holds the 12px blue chip rule). The forbidden scopeVisibility({organisations,companyIds:form.companyIds text stays absent and no scope selector was added to the page. tests/period-closing-ui.test.mjs stays at 27/27 and tests/period-locking.test.mjs at 28/28 - no engine, storage, audit or lock rule changed. Previous checkpoint, superseded in part: 2026-09-14: Period Closing request register. The Requests table is now one pageless register: REQUEST_PAGE_SIZE, the useState(1) page number and the pageCount/active/visible slice inside RequestList are deleted, so every filtered request renders in one pass and no Previous / Page <n> of <n> / Next control (or its .pc-request-pager / .pc-pager-status rules in src/period-closing.css) exists any more. RequestTable takes (requests, periods, actions) and carries seven columns - Period, one combined Organisation & Branch cell, Reason, Requested, Requested By, Status and Actions. The new requestScopeOf(request, periods) resolves the period a request belongs to and prints period.organisationName (falling back to demoOrganizations by code, so a stored ABC01 never reaches the screen) plus periodBranchLabel() of that period as the small line beneath it - All branches for a whole-period lock, real branch names (Kochi Branch, Bengaluru Branch) for a branch scope - and degrades to All branches when no period matches, rather than printing raw ids. The new requestStatusLabel(request) maps the stored Pending approval to Requested and passes Approved, Cancelled, Rejected and any other stored status through unchanged; the cell reuses the page's span.pc-badge with .requested amber, .approved green and .cancelled / .rejected red tones added in src/period-closing-focus.css, which also re-specifies the seven column widths (12 / 19 / 21 / 11 / 11 / 12 / 14%) and drops the pager gutter rule. LockRegister passes periods down to RequestList, and the Branch filter now applies to the requests half through branchMatch exactly as it already did to the lock list. The span.pc-filter-count row-count text and its three stylesheet rules are deleted from the register toolbar. IconChevronLeft / IconChevronRight left the page import with the pager. No engine, storage, audit, approval or lock rule changed. Previous checkpoint, superseded in part: 2026-09-14: Period Closing request row actions. The Requests table's Actions cell is now one shape on every row: one read-only View button (button.pc-request-view, 32px like the row menu, white with an #e1e6ed border and #475467 ink, hovering to #b9c9e3 / #f8faff / #245fd9 - neutral, never the primary blue) plus one 3-dot menu, replacing the inline emphasised Approve Unlock and the inline requester-only Cancel request. RequestActions dropped its three early returns for a single return, and its menu (details.pc-kebab.pc-request-kebab > div.pc-kebab-menu.pc-request-kebab-menu) now holds four entries in order: Approve (canDecide=canApprove&&decidable&&pending), Cancel (canCancel=self&&pending, unchanged and still requester-only), View Details (always) and Delete (canDelete=self||canApprove, className pc-kebab-danger, IconTrash newly imported, styled #b42318 with a #fff5f6 hover as the one destructive entry). Both View entries call onDetails(request) and open the same RequestDetailsDrawer. The two explanatory notes survive as one note value rendered as span.pc-action-note - empty on a decided row, 'Approval requires an approver role.' when the acting role cannot approve, 'Another approver must review your own request.' when no second approver exists - so a missing Approve entry is still explained rather than silently absent. onDeleteRequest is threaded exactly like onCancelRequest (page deleteRequest -> LockRegister / RequestsDrawer -> RequestList -> RequestActions) and the page calls the new engine verb deleteUnlockRequest(state,{requestId,user,now}) in src/period-locking.js, which throws 'Unlock request not found.' for an unknown id and 'Only the requester or an approver can delete this unlock request.' for any other role, routes a Pending approval request through cancelUnlockRequest first (restoring the previous Hard Locked / Soft Locked status and writing the Unlock cancelled entry) so deleting a live request can never strand its period, then removes the record from state.requests and prepends an 'Unlock request deleted' audit entry; deleting a decided or expired request removes the record without touching its outcome. src/period-closing.css dropped the now-dead .pc .pc-request-actions button.pc-quiet and button.primary rules - pc-quiet no longer appears anywhere in src/ - and added button.pc-request-view and .pc .pc-request-kebab-menu button.pc-kebab-danger. Presentational plus one register-only engine verb: no lock, close, enforcement, storage-key or journal rule changed, and approve/cancel permissions are exactly as before. Verified with node tests/period-closing-ui.test.mjs 27/27 (two contract names changed and the Approve Unlock / pc-quiet / button.primary assertions replaced by pc-request-view, pc-kebab-danger, canDelete=self||canApprove, IconTrash and the four-label menu), node tests/period-locking.test.mjs 28/28 (new contract 'deleting a request releases a pending period, keeps the audit trail and refuses other roles'), node tmp/pc15-requests.mjs, node tmp/pc2-ssr.mjs, node tmp/pc2-drawers.mjs 12 surfaces and node tmp/pc5-validate.mjs 17 surfaces (the last two had to be re-pointed because tmp/pc2-probe.jsx was a stale pre-table snapshot that still asserted Approve Unlock and Whole period), node tmp/check-all-jsx.mjs 49/49, node scripts/validate-knowledge-docs.mjs passed, and the full sweep FILES=64 green=50 tests=353 passed=342 failed=11 CANNOT-START(4) - the same eleven pre-existing failures, none in Period Closing. Not verified: no painted browser pass (in-app browser automation is denied in this environment) and no production build (npm run build cannot run because the esbuild binary cannot be spawned, spawn EPERM). The dev server at localhost:5173 serves the change live. Previous checkpoint, superseded in part: 2026-09-14: Period Closing attention pill and request row cleanup. RequestActions no longer renders the reviewer note - the {self&&<span className=\"pc-action-note\">{'Recorded as '+approver+', the next authorised approver.'}</span>} span left its final return - so a pending request raised by the acting role is the emphasised Approve Unlock, the requester-only quiet Cancel request and its 3-dot menu and nothing else; approver still drives decidable=!self||Boolean(approver) and the decision is still recorded under the resolved approver. The full-width pc-attention strip under the heading is replaced by a compact, never-dismissible attention pill in the heading action group: details.pc-attention-pop > summary.pc-attention-pill (className 'pc-attention-pill '+tone with tones neutral/warn/danger, IconAlertTriangle 15, span.pc-attention-count carrying aria-live=\"polite\" and the count as text, aria-label \"Review <n> checks that need attention\", title \"Before you close\", no chevron) renders only while attention.count>0, so zero checks hides the whole details and All checks passed is printed inside the popover only. One attention memo - the failed checks, the count, the tone (danger when any check is Failed, else warn at three or more, neutral at one or two, ok at zero), the singular/plural noun and verb and entries (impact.transactionImpactCount||0) - is the single computed value behind the pill, its tone, the popover summary and the count, and attentionChecks plus the pc-more-badge count are deleted. Its div.pc-attention-pop-menu[role=region][aria-label=\"Before you close\"] (min(400px,92vw), right:0, top:calc(100% + 8px), z-index 30) holds div.pc-attention-pop-head (<b>Before you close</b> plus \"N checks need attention - M entries affected\", or All checks passed), <Checklist readiness onNavigate only/> and button.pc-attention-pop-all (Review all + IconArrowRight wired to menuRun(openBeforeClose)); Checklist gained the only prop, which filters Passed out so one renderer serves the popover and the pre-close drawer, and its small line reads \"<n> item(s) needs/need attention.\" openMenus() now queries .pc-more[open],.pc-kebab[open],.pc-filters-more[open],.pc-attention-pop[open], so the pill dismisses on an outside pointerdown or Escape like the other disclosures. src/period-closing.css replaced the five .pc .pc-attention rules with the pill block (.pc-attention-pop{position:relative;display:flex;flex:none;margin-right:7px} for the wider status gap, the 999px-radius/40px/13px-650 pill with its neutral palette, .warn rgba(234,88,12,.07)/#f0d3b8/#9a3412, .danger rgba(220,38,38,.06)/#f3c9cd/#b42318, hover and focus-visible ring) and the popover shell, keeping .pc-checklist flat with its small at the 12px floor and deliberately using no primary blue. src/period-closing-focus.css appended .pc-focused>.pc-heading{position:sticky!important;top:0!important;z-index:26!important;background:#f7f9fc!important;padding:10px 0 12px!important;box-shadow:0 1px 0 #e6ebf3} after the .pc-focused>.pc-heading reset, pinning the row inside the page's .pc scroller so the count stays visible while the register scrolls and returning its height when the details collapses. Presentational only: no storage key, close, lock, audit or enforcement rule changed, and Create Lock / Close remains the page's only close entry point - no close action lives in the popover. Verified with node tests/period-closing-ui.test.mjs 27/27 (the register contract renamed to \"...compact pre-close attention pill\", the pc-more-badge assertions replaced, \"opens from the attention pill\", plus a new contract \"the attention pill states the count once and opens a read-only review popover\"), node tests/journal-actions-ui.test.mjs 5/5 (its dismissal contract had to be widened to the four-selector openMenus() string - the one real defect this change caused), node tests/period-lock-settings-ui.test.mjs, node tmp/pc15-requests.mjs, node tmp/pc2-ssr.mjs (which had begun failing on a now-singular \"1 entry affected\"), node tmp/pc2-drawers.mjs 12 surfaces, node tmp/pc5-validate.mjs 17 surfaces, node tmp/check-all-jsx.mjs 49/49, node scripts/validate-knowledge-docs.mjs passed, and the full sweep FILES=64 green=50 tests=352 passed=341 failed=11 CANNOT-START(4) - the same eleven pre-existing failures, none in Period Closing. Not verified: no painted browser pass (in-app browser automation is denied in this environment) and no production build (npm run build cannot run because the esbuild binary cannot be spawned, spawn EPERM). The dev server at localhost:5173 serves the change live. Previous checkpoint, superseded in part: Period Closing request decisions, pagination, lock type and More actions. The Requests half of the register no longer prints a per-request status: RequestList dropped the pc-badge span that echoed request.status in the card header, so a card header carries only the period and the organisation and a pending request is identified by its decision footer. That footer is now one .pc-request-actions group - the quiet Cancel request (class pc-quiet, requester only) and the emphasised Approve Unlock (class primary, with IconCheck) as the last item - so Reject is gone and onDecide is only ever called with 'Approved'; decideUnlockRequest still accepts 'Rejected' and REQUEST_STATUS_FILTERS still lists it so the register filter keeps matching requests already stored with that status, and no engine, storage or audit rule changed. The list pages six at a time: REQUEST_PAGE_SIZE=6 with a useState(1) page number inside RequestList drives pageCount/active/visible, a list that fits one page renders whole, and only pageCount>1 renders nav.pc-request-pager with Previous, Page <n> of <n> and Next, disabling the step buttons at each end; period-closing.css gained .pc .pc-request-actions, .pc .pc-request-actions button.pc-quiet plus its hover, .pc .pc-request-actions button.primary, .pc-request-pager, .pc-request-pager button and .pc-pager-status, all at the 12px floor. Hard Lock / Soft Lock no longer reaches the screen: the Lock type row left the pc-detail-summary list in DetailsDrawer, while lockPeriod still validates ['Soft Lock','Hard Lock'], newForm / newLockForm still default lockType:'Hard Lock' and the Closed Type filter (All / Manual / Automatic) stays. The More actions button carries an IconChevronDown (class pc-more-caret) after the label and now hovers, focuses and reads as selected while open (.pc-more>summary:hover, .pc-more>summary:focus-visible, .pc-more[open]>summary with background #f2f7ff and a 3px #eaf1ff ring, and .pc-more[open]>summary .pc-more-caret{transform:rotate(180deg)}), its menu anchors at top:calc(100% + 6px) and caps at max-height:min(70vh,420px) with overflow-y:auto, and its rows gained a focus-visible ring and an icon colour transition; the asserted box parity with the Create Lock / Close button (40px minimum height, padding 0 15px, 7px icon gap, 7px radius, a 14px/600 nowrap label, flex 1 1 auto) is unchanged. One useEffect in PeriodClosing dismisses .pc-more, .pc-kebab and .pc-filters-more on a pointerdown outside them and on Escape, and removes both listeners on unmount. Presentational and interaction only: no storage key, close, lock, audit or enforcement rule changed. Previous checkpoint, superseded in part: Locked-periods register page. Header is one lean row: the page title - read from the shared terminology map (t.periodClosing), so the accountant view shows Period Closing and the business owner view shows Financial Lock, matching the sidebar label instead of a hardcoded Close Books - with a Journal-Entries-style subheading beneath it; the inline read-only policy chip was removed, so the locking policy is read only inside Period Closing Settings. The page actions are one More actions menu holding Current accounting period and Period closing settings, beside a separate primary Create Lock / Close button rendered while locking is on. Before you close is no longer a menu entry: it became a one-line attention strip under the heading and then, in the newest paragraph above, a compact attention pill in the heading action group; either way it opens the same BeforeCloseDrawer with the category chips and checklist, because its duplicated heading, its verification sentence and the pc-ready-pill readiness badge were removed. The page no longer renders a composite title line, a status line, a missing-periods row or banner, a create-missing-periods action, an Activity History block in the current-period card, or a Transactions, Audit log or Close with review link row. The current accounting period is no longer a page card: it opens from the first More actions entry as a 520px right-side drawer (pc-current-drawer) that renders the real CurrentPeriodPanel - the period, the date range, the organisation and branch scope, a Status badge and the one lock-aware action the status allows (Lock this period, Request Unlock, Admin override, or the Unlocked until note) plus View details - with a No current period empty state when nothing is configured. The register toolbar is one compact row inside div.pc-register-bar: the Locked Periods / Requests toggle (pc-view-toggle) on the left, and on the right the search box, the Filters disclosure and the row count. It is a div rather than a header because the legacy app-bar element rules in src/styles.css restyle any label or input inside any header, and @media(max-width:1050px){header>button:not(.primary):not(.hamb){display:none}} was hiding the close button of every drawer and modal below 1050px until .pc-history-drawer>header>button, .pc-modal>section>header>button and .pls-popup .pls-close were given display:inline-flex!important. The panel no longer prints a Locked periods / Unlock requests heading or an Every lock for <organisation> sentence (no caller passes a title or note any more, so that heading block is not rendered anywhere); the requests half renders only while approval is on, each half carries its own count, and switching halves resets the status and period filters. What the lock list may show follows the locking mode through the new engine helper lockRegisterRows: automatic locking keeps only locks whose period has already ended, manual locking keeps only periods whose lock was created here (an audit entry with Lock period created or Period closed), and locking off hides nothing. The lock history drawer, its Audit log / History menu entry and the allLockRows / historyFilters / HISTORY_FILTERS wiring were removed, so the filtered register is the only lock list. Period (listing only the periods the register can show) and Status (lock states in the locks view, request states in the requests view) now sit inside the Filters disclosure (pc-filters-more > pc-filters-panel) ahead of financial year (defaulting to the current year), organisation, branch, Closed By and Closed Type (Manual or Automatic, derived in the page from the stored lock reason prefix so no engine change was needed), and the advanced badge counts every filter that is set, so the visible row consumes as little space as possible. It lists Period, Organisation, Branch, Status, Closed Date, Closed By and Actions; the kebab menu offers View Requests, View Audit Log, View Details, Request Unlock and Close Period, and the lock history entry is gone. Closing verifies invoices posted, bills approved, draft journals, bank reconciliation and tax adjustments, then records the closer, timestamp, checklist snapshot and a Period closed audit event before dated posting is blocked; approved unlocks reopen a period as Reopened with expiry, a request carries a whole-period or contained date-range scope with bounded start and end dates, a pending request is rejected or approved from the Requests half through Reject / Approve Unlock - decided as the next authorised approver from unlockApproverFor(requestedBy, settings) when the acting role raised it, which the card states - and can be cancelled by its requester or an approver, expiry re-locks automatically and audits both Unlock expired and Period reclosed, policy changes are audited without rewriting existing locks, and every locking audit entry records actor, timestamp, action, organisation, branch, before/after values, reason and the related request id. Period Closing Settings (src/PeriodLockSettings.jsx) leads with one sentence and keeps one headingless settings card holding two real toggle switches - Enable automatic locking (only Automatic <-> Manual) and Require approval to unlock below a pls-rule divider - around the closing schedule and its read-only preview. The former Period locking card, the former Unlock approval card and the collapsed More policy settings block are gone, so the unlock controls (default and maximum unlock duration, unlock scope, automatic re-lock, daily auto-approval), the boundary detail, the default scope, allowed roles and exemptions keep their normalisePeriodSettings defaults without being rendered. The same stored lockingMode drives Settings -> Accounting -> Period control in src/AccountingSettingsPanel.jsx, whose new Period locking switch maps Off to Manual and whose Automatic period closing switch maps Automatic to Manual (it previously wrote the derived autoLockPreviousMonth mirror, which normalisePeriodSettings overwrote on save), with Closing frequency and Close after days pointed at schedule.frequency and schedule.lockAfterDays. The Create Lock / Close button beside More actions opens a mode-aware drawer: under manual locking it collects a period, a date and an optional reason and applies one lock to every module, while under automatic locking it resolves the next free period from the saved schedule and collects Frequency, Effective from, Lock after period end, Lock at and Notify before locking with a Next lock preview, and it disables the action when every period on the schedule is already locked.Drawers are portalled into document.body inside a pc-portal-scope wrapper (display:contents) and each one declares its own fixed right-side geometry - scrim at z-index 1390, drawer at z-index 1400, right:0, 620px for settings, 560px for lock, override, audit and details, 520px for unlock requests - with a fixed 18px/22px header and footer and pc-drawer-body as the only scroll container. The page heading uses the Journal Entries heading grid, each nesting level owns one gutter, and card headers reset the legacy fixed top-bar header styling. The heading action row is one size: the More actions disclosure is a summary rather than a button, so it is given the same box by hand (40px minimum height, padding 0 15px, 7px icon gap, 7px radius, the same border colour, a 14px/600 label that never wraps) as the Create Lock / Close button beside it, sits in a flex details box as flex 1 1 auto so it stretches with that button when the row wraps on narrow screens, and carries a 16px kebab icon. Presentational only: no storage key, close, lock, audit or enforcement rule changed. Its pc-kebab row action is now the documented canonical 3-dot row menu reused by the Journal Entries register and the Chart of Accounts table; nothing inside this module changed, and the kebab geometry was deliberately left byte-identical. The unlock Requests half of the locked-periods register is now table-only: the Cards / List switch, its right-aligned div.pc-request-view-bar and the whole article.pc-request-card rendering were deleted, so LockRegister no longer holds a requestLayout useState, RequestList takes no layout prop and always renders one div.pc-request-list around function RequestTable({requests,actions}) - a div.pc-table-fit pc-request-table > table, the same fit shell the locked-periods table uses (width:100%, table-layout:fixed, overflow:visible) so the register never scrolls sideways and a row menu is never clipped - with exactly five columns: Period (with the organisation as the cell small line), Reason, Requested, Requested By and Actions. The Unlock Scope and Approver columns left the table. function RequestActions({request,canApprove,approver,role,onDecide,onCancelRequest,onDetails}) rendered that whole Actions cell at the time; both its inline buttons and its Recorded as <approver> reviewer line have since been replaced, so the Actions cell is now the read-only View button plus the 4-entry menu described in the newest paragraph. That row menu is the canonical 3-dot menu - details.pc-kebab.pc-request-kebab with IconDots size 17 and div.pc-kebab-menu.pc-request-kebab-menu, which reuses the pick helper to close itself on selection and the page's existing outside-pointerdown / Escape dismissal because openMenus() already queries .pc-kebab[open]. View Details opens function RequestDetailsDrawer({request,onClose}): button.pc-history-scrim plus aside.pc-history-drawer.pc-request-drawer (aria-label \"Unlock request details\", width:min(460px,96vw)) whose pc-drawer-body holds dl.pc-request-facts listing Requested By, Requested, Reason, Unlock scope (resolved once by the new function requestScopeLabel), Approver and then Unlock duration, Access until and Approver remarks only when present; RequestList owns const [detail,setDetail]=useState(null) and portals it through div.pc pc-portal-scope. src/period-closing.css lost every .pc-request-card rule (including .pc-request-card dl in the 1100px media query), made .pc-request-list display:block;padding:0, added justify-content:flex-end;flex-wrap:wrap to .pc .pc-request-actions, added .pc .pc-request-kebab{flex:none;position:relative} with its hover and the .pc-request-facts grid at the 12px floor, widened .pc-requests-drawer to min(820px,96vw) and added .pc-request-drawer; src/period-closing-focus.css replaced the switch and scroll rules with the pager padding, th nowrap, td vertical-align:top, five th widths (19/25/13/13/30 percent), the Period small line, the actions-cell middle alignment with a wrapping note, and a @media(max-width:1100px) width reset, while the dead pc-request-none and pc-request-list-table markers were deleted. Presentational only: no storage key, service or engine change."
    },
    {
      "id": "items",
      "label": "Items",
      "component": "src/Items.jsx",
      "status": "Prototype only",
      "accountingImpact": "Stores revenue/purchase mappings and sales-document tax/price defaults"
    },
    {
      "id": "customers",
      "label": "Customers",
      "component": "src/Customers.jsx",
      "status": "Prototype only",
      "accountingImpact": "Stores receivable account mapping"
    },
    {
      "id": "vendors",
      "label": "Vendors",
      "component": "src/Vendors.jsx",
      "services": [
        "src/vendor-store.js"
      ],
      "styles": [
        "src/vendors.css"
      ],
      "status": "Prototype only",
      "accountingImpact": "Links each vendor sub-ledger to the Accounts Payable control account used by posted Purchase Bills"
    },
    {
      "id": "purchase-orders",
      "label": "Purchase Orders",
      "component": "src/Purchases.jsx",
      "detailComponent": "src/PurchaseDetails.jsx",
      "services": [
        "src/purchase-service.js"
      ],
      "styles": [
        "src/purchases.css",
        "src/purchase-workflow.css"
      ],
      "status": "Prototype only",
      "accountingImpact": "No posting; conversion is allowed after goods receipt and creates a Purchase Bill draft"
    },
    {
      "id": "goods-receipts",
      "label": "Goods Receipts",
      "component": "src/GoodsReceipts.jsx",
      "services": [
        "src/purchase-service.js"
      ],
      "styles": [
        "src/purchases.css",
        "src/goods-receipts.css"
      ],
      "status": "Prototype only",
      "accountingImpact": "No posting; controls received quantity available for billing"
    },
    {
      "id": "purchase-bills",
      "label": "Purchase Bills",
      "component": "src/Purchases.jsx",
      "detailComponent": "src/PurchaseDetails.jsx",
      "services": [
        "src/purchase-service.js",
        "src/invoice-engine.js",
        "src/period-locking.js",
        "src/DocumentPreview.jsx"
      ],
      "styles": [
        "src/purchases.css",
        "src/purchase-workflow.css",
        "src/document-preview.css"
      ],
      "status": "Prototype only",
      "accountingImpact": "Bill posting debits purchase/expense and Input GST and credits Accounts Payable; vendor payment debits Accounts Payable and credits Bank after period validation"
    },
    {
      "id": "purchase-debit-notes",
      "label": "Debit Notes",
      "component": "src/DebitNotes.jsx",
      "services": [
        "src/purchase-service.js",
        "src/invoice-engine.js",
        "src/period-locking.js",
        "src/DocumentPreview.jsx"
      ],
      "styles": [
        "src/purchases.css",
        "src/debit-notes.css",
        "src/document-preview.css"
      ],
      "status": "Prototype only",
      "accountingImpact": "Draft, approval and approved states do not post; Post debits Accounts Payable and credits purchase/expense and Input GST/Cess; Adjusted allocates the posted credit without a second journal; Reverse posts an equal/opposite journal"
    },
    {
      "id": "global-quick-create",
      "label": "Global Quick Create",
      "component": "src/QuickCreateMenu.jsx",
      "services": [
        "src/quick-create.js"
      ],
      "styles": [
        "src/quick-create.css"
      ],
      "status": "Prototype only",
      "accountingImpact": "Navigation only; routes to existing workflows and never posts directly"
    },
    {
      "id": "vendor-payments",
      "label": "Vendor Payments",
      "component": "src/VendorPayments.jsx",
      "services": [
        "src/purchase-service.js"
      ],
      "styles": [
        "src/purchases.css"
      ],
      "status": "Prototype only",
      "accountingImpact": "Debits Accounts Payable and credits Bank with bill/vendor traceability"
    },
    {
      "id": "purchase-reports",
      "label": "Purchase Reports",
      "component": "src/PurchaseReports.jsx",
      "services": [
        "src/purchase-reports.js"
      ],
      "styles": [
        "src/purchase-reports.css"
      ],
      "status": "Prototype only",
      "accountingImpact": "Read-only reporting over purchase bills; calculates vendor spend, input tax, payments and outstanding balances without posting"
    },
    {
      "id": "sales-orders",
      "label": "Sales Orders",
      "component": "src/SalesOrders.jsx",
      "services": [
        "src/sales-order-service.js",
        "src/SalesOrderActions.jsx"
      ],
      "styles": [
        "src/sales-orders.css",
        "src/sales-order-actions.css"
      ],
      "status": "Prototype only",
      "accountingImpact": "None until converted invoice is posted"
    },
    {
      "id": "invoices",
      "label": "Invoices",
      "component": "src/InvoiceWorkspace.jsx",
      "services": [
        "src/invoice-engine.js"
      ],
      "styles": [
        "src/invoice-workspace.css",
        "src/invoice-detail.css"
      ],
      "status": "Prototype only",
      "accountingImpact": "Posts receivable, revenue, output tax and round-off lines; details derive overview summaries from existing invoice, journal, receipt and payment records"
    },
    {
      "id": "credit-notes",
      "label": "Credit Notes",
      "component": "src/CreditNotes.jsx",
      "services": [
        "src/credit-note-service.js"
      ],
      "styles": [
        "src/invoice-workspace.css",
        "src/credit-notes.css"
      ],
      "status": "Prototype only",
      "accountingImpact": "Central mapping debits Sales Adjustment and Output GST Adjustment and credits Accounts Receivable; incomplete mapping blocks posting atomically"
    },
    {
      "id": "receipts",
      "label": "Receipts",
      "component": "src/Receipts.jsx",
      "services": [
        "src/receipt.js",
        "src/receipt-ui.js"
      ],
      "status": "Prototype only",
      "accountingImpact": "Posts cash/bank against receivable or customer advance"
    },
    {
      "id": "document-preview",
      "label": "Document Preview and Templates",
      "component": "src/DocumentPreview.jsx",
      "services": [
        "src/document-templates.js"
      ],
      "styles": [
        "src/document-preview.css",
        "src/document-preview-fix.css"
      ],
      "status": "Prototype only",
      "accountingImpact": "None"
    },
    {
      "id": "company-setup",
      "label": "Company Setup",
      "component": "src/CompanySetup.jsx",
      "status": "Prototype only",
      "accountingImpact": "Creates local bootstrap metadata"
    },
    {
      "id": "banking",
      "label": "Banking",
      "component": "src/Banking.jsx",
      "components": [
        "src/BankReconciliation.jsx",
        "src/BankingSettings.jsx"
      ],
      "services": [
        "src/banking-service.js",
        "src/invoice-engine.js"
      ],
      "styles": [
        "src/banking.css",
        "src/banking-shell.css",
        "src/banking-settings.css"
      ],
      "status": "Implemented prototype",
      "accountingImpact": "Bank masters link to Assets ledgers; priority matching and categorization suggestions do not post; saved reconciliation matching does not duplicate accounting; confirmed missing receipts or expenses create balanced journals; Banking Settings stores audited import, threshold and permission policies"
    },
    {
      "id": "budget-management",
      "label": "Budget Management",
      "component": "src/BudgetWorkspace.jsx",
      "services": [
        "src/budget-store.js",
        "src/budget-statements.js"
      ],
      "styles": [
        "src/budget-workspace.css"
      ],
      "status": "Prototype only",
      "accountingImpact": "Planning and analysis layer over posted journals; references Chart of Accounts accounts without modifying them and never posts journals. `Budgets` is one flat destination inside the Accounting sidebar section in src/Navigation.jsx, declared between Journal Entries and Period Closing (never a Budget sub-group, never a top-level section and never three flat Accounting entries), and the sidebar leaf is the only budget navigation because no budget screen renders a page-level tab row; the separate Budget Reports and Budget Settings screens are deleted, so the register, the create wizard and the plan detail tabs are the whole module; the Budgets register is a four-column grid (Budget Name, Financial Year, Budget Period, Actions) whose register card opens with the shared toolbar row - the search box, the financial year select and one Filters disclosure holding the other filters - above the grid, exactly like the Period Closing and Journal Entries registers and with no status card row above the card, so the page heading leads straight into it, and whose rows offer a read-only View Details button beside one 3-dot View budget / Edit / Duplicate / Delete menu right aligned in the last cell, where View budget opens the detail screen on its first statement and View Details opens it with its Budget vs Actual tab active through the initialTab prop and the per-tab remount key; Create Budget is a three-step page (Budget Details, Account Selection, Budget Allocation) whose Budget Details step is one three-column field grid (repeat(3,minmax(0,1fr))) whose first row is Budget name in the first column beside the multi-select organisation and branch scope (div.budgetScopeRow spanning the other two) and whose second row is the Financial year select, the Budget period select and the read-only div.budgetDuration field that reads the dates those two imply - p.budgetDurationRange printing 01 Apr 2026 → 31 Mar 2027 over p.budgetDurationMeta printing 12 months · Monthly from the pure budgetDateRange() / budgetDurationLabel() - so a standard period renders no date field at all, with the two input type=date fields bounded by financialYearRange() and rendered only for a Custom period, where budgetDateError() supplies the inline p.budgetDurationError the Next action refuses to pass, and the create page asks for no Description at all (the form keeps seeding form.description from a stored budget and the detail panel still prints it), with the multi-select organisation and branch scope stated once as a span.budgetScopeSummary chip (n Organisations · n Branches, aria-live=polite) in its head rather than a second p.budgetScopeHint Planning for ... line (that line and its two rules are deleted, while scope still travels with save({...form,scope})), whose Budget period select offers exactly Monthly, Quarterly, Half-Yearly, Yearly and Custom (BUDGET_PERIODS) and whose periodLabels(period,financialYear) reads a Yearly plan as the financial year rather than a second Total column, whose createBudget / updateBudget resolve the year, the period and the range in the one budgetTiming() helper so a stored plan can never hold dates that disagree with its period, whose Create Budget / Save Changes action stores startDate and endDate beside the existing scope - opened by the compact arrow-led back head the journal create page uses, one square button.budgetHeadBack, span.budgetHeadTitle and a neutral span.budgetStepCount chip reading Step n of 3 with no subtitle, above a numbered progress stepper (nav.budgetSteps > ol, the current step aria-current=step, a finished step class=complete showing IconCheck, a later step class=upcoming and disabled, and each 2px li:not(:last-child)::after connector reporting its own progress - var(--budget-blue) behind a complete step, a half linear-gradient(90deg,var(--budget-blue) 0,var(--budget-blue) 50%,#e1e6ed 50%,#e1e6ed 100%) behind the active step, #e8edf3 behind an upcoming one) rather than an underline tab row, whose footer.budgetWizardActions is one static right-aligned row (justify-content:flex-end, never a margin-right:auto split) grouping Cancel / Back immediately left of the one primary action, and its scope comes from the shared OrganisationBranchScope and organisation-context readers instead of a page-level scope list, whose template choices sit behind a switch, whose account step is one two-column .budgetAccountGrid table of every active posting account in the Chart of Accounts rather than only one budget type, grouped under its accounting type in the shared ACCOUNT_TYPE_ORDER order from src/terminology.jsx (Assets, Liabilities, Equity, Income, Expenses, plus an Other bucket for an unrecognised type), each type row named by accountTypeLabel(type,business) and carrying its count (n of m selected, or m accounts), and each account row a labelled checkbox whose budgetAccountCell prints the name over its code and account group, where the account cell is a label bound to its checkbox by a budget-account-<code> id, the table carries an aria-label, the headers are th scope=\"col\", the type rows are th scope=\"colgroup\" colSpan={2}, no cell ever takes a display other than table-cell (a flex td leaves the table layout algorithm and collapses to the 56px choose column), and the 12px code line uses #667085 for contrast, so the step reads as the Chart of Accounts itself and the separate Account Type column is gone, where each type row is a foldable button.budgetGroupCell inside its th scope=\"colgroup\" colSpan={2} (aria-expanded, a chevron, the accountTypeLabel name and n of m selected or m accounts) so a long chart of accounts stays readable, where div.budgetSelectionTools above the grid holds the search box, one select.budgetTypeFilter account-type filter built from ACCOUNT_TYPE_ORDER narrowed to the types on screen, Select all and Clear all (Select all adds every visible account and Clear all removes only the visible ones, so the filter narrows the grid and both buttons together), where span.budgetSelectedCount prints n accounts selected in the step head, and whose only forward action is Create Budget; the approval lifecycle is advanced from the budget detail screen through setBudgetStatus and the engine FLOW The allocation step groups its chosen accounts under BUDGET_ALLOCATION_BANDS - Profit & Loss (Income, Expenses) then Balance Sheet (Assets, Liabilities, Equity) - as tr.budgetAllocBand and tr.budgetAllocGroup rows, keeps the account column sticky with td.budgetStickyCol, folds a type's accounts through closedGroups, and its shortcut row is Copy Previous Year, Distribute Equally, Use Average Spending, then a label.budgetGrowthField Growth % number input (default 10) immediately ahead of Apply Growth %, then a label.budgetImportButton that hides an input type=file behind the same button and reads a CSV through parseBudgetCSV, then a button.budgetClearAll that empties form.allocations. The allocation step then closes with a section.budgetTotals statement-totals block (div.budgetTotalsGrid) that shares the allocation grid period columns plus one row total and renders eight statement lines - Total Revenue, Total Expenses, Net Profit / Loss, Profit Margin, Total Assets, Total Liabilities, Total Equity and Total Liabilities & Equity - in that order, every figure computed through the shared storage-free statement layer src/budget-statements.js (budgetTotalRows), which BudgetWizard calls directly so the allocation step and the statements can never disagree; the deleted Budget Mismatch Adjustment row and the profit-and-loss-equals-liabilities-and-equity repetition it described are gone, so never restore that row, its budgetTotalsMismatch class or its one-line explanation. The derived rows sit on #f1f5fb, a negative figure on #b42318, the table scrolls inside div.budgetTotalsGrid, and the four removed budgetReviewTotals chips (Total income budget, Total expense budget, Expected profit, Profit margin) must never come back. Below the totals, section.budgetBalanceCheck (class balanced or unbalanced) reads Total Assets against Total Liabilities + Equity through budgetBalanceCheck(accounts,allocations,period) and prints Total Assets, Total Liabilities + Equity and Balance Difference with one sentence (Budget is balanced, or that assets exceed liabilities and equity by an amount, or the reverse); the difference is reported only - never post it to a budget account, never store it and never give it a text input. The budget detail screen is itself the statement view, and src/budget-statements.js is the one layer both it and the allocation step read: it exports BUDGET_STATEMENTS (Profit and Loss, Balance Sheet, Cash Flow Statement), budgetPeriodTotals(accounts,allocations,period), budgetTotalRows(...) and budgetStatementRows(statement,accounts,allocations,period), returns the ordered section / type / group / account / groupTotal / typeTotal / grand rows, groups the planned accounts under their chart of accounts group (account.group||account.type) inside each accounting type, closes every group, type and statement with its own total row (the profit and loss ending on Net Profit / Loss, the balance sheet on Total Liabilities & Equity and then a kind:'check' Balance Difference row that is reported and never stored or posted, and the cash flow on Net Cash Flow), and signs an expense or an asset purchase negative on the cash flow; the detail tab row is one flat eight-tab list (const BUDGET_DETAIL_TABS=[['Overview','Overview'],['Profit and Loss','Profit & Loss'],['Balance Sheet','Balance Sheet'],['Cash Flow Statement','Cash Flow'],['Budget vs Actual','Budget vs Actual'],['Accounts','Accounts'],['Activity History','Activity History'],['Revisions','Revisions']], a key and a printed label per tab, with const BUDGET_DETAIL_DEFAULT='Overview', which the detailTab state and the detail back() reset both use, so the detail screen opens on the Overview while the register's read-only View Details button still opens the Budget vs Actual tab through the register-level view() handler) while every view stays reachable, the exported BudgetStatementView renders those rows into the existing div.budgetAllocationGrid shell (section / type / group rows as full-width th scope=\"rowgroup\" colSpan={labels+2} bands, account rows printing the name over its code in the accounting view, total rows as th scope=\"row\" labels beside right-aligned figures, and the budgetEmpty sentence when nothing is planned), the flat AllocationTable component and its Budget Details tab were deleted because the statements render the same accounts, amounts and totals grouped, and the appended .budgetStatement block in src/budget-workspace.css styles the bands (#f4f7fc / #fbfcfe / #536176), the totals (#f8fafd), the derived totals (#f1f5fb), the mismatch (5% amber #fff6ec on #8a4b09) and a negative figure (#b42318) with nowrap figures and a 12px floor. Never reintroduce a page-private totals chain or restore the deleted flat tab. The detail screen's Overview is the default tab and renders the new BudgetOverview block: the four KPI cards (Revenue Budget, Expense Budget, Expected Profit / Loss, Budget Utilization), then BudgetDetailsPanel (Budget name, Financial year, Budget period, Budget scope, Start and end date, Budget type, Description and the selected accounts grouped under their chart of accounts group), then the budgetHealth bars for Revenue and Expenses against posted actuals with the Profit / Loss result and the projected year end, then budgetAlertsPanel; the KPI cards left the page head. The detail page head is the full-bleed arrow-led back head the Create Budget wizard uses: div.budgetV3Head.budgetDetailHead holds one square button.budgetHeadBack (aria-label and title Back to budgets, the horizontal IconArrowLeft at 19px), a span.budgetHeadTitle reading Budget Details and a span.budgetHeadHint reading Back to budgets, and it sits flush beneath the working-context organisation and branch bar at the header width - the page drops the shell padding and max-width through .app main:has(>.budgetV3.budgetDetailV3){max-width:none;padding:0} and .app main>.budgetV3.budgetDetailV3{max-width:none;margin:0;padding:0 0 28px}, hands the clamp(16px,2.2vw,32px) gutter back to every other child through :not(.budgetV3Head), and repeats the wizard head geometry (grid 40px minmax(0,1fr), min-height:56px, padding 8px 24px, no side borders, no radius) with a sub-760px 36px arrow and 8px 12px padding. button.budgetBack and its CSS rule are deleted, so never restore that inline text back link on either budget screen. Below the head div.budgetDetailIdentityRow is a wrapping space-between flex row: div.budgetDetailIdentity holds div.budgetDetailName (the h1 budget name with the Status badge beside it, so the lifecycle reads at the name rather than inside an action button) over p.budgetDetailMeta (financialYear · period · scopeSummary(scope) · type), and div.budgetDetailActions is right-aligned (justify-content:flex-end, gap 9px) carrying exactly one button.primary for the next allowed status from statusAction followed by one details.budgetDetailMore labelled More actions. That summary.budgetDetailMoreBtn is a labelled 38px control (padding 0 15px, 7px icon gap, 14px/600 nowrap) with the horizontal IconDots and a budgetDetailMoreCaret rotating 180deg on open, and .budgetDetailMoreMenu reuses the canonical right-aligned panel (top calc(100% + 6px), 216px minimum, padding 6px, 9px radius, shadow, 36px rows, #f4f7ff / #245fd9 hover) offering Edit, Duplicate, Export, Archive and the destructive Delete (class budgetKebabDanger). Delete is wired to the detail screen's own remove prop, which confirms, calls the engine deleteBudget(state,selected) and then clears the selection and returns to the register, because a deleted budget is only archived rather than removed from storage. Every entry closes the menu through the shared menuRun(handler) helper, and the detail screen keeps its own page-level pointerdown / Escape dismissal effect over .budgetDetailMore[open]. Never restore the separate Edit / Duplicate / Export / Archive button row in the head or move the status back into an action button. Budget vs Actual and the Accounts tab both read computeVarianceRows, whose rows are {account,budget,actual,variance,percent,achievement,direction} with variance as actual - planned, percent as the Variance % and achievement as the separate Actual-over-Budget column, and colour through varianceDirection(type,variance) (favourable #19734c, unfavourable #b42318, neutral #475467, inverting for an expense account so a lower spend reads favourable); never label an achievement figure as a Variance %. The Accounts tab is BudgetAccountsTable (Organisation, Branch, Account Type, Account and Period filters over Account, Account Type, Budget, Actual, Variance and Variance %), Activity History lists the logged budget events (created, edited, accounts, allocation, status, export, archive) with their actor and time, and Revisions lists the stored versions with their date, author and total budget; AccountTree and ForecastPanel are deleted, so never restore them for those tabs. Budgets remain a planning layer: they reference Chart of Accounts accounts, never create accounts and never post journals, and the balance difference is reported rather than stored or posted."
    },
    {
      "id": "operations",
      "label": "Expenses, Assets, Tax, Budgeting and Reports",
      "component": "src/OperationalModules.jsx",
      "services": [
        "src/operations-store.js",
        "src/invoice-engine.js"
      ],
      "styles": [
        "src/operational-modules.css"
      ],
      "status": "Prototype only",
      "accountingImpact": "Expense and depreciation actions post balanced journals through the shared engine; master, tax and budget data remains browser-local prototype data"
    },
    {
      "id": "settings",
      "label": "Settings Center",
      "component": "src/SettingsWorkspace.jsx",
      "services": [
        "src/settings-store.js",
        "src/period-locking.js"
      ],
      "styles": [
        "src/settings-workspace.css",
        "src/settings-portal.css"
      ],
      "status": "Partially implemented",
      "accountingImpact": "Versioned future-behavior preferences, dependency validation and document numbering previews; complete command-layer enforcement requires backend integration"
    },
    {
      "id": "help-center",
      "label": "Help Center",
      "component": "src/HelpCenter.jsx",
      "components": [
        "src/ContextualHelp.jsx"
      ],
      "services": [
        "src/help-content.js",
        "src/page-knowledge.js"
      ],
      "styles": [
        "src/help-center.css",
        "src/contextual-help.css"
      ],
      "status": "Implemented prototype",
      "accountingImpact": "Read-only centralized page, workflow, status, accounting-impact, blocker and troubleshooting guidance; no posting mutation"
    },
    {
      "id": "audit-log",
      "label": "Audit Log",
      "component": "src/AuditLog.jsx",
      "services": [
        "src/audit-log.js"
      ],
      "styles": [
        "src/audit-log.css"
      ],
      "status": "Implemented prototype",
      "accountingImpact": "Read-only cross-module traceability for browser persistence changes; not a production immutable audit store"
    },
    {
      "id": "other-navigation",
      "label": "Expenses, Assets, Tax, Budgeting and Reports placeholders",
      "component": "src/App.jsx",
      "status": "Planned",
      "accountingImpact": "Not implemented as complete accounting modules"
    },
    {
      "id": "period-lock-settings",
      "label": "Period Locking Settings",
      "component": "src/PeriodLockSettings.jsx",
      "service": "src/period-locking.js",
      "styles": [
        "src/period-lock-settings.css"
      ],
      "status": "Partially implemented",
      "accountingImpact": "2026-09-14: ScopeSelector reads the shared organisation/branch scope module. It still calls scopeVisibility({organisations,companyIds,branchIds}) exactly once, but that helper now comes from src/organisation-scope.js (re-exported by src/period-locking.js), and the branch cell and branch clearing use getBranchesForOrganisation(row.companyId,organisations) and branchReference() with the NO_BRANCH em dash instead of a private organisations.find(...).branches lookup. Exactly one scopeVisibility( call remains in the file and the popup still renders no ScopeSelector, so tests/period-lock-settings-ui.test.mjs stays at 16/16 and tests/period-scope-visibility.test.mjs at 16/16. Presentational wiring only: no policy field, storage key or engine rule changed. Previous checkpoint, superseded in part: Right-side locking policy popup opened from the More actions menu on Period Closing and written to wayvida-period-settings-v1. The panel holds one headingless settings card with the whole policy - no card heading, no description and no read-only policy chip, so the popup runs straight from its one-sentence lead into the switches: an Enable automatic locking switch that only moves lockingMode between Automatic and Manual, the Frequency, Effective from, Lock after period end, Lock at and Notify before locking fields (disabled while locking is manual or off; while the stored mode is Off the switch itself is disabled at 55 percent with a note pointing at Settings -> Accounting -> Period control), the read-only generated schedule, a pls-rule divider and the Require approval to unlock switch. The separate Period locking card, the separate Unlock approval card and the collapsed More policy settings block were removed, so the frequency boundary detail, organisation timezone, unlock defaults, default organisation and branch scope, allowed roles, module exemptions and read-only enforcement coverage list are no longer rendered and keep their normalised engine defaults. Both switch rows are real toggle controls: label.pls-switch with a visually hidden input[type=checkbox][role=switch] and an i track. Keeps a close button, a sticky Save changes and Cancel footer and a dirty-state guard, and renders as a popup panel so the host drawer owns the geometry. The engine keeps its three-state lockingMode and the two switches only map onto it. It is a second editor of the same policy record src/AccountingSettingsPanel.jsx already edits, and browser-local rather than authenticated protection.The popup keeps position:static inside the host drawer, so the drawer owns the scroll while the panel heading and the Save changes / Cancel footer stay sticky, the policy cards stack in a single column and each generated-schedule row collapses to two columns. No card header remains; the popup keeps only its own panel heading, so the legacy fixed top-bar header styling is neutralised for that heading and the inert .pls-card>header resets stay declarative only. The Period Locking Settings drawer is the deliberate exception: it keeps the canonical .pc-history-drawer shell class without the .pc scope wrapper, so the page base-control rules (.pc button, .pc input, .pc select) cannot reach the panel, and period-lock-settings.css neutralises the legacy global top-bar header rule for its own headings (.pls header{position:static;inset:auto;width:auto;height:auto;min-height:0;max-width:none;z-index:auto;margin:0}) so the .pls-card>header element stops inheriting position:fixed and height:58px (which used to stack the card titles on each other at the top of the viewport). The reset also matches the host .pc-history-drawer>header, which changes nothing because period-closing.css already declares that header position:static!important - the drawer header is a static flex row with pc-drawer-body as the only scroll container."
    },
    {
      "id": "organisation-scope",
      "label": "Organisation and branch scope",
      "component": "src/organisation-scope.js",
      "service": "src/organisation-scope.js",
      "styles": [],
      "status": "Implemented prototype",
      "accountingImpact": "2026-09-14: Added as the single implementation of the organisation/branch scope rule for the accounting module, consumed by Chart of Accounts (src/EnterpriseAccountForm.jsx, src/AccountWorkspace.jsx), Create Journal Entry -> Journal Lines (src/JournalEntriesPro.jsx), Period Locking settings (src/PeriodLockSettings.jsx) and Create Lock (src/PeriodClosing.jsx). getScopeVisibility({organisations,organisationIds|companyIds,branchIds,label}) returns {organisations,rows,organisationCount,orgCount,anySelectedOrgHasMultipleBranches,showOrganisation,showBranch,showOrgColumn,showBranchColumn,strip,branchRequired} where showOrganisation is orgCount>1 and showBranch is orgCount>1 || anySelectedOrgHasMultipleBranches; rows carry organisationId, companyId, organisationName, branchId, branchName, branchOptions, singleBranch, branchRequired, noBranches and allBranches. getBranchesForOrganisation(reference,organisations) is the only branch lookup, organisationBranches(organisations) annotates each branch with its owning organisation, selectedOrganisations(references,organisations) resolves the scope list, normaliseScope auto-fills a one-option branch and clears a foreign one, validateScope returns OK / NO_SCOPE / UNKNOWN_ORGANISATION / BRANCH_NOT_IN_ORGANISATION / BRANCH_REQUIRED, assertValidScope throws the message, scopeLabel composes Organisation · Branch, NO_BRANCH is an em dash for a branchless organisation and ALL_BRANCHES is All branches. Neither React nor storage: it stays free of side effects, and the interactive control lives in src/OrganisationBranchScope.jsx. Pure data: no React, no storage, no side effects. src/period-locking.js deleted its private scopeVisibility / normaliseScope bodies and re-exports these helpers instead, and the same module decides whether the Chart of Accounts grid renders Organisation / Branch columns. The Chart of Accounts domain layer enforces the same rule: src/account-master.js changeAccount(state,action,payload,external,organisations) runs validateScope straight after its branch-presence guard (the tolerated 'default' organisation code is filtered out and the check no-ops when no organisations are supplied), and src/AccountWorkspace.jsx passes demoOrganizations through both calls, so a branch that belongs to another organisation is rejected at the reducer, not only hidden in the picker. scopePickerState({organisations,companyIds,branchIds,label}) returns the state an organisation/branch picker renders from - showOrganisation, showBranch, the resolved companyId / branchId / branchLabel, organisationOptions, branchOptions (always the owning organisation branches), the read-only line and allBranches - so one control serves the journal lines, the Create Lock drawer and the request unlock drawer. No accounting rule, storage key or posting path changed."
    },
    {
      "id": "organisation-branch-scope-ui",
      "label": "Organisation and branch picker",
      "component": "src/OrganisationBranchScope.jsx",
      "service": "src/organisation-scope.js",
      "styles": [
        "src/period-closing.css"
      ],
      "status": "Implemented prototype",
      "accountingImpact": "2026-09-14: The interactive half of the shared organisation/branch scope rule, and the same control the Create Journal Entry journal lines use. OrganisationBranchScope renders from scopePickerState() in src/organisation-scope.js and holds no scope logic of its own (it imports only ALL_BRANCHES, normaliseScope and scopePickerState; no flatMap branch filtering and no local organisation filtering). One organisation with one branch renders no selector at all and states the context line; one organisation with several branches renders the Branch list alone, with All branches first so a whole-organisation period lock is never silently narrowed; several organisations render Organisation and Branch with the branch list filtered to the owning organisation; a branchless organisation keeps the em dash. onChange emits companyId, branchId, companyIds, branchIds and scopeType through normaliseScope, so changing organisation clears an incompatible branch. Used by src/PeriodClosing.jsx in the Create Lock / Close drawer (with selectors, showLine={false} because the popup header no longer prints a scope chip, writing straight into createLockWindow) and in the request unlock drawer (showFields={false}, stating the locked scope because requestPeriodUnlock derives a request scope from the locked period). Explicit validation: scopePickerState returns the no organisation selected line for an empty scope instead of throwing. No lock, close, audit or posting rule changed - the picker only writes the lock form fields that already existed."
    },
    {
      "id": "organisation-context",
      "label": "Working organisation context",
      "component": "src/organisation-context.js",
      "service": "src/organisation-context.js",
      "styles": [],
      "status": "Implemented prototype",
      "accountingImpact": "2026-09-14: The storage-backed available organisation list, extracted from src/JournalEntriesPro.jsx so Create Journal Entry and Period Closing share one reader instead of two copies. Exports readOrgSetting, readOrgList, getAccessibleOrganizations and getCurrentOrganizationContext, reading wayvida-context-companies / wayvida-context-branches with the wayvida-demo-company / wayvida-demo-branch fallback, filtering demoOrganizations to the working scope and falling back to the full list when the scope is empty or unreadable (try/catch). Expressions are byte-identical to the previous in-page copies, so no behaviour changed; src/JournalEntriesPro.jsx imports them and dropped its duplicated const lines plus the now-unused demoOrganizations import, and src/PeriodClosing.jsx resolves a scopeOrganisations memo from getAccessibleOrganizations()."
    }
  ],
  "navigation": {
    "source": "src/Navigation.jsx",
    "topLevel": [
      "Dashboard",
      "Accounting",
      "Inventory",
      "Sales",
      "Purchases",
      "Banking",
      "Expenses",
      "Assets",
      "Tax & Compliance",
      "Reports",
      "Settings"
    ],
    "budgetPages": [
      "Budgets"
    ],
    "routeMechanism": "In-memory active page selected by App.jsx with query-string compatibility; not React Router",
    "periodClosingCaveat": "Rendered from the existing navigation shell; browser-local controls are not production authorization",
    "budgetDestination": {
      "label": "Budgets",
      "parent": "Accounting",
      "position": "Between Journal Entries and Period Closing",
      "note": "One flat leaf inside the Accounting sidebar section since 2026-09-15. The Budget sub-group and the separate Budget Reports and Budget Settings pages are deleted - their components, their page routes, their styles and their App.jsx routing names are gone - so the register, the create wizard and the plan detail tabs are the whole module."
    }
  },
  "persistence": {
    "kind": "Browser localStorage and sessionStorage",
    "productionDatabase": false,
    "keys": [
      {
        "key": "wayvida-budgets-v4",
        "owner": "src/budget-store.js",
        "content": "budget masters, account mappings, allocations, revisions, settings and activity history"
      },
      {
        "key": "wayvida-accounting-v1",
        "owner": "src/invoice-engine.js",
        "content": "accounts, configuration, invoices, journals and accounting state"
      },
      {
        "key": "wayvida-accounts-v2",
        "owner": "src/account-store.js",
        "content": "Chart of Accounts workspace state"
      },
      {
        "key": "wayvida-coa-view",
        "owner": "src/AccountWorkspace.jsx",
        "content": "Chart of Accounts view preference"
      },
      {
        "key": "wayvida-terminology-view",
        "owner": "src/terminology.jsx",
        "content": "Global Business Owner or Accountant terminology preference; browser-local rather than authenticated per-user storage"
      },
      {
        "key": "wayvida-customers",
        "owner": "src/Customers.jsx",
        "content": "customer records"
      },
      {
        "key": "wayvida-vendors-v1",
        "owner": "src/vendor-store.js",
        "content": "vendor master, tax/payment configuration, opening balance metadata and audit timestamps"
      },
      {
        "key": "wayvida-purchase-orders-v1",
        "owner": "src/purchase-service.js",
        "content": "non-posting purchase orders and PO-to-bill linkage"
      },
      {
        "key": "wayvida-goods-receipts-v1",
        "owner": "src/purchase-service.js",
        "content": "non-posting PO receipt quantities and traceability"
      },
      {
        "key": "wayvida-purchase-bills-v1",
        "owner": "src/purchase-service.js",
        "content": "purchase bill drafts, calculated totals, payment projection and posting references"
      },
      {
        "key": "wayvida-debit-notes-v1",
        "owner": "src/purchase-service.js",
        "content": "bill-linked purchase debit note drafts, issued vendor credits, applications, journal and reversal references"
      },
      {
        "key": "wayvida-banking-v1",
        "owner": "src/banking-service.js",
        "content": "bank account masters, imported statement transactions, matches, saved reconciliation lifecycle/locks, matching rules and banking audit trail"
      },
      {
        "key": "wayvida-operations-v1",
        "owner": "src/operations-store.js",
        "content": "expense categories, expenses, asset register, asset transactions, GST and TDS demo registers, budgets and operational audit metadata"
      },
      {
        "key": "finance-erp-items",
        "owner": "src/Items.jsx",
        "content": "item records"
      },
      {
        "key": "wayvida-sales-orders",
        "owner": "src/SalesOrders.jsx",
        "content": "sales orders"
      },
      {
        "key": "wayvida-companies-v2",
        "owner": "src/CompanySetup.jsx",
        "content": "company setup records"
      },
      {
        "key": "wayvida-active-company",
        "owner": "src/CompanySetup.jsx",
        "content": "active company id"
      },
      {
        "key": "wayvida-demo-company",
        "owner": "src/HeaderOrgSelectors.jsx",
        "content": "header company selection"
      },
      {
        "key": "wayvida-demo-branch",
        "owner": "src/HeaderOrgSelectors.jsx",
        "content": "header branch selection"
      },
      {
        "key": "finance-erp-nav-theme",
        "owner": "src/Navigation.jsx",
        "content": "dark/light navigation preference"
      },
      {
        "key": "wayvida-period-control-v1",
        "owner": "src/period-locking.js",
        "content": "prototype periods with their lock, unlock window and lifecycle fields, unlock requests with scope, decision, cancellation and expiry stamps, override records and locking audit entries"
      },
      {
        "key": "wayvida-period-settings-v1",
        "owner": "src/period-locking.js",
        "content": "prototype locking policy: mode, automatic schedule, backdating, approval, unlock duration and scope, default scope, allowed roles, module exemptions and policy metadata"
      },
      {
        "key": "wayvida-settings-v1",
        "owner": "src/settings-store.js",
        "content": "versioned organization, accounting, transaction, numbering, operational, tax, notification, automation and security preferences"
      },
      {
        "key": "wayvida-global-audit-v1",
        "owner": "src/audit-log.js",
        "content": "latest 5000 cross-module create, update, status-change, remove and configuration-change events"
      },
      {
        "key": "wayvida-open-account",
        "storage": "sessionStorage",
        "owner": "src/AccountWorkspace.jsx",
        "content": "account detail selection"
      },
      {
        "key": "wayvida-open-invoice",
        "storage": "sessionStorage",
        "owner": "src/SalesOrderActions.jsx",
        "content": "converted invoice selection"
      },
      {
        "key": "wayvida-manual-journals-v2",
        "owner": "src/JournalEntriesPro.jsx",
        "content": "manual journal records with lines, lifecycle status, attachments and audit events; separate from the posted journals written by the posting engine"
      },
      {
        "key": "wayvida-journal-templates-v1",
        "owner": "src/journal-templates.js",
        "content": "reusable journal templates (name, journal type, lines) plus an optional repeat label; presentation-only and never read by accounting"
      },
      {
        "key": "wayvida-journal-account-prefs-v1",
        "owner": "src/journal-templates.js",
        "content": "recent and favourite account codes for the journal account picker; presentation-only"
      }
    ]
  },
  "accountingRules": {
    "moneyRepresentation": "Integer paise in accounting engines",
    "balancedJournal": "Total debits must equal total credits",
    "invoicePosting": "Debit Accounts Receivable; credit revenue and output GST/Cess; apply configured round-off line",
    "receiptPosting": "Debit cash/bank; credit Accounts Receivable and, for excess, customer advance",
    "creditNotePosting": "Debit sales return/adjustment and output tax; credit Accounts Receivable",
    "salesOrderPosting": "No journal entry; conversion produces an invoice draft",
    "purchaseOrderPosting": "No journal entry; conversion produces a Purchase Bill draft",
    "purchaseBillPosting": "Debit purchase/expense and Input GST/Cess; credit Accounts Payable after period validation",
    "purchaseDebitNotePosting": "Debit Accounts Payable; credit purchase/expense and Input GST/Cess reversal; apply against the linked bill after period validation",
    "immutability": "Posted history should be corrected by reversal or adjustment, not destructive mutation",
    "idempotency": "Posting commands must not duplicate journals for the same operation",
    "dimensions": "Branch and cost centre requirements are account/configuration-driven",
    "periodLocking": "Planned production invariant; current period-lock prototype is not integrated into posting commands"
  },
  "tests": {
    "framework": "Node built-in test runner",
    "files": [
      "tests/account-detail-drawer-ui.test.mjs",
      "tests/account-group-picker-ui.test.mjs",
      "tests/account-group-search.test.mjs",
      "tests/account-master.test.mjs",
      "tests/account-settings.test.mjs",
      "tests/account-sticky-grid-ui.test.mjs",
      "tests/account-templates.test.mjs",
      "tests/account-ui.test.mjs",
      "tests/audit-log.test.mjs",
      "tests/banking-ui.test.mjs",
      "tests/banking.test.mjs",
      "tests/budget-create-flow-ui.test.mjs",
      "tests/budget-navigation-ui.test.mjs",
      "tests/budget-register-ui.test.mjs",
      "tests/budget-statements.test.mjs",
      "tests/budget-store.test.mjs",
      "tests/credit-note.test.mjs",
      "tests/daybook.test.mjs",
      "tests/document-preview-ui.test.mjs",
      "tests/document-templates.test.mjs",
      "tests/general-ledger-ui.test.mjs",
      "tests/invoice-detail-ui.test.mjs",
      "tests/invoice-engine.test.mjs",
      "tests/item-tax-ui.test.mjs",
      "tests/journal-actions-ui.test.mjs",
      "tests/journal-business-view.test.mjs",
      "tests/journal-create-ui.test.mjs",
      "tests/journal-detail-actions.test.mjs",
      "tests/journal-detail-ui.test.mjs",
      "tests/journal-entries-ui.test.mjs",
      "tests/journal-heading-count-ui.test.mjs",
      "tests/journal-line-columns.test.mjs",
      "tests/journal-templates.test.mjs",
      "tests/operational-modules-ui.test.mjs",
      "tests/operations-store.test.mjs",
      "tests/organisation-branch-scope-ui.test.mjs",
      "tests/organisation-scope.test.mjs",
      "tests/period-closing-ui.test.mjs",
      "tests/period-lock-settings-ui.test.mjs",
      "tests/period-locking.test.mjs",
      "tests/period-scope-visibility.test.mjs",
      "tests/period-unlock-scope.test.mjs",
      "tests/purchase-reports.test.mjs",
      "tests/purchase-service.test.mjs",
      "tests/quick-create.test.mjs",
      "tests/receipt-enhancements.test.mjs",
      "tests/receipt-ui.test.mjs",
      "tests/receipt.test.mjs",
      "tests/sales-order.test.mjs",
      "tests/sites-worker.test.mjs",
      "tests/ui-quality.test.mjs",
      "tests/vendor-store.test.mjs",
      "tests/vendor-ui.test.mjs"
    ],
    "periodClosingCoverage": true,
    "lastRecordedResult": " Validation actually executed: npm run docs:check passed; navigation-structure 2/2; ui-quality 5/5; sites-worker 3/4 until dist exists. npm install restored 105 packages and refreshed package-lock.json. Unresolved: vite build failed to resolve react-is from recharts, so Sites packaging artifacts were not regenerated. 2026-09-15: Unused local junk was removed. Every src/ module remains referenced, so no product source or required knowledge document was deleted. Removed tmp/ scratch, browser profiles, .pnpm-store/, empty qa-evidence/, generated dist/, unused screenshots/ and zip, leftover QA PNGs, the accidental const-a file, historical design-qa.md, unused vite.sandbox.config.mjs, and leftover pnpm-lock.yaml / pnpm-workspace.yaml. .gitignore now excludes those artifact classes. No routing, storage, accounting, or UI behaviour changed. 2026-09-15: Budgets is the only Budget destination inside Accounting. On the user instruction the Budget sub-group and the separate Budget Reports and Budget Settings entries left the Accounting section, so src/Navigation.jsx declares {label:'Accounting',icon:IconBook,children:[leaf('Chart of Accounts'),leaf('Journal Entries'),leaf('Budgets'),leaf('Period Closing')]} and the two page names can no longer be active. src/BudgetWorkspace.jsx deleted the BudgetReports and BudgetSettings components and their two page routes and dropped the IconFilter, BUDGET_PERMISSIONS and emptyBudgetState imports, src/budget-workspace.css deleted the 24 rules those screens owned (report filters/grid/card/title, variance columns, performance and progress, settings grid, permission grid, switch) and trimmed its two media queries, src/App.jsx routes the page with a==='Budgets' alone, and the plan Overview alert hint was reworded from Alerts follow the thresholds in Budget Settings. to Alerts follow the saved alert thresholds. because it no longer has a Settings screen to point at. The register, the create wizard, the plan detail tabs, the store, the storage keys and every budget calculation are untouched. Tests: navigation-structure 2/2, budget-navigation-ui 3/3, budget-register-ui 7/7, budget-create-flow-ui, budget-store, budget-statements, tmp/budget-nav-ssr.mjs, tmp/check-all-jsx.mjs 50/50, docs check passed. Not verified: no painted browser pass (in-app browser automation is denied in this environment) and no production build (npm and npx are absent and esbuild cannot spawn, so vite build fails with spawn EPERM). Previous checkpoint, superseded in part: Budget group nested back inside Accounting. On the user instruction the Budget section left its top-level sidebar slot between Accounting and Inventory and now sits inside the Accounting section between Journal Entries and Period Closing, so src/Navigation.jsx declares {label:'Accounting',icon:IconBook,children:[leaf('Chart of Accounts'),leaf('Journal Entries'),{label:'Budget',icon:IconChartBar,children:[leaf('Budgets'),leaf('Budget Reports'),leaf('Budget Settings')]},leaf('Period Closing')]} and the Inventory section is back to a single Items child; the three page names, the App.jsx routing list and welcome-block exclusion, the BudgetWorkspace remount key and the shared nav.budgetWorkspaceTabs row are unchanged, and the rendered group id is Accounting/Budget (nav-Accounting-Budget) instead of Budget (nav-Budget). Tests: navigation-structure 2/2, budget-navigation-ui 3/3, tmp/budget-nav-ssr.mjs, tmp/check-all-jsx.mjs 50/50, docs check passed. Not verified: no painted browser pass (in-app browser automation is denied in this environment) and no production build (npm and npx are absent and esbuild cannot spawn, so vite build fails with spawn EPERM). Previous checkpoint, superseded in part: Create Budget stepper, scope line and footer. On the user instruction the details step states its plan scope once instead of twice, the progress stepper reports each step state on its own connector, and the footer groups its two buttons on the right. src/BudgetWorkspace.jsx deleted the second p.budgetScopeHint line (Planning for <organisation> · <branch>., or No organisation is in scope yet. Choose one in the header first.) so the step states its width once through the span.budgetScopeSummary chip in the budgetSelectHead; the scope value is still built from scopePickerState() and still travels with save({...form,scope}). src/budget-workspace.css deleted the two dead .budgetScopeHint rules, gave the 2px li:not(:last-child)::after connector one state per step (var(--budget-blue) behind li.complete, a half linear-gradient(90deg,var(--budget-blue) 0,var(--budget-blue) 50%,#e1e6ed 50%,#e1e6ed 100%) behind li.active, #e8edf3 behind li.upcoming) and removed the .budgetWizardActions button:first-child:not(.primary){margin-right:auto} split so Cancel / Back sit immediately left of the one primary action in the static right-aligned footer. tests/budget-create-flow-ui.test.mjs now asserts the hint is gone, the two new connector tokens and the removed split; tmp/budget-create-ssr.mjs asserts the hint is gone and the rendered stepper reads active/upcoming/upcoming on step one and complete/complete/active on the last step. No storage key, engine function, validation rule, allocation, statement, variance, terminology or accounting behaviour changed. Previous checkpoint, superseded in part: 2026-09-15: Budget details field order. On the user instruction the Create Budget - Budget Details step drops its Description field and reorders the inputs to Budget name with the organisation and branch scope on the first row over Financial year, Budget period and Budget duration on the second. src/BudgetWorkspace.jsx replaced the div.budgetDurationScope wrapper and the Description label with one div.budgetSetupFields grid holding exactly five fields - the budget name label, the div.budgetScopeRow scope control, and the Financial year, Budget period and div.budgetDuration blocks - so form.description still seeds from a stored budget (initial.description||'') and the BudgetDetail panel still prints it when set. src/budget-workspace.css moved the wizard grid to repeat(3,minmax(0,1fr)), gave .budgetScopeRow grid-column:span 2, restyled .budgetDuration as a borderless wrapper over the shared .budgetDurationRange field skin, and deleted the .budgetDurationScope* rules, the .budgetFieldWide span rule and the .budgetSetupNote textarea rules. tests/budget-create-flow-ui.test.mjs now asserts the five-field grid, the absent textarea and the rendered order. Previous checkpoint, superseded in part: 2026-09-15: Budget duration row. On the user instruction the derived Budget duration and the organisation and branch scope now read as one horizontal row on the Budget Details step. src/BudgetWorkspace.jsx wrapped the div.budgetDuration field and the div.budgetScopeRow in a new div.budgetDurationScope that carries a custom modifier class while the period is Custom, and re-indented both blocks inside it, so the read-only duration is the first column and the Organisation and Branch multi-select fields sit beside it. src/budget-workspace.css moved the top border, the padding and the margin onto that wrapper, set it to repeat(3,minmax(0,1fr)) with gap 14px and align-items start, gave the scope fields two columns of the row instead of the old four-column track, restyled the duration value as a quiet read-only field matching the two pickers beside it (min-height 40px, 8px 10px padding, 7px radius, #d9e1eb border, #f7f9fc fill, 13px/600), added a one-column fold below 900px and a one-column scope fold below 700px, and removed one stray edit-payload marker line that a previous automated edit had left in the stylesheet. Tests re-pointed: tests/budget-create-flow-ui.test.mjs asserts the wrapper in the step markup and pins the new .budgetDurationScope grid token, and tmp/budget-create-ssr.mjs asserts the rendered wrapper class and the row order duration, then organisation and branch, then the description. No storage key, engine function, validation rule, allocation, statement, variance or accounting behaviour changed. Validation actually performed: node tests/budget-create-flow-ui.test.mjs 8/8, node tests/budget-store.test.mjs 10/10, node tests/budget-statements.test.mjs 10/10, node tests/budget-register-ui.test.mjs 7/7, node tests/budget-navigation-ui.test.mjs 3/3, node tests/organisation-branch-scope-ui.test.mjs 10/10, node tmp/budget-create-ssr.mjs, node tmp/check-all-jsx.mjs 50/50, node scripts/validate-knowledge-docs.mjs, and the full sweep FILES=72 green=58 red=10 tests=414 passed=403 failed=11 with the same ten pre-existing failures and CANNOT-START(4). Not verified here and not claimed: no painted browser pass and no production build (npm and npx are absent and esbuild cannot spawn, so vite build fails with spawn EPERM). Previous checkpoint, superseded in part: 2026-09-15: Budget duration. On the user instruction the Budget Details step now derives the dates a plan covers instead of asking for them. src/budget-store.js added Custom to BUDGET_PERIODS and added budgetDateRange(financialYear,period,startDate,endDate), budgetDurationLabel(financialYear,period,startDate,endDate), budgetDateError(financialYear,period,startDate,endDate), plannedTotal(row,count) and a private budgetTiming(input,current) that resolves the year, the period and the range, so createBudget and updateBudget both store startDate / endDate derived from the financial year for every standard period and from the planner s own range only for Custom; periodLabels(period,financialYear) now reads Yearly as the financial year itself and Custom as Custom Period instead of returning Total, so the yearly allocation grid lost its duplicate Total column, and budgetToCSV passes the year through and totals with plannedTotal. src/budget-statements.js threaded the optional financialYear through budgetPeriodTotals, budgetTotalRows, budgetBalanceCheck and budgetStatementRows so the statement headers follow the same rule while the plan is still summed in one place. src/BudgetWorkspace.jsx replaced the always-on Start date and End date inputs with a div.budgetDuration block (read-only p.budgetDurationRange, p.budgetDurationMeta, p.budgetDurationHint reading Dates are based on the selected financial year., and a p.budgetDurationError driven by budgetDateError), rendered the two input type=date fields only for Custom with min / max from financialYearRange(), stopped the step from advancing on an invalid custom range, moved the optional Description into its own div.budgetSetupFields.budgetSetupNote row under the organisation and branch selection, deleted the visible span.budgetScopeLabel and its CSS rule, seeded startDate / endDate back from initial when editing, and made the allocation row total read plannedTotal(row,count) so an invisible period can never sit inside a visible total; the detail panel fact is renamed Budget duration and prints the same 01 Apr 2026 → 31 Mar 2027 reading through a new fmtDate helper. Validation actually performed: node tests/budget-store.test.mjs 10/10 (two new tests over the derived range, the custom range, budgetDurationLabel for all five periods, budgetDateError and plannedTotal), node tests/budget-create-flow-ui.test.mjs 8/8, node tests/budget-register-ui.test.mjs 7/7, node tests/budget-navigation-ui.test.mjs 3/3, node tests/budget-statements.test.mjs 10/10, node tests/organisation-branch-scope-ui.test.mjs 10/10, node tmp/budget-create-ssr.mjs (extended to render a seeded Custom budget and a Custom allocation step; it caught the lost custom dates on edit), node tmp/bw-detail-ssr.mjs, node tmp/bt-totals-ssr.mjs, node tmp/bt-statement-ssr.mjs, node tmp/bw-register-ssr.mjs, node tmp/budget-nav-ssr.mjs, node tmp/check-all-jsx.mjs 50/50, node scripts/validate-knowledge-docs.mjs, and the full sweep FILES=72 green=58 red=10 tests=414 passed=403 failed=11 with the same ten pre-existing failures and CANNOT-START(4). Not verified in this environment and not claimed: no painted browser pass (in-app browser automation is blocked here) and no production build (npm and npx are absent and esbuild cannot spawn, so vite build fails with spawn EPERM). Previous checkpoint, superseded in part: 2026-09-15: Budgets register row action renamed. On the user instruction the read-only row action in the Budgets register (src/BudgetWorkspace.jsx, BudgetList) is now labelled View Details instead of View Budget vs Actual. The button keeps className budgetRowView, the 15px IconEye and onClick={()=>view(budget)}, and only gains a new visible label and a new accessible name (aria-label View Details for the budget name), so the row still reopens the budget detail screen with its Budget vs Actual tab active through the detailTab state, the initialTab prop and the per-tab remount key. No CSS class, no class rule in src/budget-workspace.css, no handler, no storage key, no engine function and no tab name changed. tests/budget-register-ui.test.mjs renamed its two affected tests and re-pointed their assertions, and tmp/bw-register-ssr.mjs now asserts the new label and prints the updated summary. Validation actually performed: node tests/budget-register-ui.test.mjs 7/7, node tests/budget-create-flow-ui.test.mjs 8/8, node tests/budget-navigation-ui.test.mjs 3/3, node tests/budget-statements.test.mjs 10/10, node tests/budget-store.test.mjs 8/8, node tests/organisation-branch-scope-ui.test.mjs 10/10, node tmp/bw-register-ssr.mjs (budget register SSR OK - card toolbar above the grid, 4 columns, View Details plus a 3-dot View budget/Edit/Duplicate/Delete menu on 2 rows), node tmp/bw-detail-ssr.mjs, node tmp/check-all-jsx.mjs 50/50, node scripts/validate-knowledge-docs.mjs, and the full sweep FILES=72 green=58 red=10 tests=412 passed=401 failed=11 with the same ten pre-existing failures and CANNOT-START(4). Not verified in this environment and not claimed: no painted browser pass (in-app browser automation is blocked here) and no production build (npm and npx are absent and esbuild cannot spawn, so vite build fails with spawn EPERM). Previous checkpoint, superseded in part: 2026-09-15: Budget detail head. On the user instruction the budget detail screen now opens on the same full-bleed arrow-led back head the Create Budget wizard uses, naming the page Budget Details, with the lifecycle status beside the budget name and one More actions dropdown replacing the four separate head buttons. src/BudgetWorkspace.jsx replaced the inline button.budgetBack text link, the inline h1 and metadata paragraph and the four-button action row with div.budgetV3Head.budgetDetailHead (one square button.budgetHeadBack with aria-label and title Back to budgets and the 19px IconArrowLeft, a span.budgetHeadTitle reading Budget Details and a span.budgetHeadHint reading Back to budgets), a new div.budgetDetailIdentityRow pairing the name with its Status badge in div.budgetDetailName above the span.budgetDetailMeta line (financialYear · period · scopeSummary(scope) · type), and a right-aligned div.budgetDetailActions holding exactly one button.primary bound to statusAction beside one details.budgetDetailMore. The More actions disclosure renders summary.budgetDetailMoreBtn (horizontal IconDots, the More actions label, a rotating budgetDetailMoreCaret) and div.budgetDetailMoreMenu with Edit, Duplicate, Export, Archive and the destructive Delete (button.budgetKebabDanger). BudgetDetail gained a remove prop wired from BudgetWorkspace that confirms, calls deleteBudget(state,selected), clears the selection and returns to the register - deleteBudget archives the plan (deleted=true, status Archived) rather than removing it from storage - plus its own useEffect pointerdown / Escape dismissal over .budgetDetailMore[open], and every menu entry closes through the shared menuRun(handler) helper. src/budget-workspace.css deleted the now-dead .budgetBack rule, gave .budgetDetailActions justify-content:flex-end and gap 9px, and appended the detail head block: .app main:has(>.budgetV3.budgetDetailV3){max-width:none;padding:0}, .app main>.budgetV3.budgetDetailV3{max-width:none;margin:0;padding:0 0 28px}, the :not(.budgetV3Head) clamp(16px,2.2vw,32px) gutter, .budgetDetailHead at the wizard head geometry, .budgetDetailIdentityRow, .budgetDetailName, .budgetDetailMeta, .budgetDetailMore and .budgetDetailMoreMenu, with a sub-760px block (36px arrow, 8px 12px padding, stacked identity row, full-width trigger). Validation actually performed: node tmp/bw-detail-ssr.mjs (extended - it now pins the detail head class, the Budget Details title, the arrow button and its label, the status badge beside the name, the identity row sitting before the tab row, the primary status action leading the right-aligned group, Mark complete for an Active budget, the More actions menu with all five entries, the destructive class and the absence of the old inline back link), node tests/budget-register-ui.test.mjs 7/7 (one new test), node tests/budget-create-flow-ui.test.mjs 8/8, node tests/budget-navigation-ui.test.mjs 3/3, node tests/budget-statements.test.mjs 10/10, node tests/budget-store.test.mjs 8/8, node tmp/check-all-jsx.mjs 50/50, node scripts/validate-knowledge-docs.mjs, and the full sweep FILES=72 green=58 red=10 tests=412 passed=401 failed=11 with the same ten pre-existing failures and CANNOT-START(4); the test count rises by one and no new failure appears. Not verified in this environment and not claimed: no painted browser pass (in-app browser automation is blocked here) and no production build (npm and npx are absent and esbuild cannot spawn, so vite build fails with spawn EPERM). Previous checkpoint, superseded in part: 2026-09-15: Budget creation and detail refinement. Create Budget and the budget detail screen were refined in place without redesigning the application or changing the visual language, keeping the three-step workflow. src/budget-store.js: BUDGET_PERIODS is now ['Monthly','Quarterly','Half-Yearly','Yearly']; createBudget stores startDate and endDate; new logBudgetActivity(state,id,action) appends an activity entry; new parseBudgetCSV(text,accounts) parses an imported CSV into {accounts,allocations}; computeVarianceRows returns {account,budget,actual,variance,percent,achievement,direction} with variance as actual - planned; new varianceDirection(type,variance) returns favourable / unfavourable / neutral and inverts for an expense account, so a lower spend reads favourable. src/budget-statements.js: budgetTotalRows now emits eight rows - Total Revenue, Total Expenses, Net Profit / Loss, Profit Margin, Total Assets, Total Liabilities, Total Equity and Total Liabilities & Equity - and the old Budget Mismatch Adjustment row is deleted; new budgetBalanceCheck(accounts,allocations,period) returns {assets,liabilities,equity,profit,liabilitiesAndEquity,difference,balanced}; budgetStatementRows renamed the statement closes to Net Profit / Loss, Total Liabilities & Equity and Net Cash Flow and appends a kind:'check' Balance Difference row with className budgetStatementMismatch and a note that it is reported, never posted. src/BudgetWorkspace.jsx: step one gained Start date, End date and Description fields and a span.budgetScopeSummary chip; step two gained select.budgetTypeFilter, Clear all and span.budgetSelectedCount, and AccountSelectGrid folds each accounting type behind a button.budgetGroupCell with aria-expanded; step three groups the grid under BUDGET_ALLOCATION_BANDS with a sticky td.budgetStickyCol account column and foldable type rows and adds a label.budgetGrowthField Growth % input, a label.budgetImportButton CSV import and a button.budgetClearAll ahead of section.budgetTotals and the new section.budgetBalanceCheck (balanced / unbalanced); the detail head keeps only the back button, the name, the metadata line, the status badge and Edit / Duplicate / Export / Archive because the KPI cards moved into the new BudgetOverview; BUDGET_DETAIL_TABS is the flat eight-item [key,label] list with BUDGET_DETAIL_DEFAULT='Overview'; VsActual was rewritten with a Variance % column, a separate Achievement % column and a neutral total row; new BudgetAccountsTable and BudgetDetailsPanel; AccountTree and ForecastPanel were deleted; the detail tab grid moved from four to eight columns and the two dead .budgetTotalsGrid tr.budgetTotalsMismatch rules were removed. Validation actually performed: node tmp/bw-detail-ssr.mjs (new), node tmp/bt-totals-ssr.mjs, node tmp/bt-statement-ssr.mjs, node tmp/bt-stmt-check.mjs, node tmp/budget-create-ssr.mjs, node tests/budget-store.test.mjs 8/8, node tests/budget-statements.test.mjs 10/10, node tests/budget-register-ui.test.mjs 6/6, node tests/budget-create-flow-ui.test.mjs 8/8, node tests/budget-navigation-ui.test.mjs 3/3, node tmp/check-all-jsx.mjs 50/50, node scripts/validate-knowledge-docs.mjs, and the full sweep FILES=72 green=58 red=10 tests=411 passed=400 failed=11 with the same ten pre-existing failures and CANNOT-START(4); the pass count rises by two and no new failure appears. Not verified in this environment and not claimed: no painted browser pass (in-app browser automation is blocked here) and no production build (npm and npx are absent and esbuild cannot spawn, so vite build fails with spawn EPERM). Previous checkpoint, superseded in part: 2026-09-15: Budget statement view. The budget detail screen is now the statement view the standard budget sheets describe and src/budget-statements.js is the shared layer behind it. New service src/budget-statements.js (storage-free, imports only periodCount and periodLabels from src/budget-store.js) exports BUDGET_STATEMENTS (Profit and Loss, Balance Sheet, Cash Flow Statement), budgetPeriodTotals(accounts,allocations,period), budgetTotalRows(accounts,allocations,period) and budgetStatementRows(statement,accounts,allocations,period); rows are ordered {kind,key,label,values,total,className,note,account} with kind section|type|group|account|groupTotal|typeTotal|grand, accounts group under their chart of accounts group (account.group||account.type) inside each accounting type, every group, type and statement closes with its own total row, and an expense or an asset purchase is signed negative on the cash flow. src/BudgetWorkspace.jsx deleted its private planned / periodSums / sumValues / marginOf / *ByPeriod / totalsRows chain in favour of const totalsRows=budgetTotalRows(form.accounts,form.allocations,form.period), deleted the now-unused AllocationTable component and its flat Budget Details tab, added the exported BudgetStatementView, and now leads the detail tab row with the three statements through BUDGET_DETAIL_TABS and BUDGET_DETAIL_DEFAULT=BUDGET_STATEMENTS[0] (used by the detailTab state and the detail back() reset) while Budget vs Actual, Forecast, Accounts, Activity History and Revisions stay reachable. The Budgets register kebab gained View budget as its first entry ahead of Edit, Duplicate and the destructive Delete, opening the detail on the first statement through a new viewStatement handler; the visible View Budget vs Actual button and the canonical kebab geometry are unchanged. Styles were appended to src/budget-workspace.css under .budgetStatement (bands #f4f7fc / #fbfcfe / #536176, totals #f8fafd, derived #f1f5fb, mismatch #fff6ec on #8a4b09, negative #b42318, nowrap figures, 12px floor). Validation actually performed: node tmp/bt-statement-ssr.mjs (nine assertions over all three statements from a six-account plan - band order, group/type/statement totals, the 14-column band span, the account code under the name, the cash-flow negatives and the balance-sheet mismatch), node tests/budget-statements.test.mjs 9/9 (new), node tests/budget-register-ui.test.mjs 6/6, node tests/budget-create-flow-ui.test.mjs 8/8, node tests/budget-navigation-ui.test.mjs 3/3, node tests/budget-store.test.mjs 7/7, node tests/organisation-branch-scope-ui.test.mjs 10/10, node tests/ui-quality.test.mjs 5/5, node tmp/bt-totals-ssr.mjs, node tmp/bt-stmt-check.mjs, node tmp/budget-create-ssr.mjs, node tmp/budget-nav-ssr.mjs, node tmp/bw-register-ssr.mjs, node tmp/check-all-jsx.mjs 50/50, node scripts/validate-knowledge-docs.mjs, and the full sweep FILES=72 green=58 red=10 tests=409 passed=398 failed=11 with the same ten pre-existing failures and CANNOT-START(4). The file and pass counts rise by exactly one file and nine tests, which is the new test file. Not verified in this environment and not claimed: no painted browser pass (in-app browser automation is blocked here) and no production build (npm and npx are absent and esbuild cannot spawn, so vite build fails with spawn EPERM). Previous checkpoint, superseded in part: 2026-09-15: Budget allocation totals. The Budget Allocation step of Create Budget now closes with the statement totals the standard budget sheets show under the allocation grid instead of the four KPI chips. src/BudgetWorkspace.jsx replaced const summary=computeSummary({accounts:form.accounts,allocations:form.allocations}) with the derived-total chain: planned(type), periodSums(accounts), sumValues(values), marginOf(profit,revenue), the revenueByPeriod / expenseByPeriod / profitByPeriod / assetsByPeriod / liabilitiesByPeriod / equityByPeriod / netWorthByPeriod / mismatchByPeriod arrays and one totalsRows array of nine rows, and replaced the div.budgetReviewTotals render with section.budgetTotals > div.budgetTotalsGrid > one table whose header repeats the allocation grid period columns plus a Total column, whose label cell is a th scope=row and whose value cells carry budgetTotalsNegative on a negative figure; computeSummary stays imported and is still used by BudgetDetail and the reports. The sign convention is deliberate: the type totals stay positive planned magnitudes while the derived lines are signed - Total for Profit and Loss is revenue - expense, Total for Liabilities & Equities is liabilities + equity + profit because the planned result rolls into equity, and Budget Mismatch Adjustment is assets - (liabilities + equity + profit) - which reproduces the reference sheet where the profit and loss total and the liabilities and equities total print the same figure and the mismatch line cancels it, and lets a plan with no balance-sheet account balance to zero by construction. src/budget-workspace.css appended the totals block after the wizard styles (.budgetTotals on a #fbfcfe panel with a 1px solid #e1e8f2 border, .budgetTotalsGrid as its own scroll port over a min-width:680px table, a #f6f8fb header row, #f1f5fb derived rows, the mismatch row on the 5% amber tint #fff6ec / #8a4b09 with its small explanation, #b42318 for a negative figure, and a sub-700px fold) with every figure at 12px or larger. No storage key, no src/budget-store.js edit and no allocation, revision, variance, forecast, alert, permission or approval behaviour changed; the mismatch figure is computed rather than stored. Tests: tests/budget-create-flow-ui.test.mjs 8/8 (the totals assertions rewritten to the nine statement labels, the totalsRows render and the derived mismatch, plus four new CSS tokens in the style guard), the new tmp/bt-totals-ssr.mjs green on both a monthly plan with income, expense and an asset account (revenue 5,000, expense 3,000, profit and loss 2,000 twice, assets 1,000, mismatch -1,000 twice, margin 40%, 117 value cells, no-revenue periods reporting 0%) and a quarterly income-and-expense-only plan (the profit and loss total equals the liabilities and equities total and the mismatch total cancels it), tmp/budget-create-ssr.mjs, tmp/budget-nav-ssr.mjs and tmp/bw-register-ssr.mjs green, tmp/check-all-jsx.mjs 50/50, scripts/validate-knowledge-docs.mjs valid; sweep FILES=71 green=57 red=10 tests=400 passed=389 failed=11 with the same ten pre-existing failures and four esbuild-blocked suites; no painted browser pass and no production build in this environment (npm and npx are absent and esbuild cannot spawn), so the preview needs a hard reload. Previous checkpoint, superseded in part: 2026-09-15: Create Budget account selection. The Account Selection step of Create Budget now lists every active posting account in the Chart of Accounts under its accounting type instead of silently offering only the accounts of the default budget type. AccountSelectGrid in src/BudgetWorkspace.jsx used to group rows by account.group||account.type and filter the list through TYPES[form.type] (BUDGET_TYPES); because the Budget type field had been removed from step one, form.type kept its Profit & Loss Budget default, so the step offered Income and Expenses accounts only, a section heading could mix an account group such as Operating Income with a raw type, and a separate Account Type column repeated the heading. The wizard list is now accounts.filter(account=>account.active&&!account.isGroup) and the search matches name, code, account group and type together. The grid is two columns: one budgetGroupRow per type in the shared ACCOUNT_TYPE_ORDER order (Assets, Liabilities, Equity, Income, Expenses, with an Other bucket so an unrecognised type is never dropped), each row named by accountTypeLabel(type,business) and carrying its count (n of m selected, or m accounts), then one row per account with a labelled checkbox whose budgetAccountCell prints the account name over its code and, when it differs from the type, its account group. The Account Type column and const columns=business?3:4 are gone and the header reads Account / Money category. src/terminology.jsx now exports ACCOUNT_TYPE_ORDER and accountTypeLabel(type,business), which returns the raw type for the accountant view and the existing business wording (Assets -> What You Own, Expenses -> Money Spent) for the business view. src/budget-workspace.css makes the group row a display:flex space-between row (bold type, muted 12px count) and stacks the name over a 12px muted small in .budgetAccountCell; no new token is below 12px. Only the unused TYPES lookup was removed from the ./budget-store.js import - BUDGET_TYPES had to be restored because BudgetList and BudgetReports still build their Budget type filters from it, a break node tmp/budget-nav-ssr.mjs and node tmp/bw-register-ssr.mjs caught with ReferenceError: BUDGET_TYPES is not defined and no unit suite did. Validation: node tmp/dump-budget-accounts.mjs renders both views with their sections, counts and code lines; node tmp/budget-create-ssr.mjs passes with the new expectations; node tmp/budget-nav-ssr.mjs, node tmp/bw-register-ssr.mjs, node tmp/coa-detail-ssr.mjs and node tmp/probe-scope-fix.mjs report OK; node tmp/check-all-jsx.mjs reports 50/50 jsx files transformed with the same five pre-existing notices; tests/budget-create-flow-ui.test.mjs 8/8; full sweep FILES=71 green=57 tests=400 passed=389 failed=11 with CANNOT-START(4), identical to the baseline. Previous checkpoint, superseded in part: 2026-09-15: Create Budget scope universe and details grid. The Create Budget page could not be scoped: BudgetWizard read getAccessibleOrganizations(), the narrowed working context, so on a one-organisation one-branch context getScopeVisibility() reported showOrganisation:false and showBranch:false, scopePickerRows() returned no rows, and the Budget scope row rendered an empty div.pc-scope-picker beside the sentence Planning for ABC Technologies Pvt Ltd - Kochi Branch. src/organisation-context.js now owns both lists behind one scopedOrganisations(companyKey,branchKey,companyFallback,branchFallback) derivation - getAccessibleOrganizations() (wayvida-context-companies / wayvida-context-branches, unchanged) and the new getScopeOrganisations() (wayvida-accessible-organizations / wayvida-accessible-branches, falling back to the whole demo list so the universe is never empty) - and src/HeaderOrgSelectors.jsx reads its Change / Customize drawer list from that shared reader instead of deriving it inline. src/BudgetWorkspace.jsx reads the universe into state, seeds the opening scope from the working context through the one wizardScopeSeed(initial) helper, stores companyIds / branchIds on the budget scope so editing reopens the same scope, and re-reads the universe on wayvida-working-context-change, wayvida-organization-change and storage while pruning selections with normaliseScope(). No multi flag was added to getScopeVisibility() or scopePickerState(), so Journal Lines, Period Closing and Chart of Accounts keep their behaviour. src/budget-workspace.css puts step one on one four-column track (repeat(4,minmax(0,1fr))) with budgetFieldWide spanning two columns and the scope row reusing that track at grid-column:span 2 per field, folds to two columns below 1100px and one below 700px, and adds .budgetWizard .budgetSetup{overflow:visible} so the open checkbox list is not clipped. Tests: budget-create-flow-ui 8/8 (rewritten scope assertions), organisation-scope 10/10, organisation-branch-scope-ui 10/10, budget-register-ui 6/6, budget-store 7/7, budget-navigation-ui 3/3, period-closing-ui 27/27, period-lock-settings-ui 16/16, period-scope-visibility 16/16, journal-line-columns 6/6, journal-create-ui 22/22, header-context 3/3, ui-quality 5/5; harnesses tmp/probe-scope-fix.mjs, tmp/budget-create-ssr.mjs, tmp/dump-step1.mjs, tmp/ra2-profile-menu.mjs, tmp/budget-nav-ssr.mjs, tmp/bw-register-ssr.mjs, tmp/coa-detail-ssr.mjs, tmp/zscope2-ssr.mjs and tmp/check-all-jsx.mjs 50/50 all pass; full sweep FILES=71 green=57 tests=400 passed=389 failed=11 with CANNOT-START(4), identical to the baseline. Not verified: no rendered browser pass (blocked by the environment review policy) and no production build (npm absent, esbuild cannot spawn); the dev server serves the edited modules at http://localhost:5173/ and needs a hard reload. Previous checkpoint, superseded in part: 2026-09-15: Create Budget full-bleed page header. On the user instruction the Create Budget head now aligns beneath the organisation and branch customisation bar in the header, matches the header's width and reuses the Create Journal Entry page header style; this reverses the earlier same-day decision to pin that head to a 190px tile. src/budget-workspace.css treats the head as the page header: .app main:has(>.budgetV3.budgetWizard){max-width:none!important;padding:0!important} and .app main>.budgetV3.budgetWizard{max-width:none!important;margin:0!important;padding:0 0 28px!important} drop main's 22px clamp(16px,2.2vw,32px) 40px padding and 1680px cap plus the page's own padding, .app main>.budgetV3.budgetWizard>:not(.budgetV3Head){margin-left:clamp(16px,2.2vw,32px)!important;margin-right:clamp(16px,2.2vw,32px)!important} hands that gutter back to the stepper, the step cards, the error line and the wizard footer, and the head itself takes display grid with grid-template-columns 40px minmax(0,1fr), width 100%, max-width none, min-height 56px, margin 0 0 16px, padding 8px 24px, border-radius 0 with the left and right borders removed and no shadow, so the bar spans the sidebar edge to the viewport right edge - the same span the fixed .orgContextBar{left:270px;right:0} bar occupies - with 24px side padding that puts the back button on the same left edge as the organisation name in that bar. button.budgetHeadBack took the journal geometry (40px, 8px radius, hover #eef4ff / #bcd0f5 / #2463d4) and span.budgetHeadTitle took .je-compact-title's 16px 650 1.25, so 8px vertical padding plus the 40px button reproduce the journal header's 56px height; span.budgetStepCount stays in the stepper card. The 190px tile and its .companySelect{width:190px} rationale are gone - no rule in the file sets max-width 190px any more - and a new @media(max-width:760px) rule shrinks the head to a 36px column with 8px 12px padding to follow the org bar's mobile padding. src/BudgetWorkspace.jsx and src/budget-store.js are untouched, so the rendered markup is unchanged and no storage key, calculation, allocation, revision, variance, forecast, alert or settings behaviour moved. Validation: tests/budget-create-flow-ui.test.mjs 8/8 (the three shell rules, the full-bleed head declaration, the 40px back button, the 16px title, the preserved gutter, and that max-width 190px is gone), tests/budget-register-ui.test.mjs 6/6, tests/organisation-scope.test.mjs 10/10, tests/organisation-branch-scope-ui.test.mjs 10/10, tests/period-scope-visibility.test.mjs 16/16, tests/period-closing-ui.test.mjs 27/27, tests/journal-line-columns.test.mjs 6/6, tests/budget-store.test.mjs 7/7, node tmp/budget-create-ssr.mjs, node tmp/bw-register-ssr.mjs and node tmp/budget-nav-ssr.mjs green with the unchanged step-one DOM, node tmp/check-all-jsx.mjs 50/50, sweep FILES=71 green=57 tests=400 passed=389 failed=11 with the same pre-existing failures and four esbuild-blocked suites. No painted browser pass, so the alignment is reasoned from the cascade rather than seen on screen, and no production build in this environment. Previous checkpoint, superseded in part: 2026-09-15: Create Budget head width, stepper alignment and multi-select scope. On the user instruction the Create Budget head now matches the width of the header's Change / Customize organisation control and sits below it, no step name is cut by a line in the progress stepper, and the Budget type select is replaced by a multi-select organisation and branch dropdown. src/BudgetWorkspace.jsx keeps div.budgetV3Head.budgetWizardHead as the first child of section.budgetV3, but src/budget-workspace.css makes it a 190px-wide bordered tile (.app main>.budgetV3>.budgetV3Head.budgetWizardHead, 190px because .companySelect{width:190px} is the header control's own width, min-height 46px, 7px 12px padding, 10px radius) instead of a full-width bar so it lines up with the Change / Customize control above it; the back button is now 30px and the Step n of 3 chip left the head for the stepper card, where nav.budgetSteps is a minmax(0,1fr) auto grid whose second column is span.budgetStepCount (justify-self end). The stepper no longer truncates: li is flex:1 1 170px, the 2px li:not(:last-child)::after connector is painted behind every label at z-index 0 while each button sits above it at z-index 1 with a #fff background and padding 0 12px 0 0, and button span is nowrap with no ellipsis and no overflow hidden, so the connector cannot cut a step name; below 760px the stepper still stacks. Step one dropped the Budget type field - budgetSetupFields holds Budget name (now budgetFieldWide, spanning the grid), Financial year and Budget period, while form.type keeps its Profit & Loss Budget default so the account filter still works. The scope row renders OrganisationBranchScope with showLine false and the new multiple prop. src/organisation-scope.js gained ALL_ORGANISATIONS plus the pure scopePickerRows(state) and toggleScopePicker(state,row) helpers, and scopePickerState now composes getScopeVisibility() from the available organisation list and reports branchesAll, resolved, selectedBranchIds, organisationSummary and branchSummary; no multi flag was added to it, so the journal-lines and Period Closing visibility rules are unchanged, and no page re-implements the filtering. src/OrganisationBranchScope.jsx keeps its one-choice selects for those two callers and adds the opt-in multiple checkbox-list form (div.pc-scope-picker.pc-scope-multi, MultiScopeField, button.pc-multi-trigger with aria-haspopup listbox, div.pc-multi-panel with role listbox and aria-multiselectable true, closed by outside pointerdown or Escape), styled by the new src/organisation-branch-scope.css, which scopes every rule to .pc-scope-picker, keeps all text at 12px or larger and makes the panel static below 760px. No storage key, calculation, allocation, revision, variance, forecast, alert or settings behaviour changed, and src/budget-store.js is untouched. Validation: tests/budget-create-flow-ui.test.mjs 8/8 (step one field set, no Budget type, budgetFieldWide, the multiple scope call, the head without the chip, the compact head and un-clipped stepper tokens, and the shared multi-select control), tests/organisation-scope.test.mjs 10/10, tests/organisation-branch-scope-ui.test.mjs 10/10, tests/period-scope-visibility.test.mjs 16/16, tests/budget-register-ui.test.mjs 6/6, tests/journal-line-columns.test.mjs 6/6, tests/period-closing-ui.test.mjs 27/27, tests/period-lock-settings-ui.test.mjs 16/16, tests/budget-store.test.mjs 7/7, node tmp/budget-create-ssr.mjs green with the compact head, Step 1 of 3, multi-select scope and account grid assertions, node tmp/bw-register-ssr.mjs and node tmp/budget-nav-ssr.mjs green, node tmp/check-all-jsx.mjs 50/50, sweep FILES=71 green=57 tests=400 passed=389 failed=11 with the same pre-existing failures and four esbuild-blocked suites; no painted browser pass and no production build in this environment. Previous checkpoint, superseded in part: 2026-09-15: Create Budget head and progress stepper. On the user instruction the Create Budget page now uses the Create Journal Entry back navigation in its head and presents its three steps as progress rather than tabs. src/BudgetWorkspace.jsx renders div.budgetV3Head.budgetWizardHead holding one square button.budgetHeadBack (40px, 1px solid #d7e0eb, 8px radius, aria-label Back to budgets, IconArrowLeft 19px), span.budgetHeadTitle (Create Budget / Edit Budget), an span.budgetHeadHint when editing and a neutral span.budgetStepCount chip reading Step n of 3; the wizard button.budgetBack text link (still used by the budget detail screen) and the Set the budget scope, choose the accounts and plan the amounts. subtitle left the wizard. The step row is now nav.budgetSteps > ol with one li per step: the current step is class=active with aria-current=step, a finished step is class=complete and draws IconCheck instead of its number, a later step is class=upcoming and disabled, the 2px li:not(:last-child)::after connector turns var(--budget-blue) behind a completed step, and clicking an earlier step still walks back. Step one gained the div.budgetSelectHead heading its siblings already had. src/budget-workspace.css retired the underline tab row and its two media rules (overflow auto, min-width 155px), kept the detail screen .budgetBack styling, and appended the head and stepper rules plus a 760px stack. src/budget-store.js and every storage key are untouched. Tests: tests/budget-create-flow-ui.test.mjs 7/7 (one new test), tests/budget-register-ui.test.mjs 6/6, tests/budget-navigation-ui.test.mjs 3/3, tests/budget-store.test.mjs 7/7, tests/ui-quality.test.mjs 5/5, tests/organisation-branch-scope-ui.test.mjs 9/9, tmp/budget-create-ssr.mjs green with the new head and stepper assertions and tmp/check-all-jsx.mjs 50/50; sweep FILES=71 green=57 tests=397 passed=386 failed=11 with the same ten pre-existing failures and four esbuild-blocked suites; no painted browser pass and no production build in this environment. Previous checkpoint, superseded in part: 2026-09-15: Budgets register without the status cards. On the user instruction the Budgets page no longer renders the status card row (Total Budgets, Active Budgets, Total Planned Income, Total Planned Expense, Expected Profit) above the register. src/BudgetWorkspace.jsx deleted const metrics=budgetMetrics(budgets), the cards label / value / colour list and the section.budgetMetrics render, and dropped budgetMetrics from the ./budget-store.js import list, so the register page shell section.budgetV3 now runs from div.budgetV3Head straight into section.budgetListCard; src/budget-workspace.css deleted the nine .budgetMetrics / .metric rules and the two .budgetMetrics mentions in the @media(max-width:900px) and @media(max-width:700px) blocks while leaving the rest of those rows intact. src/budget-store.js is untouched, so budgetMetrics(...) is still exported and still covered by tests/budget-store.test.mjs, and no storage key, calculation, allocation, revision, variance, forecast, alert or settings behaviour changed. Tests: tests/budget-register-ui.test.mjs 6/6 (the metrics assertion replaced by a no-status-row assertion), tests/budget-navigation-ui.test.mjs 3/3, tests/budget-create-flow-ui.test.mjs 6/6, tests/budget-store.test.mjs 7/7, tests/ui-quality.test.mjs 5/5, tests/organisation-branch-scope-ui.test.mjs 9/9, tmp/bw-register-ssr.mjs and tmp/budget-nav-ssr.mjs rewritten and green (the register HTML fell from 8958 to 8465 characters), tmp/check-all-jsx.mjs 50/50; sweep FILES=71 green=57 tests=396 passed=385 failed=11 with the same ten pre-existing failures and four esbuild-blocked suites; no painted browser pass and no production build in this environment. Previous checkpoint, superseded in part: 2026-09-15: Budgets register toolbar and grid. On the user instruction the Budgets page now reuses the datagrid and the search / filter placement of the other registers, which supersedes the earlier page-heading toolbar checkpoint. src/BudgetWorkspace.jsx renders div.budgetRegisterBar as the first child of section.budgetListCard, directly above table.budgetTable, holding label.budgetFilterSearch, select.budgetFilterYear beside it and details.budgetFiltersMore (summary labelled Open filters, div.budgetFiltersPanel with four label.budgetFilterField rows - Budget type, Budget period, Scope, Status - and a div.budgetFiltersPanelActions row with Clear filters), while div.budgetHeadActions keeps only Import and Create Budget. src/budget-workspace.css replaced the heading toolbar block with .budgetRegisterBar, .budgetFilterSearch, .budgetFilterYear, .budgetFiltersMore, .budgetFiltersPanel, .budgetFilterField, .budgetFiltersPanelActions and .budgetFilterBadge copied from the Period Closing register (.pc-filters, .pc-filter-search, .pc-filters-more, .pc-filters-panel, .pc-more-badge) at the same 38px control height and 14px 20px 16px bar padding, and restates the grid as .budgetListCard .budgetTable th (12px 14px on #f5f7fb, 12px/600 #68768b) and .budgetListCard .budgetTable td (13px 14px, border-top 1px solid #edf1f6) to match .pc th and .pc td; the badge is 12px where the Period Closing one is 11px, to hold the repo-wide minimum text size, and the two .budgetListCard th:first-child / :last-child radius rules were deleted because the toolbar row now covers the card corner. .budgetListCard{overflow:visible} stays so the row menu and the filter panel can escape. The one page-level dismissal effect closes .budgetKebab[open] and .budgetFiltersMore[open] together. No storage key, calculation, allocation, revision, variance, forecast, alert or settings behaviour changed. Tests: tests/budget-register-ui.test.mjs 6/6 (the toolbar test rewritten around the card placement and the grid test around the shared .pc values), tests/budget-navigation-ui.test.mjs 3/3, tests/budget-create-flow-ui.test.mjs 6/6, tmp/bw-register-ssr.mjs and tmp/budget-nav-ssr.mjs green, tmp/check-all-jsx.mjs 50/50; sweep FILES=71 green=57 tests=396 passed=385 failed=11 with the same ten pre-existing failures and four esbuild-blocked suites; no painted browser pass and no production build in this environment. Previous checkpoint, superseded in part: 2026-09-15: Budget create flow. On the user instruction Create Budget is now three steps in BudgetWizard (src/BudgetWorkspace.jsx): Budget Details, Account Selection and Budget Allocation. Step one holds one budgetSetupFields grid (Budget name, Financial year, Budget period, Budget type), a budgetScopeRow rendering the shared OrganisationBranchScope over getAccessibleOrganizations with its visibility and branch options from scopePickerState, and a budgetTemplateSwitch that reveals budgetTemplateCards only while it is on; BUDGET_SCOPES, the hardcoded branch list and the manual scope select are gone from the page while the register scope filter still reads BUDGET_SCOPES, and the budget type cards became one Budget type select. Step two renders the new AccountSelectGrid: one .budgetAccountGrid table grouped by account group with a labelled checkbox per row and no account-type tabs, while AccountTree stays for the detail Accounts tab. Step three allocates and saves with Create Budget (Save Changes when editing); the Review Budget step, Save Draft and Submit for Approval left the create page, so BudgetDetail gained one mode-aware status action (Submit for approval, Approve budget, Activate budget, Mark complete) wired to setBudgetStatus and the engine FLOW, and an edit keeps the stored status instead of resetting it to Draft. An imported CSV also seeds its accounts in the form initialiser. src/budget-workspace.css gained a scoped block for the scope row, the template switch, the account grid, the wizard totals and the footer, all at 12px or larger. No storage key, calculation, allocation, revision, variance, forecast or settings behaviour changed. tests/budget-create-flow-ui.test.mjs 6/6; tests/budget-register-ui.test.mjs updated for the new detail signature; tmp/budget-create-ssr.mjs prints the three steps, the shared scope control and the grouped checkbox grid; sweep FILES=71 green=57 tests=394 passed=383 failed=11 with the same ten pre-existing failures and four esbuild-blocked suites; no painted browser pass and no production build in this environment. Previous checkpoint, superseded in part: 2026-09-15: Budgets register table. On the user instruction the Budgets tab now shows a four-column data grid - Budget Name, Financial Year, Budget Period and Actions. src/BudgetWorkspace.jsx renders one row per budget whose Actions cell is a read-only View Budget vs Actual button beside one details.budgetKebab 3-dot menu holding Edit, Duplicate and the destructive Delete; the previous Budget Type, Scope, Total Amount, Status, Created By and Last Updated columns and the six icon-only row buttons are gone, while exportBudget and archive stay wired to the budget detail header. View Budget vs Actual opens the existing detail screen with its Budget vs Actual tab active through a new detailTab state, the initialTab prop and the per-tab remount key. The menu reuses the canonical shared kebab geometry and closes through a new menuRun(handler) helper plus one page-level pointerdown / Escape effect in BudgetList; src/budget-workspace.css adds .budgetRowActions / .budgetRowView and moves .budgetListCard from overflow:hidden to overflow:visible with matching header-cell radii so the menu is never clipped. No storage key, workflow or calculation changed. tests/budget-register-ui.test.mjs 4/4; tmp/bw-register-ssr.mjs prints the four columns and both row controls over two seeded budgets; sweep FILES=70 green=56 tests=388 passed=377 failed=11 with the same ten pre-existing failures and four esbuild-blocked suites; no painted browser pass and no production build in this environment. Previous checkpoint, superseded in part: 2026-09-15: Chart of Accounts account details right-side popup. On the user instruction, clicking an account now opens its details as a right-side popup instead of the inline detail column. src/AccountWorkspace.jsx renders <div className=\"am-create-drawer-layer am-detail-layer\"> with the am-create-drawer-backdrop button and a full-height <aside className=\"am-create-drawer am-detail-drawer\" role=\"dialog\" aria-modal=\"true\" aria-label=\"Account details\"> whose own am-create-drawer-header states <h2>Account details</h2> and {account.name} · {account.code} above the unchanged am-detail-body and am-detail-footer; the backdrop, the header close button and Escape all call the existing setSelected(null), so open(a), the register 3-dot View Transactions entry and the wayvida-open-account sessionStorage handoff from Balance Sheet, Day Book and the ledger keep working with no route or state change. Because the details no longer take a grid column, am-layout lost the has-detail modifier and src/account-workspace.css dropped .am-layout.has-detail{grid-template-columns:minmax(330px,1fr) minmax(380px,.9fr)}, the .am-layout.has-detail .am-cards rules, the 1150px block that re-declared them plus .am-detail{position:relative;top:auto;grid-row:1} and its repeat(3) override, and the .am-detail{...position:sticky;top:82px} container, with the 700px and 420px card rules reduced to plain .am-cards, so the grid keeps the full page width; .am-detail-top stays because the Day Book transaction detail header still uses it. A new commented block appended to src/account-workspace.css sets .am-detail-layer .am-detail-drawer{width:min(560px,100vw)}, ellipsises the header subtitle, makes .am-detail-drawer .am-detail-body the only scroll container (flex:1;min-height:0;overflow-y:auto;padding:16px;background:#f6f8fb) and pins .am-detail-drawer .am-detail-footer, and declares no font size at all so the 12px-plus scale is untouched. One new useEffect on account locks document.body.style.overflow, closes on Escape and restores the previous overflow, mirroring src/EnterpriseAccountForm.jsx; start(account) already clears selected so the two drawers stay mutually exclusive. Every data point and action survives: the AccountConfiguration summary with its dated Current Balance, the Transactions and Audit Trail tabs with AccountLedger and the audit list, and the Edit Account, {t.viewLedger} and Mark Inactive / Mark Active footer actions. No account-record, storage, validation or posting change. Tests: new tests/account-detail-drawer-ui.test.mjs 4/4 (layer and dialog markup, backdrop and header close controls, the removed inline pane and its CSS, the surviving facts and actions, the Escape and scroll-lock wiring, and that the new CSS reuses the drawer shell without adding a type scale), with tests/account-header.test.mjs 5/5, tests/account-sticky-grid-ui.test.mjs 5/5, tests/account-create-drawer-ui.test.mjs 2/2, tests/account-group-picker-ui.test.mjs 6/6, tests/account-group-search.test.mjs 6/6, tests/account-settings.test.mjs 4/4, tests/account-templates.test.mjs 2/2, tests/journal-actions-ui.test.mjs 5/5, tests/organisation-scope.test.mjs 9/9, tests/organisation-branch-scope-ui.test.mjs 9/9, tests/ui-quality.test.mjs 5/5 and tests/sidebar-shell.test.mjs 2/2 all green. tmp/coa-detail-ssr.mjs renders the real page twice and reports \"account details popup SSR OK - open 12739 chars, closed 8557 chars\" (drawer markup present when an account is open, nothing rendered when none is), tmp/zscope-ssr.mjs reports \"scope SSR OK - grid 12140/12284 chars, drawer 3558/5183/5016/5504 chars\" (one character below the previously recorded 12141 because the layout class lost its modifier), tmp/check-all-jsx.mjs is 50/50 with the same five pre-existing LEFTOVER JSX? warnings, scripts/validate-knowledge-docs.mjs passes, and the full sweep reports FILES=69 green=55 tests=384 passed=373 failed=11 - the previous run (FILES=68 green=54 tests=380 passed=369 failed=11) plus this checkpoint suite - with the same ten pre-existing failing files (account-enterprise 7/2, then one each in account-master, create-page-navigation-ui, item-tax-ui, journal-business-view, journal-detail-ui, journal-heading-count-ui, operational-modules-ui, settings-ui, wayvida-date-picker-ui) and the same CANNOT-START(4) esbuild-blocked suites, which include tests/account-ui.test.mjs. The first attempt failed to parse with \"Expected corresponding JSX closing tag for <aside>\" because the old tail kept the extra </div> that used to close the inline panel. Served evidence: /src/AccountWorkspace.jsx?raw returns 200 containing am-detail-layer and no has-detail, and /src/account-workspace.css?raw contains the new .am-detail-layer .am-detail-drawer rule. No painted browser pass and no production build in this environment (npm absent, esbuild cannot spawn). Previous checkpoint, superseded in part: 2026-09-15: Top-level Budget section. Budget moved out of Accounting on the user instruction and is now its own top-level sidebar section between Accounting and Inventory. src/Navigation.jsx now declares {label:'Budget',icon:IconChartBar,children:[leaf('Budgets'),leaf('Budget Reports'),leaf('Budget Settings')]} immediately after the Accounting entry, whose children are back to Chart of Accounts, Journal Entries and Period Closing; IconChartBar was already imported and unused, so no import changed. The move only changes the group id (Budget), its list id (nav-Budget), the icon beside the label and the fact that contains(node,active) still opens it for an active budget page: every page name, the App.jsx routing list ['Budgets','Budget Reports','Budget Settings'].includes(a), the welcome-block exclusion, the BudgetWorkspace key={a} remount and the shared nav.budgetWorkspaceTabs row on the three screens are unchanged, as are budget lifecycle, allocations, revisions, variance, alerts, forecast, settings, permissions, src/budget-store.js storage keys and every accounting rule. The only source comment touched is the .budgetWorkspaceTabs header comment in src/budget-workspace.css, which now says the three pages are the top-level Budget section. Tests: tests/navigation-structure.test.mjs was rewritten (its Accounting assertion held the Budget group literally) into 2 tests - the reduced Accounting children plus the icon-carrying top-level Budget entry, and the declaration order Accounting < Budget < Inventory with {label:'Budget' no longer following leaf('Period Closing') - and tests/budget-navigation-ui.test.mjs test 1 was retitled to \"one top-level Budget section\" with the same order and non-nesting assertions, keeping its shared tab-row and design-token tests. tmp/budget-nav-ssr.mjs renders the real sidebar and reports 'budget navigation SSR OK - top-level sidebar section between Accounting and Inventory plus three-section tab row on all three pages', asserting id=\"nav-Budget\" exists, id=\"nav-Accounting-Budget\" exists nowhere, the group offset falls between the rendered Accounting and Inventory labels, the group is expanded while a budget page is active, and each screen still marks exactly one active tab with aria-current. tmp/check-all-jsx.mjs is 50/50 with the same five pre-existing LEFTOVER JSX? warnings, scripts/validate-knowledge-docs.mjs passes, and the full sweep reports FILES=68 green=54 tests=380 passed=369 failed=11 - the previous run (FILES=68 green=54 tests=379 passed=368 failed=11) plus the one test added to tests/navigation-structure.test.mjs, with the same ten pre-existing failing files (account-enterprise 7/2, then one each in account-master, create-page-navigation-ui, item-tax-ui, journal-business-view, journal-detail-ui, journal-heading-count-ui, operational-modules-ui, settings-ui, wayvida-date-picker-ui) and the same CANNOT-START(4) esbuild-blocked suites. No painted browser pass and no production build in this environment (npm absent, esbuild cannot spawn); the dev server serves http://localhost:5173/ and /src/Navigation.jsx?raw returns 200 containing the new top-level Budget section. Previous checkpoint, superseded in part: 2026-09-15: Budget group navigation. Budgets, Budget Reports and Budget Settings are now shown separately inside one Budget tab under Accounting, on the user instruction. src/Navigation.jsx nests them in the Accounting section as {label:'Budget',children:[leaf('Budgets'),leaf('Budget Reports'),leaf('Budget Settings')]}; the existing expandable-group renderer already opens the containing groups for an active budget page and derives the ids Accounting/Budget and nav-Accounting-Budget, and every page name stayed the same, so App.jsx routing (['Budgets','Budget Reports','Budget Settings'].includes(a)), the welcome-block exclusion and the BudgetWorkspace key={a} remount are unchanged. src/BudgetWorkspace.jsx gained const BUDGET_TABS=['Budgets','Budget Reports','Budget Settings'] and BudgetTabs({page,onNavigate}), rendering nav.budgetWorkspaceTabs (aria-label=\"Budget sections\") with the current section marked by aria-current=\"page\" and className=\"active\", wired to the existing onNavigate prop, and rendered once under each screen's budgetV3Head in BudgetList, BudgetReports and BudgetSettings. The budgets list is the nested BudgetList component, so page and onNavigate had to be declared there and passed from its call site (as well as added to BudgetReports and BudgetSettings); without that the new row threw \"page is not defined\" when the Budgets page rendered - the SSR probe caught it. src/budget-workspace.css gained .budgetWorkspaceTabs in the existing .budgetAccountTabs language: a bordered #f8fafc strip whose buttons are min-height:42px, font-size:13px, separated by 1px #e1e8f2 rules, the active tab marked by a 3px --budget-blue underline with a #f0f5ff fill and #2463d4 label, a :focus-visible ring, and an even split at 12px under 700px. Budget lifecycle, allocations, revisions, variance, alerts, forecast, settings, permissions, CSV import/export, the budget-store storage keys and every accounting rule are untouched, and no page was merged or renamed. Tests: new tests/budget-navigation-ui.test.mjs 3/3 (nested group, one leaf per page, the shared row on all three screens, the threaded props including the BudgetList regression, and the design tokens) and tests/navigation-structure.test.mjs 1/1 (its Accounting assertion rewritten and extended); tests/sidebar-shell.test.mjs 2/2, tests/budget-store.test.mjs 7/7, tests/header-context.test.mjs 3/3, tests/operational-modules-ui.test.mjs 2/1 and tests/create-page-navigation-ui.test.mjs 0/1 unchanged (the former asks for a Budgeting navigation group and for Budget vs Actual in App.jsx, which the Budget group does not provide); tmp/check-all-jsx.mjs 50/50; new tmp/budget-nav-ssr.mjs renders the sidebar and the three screens (budget navigation SSR OK - sidebar group plus three-section tab row on all three pages); scripts/validate-knowledge-docs.mjs passed; sweep FILES=68 green=54 tests=379 passed=368 failed=11 with the same ten failing files and the same four esbuild-blocked suites, i.e. the previous baseline (FILES=67 green=53 tests=376 passed=365 failed=11) plus this checkpoint's one new green suite. No painted browser pass and no production build here; the dev server serves the edited modules at http://localhost:5173/. Previous checkpoint, superseded in part: 2026-09-15: Chart of Accounts sticky grid head. The account grid on the Chart of Accounts page (src/AccountWorkspace.jsx) keeps its filter row and table head visible while the list scrolls, on the user instruction. The change is styling only, appended to src/account-workspace.css, and no component, filter, tree or drawer file changed: .am-coa-page>.am-filter-toolbar{position:sticky;top:0;z-index:9} pins the Search / Type / Status / Filters row to the top of the shell's only scroll port (.app>main, from src/ui-quality-polish.css), while .am-coa-page .am-table-wrap:has(.am-account-grid) keeps overflow:auto and gains max-height:max(360px,calc(100vh - 280px)) with a dvh line beside it and overscroll-behavior:contain, which bounds the wrapper that the account grid's 1120px minimum width had already turned into a scroll port; thead th is sticky at top:0 with z-index:6 and the opaque #f7f9fc fill, and .am-group-row th is sticky at top:calc(var(--am-coa-head-h) - 1px) with z-index:5 and #edf2f9, so the column header row and the Assets / Liabilities / Equity / Income / Expenses accordion row stick under the pinned filter row. Inset shadows replace the collapsed-table border browsers drop from sticky cells and --am-coa-head-h:40px carries the one header measurement. Filters, grouping, the account tree, colSpan arithmetic, scopeColumns, the empty state, .am-layout.has-detail, .am-detail{position:sticky;top:82px}, the detail-pane ledger table and the Create/Edit Account drawer are untouched. Tests: tests/account-header.test.mjs 5/5, tests/account-settings.test.mjs 4/4, tests/account-group-search.test.mjs 6/6, tests/account-templates.test.mjs 2/2, tests/account-create-drawer-ui.test.mjs 2/2, tests/account-group-picker-ui.test.mjs 6/6, tests/ui-quality.test.mjs 5/5, tests/organisation-scope.test.mjs 9/9, tests/organisation-branch-scope-ui.test.mjs 9/9, tests/journal-actions-ui.test.mjs 5/5 and the new tests/account-sticky-grid-ui.test.mjs 5/5 (a CSS contract for the pinned filter row, the bounded grid wrapper and the two sticky rows), with the pre-existing failures unchanged (account-enterprise 7/2, account-master 7/1, create-page-navigation-ui 0/1); tmp/check-all-jsx.mjs 50/50 (same five pre-existing LEFTOVER JSX? warnings); tmp/zscope-ssr.mjs scope SSR OK - grid 12141/12285 chars, drawer 3558/5183/5016/5504 chars; scripts/validate-knowledge-docs.mjs passed; sweep FILES=67 green=53 tests=376 passed=365 failed=11 with CANNOT-START(4): account-ui, document-preview-ui, general-ledger-ui, receipt-ui and the same ten failing files, i.e. the pre-change baseline plus the one new green suite added here. The dev server (node tmp/dev-live.mjs, no npm in this environment) serves the new rules at http://localhost:5173/src/account-workspace.css?raw. No painted browser pass and no production build here; the preview needs a hard reload (Ctrl+Shift+R). Previous checkpoint, superseded in part: 2026-09-14: Create Lock popup trim. The Create Lock / Close popup header in src/PeriodClosing.jsx now renders only the mode label (Manual locking / Automatic locking) and the Create Lock / Close title: the .pc-lock-scope chip (Locking for: <organisation> · All branches) and the conditional paragraph (manual Locks <organisation> for every module. A reason is optional. / automatic Locks <organisation> on the saved automatic schedule. Adjust this run if the period should follow a different cadence.) were deleted on the user instruction, together with the lockScope=getScopeVisibility({...}) constant that fed the chip, so getScopeVisibility left the page import and only validateScope remains there. Nothing else changed: the drawer still resolves scopeOrganisations through getAccessibleOrganizations() and still renders OrganisationBranchScope with label=\"Locking for\" and showLine={false}, writing companyIds / branchIds into the lock form and therefore into createLockWindow; .pc-lock-scope stays in src/period-closing.css because the request unlock drawer still renders that chip through the picker. Tests: tests/organisation-scope.test.mjs 9 passed / 0 failed (the [period closing, getScopeVisibility(] pairing dropped and two negative popup assertions added), tests/organisation-branch-scope-ui.test.mjs 9/9 (two more negative assertions), tests/period-closing-ui.test.mjs 27/27, tests/period-lock-settings-ui.test.mjs 16/16, tests/period-locking.test.mjs 28/28, tests/period-scope-visibility.test.mjs 16/16, tests/journal-line-columns.test.mjs 6/6, tests/journal-create-ui.test.mjs 22/22. Harnesses: tmp/pc2-probe.jsx and tmp/pc15-probe.jsx regenerated from the live source, then tmp/zscope2-ssr.mjs (lock drawer 1742 / 1947 -> 1578 / 1783 chars, asserting no chip and no schedule paragraph), tmp/pc2-ssr.mjs (7995 / 8547 chars), tmp/pc2-drawers.mjs (12 surfaces), tmp/pc5-validate.mjs (17 surfaces) and tmp/pc15-requests.mjs green. No lock, close, audit, storage or enforcement rule changed. Not verified: no painted browser pass (in-app browser automation denied) and no production build (esbuild cannot spawn). Previous checkpoint, superseded in part: 2026-09-14: Organisation and branch picker. src/OrganisationBranchScope.jsx is the one interactive organisation/branch control, rendering the pure scopePickerState() helper added to src/organisation-scope.js, and src/organisation-context.js now owns the storage-backed available organisation list that Create Journal Entry and Period Closing share (src/JournalEntriesPro.jsx imports it and dropped its private copies). The Period Closing Create Lock / Close drawer renders the picker with selectors and writes companyIds / branchIds straight into the lock form, keeping All branches as the first Branch option so a whole-organisation lock is never silently narrowed; the request unlock drawer renders the same control with showFields={false} to state the locked scope, because requestPeriodUnlock derives a request scope from the locked period (recorded as a limitation, not faked). New tests/organisation-branch-scope-ui.test.mjs 9 passed / 0 failed (one implementation over the shared rule, the three configurations, the cleared foreign branch, the branchless em dash, the empty scope, both drawers wired, one shared working context); tests/organisation-scope.test.mjs 9/9 with two lock-drawer assertions re-pointed; tests/journal-line-columns.test.mjs 6/6 with its harness now injecting src/organisation-context.js; unchanged: tests/journal-create-ui.test.mjs 22/22, tests/period-closing-ui.test.mjs 27/27, tests/period-lock-settings-ui.test.mjs 16/16, tests/period-locking.test.mjs 28/28, tests/period-scope-visibility.test.mjs 16/16. Harnesses: tmp/pc2-probe.jsx and tmp/pc15-probe.jsx were regenerated from the live source before tmp/pc2-ssr.mjs (7995 / 8547 chars), tmp/pc2-drawers.mjs (12 surfaces), tmp/pc5-validate.mjs (17 surfaces) and tmp/pc15-requests.mjs were re-run green; new tmp/zscope2-ssr.mjs renders the control and the drawer (no selector for one organisation with one branch, Branch only with All branches and no foreign branch, both selectors filtered by organisation, the unlock form stating its scope with no selector, the lock drawer picker) at 122 / 435 / 597 / 1742 / 1947 chars. node tmp/check-all-jsx.mjs 50/50 jsx files transformed with the same five pre-existing LEFTOVER JSX? warnings, node scripts/validate-knowledge-docs.mjs passed, sweep tmp/sweep.ps1 -> FILES=66 green=52 tests=371 passed=360 failed=11 with CANNOT-START(4): the identical eleven pre-existing failures, none in a touched module. Not verified: no painted browser pass (in-app browser automation denied) and no production build (esbuild cannot spawn). Previous checkpoint, superseded in part: 2026-09-14: Organisation and branch scope. New module src/organisation-scope.js is the single implementation of the organisation/branch scope rule and src/period-locking.js now re-exports it instead of keeping private scopeVisibility / normaliseScope bodies. Consumers: src/EnterpriseAccountForm.jsx and src/AccountWorkspace.jsx (Chart of Accounts, where the account grid renders Organisation / Branch columns only when the shared flags say so and the detail panel resolves the scope through the same helpers), src/JournalEntriesPro.jsx (Journal Lines, names rather than ids), src/PeriodLockSettings.jsx (ScopeSelector) and src/PeriodClosing.jsx (createLock validates through validateScope and the Create Lock drawer prints a Locking for: chip) and src/account-master.js (changeAccount gained an organisation-aware scope check that refuses a branch from another organisation, with demoOrganizations passed from src/AccountWorkspace.jsx, so the rule holds at the reducer as well as the picker). New regression suite tests/organisation-scope.test.mjs 9 passed / 0 failed (the ninth test proves the Chart of Accounts domain layer rejects a branch from another organisation). Updated suites: tests/journal-create-ui.test.mjs 22/22 (five source assertions re-pointed at the shared wiring) and tests/journal-line-columns.test.mjs 6/6 (its new Function harness injects the shared module source); unchanged: tests/period-scope-visibility.test.mjs 16/16, tests/period-locking.test.mjs 28/28, tests/period-lock-settings-ui.test.mjs 16/16, tests/period-closing-ui.test.mjs 27/27, tests/account-header.test.mjs 5/5, tests/account-master.test.mjs 7 passed / 1 pre-existing failure (the system-protection contract), tests/account-settings.test.mjs 4/4. Harnesses: node tmp/check-all-jsx.mjs 49/49 jsx files transformed with the same five pre-existing LEFTOVER JSX? warnings, node scripts/validate-knowledge-docs.mjs passed, and the full sweep tmp/sweep.ps1 -> FILES=65 green=51 tests=362 passed=351 failed=11 with CANNOT-START(4) - the identical eleven pre-existing failures, none in the touched modules. Rendered evidence: the new harness tmp/zscope-ssr.mjs server-renders AccountWorkspace and EnterpriseAccountForm through tmp/ssr-loader.mjs and passes - the Chart of Accounts grid keeps five columns (colSpan=\"5\") with no Organisation/Branch header for one organisation with one branch and grows to seven columns with both headers for two organisations, and the Create Account drawer states the resolved branch as read-only text while availability is off, then shows the Availability for: strip and both pickers limited to the selected organisation(s) with no foreign branch (grid 12141/12285 chars, drawer 3558/5183/5016/5504 chars). Runtime: Invoke-WebRequest for /src/organisation-scope.js, /src/period-locking.js, /src/PeriodLockSettings.jsx, /src/EnterpriseAccountForm.jsx, /src/AccountWorkspace.jsx, /src/JournalEntriesPro.jsx and /src/PeriodClosing.jsx all return HTTP 200. Not verified: no painted browser pass (in-app browser automation denied) and no production build (esbuild cannot spawn). Previous checkpoint, superseded in part: 2026-09-14: Period Closing request register. Contract tests updated: tests/period-closing-ui.test.mjs 27 passed / 0 failed (the request-list contract is now 'the request list renders every row in one table, with a scope column and a status column and no pager', the pc-filter-count assertions in the toolbar contract are inverted, and new assertions cover the seven headers, requestStatusLabel, requestScopeOf, the periods prop, the seven focus column widths and the pc-badge tones). tests/period-locking.test.mjs 28/28 (unchanged). Harnesses: node tmp/pc15-requests.mjs - with tmp/pc15-probe.jsx and tmp/pc2-probe.jsx regenerated from the live source - now proves all eight requests render with no pager, that each row carries its own kebab, and that the scope cell prints ABC Technologies Pvt Ltd with its branch line; node tmp/pc2-ssr.mjs (workdir tmp) 7995 chars neutral pill / 8547 blocking, node tmp/pc2-drawers.mjs 12 surfaces, node tmp/pc5-validate.mjs 17 surfaces, node tmp/check-all-jsx.mjs 49/49 jsx files transformed with the same five pre-existing LEFTOVER JSX warnings, node scripts/validate-knowledge-docs.mjs 'Knowledge documentation check passed: 15 required files, valid manifest, metadata and local links.', and the full sweep tmp/sweep.ps1 -> FILES=64 green=50 tests=353 passed=342 failed=11 with CANNOT-START(4) (account-ui, document-preview-ui, general-ledger-ui, receipt-ui): the same eleven pre-existing failures, none of them in Period Closing. Invoke-WebRequest http://localhost:5173/src/PeriodClosing.jsx?raw returned 200 with 'Organisation & Branch', requestScopeOf and pc-request-cell-status present and pc-request-pager, pc-pager-status, pc-filter-count and REQUEST_PAGE_SIZE absent; both stylesheets were checked the same way. No painted browser pass (in-app browser automation is denied in this environment) and no production build (esbuild cannot spawn). Previous checkpoint, superseded in part: 2026-09-14: Period Closing request row actions. The Requests table's Actions cell is now one shape on every row: one read-only View button (button.pc-request-view, 32px like the row menu, white with an #e1e6ed border and #475467 ink, hovering to #b9c9e3 / #f8faff / #245fd9 - neutral, never the primary blue) plus one 3-dot menu, replacing the inline emphasised Approve Unlock and the inline requester-only Cancel request. RequestActions dropped its three early returns for a single return, and its menu (details.pc-kebab.pc-request-kebab > div.pc-kebab-menu.pc-request-kebab-menu) now holds four entries in order: Approve (canDecide=canApprove&&decidable&&pending), Cancel (canCancel=self&&pending, unchanged and still requester-only), View Details (always) and Delete (canDelete=self||canApprove, className pc-kebab-danger, IconTrash newly imported, styled #b42318 with a #fff5f6 hover as the one destructive entry). Both View entries call onDetails(request) and open the same RequestDetailsDrawer. The two explanatory notes survive as one note value rendered as span.pc-action-note - empty on a decided row, 'Approval requires an approver role.' when the acting role cannot approve, 'Another approver must review your own request.' when no second approver exists - so a missing Approve entry is still explained rather than silently absent. onDeleteRequest is threaded exactly like onCancelRequest (page deleteRequest -> LockRegister / RequestsDrawer -> RequestList -> RequestActions) and the page calls the new engine verb deleteUnlockRequest(state,{requestId,user,now}) in src/period-locking.js, which throws 'Unlock request not found.' for an unknown id and 'Only the requester or an approver can delete this unlock request.' for any other role, routes a Pending approval request through cancelUnlockRequest first (restoring the previous Hard Locked / Soft Locked status and writing the Unlock cancelled entry) so deleting a live request can never strand its period, then removes the record from state.requests and prepends an 'Unlock request deleted' audit entry; deleting a decided or expired request removes the record without touching its outcome. src/period-closing.css dropped the now-dead .pc .pc-request-actions button.pc-quiet and button.primary rules - pc-quiet no longer appears anywhere in src/ - and added button.pc-request-view and .pc .pc-request-kebab-menu button.pc-kebab-danger. Test results recorded for this checkpoint: node tests/period-closing-ui.test.mjs 27 passed / 0 failed, node tests/period-locking.test.mjs 28 passed / 0 failed (the new 28th contract covers deleteUnlockRequest), node tmp/pc15-requests.mjs and node tmp/pc2-ssr.mjs OK, node tmp/pc2-drawers.mjs 12 surfaces, node tmp/pc5-validate.mjs 17 surfaces, node tmp/check-all-jsx.mjs 49/49 jsx files transformed with the same five pre-existing LEFTOVER JSX? warnings, node scripts/validate-knowledge-docs.mjs \"Knowledge documentation check passed: 15 required files, valid manifest, metadata and local links.\", and the full sweep tmp/sweep.ps1 FILES=64 green=50 tests=353 passed=342 failed=11 CANNOT-START(4) - the same eleven pre-existing failures (account-enterprise 2, then account-master, create-page-navigation-ui, item-tax-ui, journal-business-view, journal-detail-ui, journal-heading-count-ui, operational-modules-ui, settings-ui and wayvida-date-picker-ui), none in Period Closing. Not verified: no painted browser pass (in-app browser automation is denied in this environment) and no production build (npm run build cannot run because the esbuild binary cannot be spawned, spawn EPERM). Previous checkpoint, superseded in part: 2026-09-14: Period Closing attention pill and request row cleanup. RequestActions no longer renders the reviewer note - the {self&&<span className=\"pc-action-note\">{'Recorded as '+approver+', the next authorised approver.'}</span>} span left its final return - so a pending request raised by the acting role is the emphasised Approve Unlock, the requester-only quiet Cancel request and its 3-dot menu and nothing else; approver still drives decidable=!self||Boolean(approver) and the decision is still recorded under the resolved approver. The full-width pc-attention strip under the heading is replaced by a compact, never-dismissible attention pill in the heading action group: details.pc-attention-pop > summary.pc-attention-pill (className 'pc-attention-pill '+tone with tones neutral/warn/danger, IconAlertTriangle 15, span.pc-attention-count carrying aria-live=\"polite\" and the count as text, aria-label \"Review <n> checks that need attention\", title \"Before you close\", no chevron) renders only while attention.count>0, so zero checks hides the whole details and All checks passed is printed inside the popover only. One attention memo - the failed checks, the count, the tone (danger when any check is Failed, else warn at three or more, neutral at one or two, ok at zero), the singular/plural noun and verb and entries (impact.transactionImpactCount||0) - is the single computed value behind the pill, its tone, the popover summary and the count, and attentionChecks plus the pc-more-badge count are deleted. Its div.pc-attention-pop-menu[role=region][aria-label=\"Before you close\"] (min(400px,92vw), right:0, top:calc(100% + 8px), z-index 30) holds div.pc-attention-pop-head (<b>Before you close</b> plus \"N checks need attention - M entries affected\", or All checks passed), <Checklist readiness onNavigate only/> and button.pc-attention-pop-all (Review all + IconArrowRight wired to menuRun(openBeforeClose)); Checklist gained the only prop, which filters Passed out so one renderer serves the popover and the pre-close drawer, and its small line reads \"<n> item(s) needs/need attention.\" openMenus() now queries .pc-more[open],.pc-kebab[open],.pc-filters-more[open],.pc-attention-pop[open], so the pill dismisses on an outside pointerdown or Escape like the other disclosures. src/period-closing.css replaced the five .pc .pc-attention rules with the pill block (.pc-attention-pop{position:relative;display:flex;flex:none;margin-right:7px} for the wider status gap, the 999px-radius/40px/13px-650 pill with its neutral palette, .warn rgba(234,88,12,.07)/#f0d3b8/#9a3412, .danger rgba(220,38,38,.06)/#f3c9cd/#b42318, hover and focus-visible ring) and the popover shell, keeping .pc-checklist flat with its small at the 12px floor and deliberately using no primary blue. src/period-closing-focus.css appended .pc-focused>.pc-heading{position:sticky!important;top:0!important;z-index:26!important;background:#f7f9fc!important;padding:10px 0 12px!important;box-shadow:0 1px 0 #e6ebf3} after the .pc-focused>.pc-heading reset, pinning the row inside the page's .pc scroller so the count stays visible while the register scrolls and returning its height when the details collapses. Test results recorded for this checkpoint: node tests/period-closing-ui.test.mjs 27 passed / 0 failed, node tests/journal-actions-ui.test.mjs 5 passed / 0 failed, node tmp/pc15-requests.mjs and node tmp/pc2-ssr.mjs OK, node tmp/pc2-drawers.mjs 12 surfaces, node tmp/pc5-validate.mjs 17 surfaces, node tmp/check-all-jsx.mjs 49/49 jsx files transformed with the same five pre-existing LEFTOVER JSX? warnings, node scripts/validate-knowledge-docs.mjs \"Knowledge documentation check passed: 15 required files, valid manifest, metadata and local links.\", and the full sweep tmp/sweep.ps1 FILES=64 green=50 tests=352 passed=341 failed=11 CANNOT-START(4) - the same eleven pre-existing failures (account-enterprise 2, then account-master, create-page-navigation-ui, item-tax-ui, journal-business-view, journal-detail-ui, journal-heading-count-ui, operational-modules-ui, settings-ui and wayvida-date-picker-ui), none in Period Closing. Not verified: no painted browser pass (in-app browser automation is denied in this environment) and no production build (npm run build cannot run because the esbuild binary cannot be spawned, spawn EPERM). Previous checkpoint, superseded in part: 2026-09-14: Period Closing Requests register is table-only. The Cards / List switch and the card rendering were deleted: src/PeriodClosing.jsx dropped requestLayout from LockRegister, dropped the div.pc-request-view-bar branch and the layout prop on RequestList, and RequestTable is now a div.pc-table-fit pc-request-table > table (the same fit shell the locked-periods table uses, so no horizontal scrolling) with five columns - Period, Reason, Requested, Requested By, Actions - replacing the seven-column scrolling list, so Unlock Scope and Approver moved into the new RequestDetailsDrawer. RequestActions renders the whole Actions cell from one place: a pending decidable row keeps the emphasised Approve Unlock and the requester-only quiet Cancel request, a decided row keeps the menu alone, decidable=!self||Boolean(approver) and both explanatory notes are unchanged, and every row gains one details.pc-kebab.pc-request-kebab menu (IconDots 17) whose div.pc-kebab-menu.pc-request-kebab-menu holds Approve, Cancel and View Details (IconEye, newly imported). View Details opens RequestDetailsDrawer - pc-history-scrim plus aside.pc-history-drawer.pc-request-drawer (min(460px,96vw)) over dl.pc-request-facts with Requested By, Requested, Reason, Unlock scope from the new requestScopeLabel, Approver, Unlock duration, Access until and Approver remarks - portalled from RequestList through its new detail state. src/period-closing.css deleted every .pc-request-card rule, set .pc-request-list to display:block;padding:0, added justify-content:flex-end;flex-wrap:wrap to .pc .pc-request-actions plus the .pc-request-kebab and .pc-request-facts styles, and widened .pc-requests-drawer to min(820px,96vw); src/period-closing-focus.css swapped the switch and scroll rules for five column widths (19/25/13/13/30 percent) plus th nowrap, td vertical-align:top, the Period small line and the actions-cell wrap rules, and the dead pc-request-none / pc-request-list-table markers are gone. Verified with node tests/period-closing-ui.test.mjs 26/26 (the cards-or-list contract was rewritten as \"the requests register is one table whose rows carry the decision, the withdraw and a 3 dot menu\"), node tests/period-lock-settings-ui.test.mjs 16/16, node tmp/pc15-requests.mjs OK (five headings, no switch or card markers, both inline actions, all three menu rows and the request details drawer) after regenerating tmp/pc15-probe.jsx from the live page, node tmp/check-all-jsx.mjs 49/49 with the same five pre-existing LEFTOVER JSX? warnings, and tmp/sweep.ps1 FILES=64 green=50 tests=351 passed=340 failed=11 - the unchanged eleven pre-existing failures, none of them in Period Closing. The dev server serves the change live (HTTP 200 on /src/PeriodClosing.jsx?raw with the new strings and without pc-request-view-bar); no painted browser pass and no production build (esbuild cannot spawn). Previous checkpoint, superseded in part: 2026-09-14: Period Closing Requests register renders as cards or as a compact list. src/PeriodClosing.jsx added requestLayout state to LockRegister (useState('cards'), presentation only, no new storage key), a right-aligned div.pc-request-view-bar in the requests branch that reuses .pc-view-toggle with aria-label \"Show requests as cards or as a list\" and two aria-pressed buttons (Cards, List) rendered only while requests exist, and a layout prop on RequestList whose return branches between the untouched card markup and function RequestTable({requests,actions}) - a div.pc-table-scroll pc-request-table table with min-width:1040px and the columns Period, Requested By, Reason, Unlock Scope, Requested, Approver and Actions, carrying the organisation as the Period cell small line, Duration and Access until under the unlock scope and the approver remarks under the approver, and deliberately not carrying pc-table-fit so the locked-period register width rules cannot reach it. The per-request decision markup was extracted into one function RequestActions({request,canApprove,approver,role,onDecide,onCancelRequest}) declared above RequestList and shared by the card footer ({isPending&&<footer>{requestActions(request)}</footer>}) and the list row, keeping decidable=!self||Boolean(approver), the two explanatory notes, the Recorded as <approver> reviewer line, the quiet Cancel request and the single emphasised Approve Unlock; a decided list row shows a muted pc-request-none dash. The REQUEST_PAGE_SIZE=6 pager, the empty state and both business labels stay shared, so paging behaves identically in both views. src/period-closing-focus.css owns the new geometry (.pc-request-view-bar, .pc-request-list-table padding, .pc-request-table min-width, th nowrap with td white-space:normal, the seven column widths, right-aligned actions, overflow-wrap on the reason and .pc-request-none). Verified with node tests/period-closing-ui.test.mjs 26/26 (new contract: the requests register renders as cards or as a compact list from one switch; the older request-card, pager and toolbar contracts still pass), node tests/period-lock-settings-ui.test.mjs 16/16, node tmp/pc15-requests.mjs (regenerated from a refreshed tmp/pc15-probe.jsx snapshot, now also asserting the switch, the seven list headings, no pc-request-card in list mode, the retained reason/scope/duration/access-until/remarks, both decision buttons, pc-request-none on a decided row and six-per-page paging) and node tmp/check-all-jsx.mjs 49/49 with the same five pre-existing LEFTOVER JSX? warnings. The dev server serves the change live; no painted browser pass and no production build (esbuild cannot spawn). Previous checkpoint, superseded in part: 2026-09-14: Header profile menu owns the terminology switch and Create Account is a right-side drawer. src/App.jsx renders the header account chip as details.profileMenu with a summary.profileButton trigger and a profileMenuPanel holding the Admin / Super Admin identity, a View as label and two profileMenuRole buttons - Business owner (Admin) and Head of Accountant (Staff) - each type=\"button\" with aria-pressed and an IconCheck on the active view, writing wayvida-terminology-view (and the compatible wayvida-coa-view key) through the existing setMode; App now calls useTerminology() and one useEffect closes any open .profileMenu on an outside pointerdown or Escape. The old View As: control is gone: the src/Navigation.jsx .terminologySwitch injection and its document.querySelector('.headerActions') call were deleted (the hook destructure is now const {t}=useTerminology()), the unused TerminologySwitch export left src/terminology.jsx, the dead .terminologySwitch rules left src/chart-of-accounts-simple.css, and the new styles are appended to src/styles.css (272px panel, 1px solid #e1e6ed, 12px radius, 0 18px 44px #10182824, 48px rows, #f4f7ff hover, #245fd9 active). Create Account / Edit Account in src/EnterpriseAccountForm.jsx gained a div.am-create-drawer-header above the form carrying the Create Account / Edit Account title, a one-line description and a close button (aria-label=\"Close create account\", the same close handler as the backdrop), replacing the in-body .am-create-drawer-page-title, and src/account-create-drawer.css now anchors the layer with position:fixed;inset:0;z-index:1200;display:flex;align-items:stretch;justify-content:flex-end and pins the drawer top:0;right:0;bottom:0;left:auto;width:min(560px,100vw);height:100%;border-left:1px solid #dfe6ef;border-radius:0;box-shadow:-18px 0 48px rgba(16,24,40,.15) (min(88vw,560px) under 900px, 100vw under 640px), with its five 11px helper sizes raised to 12px. Field order, validation, applyGroup, MultiSelect, storage and save logic are unchanged. Verified with node tmp/ra2-profile-menu.mjs (the real App rendered in both preferences plus the real drawer), node tests/header-context.test.mjs 3/3 (new contract: the header profile menu owns the terminology view switch), the new node tests/account-create-drawer-ui.test.mjs 2/2, node tmp/ag2-drawer.mjs, node tmp/check-all-jsx.mjs 49/49 with the same five pre-existing LEFTOVER JSX? warnings, and tmp/sweep.ps1 FILES=64 green=50 tests=350 passed=339 failed=11 with CANNOT-START(4) and the same 11 pre-existing failures. The dev server serves the change live; no painted browser pass and no production build (esbuild cannot spawn). Previous checkpoint, superseded in part: 2026-09-14: Shared row action menu across data grids. The Period Closing kebab (details.pc-kebab with a horizontal IconDots, plus .pc-kebab-menu) became the canonical row action and the other two grids were restyled onto it. src/JournalEntriesPro.jsx now renders <summary aria-label={'More actions for '+j.number}><IconDots size={17}/></summary> - the title=\"More actions\" tooltip and the last IconDotsVertical left the file and the import takes IconDots - every menu button gained type=\"button\" plus the menuRun(handler) closer declared beside creators, and one useEffect registers pointerdown and Escape listeners over .je-row-actions details[open] and .je-detail-more[open]. src/AccountWorkspace.jsx did the same for account rows: menuRun above const more=a=>..., IconDots size 17 in the am-more summary, type=\"button\" and menuRun(...) on all five entries, and one useEffect over .am-table .am-more[open]; .am-danger keeps its red styling. src/journal-entries.css lines 8/9 and src/account-workspace.css line 11 now carry the same values as .pc-kebab in src/period-closing.css: a 32px square trigger with 1px solid #d9e1ec, a 7px radius, #fff background and #475467 icon, an open state of border-color:#3478f6;color:#245fd9, and a right-aligned menu at top:calc(100% + 6px) with min-width:196px, padding:6px, 1px solid #e1e6ed, border-radius:9px and box-shadow:0 12px 30px #1018281f whose rows are min-height:36px with gap:9px, padding:0 10px, a 6px radius, transparent background, #344054 labels and the #f4f7ff / #245fd9 hover; the Journal Entries menu lost its details>div:before caret and its trigger hover fill, and the Chart of Accounts menu went from a fixed 224px to min-width:196px with no open-state fill. PeriodClosing.jsx and period-closing.css were not touched, and the Chart of Accounts header Templates menu, the je-edit-journal button, account/journal logic, storage keys and posting rules are unchanged. Tests: journal-actions-ui 5/5 (up from 2, three new shared-menu contracts including the three-stylesheet geometry pin), period-closing-ui 25/25, journal-entries-ui 1/1, journal-detail-actions 2/2, ui-quality 5/5, check-all-jsx 49/49; harness tmp/ra1-row-menus.mjs server-renders both registers with tabler-icon-dots, no tabler-icon-dots-vertical and type=\"button\" rows (Journal Entries register 9028 chars, Chart of Accounts 29146 chars); full sweep FILES=63 green=49 tests=347 passed=336 failed=11 with the same 11 pre-existing failures and CANNOT-START(4); the four changed files were re-fetched from http://localhost:5173 with ?raw and return 200 with the new strings. Known dead code reported, not deleted: the second register in src/JournalEntriesPro.jsx after the unconditional return <JournalRegister .../> is unreachable, and so are BusinessJournalRegister, BusinessJournalForm and BusinessJournalDetail. Not verified: no painted browser pass (in-app browser automation is denied) and no production build (esbuild cannot spawn). Previous checkpoint, superseded in part: 2026-09-14: Period Closing request decisions, pagination, lock type and More actions. RequestList no longer renders the per-request status badge and no longer offers Reject: the pending card footer holds one .pc-request-actions group with the quiet Cancel request (requester only) and the emphasised Approve Unlock with a check icon as the last action, so onDecide is only ever called with 'Approved' while decideUnlockRequest keeps accepting 'Rejected' and REQUEST_STATUS_FILTERS keeps listing it for stored data. Requests page six at a time (REQUEST_PAGE_SIZE=6, a local useState page number, nav.pc-request-pager with Previous / Page <n> of <n> / Next rendered only past one page). DetailsDrawer no longer prints a Lock type row, so Hard Lock / Soft Lock stays engine-internal; the lockPeriod validation, the newForm / newLockForm defaults and the Closed Type filter are untouched. More actions gained a rotating pc-more-caret, hover / focus-visible / open styling, a calc(100% + 6px) anchored menu that scrolls at max-height:min(70vh,420px), and a page-level useEffect that dismisses .pc-more, .pc-kebab and .pc-filters-more on outside pointerdown or Escape; the asserted box parity with Create Lock / Close is preserved. Tests: period-closing-ui 25/25 (2 new, the Reject assertion inverted), period-locking 27/27; harnesses tmp/pc15-requests.mjs REQUEST ACTIONS OK, tmp/pc2-drawers.mjs 12 surfaces (the probe was regenerated and two stale assertions corrected), tmp/pc5-validate.mjs 17 surfaces, tmp/pc2-ssr.mjs 7080 chars, tmp/pc14-title.mjs HEADING OK, tmp/check-all-jsx.mjs 49/49; full sweep 63 files, 344 tests, 333 passed, 11 pre-existing failures, 4 esbuild-blocked suites. Not verified: no painted browser pass and no production build. Previous checkpoint, superseded in part: 2026-09-14: Period Closing header chip, request decisions and lock history. The inline read-only locking policy chip left the Period Closing heading: the pc-policy-chip button, the chip binding, the policyChip import and the single-child pc-title-row wrapper are deleted (pc-title-block holds only the h1 above the pc-heading-note subheading), the dead .pc-policy-chip and .pc-title-row rules left period-closing.css and period-closing-focus.css, and the Period closing settings menu sub-label became the static Locking schedule and unlock approval, so no Auto-lock string renders on the page. The lock history surface was removed with it - the Audit log / History menu entry, openHistory, historyFilters and HISTORY_FILTERS, allLockRows, lockMeta, the closedCount / pendingRequests / lastClosed memos, the lastClosedPeriod import and the panel===history pc-history-wide portal - so the filtered register is the only lock list and .pc-history-wide left the stylesheet. Unlock requests became decidable from the Requests half: period-locking.js now exports UNLOCK_APPROVER_ORDER and unlockApproverFor(requestedBy, settings), submitUnlockRequest reuses that helper for its daily auto-approval, and RequestList renders Reject / Approve Unlock whenever canApprove && (!self || approver), recording the decision as the next authorised approver and stating Recorded as <approver>, the next authorised approver. for a self-raised request, while Another approver must review your own request. stays only when the policy leaves no other approver. The pre-close drawer lost its duplicated heading, the sentence The system verifies the following before <period> can be closed. and the pc-ready-pill badge, so its body runs from the entries-affected count and category chips into the checklist. Tests: period-closing-ui 23/23, period-lock-settings-ui 16/16, period-locking 27/27 (one new test for the self-approval guard and unlockApproverFor), period-scope-visibility and period-unlock-scope unchanged; the sweep reported 63 files, 342 tests, 331 passed, 11 pre-existing unrelated failures and 4 esbuild-blocked suites. No rendered browser pass and no production build (esbuild spawn EPERM). 2026-09-14: Period Closing heading actions and the business view title. The More actions disclosure on Period Closing is a summary inside details.pc-more, not a button, so it never received the heading action row box and read as a second smaller control beside Create Lock / Close. period-closing.css now gives it the box the journal page uses for its footer buttons (journal-form.css .je-create > footer button): 40px minimum height, padding 0 15px, a 7px icon gap, 7px radius, border 1px solid #d9e1ec, a 14px/600 label and white-space nowrap, replacing the previous 13px label and 8px gap; period-closing-focus.css raises the neighbouring heading button to the same padding 0 15px, font-weight 600 and white-space nowrap, so the pair is one size. Width: .pc-more is position relative, display flex, min-width 0 and its summary is flex 1 1 auto so the disclosure fills its own box instead of staying an intrinsic-width island, and the @media(max-width:650px) rule that stretches the heading buttons now covers .pc-heading-actions .pc-more too, which previously left the menu trigger at its natural width while the create action grew to fill the row. The kebab icon is 16px, matching the button icon beside it. The business-view title was a diverging duplicate: terminology.jsx already maps business.periodClosing to Financial Lock and the sidebar resolves its label through pageLabel('Period Closing', t), but PeriodClosing.jsx hardcoded the business title as Close Books, so the sidebar read Financial Lock while the page read Close Books. The heading now renders <h1>{t.periodClosing}</h1> from the same useTerminology() result the page already consumed for business, so the two cannot disagree: the accountant view reads Period Closing, the business owner view reads Financial Lock, and Close Books is gone. No terminology value, engine rule, storage key, lock or audit contract changed. Tests: period-closing-ui 23/23 (two new contracts: the shared action-row box for the summary and the create button, and the business title resolving through the terminology map), period-lock-settings-ui 16/16, period-locking 26/26, account-group-picker-ui 6/6, account-group-search 6/6; harness tmp/pc14-title.mjs renders the page with wayvida-terminology-view set to each mode and reports accountant h1: Period Closing, business h1: Financial Lock, HEADING OK; tmp/check-all-jsx.mjs 49/49 with the 4 pre-existing LEFTOVER JSX? warnings; tmp/pc2-ssr.mjs 8293 chars. Full sweep 63 files, 341 tests, 330 passed, 11 pre-existing failures (journal-business-view fails on an unrelated Journal Entries label), 4 esbuild-blocked. The dev server at localhost:5173 serves the new source. Not verified: no painted browser pass (in-app browser automation is denied in this environment) and no production build (esbuild cannot spawn). Previous checkpoint, retained: Account Group search picker and Period Closing settings heading trim. Create Account / Edit Account (src/EnterpriseAccountForm.jsx) replaced its native Account Group select with its optgroups with src/AccountGroupPicker.jsx, the searchable combobox the Journal lines account cell already uses, backed by the DOM-free src/account-group-search.js and styled by src/account-group-picker.css. The new module exports accountGroupKey, matchesAccountGroup, flattenAccountGroups, accountGroupRows and selectedAccountGroup - the same shape journal-templates.js gives the journal picker - so matching is testable without a DOM. The control preserves the existing contract: optionId derives agp-option-<type>-<purpose> from the unchanged type::purpose key and onChange splits that key back through applyGroup(nextType,nextPurpose), while groupOptions, applyGroup, the fallback group for an unknown type/purpose and every stored field are untouched, so creation, editing, validation and posting did not change. The list groups options by account type, matches label, type, purpose and group text case-insensitively, drops a section that loses every option, keeps role=combobox / role=listbox / role=option with aria-selected and aria-activedescendant, and supports Arrow/Enter/Escape, hover, outside pointerdown closing and scrollIntoView block nearest exactly like the journal picker. Two deliberate departures from the journal picker exist because the drawer renders the field inside label.am-field: the search wrapper is a div rather than a label (a label may not contain another label, and the input is already named by its aria-label), and the picker root carries onClick with preventDefault so a click on a non-interactive descendant such as an option row cannot reach the host label and re-fire the trigger button, which would reopen the list right after a selection - the visible Account Group * text sits outside that root so it still opens the picker. No DOM library is installed, so that was reasoned from the label activation rules and pinned in the suite. The stylesheet mirrors the .je-account-* rules under .agp* class names including their !important flags, because .am-create-drawer .am-field input would otherwise restyle the search box and its focus ring; every text size is 12px or 13px, so the AGENTS.md minimum holds. In the same pass src/PeriodLockSettings.jsx lost the read-only text block: the Automatic locking <h2>, its Periods lock on this schedule... description and the policyChip() label are gone, so the popup runs header (<h1>Period Closing Settings</h1> plus the one-sentence lead) -> warnings -> one section.pls-card.wide opening directly with the Enable automatic locking switch -> sticky footer. policyChip is no longer imported or called there but stays exported for the Period Closing page heading, and period-lock-settings.css lost its dead .pls-popup .pls-card>header h2{font-size:15px} rule. No setting, stored value, lock, audit or enforcement rule changed. The .pls header reset was re-examined and left alone: it also matches the host .pc-history-drawer>header, which is harmless because period-closing.css already makes that header position:static!important as a flex row with pc-drawer-body as the only scroll container. Tests: account-group-search 6/6, account-group-picker-ui 6/6, period-lock-settings-ui 16/16, period-closing-ui 21/21; harness tmp/ag1-picker.mjs (derives tmp/ag1-probe.jsx from the real component to force the list open) SSR OK closed=536 open=1716 disabled=548 empty=539; tmp/ag2-drawer.mjs ACCOUNT DRAWER SSR OK create=3131 edit=3126 (the real EnterpriseAccountForm drawer mounts the picker inside label.am-field with Account Group * and no select or optgroup left); tmp/check-all-jsx.mjs 49/49 with the 4 pre-existing LEFTOVER JSX? warnings; tmp/pc12-pls-dump.mjs renders Automatic, Manual and Off with no <h2>, no pls-chip and no policyChip; tmp/pc13-card-check.mjs CARD NESTING OK in all three modes; tmp/pc2-ssr.mjs 8293 chars; tmp/pc2-drawers.mjs 12 surfaces; tmp/pc5-validate.mjs 17 surfaces. Full sweep 63 files, 339 tests, 328 passed, 11 pre-existing failures, 4 esbuild-blocked. The dev server at localhost:5173 serves /src/AccountGroupPicker.jsx (200), /src/EnterpriseAccountForm.jsx (200, AccountGroupPicker present and optgroup absent) and /src/PeriodLockSettings.jsx (200, no heading and no policyChip). Not verified: no painted browser pass (in-app browser automation is denied in this environment) and no production build (esbuild cannot spawn). Previous checkpoint, superseded in part: Period Closing settings popup consolidation. 2026-09-14: Period Closing settings popup consolidation. src/PeriodLockSettings.jsx went from three cards to one: the Period locking card and the Unlock approval card were deleted and the collapsed More policy settings details block was removed whole. The surviving Automatic locking card is now the master control and carries the read-only policy chip, the Enable automatic locking switch (which writes only patch({lockingMode:next?'Automatic':'Manual'})), the five schedule fields with their pls-fields-off disabled state, the read-only generated schedule, a new pls-rule divider and the Require approval to unlock switch. Both rows come from one local Switch helper rendering label.pls-switch with the text span, a visually hidden input[type=checkbox][role=switch] and a decorative i track; period-lock-settings.css gained that 38x22 toggle idiom with a checked fill, a focus-visible ring on the track and a 58px row, plus the .pls-rule divider, and lost every .pls-advanced rule. lockingMode 'Off' is no longer reachable from the popup, so the master switch renders disabled with pls-switch-off at 55 percent opacity and a note that period locking is switched off in Accounting settings; the only writer of Off stays Settings -> Accounting -> Period control. Removed with the disclosure: the boundary fields, organisation timezone, default scope, allowed roles, exemptions and enforcement coverage rendering, the MODE_COPY constant, the scope/setScope locals and the IconArrowLeft, IconShieldCheck, LOCKING_MODES, PERIOD_MODULES, PERIOD_ROLE_OPTIONS and ENFORCED_OPERATIONS imports; export function ScopeSelector stays as the single consumer of scopeVisibility but is no longer rendered, and normalisePeriodSettings still supplies defaultScope, allowedRoles and exemptModules, so no storage key, lock, audit, enforcement or journal rule changed. Tests: period-lock-settings-ui 16/16 (rewritten contract: one card, two switches, the five schedule fields, role=switch and the track styling, no boundary fields, no pls-advanced), period-closing-ui 21/21, period-locking 26/26, period-scope-visibility 16/16, period-unlock-scope 9/9, ui-quality 5/5; harnesses tmp/pc12-pls-dump.mjs Automatic 6184 chars / Manual 3171 chars / Off 3297 chars, tmp/pc2-ssr.mjs 8293 chars, tmp/pc2-drawers.mjs 12 surfaces, tmp/pc5-validate.mjs 17 surfaces, tmp/check-all-jsx.mjs 48/48; tmp/pc13-card-check.mjs CARD NESTING OK (Automatic, Manual and Off). One structural defect was caught during verification and fixed: the card's closing section tag initially sat above the pls-rule divider, which rendered the approval switch outside the card; the tag moved below the switch, and both the source suite and the new rendered-markup harness pin that the single card contains both switch rows. Full sweep 61 files, 327 tests, 316 passed, 11 pre-existing failures, 4 esbuild-blocked. The dev server at localhost:5173 serves the new module. Not verified: no painted browser pass (in-app browser automation is denied in this environment) and no production build (esbuild cannot spawn). Previous checkpoint, superseded in part: Period Closing register toolbar, current period popup and settings polish. The page subheading is now one line (Lock accounting periods and control when postings stop.) - the audit-trail sentence was removed. LockRegister renders one compact toolbar row inside div.pc-register-bar: the Locked Periods / Requests toggle on the left (plus the pc-register-heading block that only the history drawer still passes), and on the right the reused .pc-filters row holding the search box, the Filters disclosure and the row count; Period and Status moved inside .pc-filters-panel ahead of Financial Year, Organisation, Branch, Closed By and Closed Type, and the advanced badge counts Status and Period too. The bar is a div rather than a header because the legacy app-bar element rules in src/styles.css (header label{height:38px;background:#f6f8fb;padding:0 12px}, header input{...}, header>button{...}) restyle any control inside any header. Real defect fixed: @media(max-width:1050px){header>button:not(.primary):not(.hamb){display:none}} hid every direct child button of a header below 1050px, silently removing the close button from all Period Closing drawers, the .pc-modal dialogs and the settings popup; restored with .pc-history-drawer>header>button,.pc-modal>section>header>button{display:inline-flex!important} and .pls-popup .pls-close{display:inline-flex!important}. The current accounting period left the page body: it is now panel===current, a 520px right-side drawer (pc-current-drawer) opened from the first More actions entry Current accounting period (sub-labelled from the new currentMeta memo), rendering the real CurrentPeriodPanel - now a named export - with its status badge and its single status-dependent action plus View details; CSS flattens the card inside the drawer and a missing period shows the No current period empty state. Period Closing Settings popup polish: restored close button, 16px/18px card padding, switch hover, one 22px gutter (16px below 760px), separation shadows on the sticky heading and footer, and a @media(max-width:760px) block that reflows grid, warnings, heading and footer. Tests: period-closing-ui 21/21 (4 new), period-lock-settings-ui 15/15 (1 new), period-locking 26/26, period-scope-visibility 16/16, period-unlock-scope 9/9, ui-quality 5/5; harnesses tmp/pc2-ssr.mjs 8293 chars, tmp/pc2-drawers.mjs 12 surfaces (now rendering the real CurrentPeriodPanel), tmp/pc5-validate.mjs 17 surfaces, tmp/check-all-jsx.mjs 48/48; full sweep 61 files, 326 tests, 315 passed, 11 pre-existing failures, 4 esbuild-blocked. Not verified: no rendered browser pass and no production build. The type rows then rendered in the running preview as a column of one letter per line: the flex rule had been applied to the td itself, and a cell whose display is not table-cell leaves the table layout algorithm, so the browser wrapped it in an anonymous cell sized by the 56px choose column. src/budget-workspace.css now keeps every cell a real table cell and moves the flex row into div.budgetGroupCell inside the spanning th and into the account label, and the same pass hardened accessibility: aria-label on the table, th scope=col headers, a th scope=colgroup colSpan={2} per type, a stable budget-account-<code> id per checkbox that its label htmlFor points at, an 18px checkbox with a 2px #245fd9 :focus-visible ring, a tbody tr:focus-within row tint, and #667085 (4.97:1) instead of the lighter grey that measured 3.66:1 for the 12px code line. tests/budget-create-flow-ui.test.mjs now also asserts !css.includes('td{display:flex')."
  },
  "documentation": {
    "index": "docs/README.md",
    "handbook": "docs/WAYVIDA-BOOKS-HANDBOOK.md",
    "aiPrompt": "docs/AI-HANDOFF-PROMPT.md",
    "transferGuide": "docs/TRANSFER-TO-ANOTHER-AI.md",
    "modularDocuments": [
      "docs/PROJECT-OVERVIEW.md",
      "docs/TECHNICAL-ARCHITECTURE.md",
      "docs/ACCOUNTING-DOMAIN.md",
      "docs/MODULES.md",
      "docs/DATA-MODEL.md",
      "docs/UI-DESIGN-SYSTEM.md",
      "docs/DEVELOPER-GUIDE.md",
      "docs/TESTING-AND-QA.md",
      "docs/SECURITY-AND-CONTROLS.md",
      "docs/KNOWN-GAPS-AND-ROADMAP.md",
      "docs/GLOSSARY.md",
      "docs/PERIOD-LOCKING-SPECIFICATION.md"
    ]
  },
  "constraints": [
    "Treat source and tests as truth for current behavior",
    "Do not describe browser persistence as a production database",
    "Do not expose accounting account selection in normal operational workflows when it can be derived from master data",
    "Use Instrument Sans and never render visible text below 12px",
    "Preserve Wayvida Books branding and supplied logo assets",
    "Sales orders never post accounting directly",
    "Posted transactions require reversal or adjustment",
    "Role selectors and UI gates are simulations, not authorization",
    "Keep Sites hosting files and SPA fallback behavior intact",
    "Every completed feature must update and validate the knowledge-transfer package",
    "Every material project change must synchronize the provider-neutral cross-AI transfer checkpoint without storing raw chats, secrets, credentials, or personal browser data",
    "Invoice details preserve all operational and accounting data while using breadcrumb navigation, prioritized actions, four summary metrics and responsive tab content",
    "Organisation and branch scope has exactly one implementation, src/organisation-scope.js (getScopeVisibility, getBranchesForOrganisation, validateScope, normaliseScope); Chart of Accounts, Create Journal Entry -> Journal Lines, Period Locking settings and Create Lock must read it instead of comparing organisation counts or filtering branch lists per screen, a single-branch organisation must auto-resolve as read-only text rather than a one-option dropdown, a branchless organisation prints the NO_BRANCH em dash, and a branch outside the selected organisation must never be offered or accepted",
    "One shared horizontal three-dot row action menu across data grids: Period Closing (pc-kebab) is the canonical control and the Journal Entries register, the Chart of Accounts table and the Budgets register (budgetKebab) must match its trigger, menu box, row geometry, close-on-select behaviour and outside pointer/Escape dismissal",
    "One header account menu for the display vocabulary: the account chip (details.profileMenu in src/App.jsx) is the only home for the Business owner / Head of Accountant switch - never a View As: control in the header action row nor an imperative injection from src/Navigation.jsx - and Create/Edit Account stays a right-side drawer, not a centred card or a full page",
    "The Period Closing Requests register is table-only and pageless: one row per filtered request, in the shared pc-table-fit / pc-request-table shell, with the columns Period, one combined Organisation & Branch cell (the organisation name with its branch scope beneath it), Reason, Requested, Requested By, Status and Actions - never the deleted Cards / List switch, div.pc-request-view-bar, article.pc-request-card rendering, pc-request-none placeholder, Reject action, pager or a status chip inside the Actions cell. Every row's Actions cell holds the same two controls: one View button and one pc-kebab 3-dot menu (Approve, Cancel, View Details, Delete), opening the single-request RequestDetailsDrawer popup.",
    "Organisation and branch selection is one control, not one per page: render OrganisationBranchScope from src/OrganisationBranchScope.jsx, whose every decision comes from the pure scopePickerState() helper in src/organisation-scope.js. Do not hand-roll another organisation/branch picker or copy its visibility or filtering logic into a page. The available organisation list is read through src/organisation-context.js (getAccessibleOrganizations()), shared by Create Journal Entry and Period Closing - never re-add a private readOrgList / getAccessibleOrganizations copy. The Period Closing Create Lock / Close drawer uses the picker with selectors; a period lock may cover a whole organisation, so the Branch list keeps All branches as its first option and a whole-organisation lock is never silently narrowed to one branch. The request unlock drawer renders the same control with showFields={false} because requestPeriodUnlock derives a request scope from the locked period; do not add a selectable branch there without also adding an enforcement rule for temporary unlocks. The Create Lock / Close popup prints no scope chip and no schedule paragraph - its header is the mode label and the title only, and the scope is read from the pickers - so do not reintroduce a Locking for: chip or a Locks <organisation> ... paragraph there.",
    "Chart of Accounts keeps its own scroll inside the account grid: the filter row (.am-coa-page>.am-filter-toolbar) pins to the top of the shell's only scroll port (.app>main), and the grid wrapper stays a bounded scroll port (a max-height with a dvh line beside the vh fallback, plus overscroll-behavior:contain) so the column header row and the account-group accordion row stick inside it - never unbind that wrapper, because an unconstrained scroll port sticks nothing vertically, and keep the pinned rows below the .am-more row menu and the heading Templates disclosure.",
    "Chart of Accounts account details open as a right-side popup, not a second grid column: src/AccountWorkspace.jsx renders div.am-create-drawer-layer.am-detail-layer with button.am-create-drawer-backdrop and a full-height aside.am-create-drawer.am-detail-drawer (role=\"dialog\", aria-modal=\"true\", aria-label=\"Account details\", width:min(560px,100vw), am-detail-body as the only scroll container and am-detail-footer pinned) whose header states Account details with {account.name} · {account.code}; the backdrop, the header close button and Escape clear selected and the body scroll lock mirrors src/EnterpriseAccountForm.jsx. Do not restore the inline panel: keep am-layout unmodified, keep the two-column .am-layout.has-detail rules and the .am-detail{position:sticky;top:82px} container deleted, and keep .am-detail-top for the Day Book transaction detail header.",
    "Create Budget is three steps and one control per fact: Budget Details (name, financial year, period, type, the shared OrganisationBranchScope over the getScopeOrganisations scope universe, and a template switch), Account Selection (one two-column .budgetAccountGrid listing every active posting account in the Chart of Accounts - src/BudgetWorkspace.jsx no longer filters the step through TYPES[form.type] or BUDGET_TYPES, which stay only for the register Budget type filter - grouped under its accounting type in the shared ACCOUNT_TYPE_ORDER order from src/terminology.jsx with accountTypeLabel(type,business) naming each counted type row, one labelled checkbox per account whose budgetAccountCell label prints the name over its code and account group; the separate Account Type column is gone), where every cell stays a real table cell (never a `display` other than table-cell on a td/th, since a flex cell leaves the table layout algorithm and collapses to the choose column) and the grid keeps its a11y contract (table aria-label, th scope=col headers, a th scope=colgroup per type, an 18px checkbox with a :focus-visible ring, a tbody tr:focus-within tint and a 12px #667085 code line) and Budget Allocation (the allocation grid, then the section.budgetTotals statement-totals block in the same period columns - Total for Revenue, Total for Expense, Total for Profit and Loss, Profit margin, Total for Assets, Total for Liabilities, Total for Equities, Total for Liabilities & Equities and Budget Mismatch Adjustment, all nine computed in the page from the allocation cells so nothing extra is stored, with the profit and loss line and the liabilities and equities line repeating one figure and the mismatch line cancelling it, and the four budgetReviewTotals chips deleted - saved with one Create Budget action). Read the scope from scopePickerState instead of restoring BUDGET_SCOPES or a hardcoded branch list on that page, keep AccountTree for the detail Accounts tab, keep the approval lifecycle on the budget detail screen instead of the create footer, and keep every new wizard style at 12px or larger.",
    "Budgets is one flat Accounting destination between Journal Entries and Period Closing, and Budget screens carry no page-level tab row: the single Budgets sidebar leaf is the only budget navigation, so keep BUDGET_TABS, BudgetTabs and budgetWorkspaceTabs deleted from src/BudgetWorkspace.jsx and src/budget-workspace.css and keep page / onNavigate out of BudgetList. The Budget sub-group and the separate Budget Reports and Budget Settings screens are deleted, so never restore their components, page routes, styles or App.jsx routing names. The Budgets register reuses the datagrid and the search / filter placement of the other registers: div.budgetRegisterBar is the first child of section.budgetListCard and holds label.budgetFilterSearch, select.budgetFilterYear and one details.budgetFiltersMore Filters disclosure (div.budgetFiltersPanel = Budget type, Budget period, Scope, Status, plus Clear filters and a badge driven by the one activeFilterCount memo), with table.budgetTable following inside the same card and only Import / Create Budget left in div.budgetHeadActions. Copy the geometry from .pc-filters / .pc-filter-search / .pc-filters-more / .pc-filters-panel and .pc th / .pc td - 38px controls in a 14px 20px 16px bar, a 12px 14px header on #f5f7fb in 12px/600 #68768b, 13px 14px body cells with border-top 1px solid #edf1f6, and a right aligned last column - instead of inventing new values, and never restore the full-width filter row inside the card, the deleted budgetRegisterHead / budgetFilterMore / budgetSearch classes or the header-cell radius workaround. The page renders no status card row: keep section.budgetMetrics, the .budgetMetrics / .metric rules and the Total Budgets / Active Budgets / Total Planned Income / Total Planned Expense / Expected Profit labels deleted, and let div.budgetV3Head lead straight into section.budgetListCard even though budgetMetrics(...) stays exported from src/budget-store.js. The Create Budget wizard head and step row are the journal pattern: div.budgetV3Head.budgetWizardHead holds button.budgetHeadBack, span.budgetHeadTitle and a neutral span.budgetStepCount reading Step n of 3 with no subtitle, and nav.budgetSteps > ol renders one li per step (current class=active with aria-current=step, finished class=complete showing IconCheck, later class=upcoming and disabled) - never the underline tab row, the wizard button.budgetBack text link, the removed subtitle or the tab-era .budgetSteps{overflow:auto} / .budgetSteps button{min-width:155px} rules, and keep the detail screen .budgetBack styling. The Create Budget wizard head is a 190px tile matching the header's working-context control (.companySelect{width:190px}) placed under that bar rather than a full-width bar, and the Step n of 3 chip lives inside the stepper card, whose nav.budgetSteps is a minmax(0,1fr) auto grid with span.budgetStepCount justified to the end - so keep the head slice free of the chip, keep button.budgetHeadBack at 30px in a 7px radius, and keep the .app main>.budgetV3>.budgetV3Head.budgetWizardHead rule above the generic .budgetV3Head reset. The stepper must never truncate a step name: li is flex:1 1 170px, the li:not(:last-child)::after connector is painted behind every label at z-index 0 while each button sits above it at z-index 1 with a #fff background and padding 0 12px 0 0, and button span stays nowrap with no text-overflow ellipsis and no overflow hidden, with the connectors hidden and button span back to white-space normal below 760px. Step one of Create Budget asks for each fact once: it no longer renders a Budget type field (form.type keeps its Profit & Loss Budget default so the account step still filters), spreads Budget name across the row with budgetFieldWide, and renders OrganisationBranchScope in the opt-in multiple checkbox-list form. That multi-select form is the only place checkbox lists are used - Create Journal Entry journal lines and the Period Closing lock drawer keep their one-choice selects - and both forms read the same rows from the pure scopePickerRows(state) and toggleScopePicker(state,row) helpers in src/organisation-scope.js, whose scopePickerState also reports branchesAll, resolved, selectedBranchIds, organisationSummary and branchSummary; never add a multi flag to getScopeVisibility() or scopePickerState(), because visibility stays derived from the available organisation list, and never rebuild those option lists in a page. Style that form from src/organisation-branch-scope.css with every rule scoped to .pc-scope-picker, all text at 12px or larger, and the panel static rather than absolutely positioned below 760px. The Create Budget head is the page header, not a card: it must align beneath the working-context organisation and branch bar and match the header width, exactly like the Create Journal Entry header, so keep .app main:has(>.budgetV3.budgetWizard) and .app main>.budgetV3.budgetWizard dropping the shell padding and max-width, keep the head at width 100%, max-width none, min-height 56px, a 0 0 16px bottom margin that keeps the step card clear of the bar, padding 8px 24px, border-radius 0 with both side borders removed and no shadow, keep button.budgetHeadBack at the journal's 40px with an 8px radius, keep span.budgetHeadTitle at 16px 650 1.25, and keep .app main>.budgetV3.budgetWizard>:not(.budgetV3Head) restoring the clamp(16px,2.2vw,32px) gutter on the stepper, the step cards, the error line and the wizard footer. Never pin that head to a fixed 190px tile or match it to the .companySelect width, and never put a card border, radius or shadow back on it. span.budgetStepCount stays inside the stepper card, below 760px the head shrinks to a 36px column with 8px 12px padding to follow the org bar, and the stepper still must not truncate a step name. The Create Budget scope row reads its scope universe from getScopeOrganisations() in src/organisation-context.js, never from the narrowed working context getAccessibleOrganizations(): a plan may cover any organisation the header Change / Customize drawer can offer, so the multi-select can never be left with nothing to choose, the wizard opens seeded from the working context through the one wizardScopeSeed(initial) helper, and a saved budget keeps its stored companyIds / branchIds so editing reopens the same scope. Step one is one four-column field grid (repeat(4,minmax(0,1fr)) with budgetFieldWide spanning two columns and the scope fields at grid-column:span 2, folding to two columns below 1100px and one below 700px) and .budgetWizard .budgetSetup{overflow:visible} keeps the open checkbox list from being clipped by the card. Never add a private organisation/branch reader, a second scope universe or a page-local copy of the scope rows."
  ],
  "implementationStatus": {
    "overall": "Prototype only",
    "verifiedImplementation": "See source, tests and modular documents",
    "productionBackend": "Planned",
    "periodClosing": "Partially implemented"
  }
}
