# AI Handoff Prompt

**Last verified:** 2026-09-26

Day Book invariant: `src/daybook-service.js` is a report projection. It must read only posted journals, never alter postings, exclude cancelled source documents unless `includeCancelled` is explicit, and keep a complete journal voucher when filtering by account or dimension; never total a subset of its lines.

Current Credit Note invariant (2026-09-26): a note stores `creditMethod` (`Item Based Credit` / `Amount Based Credit`) and `inventoryImpact` (`Return Stock To Inventory` / `Financial Adjustment Only`), both normalised through `creditMethod()` / `inventoryImpact()` in `src/credit-note-service.js` on every save. An amount-based note has no quantities: `calculateCredit` builds ONE synthetic line with `invoiceItemIndex:-1`, takes its tax from the invoice's own blended component rates (`billedTaxRates`) or from `chosenTaxRates(form,taxPolicy)` on a standalone note, and caps the credit in total per tax component. Never let an amount-based note raise a Sales Return. `creditPostingPlan` may be overridden for one document by the note's own `adjustmentAccount`, which is why the form only offers Income accounts. The type vocabulary is `Against Invoice` / `Standalone Credit Note`, with the earlier `Without Invoice` still read through `noteType` - never assume a stored value is current. The create page shows NO Tax summary and NO posting preview: the Tax summary card and the `button.cnImpactToggle` Accounting Impact Preview were both removed on 2026-09-26 and their state and CSS went with them, so the page runs Customer, Credit note details, Original invoice summary, Credit items or Adjustment, Credit summary, Additional details. The Credit summary is the only statement of the figures on the form and it closes on a Total tax line; the posting preview and the account-mapping error stay visible on the detail page's Accounting tab and at Post time. The Adjustment card states four fields to a line - Adjustment amount, Tax treatment, Place of supply and Adjustment account - and the place of supply moved there from the standalone Original invoice details card, which is hidden while the Adjustment card shows. Two defects from this pass are worth remembering because neither the bundler nor a server-render test can see them: a constant used in `start()` but missing from the `./credit-note-service.js` import crashed the running app with `DEFAULT_CREDIT_METHOD is not defined`, and a fragment conversion that left one extra `}` printed a literal brace on the page - both only appear when the form actually renders, so open Credit Notes -> New credit note in the running app before believing this page works. `tests/credit-note-ui.test.mjs` now guards both.

Current portalled-popup invariant (2026-09-26): anything rendered with `createPortal` into `window.document.body` - a drawer, popup or menu panel - must be a `div`, never an `aside`. The prototype still carries `@media(max-width:700px){aside{transform:translateX(-100%);transition:.2s;width:250px}}` in `src/styles.css` to hide the app sidebar on a phone, and it slides any portalled `aside` a full width off the left edge; the Credit Note invoice-facts drawer measured `transform:matrix(1,0,0,1,-404.8,0)` at 420px until it was changed from `aside` to `div`. Keep the overlay `position:fixed;inset:0` with the panel right-aligned at `width:min(430px,94vw)` (`100%` below 560px), a pinned head above one scrolling body, dismissal from a backdrop, a close button and `Escape`, and every value inside read-only and never recomputed. The open state should be an id that changes with the record, so switching records closes the popup without an effect. `src/CreditNotes.jsx` (`.cnInvoiceLayer` / `.cnInvoiceDrawer`, `tests/credit-note-ui.test.mjs`) is the reference implementation.

Current invoice control invariant (2026-09-25): `command(state, action, payload, ctx)` in `src/invoice-engine.js` enforces duty through `invoiceAllowed(role, action)` when a role is supplied (Sales Executive and Accountant save/submit/send; Finance Manager and Admin also post/cancel/recurring), **requires `Pending Approval` to post**, and requires the invoice to carry its own `organizationId` and `branchId`. The role-less call is a compatibility shim for tests and importers, not an authentication boundary, and no page may call `command` without a role - `InvoiceWorkspace` derives it from the View as switch and renders "Approve & post" only where the viewer may post, with `p.ivPermissionNote` naming who can otherwise. `convertSalesOrder(state, order, ctx)` refuses anything but a Confirmed order and dates the invoice when it is raised (`date = today()`, `dueDate = today + terms`). The customer tax identity is frozen on the document (`customerGstin`, `customerGstTreatment`, `customerState`), which is also what makes `src/DocumentPreview.jsx` print `GSTIN:` on a sales invoice. Cancelling an order is blocked while a live invoice exists; converting and completing need Confirmed; `trail(order, action, from)` records the acting role and the transition. `nextInvoiceNumber` scans for the first free number. `src/GeneralLedgerPro.jsx` must keep reading only the real journal store - its sample ledger and sample account list were deleted - and must keep deriving its Branch and Cost-centre options from the rows.

Current sales document create page invariant (2026-09-25): Create Sales Order and the New sales invoice page render one shared component, `src/SalesDocumentFields.jsx`, as one stacked column in the order Customer, then the details card, then the lines, then the Billing summary. The customer card names itself once (`<h3>Customer *</h3>` carries the label and the required mark) and the customer field is a PLAIN SEARCH BOX (`CustomerSearch`), not the shared `SearchSelect`; its focus handler must clear the query and re-open the list and its `pick` must blur the input. `div.soCustomerRow` is `display:flex;align-items:center;gap:10px` with the search box at `flex:0 1 640px` and a 40x40 `aria-label="Create customer"` `IconPlus` at the end, wired to `CUSTOMER_CREATE_KEY` (`wayvida-open-customer-create` in `src/quick-create.js`) with the draft stashed and `wayvida-return-to` set, which `src/Customers.jsx` reads once to open a blank form. The chosen customer reads in `div.soCustomerProfile` as a 44px initials avatar beside `div.soCustomerLines` holding TWO lines - `div.soCustomerHead` with `strong.soCustomerName` at 14px/650 plus an ALWAYS-rendered 12px `span.soCustomerType` whose text is `customerTypeOf(selectedCustomer)` exported by `src/Customers.jsx` (the one rule the master normaliser uses: anything not `Individual` is `Business`; never print the raw `type`, which left the chip blank and disagreed with the master), then `span.soCustomerAddress` reading `Billing Address: <full address>` at 12.5px/400 - and `div.soCustomerMeta`, a right-aligned `Net receivable` stat over `money(creditOutstanding)`, which BOTH hosts must pass from `customerCreditSummary(state,customerId).outstanding` and which is hidden below 620px. `div.soCustomerRef` is `display:grid;grid-template-columns:minmax(0,1fr) auto;align-items:center;gap:10px` holding the strip and `div.soCustomerActions`, whose 34x34 `aria-label="Edit address"` pencil and 34x34 `aria-label="View customer details"` eye button both keep `margin-top:0`; `div.soCustomerMoreBody`, `p.soCreditNotice` and the empty state span both columns with `grid-column:1/-1`. The details card scope row is THREE fields - Organisation, Branch and Place of supply - through `.soFieldRow3`, and `PlaceOfSupplyField` is rendered by the shared component itself. Additional details holds Terms, History and Documents only, one `section.soMoreGroup` per job, and the Terms group carries NO `soMoreGroupTitle` because the field already labels itself "Terms & conditions". Every order/invoice LINE has ONE item box, `ItemSearch` in `src/InvoiceLineControls.jsx`, which takes `onAdHoc` and `onType` beside `onSelect` and builds ONE option list - the Item master matches plus `Use "<text>" as an ad hoc item` - walked by ArrowUp/ArrowDown/Enter through `const move=d=>setActive(a=>Math.max(0,Math.min(a+d,rows.length-1)))`; `pick` sends an adHoc row to `onAdHoc(row.text)` and resets the line with `{...blankLine(),adHoc:true,description:text}`, the same shape `addLine()` then the ad-hoc offer produces, and `onType` is wired ONLY when the line is already ad hoc. `input.soAdHocName` is DELETED - never put a second item-name field back on the line. The ad-hoc option row is `span.ivAdHocOption` (primary colour) styled in `src/invoice-workspace.css`. Each field is the Record Transaction control (`Field` over `label.soField` + `span.soControl` with an 18px icon inside it), and the document ID is editable with the number the save will take as its placeholder from `autoNumber` - never as a `value`. The Billing summary is the last card: `div.soSummaryGrid` is `minmax(0,50fr) minmax(0,35fr)` with `justify-content:space-between`, `div.soSummaryLeft` holds the Customer note textarea at `form.notes` then the host `{children}`, and `div.soSummaryHead` is a `space-between` flex row holding the `h3` and the `span.soCurrencyChip` reading `INR` together, with the totals and the posting note beneath in `div.soSummaryRight`; it folds to one column below 900px. The line grid is SIX columns and SEVEN cells with the unit inside `div.ivQtyCell`, and its cell padding must carry !important over the global `td{padding:16px 20px !important;height:64px}` block at the end of `src/ui-system.css`. `+ Add item` is `details.soAddItem` with exactly TWO entries since 2026-09-25 - `Add item row` (`addLine()`, one blank line) and, only with `onCreateItem`, `Create new item` - because the line's own item box already offers the typed text as an ad hoc item. Each line states the HSN/SAC code it inherited as `small.soHsnValue` reading `HSN 4820` or `SAC 9983` (`itemDefaults` copies `hsnSac` and `itemType`), and `src/DocumentPreview.jsx` prints an `HSN / SAC` column between the item and its quantity, which made that table EIGHT columns: every `.dpItemsTable` width is positional and is restated 4/21/13/12/12/12/10/16 since 2026-09-26 with the right-align rule on `nth-child(n+5)`. Each create head also ends in `<NumberFormatControl/>`, the 40px light-primary `button.soHeadSettings` that opens the Number and Currency Format dialog. The shipment, delivery and salesperson fields are gone.

Current Number and Currency Format invariant (2026-09-25): `src/number-format.js` is the ONE formatter. It reads and writes its preference through `src/settings-store.js` - `numberFormat`, `currencySymbol`, `roundOffQuantity`, `roundOffRate` plus the existing `decimalPlaces`, all under `organization` - and exports `formatRupees(value,override)` for rupee values, `formatMinor(minor,override)` for the engine stored paise, `formatQuantity` / `formatRate` for the two round-off switches, `numberSample`, `decimalChoiceOf` / `digitsOfChoice`, `saveNumberFormat` and `useNumberFormat()`. Every money helper in the app delegates to it: the engine `money`, and the file-local helpers in `Customers.jsx`, `GeneralLedgerPro.jsx`, `SalesOrderActions.jsx`, `SalesOrders.jsx`, `Vendors.jsx`, `simple-journal-transaction.js`, `journal-templates.js`, `inventory-adjustments.js` and `Items.jsx` - never reintroduce a `toLocaleString(posix,{style:"currency"})` literal on a money surface, and never hand a rupee value to `formatMinor` (item prices are rupees; engine amounts are paise). `src/NumberCurrencyFormatDialog.jsx` renders `NumberFormatControl`, a 40px `button.soHeadSettings` in the LIGHT primary variant because the one solid primary fill on the page is Save, mounted by BOTH create hosts inside `.soCreateHead`; its dialog is a `.nfLayer` fixed layer copying the address editor tokens, and its label rules are scoped `.nfLayer .nfFormat` / `.nfLayer .nfDigits label` because `.invoiceWorkspace label` outranks a bare single-class selector and will otherwise stack and centre the radios. `decimalChoiceOf(2)` returns `default` on purpose: Default IS two decimals, so it and `99.00` are the same precision.

Current Journal Entry invariant (2026-09-24): the Journal Entries module is transaction-first. `src/SimpleTransactionForm.jsx` is the Record Transaction create/edit page with a Transaction type dropdown (Expense, Income, Transfer, Customer Payment, Vendor Payment), Transaction name, Date, the type's own money fields, Amount, Organisation and Branch, and a collapsed Additional details section. Never reintroduce debit/credit or journal-line vocabulary into the normal journey, and keep the double entry behind the collapsed `View accounting entry` block. `src/simple-journal-transaction.js` owns the vocabulary, the `journalAllowed()` duty table, validation, posting lines, `nextJournalNumber()`, `duplicateTransaction()`, `reversalRecord()` and the demo sample definitions.

Current Journal Entry lifecycle invariant (2026-09-24): Draft -> Pending -> Published, Pending -> Rejected, Rejected -> Resubmit, Published -> Reversed. Only Draft and Rejected are editable; a Published transaction is corrected by its reversal. One `action()` in `src/JournalEntriesPro.jsx` owns every transition, rejection demands a reason, publishing posts through `journal()` with the `Journal Transaction` source and one idempotent `journal-transaction:<id>` token, and a payment against an invoice or bill is settled by that document's engine only. The acting role follows the View as switch through `reviewRole`, exactly as `src/inventory-adjustments.js` simulates approval duty, so Publish, Reject and Reverse must stay out of the menu for a role that may not approve.

Current Journal Entry register invariant (2026-09-24): the register shows exactly Journal ID & Date, Transaction details, Account, Transaction Type, Amount, Status, Organisation, Branch, Actions - nine columns, no Debit or Credit column - and the amount is the user-facing transaction amount from `journalRecordAmount()`. Search covers Journal ID, transaction name and reference; the filter panel covers Status, Transaction type, Account, Organisation, Branch and date. `src/journal-register-display.js` projects those cells for simple and advanced rows without mutating stored records, and maps `Pending Approval` and `Approved` to Pending and `Posted` to Published on read. Keep `src/demo-data.js` seeding the register (the DEMO_VERSION marker) with every status and every transaction type.

Current Record Transaction layout invariant (2026-09-24): the register's third column is **Transaction details** - the transaction name with its reference stacked beneath it - and must not be renamed back or flattened into one line. The entry mode is one **Simple / Advanced** `ModeToggle` (exported by `src/SimpleTransactionForm.jsx`) rendered by both the simple page and the advanced editor, so it must never regress to a single "Advanced mode" button or a one-way "Simple mode" link. The **Wayvida will record** card was removed at the user's request: the register's first column is **Journal ID & Date** (the id over the date), the form fields run four to a line inside one full-width card, and the page ends in a footer holding the one-line summary, a View accounting entry tooltip and the Cancel / Save draft / Submit for approval / Publish now actions, which never sit in a side card and must pin to the window while the page scrolls; Description, Reference number and the attachment control must stay on one row of Additional details, and the accounting entry must stay a tooltip rather than a disclosure that reflows the page. A Transaction type is always selected - the dropdown has no empty option - so `blank()` keeps `transactionType:TRANSACTION_TYPES[0][0]`.

Current register pagination and gutter invariant (2026-09-24): the Journal Entries register shows ten transactions to a view through the shared `.pagination-footer` pager, keeps its first and last cell inset at 24px (through the extra `.je-card` ancestor, because `src/journal-preview.css` loads later and restates the last cell) and exports every filtered row rather than the visible page. The Chart of Accounts "Account Name" header must keep the 58px inset with `!important` - `src/ui-system.css` pins every first cell to 24px with `!important`, and without the restatement the account names sit 34px right of their own header.

Current sidebar and account-drawer invariant (2026-09-24): Budgets and Period Lock live in their own **Other** section (`{label:'Other',icon:IconFolders,children:[leaf('Budgets'),leaf('Period Lock')]}`) directly after Accounting, which holds only Chart of Accounts and Journal Entries - do not move them back. The Create Account drawer keeps Account Code as an editable input (blank still auto-generates, a typed code is kept, duplicates and malformed codes are refused by `src/account-master.js`), disabling it only for a system account or one a posted transaction references. Its close control is a 38px square with a 20px icon, written through a `.am` ancestor because the generic `.am button` padding outranks a bare class.

Current reserved system ledger invariant (2026-09-25): `RESERVED_POSTING_ACCOUNTS` in `src/invoice-engine.js` is the one table of system posting ledgers and `account()` is the one guard that accepts or repairs them. It must repair a reserved ledger for a journal line that names no expected type, because `journal()` never names one and the receipt, purchase, banking and demo-seed paths all post through the hardcoded bank ledger `1010 · Bank`; three private healers (`ensureDefaultBankAccount()` in `src/purchase-service.js`, `ensure()` in `src/operations-store.js`, `ensure()` in `src/banking-service.js`) cover the other paths but must not be the only cover. A shortage here is fatal rather than cosmetic: `bootstrapDemoData()` posts the demo transactions before it writes `wayvida-demo-data-version`, so a throw leaves the marker unwritten and every later load reports **Wayvida Books could not start.** Never remove `1010` or `2000` from the reserved table, and never reorder that write so the failure becomes permanent-but-silent. A repair keeps the name and purpose an operator gave the ledger and restores only the type, nature, group, active state and control-account role the reserved definition owns; when a caller names no type and the account is not reserved, the message must name the account's own type (`Configure an active posting Expenses account for 5300.`) rather than the word "ledger". Since 2026-09-26 the table also owns `2000 · Accounts Payable`: `src/purchase-service.js` repaired it privately in `ensureDefaultPayableAccount()`, but `createBankingDemo()` in `src/banking-service.js` posts a demo vendor payment through `2000` from `journal()`, which names no type, so a stored payable that an operator had deactivated or turned into a heading aborted `bootstrapDemoData()` with `Configure an active posting Liabilities account for 2000.` before the version marker was written. Keep every hardcoded system ledger in the reserved table, not only the bank ledger.

Current journal approval invariant (2026-09-25): `postsToLedger(status)` in `src/simple-journal-transaction.js` is the one answer to whether a journal owns a ledger posting - true only for `POSTING_STATUS` (Published, the approved status) and false for Draft, Pending, Rejected and Reversed - and the single `action()` in `src/JournalEntriesPro.jsx` must re-check the duty (`journalAllowed(role,duty)`), the status (`JOURNAL_ACTIONS[status]` must offer the step) and a prior `ledgerJournalId` before it posts, so hiding the Publish or Reject button is never the protection. Keep the engine token `journal-transaction:<id>` as the second guard. The operator-facing label for that step is **Approve** while the action key stays `Publish` and the status stays `Published` - do not rename the key or the status, because `TRANSACTION_STATUSES`, `JOURNAL_ACTIONS` and `normaliseStatus` are asserted by tests and the legacy `Posted` wording depends on that mapping. Do not invent a second status vocabulary: draft, pending approval, approved and rejected map onto Draft, Pending, Published and Rejected, and cancellation maps onto delete and reversal. The detail Audit Trail must keep stating who submitted, approved, rejected and posted with their timestamps, reading `publishedBy`/`publishedAt` while still accepting the older `approvedBy`/`approvedAt`. The Record Transaction card opens with the full-width **Journal details** section head and keeps the money and category guidance as visible helper text under the control, not only inside the closed select. The application header must not regain a global search: `navToggle`, `hamb` and `headerActions` are its children, `.headerActions` owns `margin-left:auto`, and `header label`, `header input`, `.app header label`, `.app header>label` and its two media-query overrides stay deleted from `src/styles.css`.

Current journal status vocabulary invariant (2026-09-25): the journal module stores **Draft, Pending Approval, Approved, Rejected, Reversed, Cancelled** in `TRANSACTION_STATUSES`, the same words budgets, credit notes, invoices and inventory adjustments use, because Period Lock counts pending journal approvals with `status==='Pending Approval'`. Do not reintroduce the retired Pending or Published spelling in new code; `normaliseStatus()` maps Pending to Pending Approval and Published/Posted to Approved on read, and nothing rewrites stored records. Only Approved posts (`POSTING_STATUS`, `postsToLedger`), Reverse requires a real `ledgerJournalId`, and Cancelled is an unposted step from Pending Approval that needs a reason and stores `cancelledBy`/`cancelledAt`/`cancellationReason`. The approval action key is `Approve` and must stay re-validated in `action()` (duty, status, prior posting) and in the advanced editor's `commit()`; `statusText(status,business)` is the one place the displayed words are chosen.

Current journal detail layout invariant (2026-09-25): the journal detail is ONE section — `div.je-detail-single` as a 7fr/3fr grid whose left column (`div.je-detail-main`) holds `section.je-detail-facts` (heading Journal details), `details.je-impact` (heading View Accounting Impact, closed by default, containing the debit and credit table) and `section.je-detail-docs` (heading Supporting Documents), and whose right column is `div.je-detail-audit` (heading Audit Trail, sticky). There is no tab row and no detailTab state; do not restore them. The audit column must be a `div`, never an `aside`, because `src/styles.css` styles every `aside` as the fixed navigation drawer and would tear it out of the grid. A Draft journal shows Submit for approval and a Pending Approval journal shows Publish journal, both as `je-primary-action`; the action key stays `Approve` and the status stays `Approved`, with the label set once in `ACTION_LABELS`.

Current journal detail field grid invariant (2026-09-25): the Journal details card lists its fields FOUR to a line, each label rendered as `<dt>{Icon&&<Icon size={15} stroke={1.8} aria-hidden="true"/>}{label}</dt>` over its value, folding to three at 1280px, two at 1050px and one at 700px; keep an icon on every field row. `section.je-detail-docs` (Supporting Documents) belongs INSIDE `section.je-detail-facts`, not as a sibling card, so the card holds the fields, the attachments and then the accounting-impact accordion, with the audit log in the right column. A Pending Approval journal must render `div.je-permission-note` for a role that may not post it - naming the View as switch - so the publish action is never silently absent; do not turn that note into a second way to approve.

Current journal field-order invariant (2026-09-25): the Journal details card lists its fields FOUR to a line (three at 1280px, two at 1050px, one at 700px) with an icon on every label. The creation form in `src/SimpleTransactionForm.jsx` must ask for the scope first: Organisation, Branch, Transaction Type, Transaction Name, Date, the dynamic accounting fields, Amount, then Additional details — do not move Organisation and Branch back below Amount, and keep the dynamic block filtered by Transaction Type (Pay From plus Category for Expense, Deposit To plus Category for Income, From and To Account for Transfer, Customer plus optional Invoice plus Deposit To for Customer Payment, Vendor plus optional Bill plus Pay From for Vendor Payment). `tests/journal-create-ui.test.mjs` asserts that order.

Current journal audit timeline invariant (2026-09-25): the Journal details field list must NOT repeat the status - the chip beside the journal number states it and only the CSV export keeps a status row. The audit panel is a timeline built by `journalTimeline(journal)` in `src/simple-journal-transaction.js`, which returns one `{key,kind,actor,at,when,text,note}` per material event newest first from `journal.audit` or from the stored stamps, with `relativeTime(value)` supplying just now / Nm ago / Nh ago / Yesterday / Nd ago. Render it as `ol.je-timeline` with a 34px icon mark per event on the connecting rail, the actor in bold followed by the action sentence, the reason as a note and the relative time in 12px grey - do not fall back to a flat list of sentences.

Current journal publish invariant (2026-09-25): the approval guard must be written against the status being left, not the status being reached - the earlier `if(next===String.raw`Approve`&&!postsToLedger(current))return;` refused every publish because Pending Approval is not the posting status, so the button did nothing. It reads `if(next===String.raw`Approve`&&postsToLedger(current))` and refuses only an already-posted journal. An approver must be offered **Publish journal** and not **Submit for approval** on a draft (JOURNAL_ACTIONS.Draft carries Approve, and actionEntries swaps the two by the post duty), so a draft always shows exactly one primary action. The audit timeline body carries a 6px top padding so its first line sits against the mark centre, and the attachment empty state inside the Journal details card is body sized - 13px heading, 12px sentence - never a headline.

Current journal history timeline style (2026-09-25): the history card is 24px padded with a 14px radius; the icon column is a fixed 40px holding 40x40 circular badges; a thin 1px connector sits at left 19.5px from 46px to 6px above the bottom, on every item except the last; each entry is a flex row whose body is flex:1 min-width:0 with a 7px top padding; events are 24px apart; the username is semibold and MUST keep white-space:nowrap because a name like Admin used to break across two lines in the narrow column; the description stays regular at 13px; a status change renders as two compact je-status-badge pills (approved green, rejected red, pending blue, reversed amber, draft and cancelled neutral); a reason sits in the subtle note box; the timestamp is a 12px muted block below. Do not change the history logic, the event set or the stored data.

Current journal audit panel invariant (2026-09-25): the timeline must stay inside `div.je-timeline-panel` and never inside `div.je-audit`. `src/journal-entries.css` still carries legacy rules for the older panel markup - `.je-audit p{display:flex!important;...;border-bottom:1px solid #edf0f4!important}`, `.je-audit p span{display:flex;flex-direction:column}` and `.je-audit p small{...}` - and because they are `!important` they beat any new rule. Putting the sentence in the `je-audit` scope makes it a flex row whose text runs shrink to one character per line and draws a full-width divider under every event; that was a reported defect. Keep `.je-timeline-body>p` explicitly `display:block` as a second guard.

Current Supporting Documents invariant (2026-09-25): the documents block inside the Journal details card carries exactly ONE heading ("Supporting Documents", or Documents in business mode) followed directly by its content. A journal with no attachments states it in one quiet 12px line - `p.je-docs-empty` with a 14px paperclip, "No documents attached yet. Attachments uploaded with this journal will appear here." - never a second heading over a sentence, because that pair read as another row of the field list and was reported as a layout defect. Do not reintroduce the `.je-empty` heading block or the `.je-detail-facts .je-empty` rules in this card.

Current advanced journal entry structure (2026-09-25): the advanced create page shares Simple header markup - `header.rt-header` with `button.rt-back`, an `h1`, then ModeToggle and the status badge - so the Simple / Advanced control sits in the same fixed position in both modes; `.je-create .rt-header{margin:0 0 16px}` drops the bleed. Organisation and Branch are journal level header fields placed before Date, in the order Organisation, Branch, Date, Journal Type, Reference Number, Reason, and a line is exactly Account, Debit, Credit, Description and the delete action on a five track grid. Never put Organisation or Branch back on a line: the header owns the scope and `alignLine` stamps every line from it, which is what keeps `journalOrg`/journalBranch`, the register columns, the scope filters and the Period Lock branch check working. The header fields now carry Simple field tokens and the footer opens with Cancel. Three things to preserve: the advanced page has its own page scoped layout (`.app main:has(> .je-create:not(.je-business)) .je-create .je-form-card` and its grid descendant) that pins four columns and 14px/16px padding at high specificity - the alignment rules restate it at equal specificity later in the cascade and are marked for deletion if the denser layout is wanted back; the header must stay a `div.rt-header` because Simple uses a div; and below 760px the advanced header hides its status badge and drops to 16px card padding so it does not wrap to two rows. Never reintroduce Organisation or Branch on a line, and never re-add the dead `.je-line.no-organization` rules.

Copy the prompt below into a new AI engineering session and attach the repository or `docs/` folder.
---

You are working on **Wayvida Books**, an Indian accounting ERP React/Vite prototype. Begin by reading `AGENTS.md`, `docs/TRANSFER-TO-ANOTHER-AI.md`, `docs/README.md`, `docs/PROJECT-OVERVIEW.md`, `docs/TECHNICAL-ARCHITECTURE.md`, `docs/ACCOUNTING-DOMAIN.md`, `docs/MODULES.md`, and `docs/project-manifest.json`. Treat source code and tests as truth for implemented behavior. Treat prose requirements as target behavior only when they are marked Planned or Partially implemented.

Resume checkpoint: work from the repository root and run `git status` before editing. The current branch is `main`; the committed baseline is `897da03 Roll the register shell across the app and flatten Chart of Accounts`, already pushed to `origin/main`, and it contains Inventory Adjustments, the Items multi-scope master, the field-first New / Edit Inventory Adjustment UI and the merged Item Details header panel. Treat the working tree as clean: the only untracked file is the local scratch artifact `.codex-item.patch`, which `.gitignore` excludes, so never commit it. Do not treat it, `dist/`, screenshots, browser storage or a machine-specific path as product source. Latest Items-focused checks: 24/24 passed, Inventory Adjustment 25/25, and the Item Details page was inspected at port 4001 without changing data. The production build has not been re-run since the header merge, so run the documentation validator and the production build again after any continuation change.

Preserve these invariants:

1. Store/calculates accounting amounts as integer paise.
2. Never persist an unbalanced journal.
3. Operational documents generate journals automatically; do not add debit/credit inputs for normal users.
4. Sales orders do not post accounting and may only generate invoice drafts.
5. Posted transactions are corrected by reversal, credit note, or current-period adjustment—not deletion or silent mutation.
6. Account/tax/company/customer/item/branch mappings come from masters where possible.
7. Reports derive from posted journal lines and clearly separate unposted documents.
8. Browser storage and role selectors are prototype-only, not production security.
9. Keep Wayvida Books branding, Instrument Sans, minimum 12px text, Tabler icons, consistent shell/navigation, responsive non-overlapping tables, and accessible labels/focus.
10. Preserve Sites files and run tests/build before handoff.
11. Preserve the global terminology layer in `src/terminology.jsx`: Business Owner and Accountant views change display labels only, never routes, stored accounting values, workflows, or calculations. The preference key is `wayvida-terminology-view`, with `wayvida-coa-view` retained for compatibility. The switch lives in the header account menu (`details.profileMenu` in `src/App.jsx`) as `Business owner` (Admin) and `Head of Accountant` (Staff) - keep `View As:` out of the header action row and out of `src/Navigation.jsx`.
12. After every material change, synchronize the cross-AI transfer package according to `AGENTS.md`. Summarize durable decisions and current behavior; never store raw chat transcripts, secrets, credentials, or personal browser data.
13. Journal Entries defaults to the transaction-first **Create Journal Entry** flow. Keep `src/simple-journal-transaction.js` as the single adapter from selected business accounts to two balanced journal lines, use only active Chart of Accounts records (never hardcoded names), and keep **Accounting Details** secondary. The form has **Journal Details** and **Entry Details** sections but no duplicate global switcher summary. Multiple selected scopes still require one organisation and its branch, and a switcher change clears the form choice. The existing `JournalForm` remains the **Advanced Mode** path for accountants; preserve its controlled approval/posting/reversal lifecycle and never allow a manually selected reversal type.
14. Period Lock shows only four accountant-facing statuses — `Open`, `Closing`, `Closed` and `Reopened` — produced by `closingStatus()` in `src/period-locking.js`. Do not render engine statuses such as `Soft Locked`, `Hard Locked`, `Reclosed`, `Temporary Unlock` or `Pending Unlock Approval` in the interface, and do not change `periodStatus()`, `validatePeriodAction()` or `validatePostingDate()` semantics to match the display vocabulary. Closing must run the five-point checklist (invoices posted, bills approved, no draft journals, bank reconciliation, tax adjustments) through `closePeriod()`, which records the closing user, timestamp, checklist snapshot and audit event. Unlock requests stay separated from approver decisions, self-approval stays blocked, and period policy keeps the single storage owner `wayvida-period-settings-v1`, edited from both Period Lock → Settings and Settings → Accounting → Period control. One stored `lockingMode` is the switch: the Settings card maps `Off` ↔ `Manual` and `Automatic` ↔ `Manual` onto it through `normalisePeriodSettings`, and switching automatic locking off means manual locking. Never drive the derived `autoLockPreviousMonth` mirror directly, and keep its Closing frequency and Close after days fields on `schedule.frequency` / `schedule.lockAfterDays`.
15. Keep `WayvidaDatePicker` centrally mounted from `src/main.jsx`. It is presentation-only: confirmed dates must keep using existing ISO values and dispatch normal events so module validation and accounting-period controls continue to work.
Show right-aligned Total Debit and Total Credit under the Journal lines grid, with the balance status as the only posting indicator on the page (`Debit equals credit`, or `Difference: <amount>`) reading directly beneath those totals, and the Attach files button aligned with the left edge of the lines grid
16. On the Create Journal Entry page, `Post journal` is gated on the posted values alone: balanced totals (`difference === 0 && totals.debit > 0`) inside an open accounting period (`periodCheck.allowed`). Do not re-add an approved-status requirement there, and do not restore the removed Cancel button, the gate explanation paragraph or the three-dot More actions menu. A Draft journal is submitted for approval from the journal detail screen (`Submit for approval` then `Approve` then `Post`), while Duplicate and Reverse entry stay on the register rows and the detail screen.
17. The Create Journal Entry line grid renders the Organisation and Branch columns only while the working context needs them: one accessible organisation hides Organisation, a single branch under it hides Branch as well (five columns), one organisation with several branches keeps Branch only (six columns), and several organisations keep both (seven columns). Keep the Branch dropdown filtered to the selected organisation, reset a line's branch when its organisation changes, and never add a second organisation or branch selector - report the implicit choice in the Journal lines subtitle instead.
18. Period Lock stays a lean single-purpose screen, not a dashboard. Its body is the locked-periods register: the heading carries the page title alone - and that title is `t.periodClosing` from `useTerminology()`, so it reads `Period Lock` for accountants and `Financial Lock` for the business owner, never a hardcoded `Close Books` - with a one-line journal-style subheading beneath it (`Lock accounting periods and control when postings stop.`), then one right-aligned action group: the `Before you close` attention pill first (hidden while every check passes, a slightly wider gap after it so the pill reads as status and the controls beside it as actions), one `More actions` menu holding Current accounting period and Period closing settings, and one separate `Create Lock / Close` button. The register's `Requests` half is one table, never a card grid or a view switch: the columns Period, one combined `Organisation & Branch` cell, Reason, Requested, Requested By, Status and Actions in the shared `pc-table-fit` / `pc-request-table` shell, one `RequestActions` renderer per row (one read-only `View` button plus one `.pc-kebab.pc-request-kebab` 3-dot menu holding `Approve`, `Cancel`, `View Details` and `Delete`; `View` and `View Details` both open the single-request `RequestDetailsDrawer`, and `Delete` drops the record through `deleteUnlockRequest`), and every filtered request rendering in one pass with no pager. Do not restore the `Cards` / `List` toggle, `div.pc-request-view-bar`, `article.pc-request-card`, `pc-request-none`, a `Reject` action or a pager. `Before you close` is a compact, never-dismissible attention pill (`details.pc-attention-pop` > `summary.pc-attention-pill`) in that action group: an amber warning icon plus the count as text (`3 checks`, never a badge), the accessible label `Review <n> checks that need attention`, no trailing chevron, and one computed tone - neutral grey for one or two soft checks, 5% amber for three or more, red when a check blocks the close, never the primary blue. Its `pc-attention-pop-menu` popover is read-only: `Before you close` with `<n> checks need attention · <m> entries affected`, the failing checks only, then `Review all`, which opens the pre-close review drawer. Zero checks hides the pill, `All checks passed` prints inside the popover only, one `attention` memo drives the pill, its tone, the summary and the `aria-live="polite"` count, the heading row is sticky inside the `.pc` scroller, and the popover holds no close action because there is one place to act and one place to look. The register toggles between `Locked Periods` and `Requests` (the requests half only while approval is on); the toggle opens that toolbar on the left, the panel prints no `Locked periods` / `Every lock for <organisation>` heading sentence, the lock list shows only locks whose period already ended in automatic mode and only locks created here in manual mode (through `lockRegisterRows`), and its toolbar is one compact row (`div.pc-register-bar`): the toggle on the left and, on the right, the search box and one `Filters` disclosure, with Period and Status inside that `Filters` panel ahead of Financial Year, Organisation, Branch, Closed By and Closed Type, so the visible row consumes as little space as possible. Do not restore the page subtitle, the composite period/status/scope line, the locked-periods status line, the inline missing-periods row, the Create missing periods action, the Activity History block in the current-period card, the `Transactions` / `Audit log` / `Close with review` link row, a second close button, the lock history drawer or its `Audit log / History` entry, or the inline read-only locking policy tag; no tab row; the current accounting period opens from the first `More actions` entry as a 520px right-side popup (`pc-current-drawer`) that renders the real `CurrentPeriodPanel` with its lock-aware inline actions, instead of a page card; the acting role stays only in the global `VIEW AS` control; the pre-close review keeps the entries-affected count and the wrapped category chip row and drops its duplicated heading, its verification sentence and its readiness pill; the lock history drawer and its `Audit log / History` entry are removed, so the filtered register is the only lock list. `Create Lock / Close` is mode aware: manual locking takes a period, a date and an optional reason and applies one lock to every module, while automatic locking renders the saved schedule inputs (Frequency, Effective from, Lock after period end, Lock at, Notify before locking), resolves the next unlocked period through `autoTarget()` and previews it as `Next lock`. Unlock requests are decided from the `Requests` half of the register. The row carries a Status column (`Requested` while pending, then `Approved`, `Cancelled` or `Rejected`) beside its combined Organisation & Branch column, and a pending request shows the read-only `View` button beside the row menu, whose `Approve` is gated on a decidable pending request and whose `Cancel` stays requester-only. There is no reject option - an unlock request is approved, withdrawn or deleted - and every filtered request renders in one pass, because the register has no pager. A request the acting role raised itself is decided as the next authorised approver from `unlockApproverFor(requestedBy, settings)`, and the row never restates that approver because the buttons are the whole action cell, so `Another approver must review your own request.` appears only when the policy leaves no other approver. Period Locking Settings lives in `src/PeriodLockSettings.jsx`: one-line lead and exactly one headingless settings card holding two real toggle switches (`Enable automatic locking` between `Automatic` and `Manual`, then `Require approval to unlock` below a `pls-rule` divider) around the closing schedule and its read-only preview. The separate `Period locking` card, the separate `Unlock approval` card and the whole `More policy settings` disclosure are gone; the removed policy values keep their engine defaults, `lockingMode: 'Off'` is only writable from Settings -> Accounting -> Period control, and a stored `Off` disables the master switch with a note. The popup is tuned for its 620px drawer (16px/18px card padding, one 22px gutter tightening to 16px below 760px, sticky heading and footer with separation shadows).
19. Period locking fails closed. An unlock request must sit completely inside the lock it targets, an approved date-range window releases only the dates it names, pending requests can be cancelled but decided or expired ones cannot, expiry re-locks automatically and writes both `Unlock expired` and `Period reclosed` with the explicit `System` actor, policy changes are audited and never rewrite existing locks, and every locking audit entry records actor, timestamp, action, organisation, branch, before/after values, reason and the related lock or request id. Never present browser-local enforcement or simulated roles as production protection.

20. Period Lock drawers are portalled into `document.body`, so keep each one wrapped in `<div className="pc pc-portal-scope">` and give it explicit right-side geometry: a scrim at `z-index:1390`, the drawer at `z-index:1400` with `position:fixed`, `right:0` and `width:min(<n>px,96vw)`, a fixed header and footer, and `pc-drawer-body` as the only scroller. Without that wrapper the legacy shell rule `aside{position:fixed;inset:0 auto 0 0;width:206px}` applies the navigation rail geometry to the drawer and the popup renders on top of the sidebar. Keep one gutter per nesting level, keep `.pc-heading` on the Journal Entries heading grid, and keep the card header resets so the legacy fixed top-bar `header` rule cannot leak into nested cards.
Never put a page control row inside a `<header>` unless you neutralise the legacy app-bar element rules in `src/styles.css` (`header{position:fixed;height:58px}`, `header label{height:38px;background:#f6f8fb;padding:0 12px}`, `header>button{height:38px}`, and below 1050px `header>button:not(.primary):not(.hamb){display:none}`); that is why the register toolbar is a `div.pc-register-bar`, and why every drawer and modal close button needs an explicit `display:inline-flex!important` (`.pc-history-drawer>header>button,.pc-modal>section>header>button`, `.pls-popup .pls-close`) or it disappears below 1050px. The Period Locking Settings drawer is the deliberate exception to the wrapper rule: it keeps the canonical `.pc-history-drawer` shell class but not the `.pc` scope wrapper, so the page base-control rules (`.pc button`, `.pc input`, `.pc select`) cannot reach the panel. `src/period-lock-settings.css` therefore neutralises the legacy global top-bar `header` rule for its own headings with `.pls header{position:static;inset:auto;width:auto;height:auto;min-height:0;max-width:none;z-index:auto;margin:0}`. Without that reset every `.pls-card>header` inherited `position:fixed;height:58px` and the `.pls-card>header` titles stacked on top of each other at the top of the viewport.
21. Organisation and branch scope has exactly one implementation, `src/organisation-scope.js`, shared by Chart of Accounts, Create Journal Entry -> Journal Lines, Period Locking settings and Create Lock. Use `getScopeVisibility()` (aliases `showOrganisation` / `showBranch`, `showOrgColumn` / `showBranchColumn`) instead of comparing organisation counts per screen, use `getBranchesForOrganisation()` instead of filtering a branch list locally, and validate with `validateScope()` so `UNKNOWN_ORGANISATION` and `BRANCH_NOT_IN_ORGANISATION` are rejected rather than silently dropped. Hide a one-option selector (a single-branch organisation auto-resolves to read-only text), print the em dash `NO_BRANCH` for a branchless organisation, keep the read-only `Locking for:` context line visible even when a control is hidden, and re-evaluate visibility on every scope change rather than on first render. `src/period-locking.js` re-exports these helpers; never reintroduce a private copy there or in a page. The interactive control is `OrganisationBranchScope.jsx` (`src/OrganisationBranchScope.jsx`), which renders the pure `scopePickerState()` state from the same module - do not hand-roll a second picker per page. The available organisation list is read through `src/organisation-context.js`, which owns two lists behind one `scopedOrganisations(...)` derivation and must never be copied into a page: `getAccessibleOrganizations()` is the narrowed working context that Create Journal Entry and Period Lock share, and `getScopeOrganisations()` is the universe a deliberate scope is chosen from - the list the header Change / Customize drawer offers, which now reads it from there too. Create Budget plans over `getScopeOrganisations()`, so a narrowed working context can never leave its scope control with nothing to choose. A period lock may cover a whole organisation, so the picker's Branch list keeps `All branches` as its first option and only a real choice renders a selector. The request unlock drawer states the locked scope (`showFields={false}`) because the engine derives an unlock request's scope from the locked period - do not add a selectable branch there without also adding an enforcement rule for temporary unlocks. The documented exception is the Create Account drawer's Organisation multi-select, which stays visible because it is the customisation control.
22. Use one horizontal three-dot row action menu everywhere. The Period Lock kebab (`details.pc-kebab` > `summary` holding `IconDots`, plus `.pc-kebab-menu`) is the canonical control; the Journal Entries register (`.je-row-actions`) and the Charts of Accounts table (`.am-table .am-more`) must keep its 32px trigger with a `1px solid #d9e1ec` border and 7px radius, its `border-color:#3478f6;color:#245fd9` open state, and its right-aligned `min-width:196px`, `padding:6px`, `border:1px solid #e1e6ed`, 9px-radius, `0 12px 30px #1018281f` menu with 36px rows (`gap:9px`, `padding:0 10px`, `#f4f7ff` / `#245fd9` hover). Charts of Accounts renders Edit only as its first menu item: do not restore a visible `.am-row-edit` control. Its always-visible Organisation and Branch columns derive exclusively from the global switcher through `src/finance-category-scope.js`; multiple selections use a count with the exact organisation/branch mapping in the native hover title. Each row menu closes through `menuRun(...)` when an action runs and through one page-level `pointerdown` / `Escape` effect per module. Do not restore `IconDotsVertical`, the caret arrow, or the Chart of Accounts header Templates menu styling.
23. Create Budget is one three-step page, `BudgetWizard` in `src/BudgetWorkspace.jsx`: `Budget Details`, `Account Selection`, `Budget Allocation`. Step one lays the details on one `budgetSetupFields` grid on a three-column track (`repeat(3,minmax(0,1fr))`) whose first row is the budget name taking the first column beside the `div.budgetScopeRow` that spans the other two and renders the shared `OrganisationBranchScope` in its `multiple` checkbox-list form, and whose second row is the Financial year select, the Budget period select (offering exactly the five `BUDGET_PERIODS` - Monthly, Quarterly, Half-Yearly, Yearly, Custom) and the read-only `div.budgetDuration` that prints the range and its `12 months · Monthly` reading from `budgetDateRange()` / `budgetDurationLabel()` and opens the two date inputs only for a Custom period, the only range `budgetDateError()` can reject - folding to two columns below 1100px and one below 700px, with the Description `textarea` and its `budgetSetupNote` row both deleted from the create page - reading the scope universe `getScopeOrganisations()` and never the narrowed working context, because a one-organisation/one-branch context would hide both selectors and leave the scope unpickable; the wizard opens seeded from the working context through the one `wizardScopeSeed(initial)` helper, keeps `companyIds` / `branchIds` on the stored scope so an edit reopens the same scope, re-reads the universe on `wayvida-working-context-change` / `wayvida-organization-change` / `storage`, and `.budgetWizard .budgetSetup{overflow:visible}` keeps its open checkbox list from being clipped; the `Budget type` field is removed and `form.type` keeps its `'Profit & Loss Budget'` default for the stored budget only, so the account step lists every active posting account instead of filtering by type, and those multi-select rows come from the pure `scopePickerRows()` / `toggleScopePicker()` in the same module rather than a page-local list - read the scope from `scopePickerState()`, keep `companyIds` / `branchIds` on the form, and never reintroduce `BUDGET_SCOPES`, a hardcoded branch list or a manual scope-type select on that page (the register scope filter still reads `BUDGET_SCOPES`) - and a `budgetTemplateSwitch` (`input[type=checkbox][role=switch]`) that reveals `budgetTemplateCards` only while it is on and resets the template to `Custom Budget` when switched off. Step two renders `AccountSelectGrid`, one `.budgetAccountGrid` two-column table of every active posting account in the Chart of Accounts rather than only the accounts of one budget type, grouped under its accounting type in the shared `ACCOUNT_TYPE_ORDER` order from `src/terminology.jsx` (Assets, Liabilities, Equity, Income, Expenses, then an `Other` bucket for an unknown type), each section row named by `accountTypeLabel(type,business)` and carrying its count (`n of m selected`, or `m accounts`), and one labelled checkbox per account in a `budgetAccountCell` that prints the account name with its code - and its account group when it has one - beneath it, so the step reads as the Chart of Accounts itself; the separate Account Type column is gone, so never re-narrow this step through `BUDGET_TYPES` / `TYPES[form.type]` (`BUDGET_TYPES` stays only for the register Budget type filter), and it keeps no account-type tabs. Never set a `display` other than `table-cell` on a cell of that grid - put the flex row in `div.budgetGroupCell` or the account `label`, or the cell leaves the table layout algorithm and collapses to the 56px choose column (which printed the type name one letter per line) - and keep its accessibility contract: `aria-label` on the table, `th scope="col"` headers, `th scope="colgroup" colSpan={2}` per type, an 18px checkbox with a `:focus-visible` ring, a `tbody tr:focus-within` tint, the 12px code line at `#667085`, and a `label htmlFor` bound to each checkbox's `budget-account-<code>` id; `AccountTree` and `ForecastPanel` are deleted, so never restore them for the detail screen Accounts tab, which is now the filtered `BudgetAccountsTable` (see item 27). Step three allocates the chosen accounts over the `BUDGET_ALLOCATION_BANDS` grid - one sticky `td.budgetStickyCol` account column, a foldable `tr.budgetAllocGroup` per accounting type, a Growth % input ahead of `Apply Growth %`, an `Import Budget` CSV button and `Clear All` - and saves with `Create Budget` (`Save Changes` when editing); do not restore the separate `Review Budget` step, the `budgetPurposeCards` type grid, `Save Draft` or `Submit for Approval`. The approval lifecycle is advanced from `BudgetDetail` through one mode-aware status action (`Submit for approval`, `Approve budget`, `Activate budget`, `Mark complete`) wired to `setBudgetStatus` and the engine `FLOW`, and an edit must keep the stored status instead of resetting it to `Draft`.
24. `Budgets` is one flat destination inside the Accounting section, between the Journal Entries and Period Lock leaves (`leaf('Budgets')`, never a `{label:'Budget',...}` group and never a top-level section between Accounting and Inventory). The separate Budget Reports and Budget Settings screens are deleted - their `BudgetReports` / `BudgetSettings` components and `page` routes in `src/BudgetWorkspace.jsx`, their `.budgetReportFilters` / `.budgetReportGrid` / `.budgetReportCard` / `.budgetReportTitle` / `.budgetVarianceColumns` / `.budgetPerformance` / `.budgetProgress` / `.budgetPerformanceMeta` / `.budgetSettingsGrid` / `.budgetPermissionGrid` / `.budgetSwitch` rules in `src/budget-workspace.css`, and the `Budget Reports` / `Budget Settings` names in the `src/App.jsx` routing list and welcome-block exclusion - so never restore them. Budget screens carry no page-level tab row: the sidebar leaf is the only budget navigation, so keep `BUDGET_TABS`, `BudgetTabs` and `budgetWorkspaceTabs` deleted from `src/BudgetWorkspace.jsx` and `src/budget-workspace.css` and keep `page` / `onNavigate` out of `BudgetList`. The Budgets register reuses the datagrid of the other registers: `div.budgetRegisterBar` is the first child of `section.budgetListCard` and holds `label.budgetFilterSearch`, `select.budgetFilterYear` and one `details.budgetFiltersMore` Filters disclosure (`div.budgetFiltersPanel` = Budget type, Budget period, Scope, Status, plus `Clear filters` and a badge driven by the one `activeFilterCount` memo), with `table.budgetTable` following inside the same card and only `Import` / `Create Budget` left in `div.budgetHeadActions`. Copy the geometry from `.pc-filters` / `.pc-filter-search` / `.pc-filters-more` / `.pc-filters-panel` and `.pc th` / `.pc td` - 38px controls in a `14px 20px 16px` bar, `12px 14px` header on `#f5f7fb` in `12px/600 #68768b`, `13px 14px` body cells with `border-top:1px solid #edf1f6`, right aligned last column - instead of inventing new values, and never restore the full-width filter row inside the card, the deleted `budgetRegisterHead` / `budgetFilterMore` / `budgetSearch` classes or the header-cell radius workaround. The page renders no status card row: `section.budgetMetrics` and the `.budgetMetrics` / `.metric` rules are deleted, `div.budgetV3Head` leads straight into `section.budgetListCard`, and the `budgetMetrics(...)` engine export in `src/budget-store.js` stays exported but unrendered - never restore Total Budgets, Active Budgets, Total Planned Income, Total Planned Expense or Expected Profit above the card. The Create Budget wizard head and step row follow the Create Journal Entry page, including the head: `div.budgetV3Head.budgetWizardHead` is a full-bleed page header spanning the same width as the working-context organisation bar above it, made by dropping the shell padding and `max-width` on `.app main:has(>.budgetV3.budgetWizard)` and `.app main>.budgetV3.budgetWizard`, taking `width:100%`, `max-width:none`, `min-height:56px`, `padding:8px 24px`, `border-radius:0` with no side borders or shadow on the head, and restoring the `clamp(16px,2.2vw,32px)` gutter through `.app main>.budgetV3.budgetWizard>:not(.budgetV3Head)`, and it holds `button.budgetHeadBack` (40px) and a 16px `span.budgetHeadTitle` with no subtitle and no step chip, while the neutral `span.budgetStepCount` reading `Step n of 3` sits inside the stepper card, whose `nav.budgetSteps` is a `minmax(0,1fr) auto` grid with the chip in its second column. `nav.budgetSteps > ol` renders one `li` per step (current `class="active"` with `aria-current="step"`, finished `class="complete"` showing `IconCheck`, later `class="upcoming"` and `disabled`), and no step name may be cut: `li` is `flex:1 1 170px`, the connector paints behind every label (`li:not(:last-child)::after` at `z-index:0`, the `button` at `z-index:1` on a `#fff` background), each connector states its own progress (`var(--budget-blue)` behind `li.complete`, a half `linear-gradient(90deg,var(--budget-blue) 0,var(--budget-blue) 50%,#e1e6ed 50%,#e1e6ed 100%)` behind `li.active`, `#e8edf3` behind `li.upcoming`) and `button span` is nowrap with no ellipsis and no `overflow:hidden`. The wizard footer `footer.budgetWizardActions` is `justify-content:flex-end` alone - the removed `button:first-child:not(.primary){margin-right:auto}` split is why `Cancel` / `Back` now sit immediately left of the one primary action in a static row - and the details step states its scope once, as the `span.budgetScopeSummary` chip, because the second `p.budgetScopeHint` line and its rules are deleted (`scope` still travels with `save({...form,scope})`). Never restore the underline tab row, the wizard `button.budgetBack` text link, the removed subtitle, the `.budgetWizardActions button:first-child` split or the `Budget scope` hint line, or the tab-era `.budgetSteps{overflow:auto}` / `.budgetSteps button{min-width:155px}` rules.
25. Budget Allocation ends with the statement totals the standard budget sheets show under the allocation grid, not four KPI chips. `section.budgetTotals` inside `section.budgetAllocation` holds `div.budgetTotalsGrid` - a table that shares the allocation grid period columns plus one row total - whose rows are `Total Revenue`, `Total Expenses`, `Net Profit / Loss`, `Profit Margin`, `Total Assets`, `Total Liabilities`, `Total Equity` and `Total Liabilities & Equity`, in that order, and the `Budget Mismatch Adjustment` line is deleted. Every figure comes from `budgetTotalRows(form.accounts,form.allocations,form.period)` in the shared storage-free `src/budget-statements.js`, so a total can never drift from the cells it summarises and nothing new is stored. Below the totals, `section.budgetBalanceCheck` reads Total Assets against Total Liabilities + Equity through `budgetBalanceCheck(...)` and prints `Balance Difference` with one sentence (`Budget is balanced`, or that assets exceed liabilities and equity by an amount, or the reverse); that figure is reported only - never post it to a budget account, never store it, never give it a text input, and never bring back the deleted `Budget Mismatch Adjustment` line or the `.budgetTotalsGrid tr.budgetTotalsMismatch` rules. Style the derived lines on `#f1f5fb`, a negative figure on `#b42318`, nothing below 12px, keep the table scrolling inside `div.budgetTotalsGrid`, and tone the balance check with `balanced` / `unbalanced`. Never restore the four `budgetReviewTotals` chips (Total income budget, Total expense budget, Expected profit, Profit margin) or a signed-magnitude flip on the positive type totals.
26. The budget detail screen is a statement view and the shared statement layer owns every figure. `src/budget-statements.js` exports `BUDGET_STATEMENTS` (`Profit and Loss`, `Balance Sheet`, `Cash Flow Statement`), `budgetPeriodTotals(accounts,allocations,period)`, `budgetTotalRows(accounts,allocations,period)`, `budgetBalanceCheck(accounts,allocations,period)` and `budgetStatementRows(statement,accounts,allocations,period)`, and it is the only place the planned amounts are totalled: `BudgetWizard` calls `budgetTotalRows(...)` for its allocation-step totals and `BudgetStatementView` calls `budgetStatementRows(...)` for the statements, so the two screens can never disagree - never reintroduce a private `planned`/`periodSums`/`sumValues`/`marginOf`/`totalsRows` chain in `src/BudgetWorkspace.jsx`, and never re-add the deleted `AllocationTable` or the flat `Budget Details` tab it rendered into. `budgetStatementRows` returns ordered `{kind,key,label,values,total,className,note,account}` rows whose `kind` is `section|type|group|account|groupTotal|typeTotal|grand`; accounts group under their chart of accounts group (`account.group||account.type`) inside each accounting type, and every group, type and statement closes with its own total row. The Profit and Loss block reads Revenue then Expense and closes on `Net Profit / Loss`; the Balance Sheet reads Assets, Liabilities and Equity and closes on `Total Liabilities & Equity` then a `kind:'check'` `Balance Difference` row that is reported and never stored or posted; the Cash Flow reads Operating, Investing and Financing and closes on `Net Cash Flow`, signing an expense or an asset purchase negative because it moves cash out. `BudgetStatementView` renders those rows into the existing `div.budgetAllocationGrid` shell - `section`/`type`/`group` rows are full-width `th scope="rowgroup" colSpan={labels+2}` bands, account rows print the name over its code in the accounting view, and total rows are `th scope="row"` labels beside right-aligned figures - and an unknown statement or an unplanned budget renders the `budgetEmpty` sentence rather than a broken table. The detail screen's tab row is one flat list, `const BUDGET_DETAIL_TABS=[['Overview','Overview'],['Profit and Loss','Profit & Loss'],['Balance Sheet','Balance Sheet'],['Cash Flow Statement','Cash Flow'],['Budget vs Actual','Budget vs Actual'],['Accounts','Accounts'],['Activity History','Activity History'],['Revisions','Revisions']]` - a `[key,label]` pair per tab, so the key drives the view and the label is what the user reads - with `const BUDGET_DETAIL_DEFAULT='Overview'`, which the `detailTab` state and the detail `back()` reset both use; keep every one of those views reachable and keep the register `View Details` button opening `Budget vs Actual` (the register's `view()` sets `detailTab` to `Budget vs Actual`, while `viewStatement` opens the default Overview). The Budgets register kebab is `View budget` first, then `Edit`, `Duplicate` and the destructive `Delete`, all on the canonical kebab geometry. Styles live in the appended `.budgetStatement` block of `src/budget-workspace.css` (bands `#f4f7fc` / `#fbfcfe` / `#536176`, totals `#f8fafd`, derived `#f1f5fb`, balance check `#fff6ec` on `#8a4b09`, negative `#b42318`, 12px floor).

27. Budget Creation and the budget detail screen were refined in place on 2026-09-15; keep the three-step workflow and the Wayvida visual language rather than redesigning the module, and change only the information architecture, accounting terminology, statement hierarchy, allocation usability, balance validation, variance maths, overview and scope handling. Step one (Budget Details) is one three-column field grid holding exactly five fields - Budget name beside the multi-select organisation/branch scope on the first row, then Financial year, Budget period and the read-only Budget duration on the second - with no Description field on the create page - and states the scope once as a `span.budgetScopeSummary` chip (`n Organisations · n Branches`, `aria-live="polite"`) - the second `p.budgetScopeHint` `Planning for ...` line is deleted, while `scope` still travels with `save({...form,scope})`; `BUDGET_PERIODS` in `src/budget-store.js` is `['Monthly','Quarterly','Half-Yearly','Yearly','Custom']` and `createBudget` stores `startDate` and `endDate`. Step two (Account Selection) prints `span.budgetSelectedCount` (`n accounts selected`) in its head, and a `div.budgetSelectionTools` row above the grid holds the search box, one `select.budgetTypeFilter` account-type filter built from `ACCOUNT_TYPE_ORDER` narrowed to the types on screen, `Select all` and `Clear all` - `Select all` adds every visible account and `Clear all` removes only the visible ones, so the filter narrows the grid and both buttons together; the type sections of `AccountSelectGrid` each fold their rows behind a `button.budgetGroupCell` (`aria-expanded`, chevron, `accountTypeLabel(type,business)` and `n of m selected`, or `m accounts`). Step three (Budget Allocation) groups the chosen accounts under `BUDGET_ALLOCATION_BANDS` - Profit & Loss (Income, Expenses) then Balance Sheet (Assets, Liabilities, Equity) - as `tr.budgetAllocBand` and `tr.budgetAllocGroup` (foldable through `closedGroups`), keeps the account column sticky with `td.budgetStickyCol`, and its shortcut row is Copy Previous Year, Distribute Equally, Use Average Spending, a `label.budgetGrowthField` Growth % number input (default 10) ahead of Apply Growth %, a `label.budgetImportButton` that hides an `input type=file` behind the same button and reads a CSV through `parseBudgetCSV`, and a `button.budgetClearAll` that empties `form.allocations`. The detail screen's `Overview` is the default tab (`BUDGET_DETAIL_DEFAULT='Overview'`) and renders `BudgetOverview`: the four KPI cards (`Revenue Budget`, `Expense Budget`, `Expected Profit / Loss`, `Budget Utilization`), then `BudgetDetailsPanel`, then the `budgetHealth` bars and result, then `budgetAlertsPanel` - the KPI cards left the page head, which keeps only the back button, the budget name, the `{financialYear} · {period} · {scopeSummary(scope)} · {type}` metadata line, the status badge and Edit / Duplicate / Export / Archive. `BUDGET_DETAIL_TABS` is the flat `[key,label]` list Overview, Profit & Loss, Balance Sheet, Cash Flow, Budget vs Actual, Accounts, Activity History and Revisions, with the key driving the view and the label printed to the user. Budget vs Actual and the Accounts tab both read `computeVarianceRows`, whose rows are `{account,budget,actual,variance,percent,achievement,direction}` - `variance` as `actual - planned`, `percent` as the Variance %, `achievement` as the separate Actual-over-Budget figure - and colour through `varianceDirection(type,variance)` (`favourable` / `unfavourable` / `neutral`, inverting for an expense account so lower spend reads favourable); never label an achievement figure as a Variance %. The Accounts tab is `BudgetAccountsTable` (Organisation, Branch, Account Type, Account and Period filters over Account, Account Type, Budget, Actual, Variance and Variance %), Activity History lists the logged budget events (created, edited, accounts, allocation, status, export, archive) with actor and time, and Revisions lists the stored versions with their date, author and total budget. `AccountTree` and `ForecastPanel` are deleted, so never restore them for those tabs. The module stays a planning layer that references Chart of Accounts accounts and never creates accounts or posts journals.

28. The budget detail screen opens on the same full-bleed arrow-led back head the Create Budget wizard uses, not on the removed inline text back link: `div.budgetV3Head.budgetDetailHead` holds one square `button.budgetHeadBack` (`aria-label="Back to budgets"`, the horizontal `IconArrowLeft` at 19px), a `span.budgetHeadTitle` reading `Budget Details` and a `span.budgetHeadHint` reading `Back to budgets`, and it sits flush beneath the application header at the application header width - the detail page drops the shell padding and `max-width` through `.app main:has(>.budgetV3.budgetDetailV3){max-width:none;padding:0}` and `.app main>.budgetV3.budgetDetailV3{max-width:none;margin:0;padding:0 0 28px}`, hands the `clamp(16px,2.2vw,32px)` gutter back to every other child through `:not(.budgetV3Head)`, and the head itself repeats the wizard head geometry (`display:grid`, `grid-template-columns:40px minmax(0,1fr)`, `min-height:56px`, `padding:8px 24px`, no side borders, no radius) with a `@media(max-width:760px)` 36px arrow and `8px 12px` padding. `button.budgetBack` and its CSS rule are deleted - never restore that inline text link on either budget screen. Below the head, `div.budgetDetailIdentityRow` is a wrapping space-between flex row: `div.budgetDetailIdentity` holds `div.budgetDetailName` (the `h1` budget name with the `Status` badge beside it, so the lifecycle reads at the name rather than inside a button) over `p.budgetDetailMeta` (`financialYear · period · scopeSummary(scope) · type`), and `div.budgetDetailActions` is right-aligned (`justify-content:flex-end`, `gap:9px`) carrying exactly one `button.primary` for the next allowed status from `statusAction`, followed by one `details.budgetDetailMore` labelled `More actions`. That disclosure's `summary.budgetDetailMoreBtn` is a 38px labelled control (`padding:0 15px`, 7px icon gap, 14px/600 nowrap) with the horizontal `IconDots` and a `budgetDetailMoreCaret` that rotates 180deg on open, and its `.budgetDetailMoreMenu` reuses the canonical right-aligned panel (`top:calc(100% + 6px)`, 216px minimum, `padding:6px`, 9px radius, `box-shadow`, 36px rows, `#f4f7ff` / `#245fd9` hover) offering Edit, Duplicate, Export, Archive and the destructive Delete (`class="budgetKebabDanger"`). Delete is wired to the detail screen's own `remove` prop, which confirms, calls the engine `deleteBudget(state,selected)` and then clears the selection and returns to the register, because a deleted budget is only archived rather than removed from storage. Every entry closes the menu through the shared `menuRun(handler)` helper, and the detail screen keeps its own page-level `pointerdown` / `Escape` dismissal effect over `.budgetDetailMore[open]` beside the register's. Never restore the separate Edit / Duplicate / Export / Archive button row in the head, never move the status back into an action button, and keep the head free of summary cards.

29. Local Vite defaults to port 4001 and VM Docker defaults to port 4002. Keep `vite.config.mjs` `server.port` / `preview.port` at 4001 with `strictPort`, keep `allowedHosts: true` on both (never restore `["terminal.local"]`), and keep `package.json` `dev` / `preview` as `vite --port 4001 --strictPort` and `vite preview --port 4001 --strictPort`. VM hosting is Docker Compose serving the static Vite client. Keep `Dockerfile`, `docker-compose.yml`, `docker/nginx.conf` and `.dockerignore`. nginx must 404 missing `/assets/` files instead of rewriting them to `index.html`. Keep `src/crypto-uuid.js` and the `index.html` boot script so `crypto.randomUUID` works on plain HTTP VM hosts; do not assume a secure context. `docker compose up --build -d` (or `npm run docker:up`) publishes nginx on host port 4002 (`WAYVIDA_PORT` overrides it). This is not a production accounting backend and does not replace Sites: browser `localStorage` stays in the client, `/api/` stays 404, and `.openai/hosting.json`, `worker/index.js`, `scripts/prepare-sites-build.mjs` and `tests/sites-worker.test.mjs` remain intact.
30. Empty tables and lists use the one shared `src/EmptyState.jsx` component with `src/empty-state.css` - a small neutral illustration, a short title, a concise description and an optional primary action - rendered as a spanning `tr.emptyStateRow > td.emptyStateCell` row inside the table body so it is centred in the table's own content area. Keep the variants `budget`, `lock`, `request`, `journal`, `adjustment` and `default` on one plate, one 2.4 stroke weight and one grey, keep the illustration between 80px and 120px, never add a second empty-state design, an emoji or a large colourful graphic, and offer the primary action only when the user can create the missing record (Unlock Requests offer none). The illustration is animated inside that same stylesheet - a staggered per-shape draw-in of about a second followed by a slow 7s `emptyBreathe` drift, with the finished drawing as the base state when no animation runs and a `@media (prefers-reduced-motion:reduce)` block that removes all motion - so never add motion outside `src/empty-state.css` and never let the empty state depend on it.
31. The Create Item page is one of the full-bleed create pages and follows the Create Journal Entry contract. It scrolls as one document rather than inside the shell scroll port - `html:has(.itemsPage .itemCreatePage)`, `body:has(.itemsPage .itemCreatePage)`, `.app:has(>main>.itemsPage:has(.itemCreatePage))` and `.app main:has(>.itemsPage:has(.itemCreatePage))` all stop being the scroll port - because `.app>main` carries `scrollbar-gutter:stable` and a full-bleed bar rendered inside it loses roughly one scrollbar of width against the fixed application header. Keep both page gutters at `clamp(16px,2.2vw,24px)` so the head bar and its body line up with the header controls, keep the Cancel / Create Item action row a static white card under the form (`position:static`, `margin-top:12px`, `margin-bottom:28px`) rather than a sticky translucent bar, and keep no heading element inside a form section at all: `function Section({children}){return <section className='itemSection'>{children}</section>}` is the whole component in `src/Items.jsx` and no call site passes a `title`, because `src/styles.css` renders a bare `header` element as the fixed application bar and `.app header` adds `border-top:3px solid #1f5fae`, which `src/items.css` cannot outrank since the shell injects `./App.jsx` ahead of `./styles.css` - that combination painted each Create Item heading as a white 59px block with the dark blue line. Keep its head bar on the journal compact geometry (`display:grid;grid-template-columns:42px minmax(0,1fr);gap:14px;min-height:56px`, one 1px `#dfe6ef` top and bottom edge, no radius and no shadow) and keep it flush under the working-context bar: `src/ui-system.css` `.app main{padding:22px clamp(16px,2.2vw,32px) 40px;max-width:1680px}` must stay cleared by `.app main:has(>.itemsPage:has(.itemCreatePage)),body:has(.itemsPage .itemCreatePage) .app main{max-width:none!important;padding:0!important}` - both shapes carry `!important` because the single direct-child shape without it did not reach the bar in the running app - and the head gutter must stay `.orgContextBar`'s own `8px 24px`, narrowing to `8px 12px` at the same 760px breakpoint.
32. The item form preserves its stored fields. Item Type is a Goods-default Goods / Service radio group; the one stored `hsnSac` field changes only its label. Do not render Description, but retain the stored property. Keep SKU, HSN/SAC and Unit in one row and the three value-preserving switches. There is NO Track inventory control and NO Opening stock field since 2026-09-25, so the Inventory Asset / Cost of Goods Sold grid they owned is gone, `changeType` must NOT set `trackInventory` (the form no longer offers the accounts that flag would then demand), and the Inventory & Stock card on the item detail is deleted too; the stored fields stay in the data model and the opening-journal branch in the save path is kept as an unreachable guard. `div.itemAdvanced` is a plain ALWAYS-VISIBLE block, not a `<details>` accordion, holding the complete Tax settings block and then the minimal Item image attachment row - do not put the disclosure back, or tax and the image become unreachable again. Blank items default to `taxPreference:'Non-taxable'` and `taxApplicable:false`, but retain seeded rates for a Taxable opt-in; saved items keep their treatment. Keep the existing image MIME, 2 MB, FileReader, preview and remove behavior. Use the scoped document-level scroll contract in `src/items.css`: `html`, `body` and `#root` are auto-height/scrollable and `.app` / `main` release their shell height/overflow locks, while the fixed responsive action bar retains bottom clearance. Never change tax calculations, opening-stock validation or its journal.
33. Inventory has a second destination: `Inventory > Inventory Adjustments`, declared directly below Items, opens `src/InventoryAdjustments.jsx`, whose rules live in the storage-free `src/inventory-adjustments.js`. Only a document in the `Adjusted` status moves the inventory position and posts exactly one balanced journal entry through the shared `journal(...)` in `src/invoice-engine.js`; drafts and cancellations change nothing, and an adjusted document is corrected only by a reversal that links both ways. The register keeps no acting-as selector - the acting role is the header account menu's View as switch through `useTerminology()` - gathers every dropdown filter into one `details.iaFilters` disclosure with the `IconFilter` icon, prints no record count and offers no column sorting.
34. New / Edit Inventory Adjustment is a full-width, field-first document workflow. Keep `div.iaCreateHead` beneath Working Context and render no Step labels, Adjustment details or Items heading, or explanatory section copy. Use Mode of adjustment, Reference Number before Date, Account, Reason and Description; use Item Details, Location, Quantity Available, New Quantity on hand and Quantity Adjusted in the grid, swapping the input/derived cells under Set New Quantity so the headings stay truthful. Keep the no-item recovery, chevron-led accounting preview and sticky footer. Do not change its accounting behavior.
35. Items use one backward-compatible multi-scope master: Create/Edit renders `OrganisationBranchScope` with `multiple`, stores `organizationIds` / `branchIds`, and retains primary `organizationId` / `warehouseId` for existing integrations and opening-stock posting. The register first cell is item name over SKU and HSN/SAC; actions are View Details plus the 3-dot Edit Item, Enable/Disable Item, Duplicate Item and Delete menu. Keep Item Details as a dashboard document page: a full-width back header directly beneath Working Context, then an identity card with item metadata on the left and status/Edit/More actions on the right, followed by Basic Data, Transactions and History tabs with counts. Basic Data shows all saved commercial, tax, accounting, inventory and scope data; the other tabs show linked transactions and the item's `auditTrail`. Do not create a second item store or post a journal for scope assignment, toggling, duplication or deletion.

36. Keep the Item Details page on one 14px vertical rhythm and its header deterministic. `src/items.css` must keep `itemDetailBody{display:flex;flex-direction:column;gap:14px;margin:16px clamp(16px,2.2vw,28px) 0}`, `itemDetailFacts{padding:16px;gap:16px 22px}`, the 48px identity tile, the 36px Edit Item and More-actions controls, and no second body bottom margin, so the page ends with the `.itemDetailPage` 32px bottom padding. The identity row and the tab row live in one header panel - `<section className="itemDetailPanel">` in `src/Items.jsx` - so the tabs read as part of the item header instead of a separate floating pill: `.itemDetailPage .itemDetailPanel{background:#fff;border:1px solid #e6ebf2;border-radius:10px}` owns the single card edge, `.itemDetailPage .itemDetailIdentity{display:grid;grid-template-columns:48px minmax(0,1fr) auto;align-items:center;gap:16px;padding:14px 16px}` is a borderless row inside it, and the panel must never take `overflow:hidden`, because that would clip the More actions menu (`z-index:90`) that opens from the identity row. `.itemDetailTabs` is the panel's full-width footer bar rather than a bordered pill: `display:flex;align-items:center;flex:0 0 auto;gap:0;height:46px;padding:0 4px;border-top:1px solid #e6ebf2;background:transparent;overflow:hidden`, with content-sized `flex:0 0 auto;width:auto;min-width:0;height:100%` tabs at `0 12px` padding, no vertical separators, a transparent 2px bottom border that turns `#3478f6` on `.active`, `white-space:nowrap` labels and count pills; the 4px bar padding plus each tab's own 12px padding is what lines the first label up with the 16px card gutter, and the 1px `border-top` is what divides the tabs from the identity row. Earlier user requests shaped this and must hold: equal `flex:1 1 0` thirds were rejected because on a wide screen they pushed the Basic Data, Transactions and History labels hundreds of pixels apart, so the tabs stay content-sized and left-aligned - never put `flex:1 1 0` back on the base tab rule. `width:auto` on the tab buttons is load-bearing, not decorative: `src/styles.css` carries a bare `nav button{width:100%}` element rule, and because `flex:0 0 auto` resolves flex-basis to `auto`, that `width` becomes the flex base size of every tab; an earlier release of this work omitted `width:auto`, so each of the three tabs claimed the whole bar and `overflow:hidden` clipped Transactions and History off the right edge, which the user reported as those two tabs not being visible. `flex:1 1 0` had been hiding the same landmine behind flex-basis:0. Never restore the old `padding:4px;gap:4px;overflow-x:auto` pill strip, and never split the identity card and the tab row back into two siblings - the merge is deliberate. Because content-sized tabs can outgrow a narrow card and be clipped by that `overflow:hidden`, the `@media(max-width:520px)` block declaring `.itemDetailTabs{padding:0}` beside `flex:1 1 0`, `0 8px` padding and 12px text returns the tabs to equal cells on small screens; it must stay declared after the base rules, because at equal specificity the later rule wins and an earlier placement of those declarations inside the shared 520px block was silently overridden by the base rule below it. The explicit height plus `flex:0 0 auto` is still what stops an ancestor from stretching the bar into a tall empty box. The identity meta line prints the saved SKU (`No SKU · Goods/Service · unit · HSN/SAC code`). A user-reported screenshot once showed a large empty grey block above the tabs, which this cascade cannot produce - if it reappears, measure the live page at `http://localhost:4001/` before changing any other rule, and suspect a stale build or a cached stylesheet first.
37. Customers uses the shared register + document-detail pattern. `src/Customers.jsx` renders one `Customer Details` column carrying the customer name over its id (the `code`), the contact, opening-balance and credit-limit columns stay removed, and each row ends in one `View Customer` button beside one canonical `details.customerKebab` menu (Edit Customer, Activate/Deactivate Customer, Duplicate Customer, Delete Customer) whose geometry and single page-level `pointerdown` / `Escape` dismissal match the other registers. `View Customer` opens the full-width `Customer Details` screen with `Basic Data`, `Transactions` (the shared `customerStatement` sub-ledger, with `Open customer statement` keeping the `wayvida-credit-customer` handoff) and `History` (the additive optional `auditTrail`). Deleting is refused while `referenceCount` finds the customer on an invoice, payment, receipt, credit note or sales order, so a customer with history is retired by deactivation; keep `src/customers.css` free of `overflow:hidden` on `.customerDetailPanel` and keep `width:auto` on `.customerDetailTabs button`, because `src/styles.css` declares `nav button{width:100%}`. Create and Edit Customer is a full page on that same shell (`form.itemCreatePage`, `div.itemDialogHead` with `button.itemBack` and `div.itemHeadText`, one `div.itemDialogBody` card of four `section.itemSection` blocks labelled by `div.itemSectionTitle` plus the collapsed `details.itemAdvanced` More details block, and the shared fixed `footer.itemDialogFooter`), never a modal overlay. Keep `overscroll-behavior:auto` on `.itemCreatePage .itemDialogBody`: `src/ui-system.css` gives `.itemDialogBody` `overscroll-behavior:contain`, and with the card's `overflow:hidden` that trapped the wheel over every field, so a create page taller than the viewport would not scroll while the pointer was over the form.
38. The Create / Edit Customer form is the simplified, conditional, GST-ready page. Its visible body is three `section.itemSection` blocks in the order Customer details, GST & Tax Details, Billing address, then one collapsed `details.itemAdvanced` (More details) owning Payment & accounting, Opening balance & website, the Contact persons repeater and Documents - never a flat list of every field. `src/customer-tax.js` is the only place the GST treatment metadata (`GST_TREATMENTS`, `gstinApplicable`, `gstinLabel`, `panRequired`, `isBusinessTreatment`), the GSTIN decoder (`decodeGstin` over the mod-36 `gstinChecksum`), the address composer (`composeAddress` / `parseAddress`), the payment-term bridge (`paymentTermDays` / `daysToPaymentTerm`) and the contact helpers (`normaliseContacts`, `setPrimaryContact`, `contactFromName`) live; keep them pure and unit-tested. Do not drop `customer.billing`, `customer.shipping` or `customer.state` as strings, because `src/InvoiceWorkspace.jsx` and `src/SalesOrders.jsx` read them - the form stores the structured `billingAddress` / `shippingAddress` objects alongside them, keeps both in step through `composeAddress` only, and `parseAddress` must preserve a legacy composed string verbatim rather than guessing it apart. `contact` is derived from the primary contact person (or the customer name for an Individual) but is still written, and a legacy free-text `contact` becomes the first contact row. The customer code stays required and unique and is prefilled from `nextCustomerCode(rows)`. Legal name and trade name must never be required, and Get Taxpayer Details must never claim to fetch them: the decoder reads only what a GSTIN carries (state code, state name, PAN) because there is no GST portal connection. Place of supply stays a required searchable combobox (`SearchSelect` over `customerStates`) whose root is a `div`, never a `label`, so an option row cannot re-fire the trigger. Keep the create grid at three fields per row (`.customersPage .itemCreatePage .itemFormGrid`, folding to two below 1100px and one below 700px) with `0 0 16px` on `.itemTypeField`, so the Customer type choice never touches the name row, and keep Billing and Shipping as plain Address 1 / Address 2 inputs over `line1` / `line2` rather than one Address textarea. The Display name, Tags, Custom fields and Notes inputs are removed on purpose, along with the duplicated Contact persons heading and its empty-state sentence; do not restore them, but do not stop writing `displayName` (derived from the name when blank), `tags`, `notes` and `customFields` either, because existing customers keep their data that way. Keep the disclosure controlled through `open={moreOpen}` so a failed save on a hidden required field opens it instead of blocking invisibly. The GST and tax fields are dynamic: each `GST_TREATMENTS` entry declares `gstin` (required or hidden), `pan`, `business`, `placeOfSupply`, `countryRequired` and a `note`, and those flags decide what renders - never a hardcoded field list. Only `Tax Exempt` requires an exemption reason, and an Overseas customer hides the Indian state picker, requires the billing country and stores that country as `state`, because the invoice and sales-order screens read `customer.state` as the place of supply. Get Taxpayer Details must stay server-side: `src/gst-lookup.js` may call only the app`s own `/api/gst/lookup`, and no GST/GSP URL, API key, client secret, access token or `Bearer` header may appear anywhere under `src/` - the credential lives in the Sites worker environment (`GST_API_URL`, `GST_API_KEY`). `worker/index.js` is copied verbatim into the build, so it must stay self-contained and must match the API path before the asset handler. A failed lookup must never block customer creation: show the standard message, fill the state and PAN the GSTIN carries, keep every taxpayer field editable, and keep the `gstLookup` timestamp/status stamp on the record. Add no Customer Portal field or setting.
39. Every status display uses the one shared pill: `src/StatusPill.jsx` over `src/status-pill.css`, taking `status` and a `tone` of `ok` (default), `neutral`, `warn`, `info`, `violet` or `danger`. It is the Chart of Accounts shape (`.am-status`: `padding:4px 9px`, `border-radius:14px`, a 7px dot, 12px/550) with the Journal Entries tones (`.je-status`). It is used by the Customers register and detail, the Period Lock register (`CLOSING_TONES`), Inventory Adjustments (`STATUS_TONES`, which also feeds the create and detail head badges) and the Items Status column between Default tax and Actions. Never reintroduce a page-local status badge (`.customerStatus`, `.pc-badge.pc-status.*`, `.iaBadge`, `.itemDisabledBadge` are all retired), never add a second badge style - add a tone to the shared sheet instead - and never restyle the `.am-status` or `.je-status` pills, because the shared sheet is asserted to keep matching the Chart of Accounts tokens.
40. The Inventory Adjustments register is six merged columns: `['Reference','Type / Mode of adjustment','Organisation','Items','Status','Actions']`. The first four each carry two facts - the voucher number over the date, the adjustment type over `Entry mode · <entryModeLabel>`, the organisation over its branch, and the item count over the signed amount - and Status keeps its own column with the shared pill. Never split those pairs back into ten columns, never re-add Reason, Adjustment account or Created by to the register (the detail screen shows them under Adjustment information, and it also states the Entry mode), and keep the signed amount's `iaUp` / `iaDown` colouring inside the Items cell through `.iaAmountLine`. No stored field, filter, search term or export changes with the layout. The row menu always offers View, Edit, Duplicate, Export and Delete plus the one status action, and Edit and Delete are shown disabled with the reason rather than hidden: only a Draft is editable, and a record that posted its journal is never deleted because the ledger would lose an entry with nothing pointing at it - keep `actionEntries` as the single builder for the register menu and the detail menu, keep `Delete` on the `cancel` duty, and keep the engine`s `delete` command refusing a posted record while writing an `inventory-adjustment-delete` audit event. The detail screen opens `src/AdjustmentPreview.jsx` from its Preview button, replacing the detail screen the way Journal Entries opens JournalPreview; it reuses the internal-document preview shell (`jp-*` over `.dpPaper`), never the customer-facing DocumentPreview, and it must never recalculate a line because every figure comes from `storedLines(row)`.
41. The Create / Edit Sales Order page is a first-class invariant surface. Its head is the full-bleed arrow-led `div.soCreateHead`, its order-items table is `table.ivLineTable` with `table-layout:fixed` and percentage columns (Item 19, Quantity 7, Unit 5.5, Rate 8, Discount 15.5, Tax 29.5, Amount 11, Delete 4.5) so it can never scroll sideways, its line Unit is read-only text from the Item master (`span.soUnitValue`) rather than an input, but only on the `kind==='order'` branch - the Invoice create page shares the component and keeps its editable unit input, because `src/invoice-engine.js` refuses a line with no unit, its discount number yields (`flex:1 1 0`, `width:auto!important`) so the `%` / `₹` selector keeps `flex:0 1 54px;min-width:46px` inside an `overflow:hidden` row, and its remove-line action is a 32px red target (`#b42318` on `#fff5f4`, 7px radius). The header card carries required **Organisation \*** and **Branch \*** selects for `kind==='order'` only, read from `getAccessibleOrganizations()` and seeded from `getCurrentOrganizationContext()`, with the branch cleared when the organisation changes; `save()` refuses without both and then runs `orderScopeError(form.lines, items, scope)`. `itemScopeError(item, scope)` / `orderScopeError(lines, items, scope)` in `src/sales-order-service.js` are the only item-scope rule: an item's `organizationIds` (fallback `organizationId`) must include the chosen organisation and, for a `Goods` that `trackInventory`, its `branchIds` (fallback `warehouseId`) must include the chosen branch; a Service, an item that tracks no inventory and an item with no organisation scope recorded are never blocked. Both sides are reference **sets** (organisation id or code, branch id or name), because the Item create form seeds an organisation id while its scope picker writes the code and the opening-stock branch field takes an id or a name - a single-value comparison reports valid items as out of scope. The sales line keys its item as `line.item`; Inventory Adjustments keys it `itemId`. The page is one section per job - Customer (picker, organisation, branch, then the read-only customer facts), Order details (number, date, reference, payment terms, due date), Order items, Billing summary, then the collapsed Additional details - and the customer's GST treatment, GSTIN, PAN and addresses are a read-only `dl.soFactList` (`dt` 12px muted over `dd` 13px strong, the `itemDetailFacts` pattern) with one `p.soFactCaption` naming the source; never turn those into inputs, show the billing address only with the labelled Edit as the panel's only control (a matching shipping address is one sentence plus the split action, and its own row appears only when the two differ), and keep Place of supply OUT of it - it is editable and belongs in Order details. `dt{display:flex;align-items:center;min-height:30px}` keeps an address label on the same baseline as the plain values, which is what stopped the row looking broken. The Billing summary block is `width:min(420px,100%);margin-left:auto` so its heading sits directly above the totals panel, and its Item-master note renders for the invoice page only. Payment terms come from the shared `PAYMENT_TERM_DAYS` table plus a Custom row and the day count from `customerTermDays()`, never `c.days||'30'`. `convertSalesOrder` carries the order's organisation, branch and the customer's receivable ledger onto the invoice (posting resolves AR from `i.receivableAccount||s.config.ar`) and refuses a legacy order whose `taxRate`/`adjustment` is non-zero. The register offers Duplicate, Export (pure `ordersCsv`) and the destructive Delete, disabled with its reason when an invoice exists, and every write appends to an additive optional `auditTrail` shown as History. Order attachments declare 10 files / 1 MB each / 2 MB in total - the same numbers the reader enforces. The page opens on `div.soOrderTop`, the Customer card left at 60% and Order details right at 40% with `align-items:stretch` so both cards are one height (drop the stacked-card margin inside the row - `.soOrderTop>.ivCard+.ivCard.ivCard{margin-top:0}` - or a grid item's stretched margin box leaves the right card 14px shorter), stacking to one column below 1240px, where the eight order fields return to four across; the read-only facts are one three-column strip with the two address facts spanning it, and the narrower 40% column pairs those eight fields two-by-two above the fold. The customer is chosen with the shared searchable combobox (`src/SearchSelect.jsx` over `src/search-select.css`) inside `div.soCustomerRow` - never a native select, and never mounted inside a `label`, or an option row re-fires the trigger; keep that sheet scoped to the component root rather than a page class, because `src/invoice-workspace.css` styles every bare `button` / `input` at class-plus-element specificity. Organisation and Branch live in Order details, which is one `soFieldRow4` four-column grid over eight fields and renders as two complete rows of four (organisation, branch, number, date / reference, place of supply, payment terms, due date) - never a five- or six-column row. The field rows are `soFieldRow` (three columns) and `soFieldRow2`, and the action bar is `ivFooter.soActionBar`, sticky because `html` alone keeps the page overflow while `body` and `#root` stay `overflow:visible` - any other overflow turns that element into the scrollport and the bar scrolls off screen. Attachments use `label.soUploadDrop` with real drag and drop over the same `readFiles` the input uses, and `ul.soFileList` rows; the printed limit must stay the 5 MB the code enforces. When overriding the shared sheet on this page, remember that `src/invoice-workspace.css` is imported after `src/sales-orders.css`: a one-class page rule that ties on class-level specificity loses, which is why `.soCreatePage .ivLineTable{min-width:0}` never applied against `.invoiceWorkspace .ivLineTable{min-width:980px}` and the table scrolled below 1440px until the selector gained the element.

42. The invoice detail screen is one card under an arrow-led full-bleed head (`div.ivDetailHead`: a 40px `button.ivDetailBack` + `Invoice details` + a hint naming the document), never a breadcrumb plus an eyebrow. `section.ivDetailSheet` holds the identity row, the four figures (`div.ivDetailStats`), the tabs (`div.ivDetailTabs`) and the tab body, and stays `overflow:visible` because the More actions menu opens inside it. The tabs are exactly Overview / Accounting / Activity - Items and Payments are sections of Overview - and `InvoiceOverview` reads Organisation & branch, Customer, Items, Invoice information, Amount summary, Payment summary, Accounting summary and Related documents. Preview & print, Send or share invoice, Duplicate and the destructive Cancel invoice live in the More actions menu. The invoice save stamps organisationId/organisationName/branchId/branchName from `getCurrentOrganizationContext()` keeping any stored value.
43. A register's secondary filters live behind one `details` disclosure in the toolbar, beside the search box and the primary status select, never in a second filter row. The Customers register is `details.customerFilters`: a `Filters` summary with a count badge derived from the filter values themselves (`Object.values(filters).filter(value=>!value.startsWith('All ')).length`) and a panel of `label.customerFilterField` selects (Type, State / place of supply, GST treatment, Payment terms, Balance) over one `Clear filters` action that also resets the visible status select. Keep the panel's fields reset against the shared `.itemsTools label` search-pill rule at higher specificity (`.customersPage .itemsTools .customerFilterField`: `width:auto`, no border, no padding, `align-items:stretch`), and keep the sub-760px fallback that wraps the toolbar and makes the panel static and single-column - an absolutely positioned panel anchored `right:0` to a trigger near the left edge of a small viewport runs off the screen. Close the disclosure from the page's existing `pointerdown` / `Escape` effect; never add a second page-level listener. Build each option list from what the records actually carry so no option can return an empty register, and put a register's search icon inside the field on the wrapper rather than the input, so a focused search draws exactly one ring (the Sales invoices register is `label.ivToolSearch` in `src/invoice-workspace.css`).

44. Every record-detail screen renders on the one document shell: an arrow-led full-bleed head, then `div.ivDetailBody > section.ivDetailSheet` holding an identity row, a figures row, the shared `nav.itemDetailTabs.ivDetailTabs` row and one padded tab body. The customer receipt detail (`src/Receipts.jsx`) joined it with `const detailOpen=!!r&&!form&&!bankView` and the `ivDetailOpen` class, and its old `header(r.number,r.customerName,...)`, `div.ivSummary` and `div.receiptContext` blocks are deleted along with their CSS. Its figures are Receipt date / Payment mode / Received into / Received / Available to allocate / Allocation, its lifecycle is the shared `StatusPill` on `RECEIPT_TONES`, and its accounting toggle is the trailing `.receiptViewSwitch` inside `div.receiptTabsRow` - which owns the divider, with the nav forced to `border-top:0` through the higher-specificity `.receiptWorkspace .ivDetailSheet>.receiptTabsRow>.itemDetailTabs` selector because `src/receipts.css` is bundled ahead of `src/invoice-detail.css`. `src/ReceiptImpact.jsx` uses the same tab classes for its three ledger views. Tab bodies take `padding:18px` (12px below 720px).

54. The Purchases module renders inside `main` through `src/App.jsx`, never through the `createPortal` overlay shells (`src/Navigation.jsx` no longer mounts `Vendors`, `Purchase Orders`, `Purchase Bills`, `Goods Receipts`, `Debit Notes`, `Payments Made` or `Purchase Reports`, and `.purchasePortal` / `.vendorPortal` are deleted). Every Purchases page carries `invoiceWorkspace purchaseWorkspace`: registers add `div.ivHeading`, `div.ivCard.ivRegisterCard`, `div.ivTools` (`label.ivToolSearch` plus one `details.soFiltersMore` Filters disclosure), `div.ivScroll > table.ivInvoiceTable`, `div.ivRowActions` and `div.ivActions.purchaseRegisterFoot`; create pages add `soCreatePage ivCreatePage`, `div.soCreateHead` with a 40px `button.soBack`, `form.ivCreateForm`, `ivLineTable`, `soBillingSummary` and the pinned `div.ivFooter.soActionBar`; detail pages add `ivDetailOpen`, `div.ivDetailHead` + `button.ivDetailBack`, `div.ivDetailBody > section.ivDetailSheet`, an identity row with the shared `StatusPill`, a six-cell `div.ivDetailStats` row and `nav.itemDetailTabs.ivDetailTabs`. Keep the register tables at seven columns - `table.ivInvoiceTable` fixes seven percentage widths totalling 100%, so an eighth column collapses to zero width - and keep `.purchaseStatus` / `.vendorStatus` badges, the modal `vendorOverlay` / `vendorDialog` editor, the separate vendor-ledger page and `src/goods-receipts.css` / `src/purchase-safety.css` / `src/purchase-workflow.css` deleted. Row menus are `src/PurchaseRowActions.jsx`, `src/VendorRowActions.jsx` and `src/DebitNoteRowActions.jsx` on the sales order register's `.soMoreButton` / `.soActionMenu`.

57c. The Period Lock page is the exception that proves the container rule: it is a fixed panel pinned over the shell's main with its own scroll port, so it takes the shell padding tokens by hand and src/register-head.js accepts .pc as a scroller. Its page header element is gone - PeriodClosing passes title, note, actions and merged into LockRegister, which renders the one merged row, and its action group ends in the shared split button whose menu holds Current period, Period lock settings and Export.
57b. A migrated register must sit on the same content container as the pages beside it. A page with its own padding - `.app main>.budgetV3{padding:14px 18px 28px}` - insets its card and lowers its first row, and that is the difference a reader notices first when navigating between Accounting pages. Scope it away for the register screen only (`.app main>.budgetV3:not(.budgetWizard):not(.budgetDetailV3){max-width:100%;margin:0;padding:0}`) and take the grid cell tokens from the reference page, so the card lands at the same x, width and top and the header row is the same height. Two references can disagree - the Journal Entries grid runs a fixed 52px row for its nine columns while Chart of Accounts runs 42px - so match the flat register grid and leave the other alone.
57a. The Budgets register is the third migrated page and the second bespoke one: its heading is declared in `src/budget-workspace.css` from `.app main>.budgetV3>.budgetV3Head` with `!important`, so the shared rules repeat that whole prefix plus `.registerHead` to outrank it; its title and description are sized off the Journal Entries head (24px/700 `#172033`, 12px `#667085`); its financial year select is a panel filter rather than a row control, which is what puts the search beside Filters; and its Create Budget split button carries Import and Export, the Import passing the parsed plan to the wizard through `setImportPayload(parsed)`.
64. The register date filters come from `src/date-range-filter.js` and never from a page. `DATE_RANGE_PRESETS` is `All dates` / `Today` / `This week` / `This month` / `This year`, `resolveDateRange(key, today)` returns the `YYYY-MM-DD` pair and takes a fixed today so it stays testable, and `matchDateRange(from, to, today)` names the period the two dates currently describe. Each range is the whole calendar period rather than the part that has happened, and weeks run Monday to Sunday. Sales Orders, Invoices, Credit Notes, Payment Receipts and Journal Entries each render one `label>select aria-label="Filter by date range"` before their own date fields, and the range only writes those existing fields - `invFilters.from/to`, `from/to`, `fromDate/toDate`, `from/to` and `receiptFilters.from/to` - so no page filtering, count badge or Clear filters row had to change. Drive the select value from `matchDateRange` rather than from state, or it will claim a period the reader has since edited; keep the `Custom range` entry for that case. Payment Receipts had no date filtering at all before this, so its two dates live in `RECEIPT_FILTER_DEFAULTS` and its badge requires a truthy value before its `startsWith('All ')` test, or the two empty dates read as active filters.
 71. The switcher card and its panel are detailed, and the details are load-bearing. `div.workingContext` is `display:flex;flex-direction:column;margin:0 0 12px;padding:7px;border-radius:14px` with `box-shadow:0 1px 2px #1018280f,inset 0 1px 0 #fff`, and the `margin:0 0 12px` is what keeps the card clear of the bottom of the window; a `span.wcCardLabel` section title was tried and removed on the user instruction "no need of showing Organisation & branch, just need card style change give bottom padding", so do not restate it. The trigger chevron is an always-visible dropdown affordance: an 18px glyph with 4px padding inside a full-radius chip (`#E3ECFA`/`#245FD9` light, `#2B3D5C`/`#CFE0FF` dark) that deepens to `#CFE0F9`/`#1C4FB8` (dark `#3A527C`/white) on hover or while the panel is open and rotates 180 degrees when it is. It is stated once: a legacy `.wcIdentity[data-theme=dark] .wcTrigger>svg{color:#9fb0c6}` rule outranked the new dark fill and was deleted. In the panel, `div.wcMenuIdentity` is a header strip (`#F4F7FB` light, `#1F2C40` dark, 10px radius) whose right edge carries `span.wcMenuBadge` with the branch code or `n branches`; rows are separated by an inset hairline (`.wcMenuGroup .wcMenuRow:not(:last-child)`, `#F1F4F9` / `#2A3A54`) rather than a border so the row radius survives; and the branch list is a tree through `.wcMenuGroup.branches{margin-left:6px;padding-left:4px;border-left:2px solid #EEF1F6}` (`#2A3A54` dark), which depends on the component keeping the `wcMenuGroup organisations` / `wcMenuGroup branches` class names. `.wcMenu` is `z-index:300` with a two-layer shadow (`0 24px 56px`, `0 2px 6px`) and a `#DBE3EE` border (`#3A4C68` dark) so it reads against a white sidebar as well as the dark one, and `placeMenu()` floors its height at 240px and opens on whichever side has more room, recording that in `data-side` - so it can never open partly off-screen. `tests/org-switcher-ui.test.mjs` pins all of it - do not simplify those rules away.
 70. The sidebar footer is one block: `div.navThemeFooter` is `display:flex;flex-direction:column;gap:10px;padding:12px 0 14px` holding the `Help Center` leaf and the theme segment, and `div.workingContext` follows with `padding:6px`, `margin:0`, a 12px radius, the tinted surface and a `0 1px 2px #1018280a` shadow - all in the sidebar's 14px gutter, so the three rows share one left edge. The theme control is the reference segment, not a switch: `div.navThemeSegment` (4px padding, 10px radius, `#EEF1F5` light / `#1E2C41` dark, `grid-template-columns:1fr 1fr`, `role="radiogroup"`, `aria-label="Navigation theme"`) holding two 34px `button.navThemeOption` rows (a 16px `IconSun` or `IconMoon` beside `Light` or `Dark`, `role="radio"`, `aria-checked`), the active one on `#fff` with `0 1px 2px #10182814` in light or `#31415C` with white text in dark. The old `.navThemeSwitch` and `.navThemeTrack` rules are deleted; do not resurrect them. `setTheme(next)` must normalise with `const isDark=next==='dark'` before `setDark` and the `finance-erp-nav-theme` write, or the state becomes the string `'light'` and `data-theme` silently stays dark.
 69. The search beside a register's filter button is 300px wide in every module, and that width is stated once. The block at the END of `src/register-head.css`, inside `@media(min-width:761px)`, repeats each module's own selector with `flex:0 0 300px;width:300px;max-width:300px` - several with `!important`, because those module sheets state their own flex that way: `.registerHead .ivTools>label`, `.registerHead .itemsTools>label`, `.je-page .registerHead .je-tools>label`, `.budgetV3Head.registerHead .budgetFilterSearch`, `.pc-register-tabs>.pc-filters>.pc-filter-search`, `.iaHeading.registerHead>.iaRegisterBar .iaSearch`, `.am-coa-page>.am-filter-toolbar .am-search`, `.am-filter-toolbar .am-search`, `.ivTools>.ivToolSearch`, `.itemsTools>label`, `.je-tools>label`, `.gl-filter-toolbar .gl-search`, `.pc .pc-filters>.pc-filter-search`, `.auditTools>label`, `.trTools>label`, `.tabletools>label`, `.bankTools>label`, `.bankTools .bankSearch`, `.purchaseTools>label`, `.opsTools>label`, `.coaTools>label`, `.coaSimpleTools>label`, `.day-filter-row .day-search`, `.swSearch` and `.budgetSelectionTools .budgetFilterSearch`. Keep `register-head.css` last in `src/main.jsx` or the block loses on order, keep the block behind `min-width:761px` because narrow screens use a full-width search, and leave picker, form and drawer searches alone - they are not toolbar searches. `tests/ui-quality.test.mjs` pins all of it.
 68. The Company and Branch switcher is the bottom row of the sidebar, below the Help Center and Dark navigation footer, and it sits on its own tinted surface. `src/Navigation.jsx` renders `<WorkingContextSwitcher theme={dark?'dark':'light'}/>` as the LAST child of `nav.erpNavigation`; `div.workingContext` carries the card - `#EEF3FB` on a `#DBE6F5` line in light, `#22304A` on a `#31425B` line in dark, a 12px radius, 5px padding - and the trigger hover/open states are re-stated as `.workingContext .wcTrigger:hover` and `.workingContext .wcTrigger[aria-expanded=true]` (with the `[data-theme=dark]` pair) so the tint survives the nav background. Because the trigger now sits at the bottom of the window, `placeMenu()` in `src/WorkingContextSwitcher.jsx` measures the space below it and, when less than 360px remains, anchors the panel with `bottom: window.innerHeight - box.top + 8` plus a `max-height` instead of `top`, so the panel always opens upward on screen; never assume the panel opens downward again. The hover summary tooltip opens upward for the same reason (`.workingContext .wcHoverSummary::after{bottom:calc(100% + 10px)}`). The company displays as `Wayvida Learning` while its id stays `abc`.
 67. Spacing comes from one scale and an organisation mark is a fixed-size tile. `src/ui-system.css` holds the design sheet's tokens - `--wvb-space-1` 4px to `--wvb-space-11` 60px on 4/6/8/10/12/16/20/24/30/32/60, the ink ramp `--wvb-ink-1400` `#0C0C0D` to `--wvb-ink-600` `#A3AEB8`, `--wvb-surface-100` `#F4F7F9`, `--wvb-surface-200` `#E9ECEF`, `--wvb-line-400` `#DFE3E6`, the blues `#1279FF` / `#EAF3FF` / `#CDE2FD` and the 8/10/12 radii - and `--ui-bg`, `--ui-border` and `--ui-radius` point at it, so take new spacing from a token rather than a fresh literal. The dashboard and shared-card rhythm lives in the last block of `src/ui-quality-polish.css` inside `@media(min-width:721px)`; keep it there so the narrow-screen values stay, and leave the register tables and feature forms on their own pinned paddings, which tests and `AGENTS.md` assert. In the switcher, `.erpNavigation button>span{flex:1;min-width:0}` outranks a bare `.wcMark` rule and stretches the logo tile (it measured 95x32 before the guard), so keep `.wcTrigger>.wcMark{flex:0 0 var(--wc-mark-w,32px)}` and `.wcMenuGroup>.wcMenuRow>.wcMenuMark{flex:0 0 var(--wc-mark-w,28px)}`, keep the tile `position:relative;overflow:hidden` with the image absolutely filling it (`inset:0`, `object-fit:cover`, `border-radius:inherit`, `transform:scale(var(--wc-logo-scale,1))`), and keep `logoScale` / `logoWide` per company in `src/demo-organisations.js` - Wayvida scales 1.35 because its glyph sits inside a wide white canvas, and a wordmark takes a 48px (panel) or 56px (trigger) chip instead of a square.
 66. The demo companies are Wayvida and Viskool, and the contract is that ids are keys while names are display. `src/demo-organisations.js` holds exactly two: Wayvida Learning (id `abc`, code `ABC01`, logo `/wayvida-logo.png`) with Kochi (`abc-kochi`, `KOC01`, listed first on purpose - `src/organisation-context.js` falls back to the first branch of the first organisation and every module's `wayvida-demo-branch` fallback is the Kochi branch), Trivandrum (`abc-trivandrum`, `TVM03`) and Bengaluru (`abc-bengaluru`, `BLR02`); and Viskool (id `northstar`, code `NSR02`, logo `/viskool-logo.png`) with Trivandrum (`northstar-chennai`, `TVM01`) and Thrissur (`northstar-coimbatore`, `TSR02`). Renaming a company must move `name`, `logo`, branch `name` and branch `code` only: the working-context storage pair, the period-control records and the journal posting context resolve through the ids and codes, which is why Viskool's Trivandrum branch keeps the id `northstar-chennai`. Everything that displays an organisation moves with the list - `PERIOD_ORGANISATIONS` / `PERIOD_BRANCHES` and the auto-create list in `src/period-locking.js`, `seededCompanies` / `seededBranches` in `src/App.jsx`, the company defaults in `src/settings-store.js`, the id-to-code maps in `src/invoice-engine.js` and `src/JournalEntriesPro.jsx` (now `{abc:'ABC01',northstar:'NSR02'}`), the header select labels and the welcome subtitle, and the sample place lists in `src/AccountWorkspace.jsx`, `src/OperationalModules.jsx` and `src/Vendors.jsx`. The four old companies and their six extra branches are deleted from the list, not renamed into it.
 65. The Company and Branch switcher lives in the sidebar, directly under the Wayvida brand, and its two levels must stay distinct. `src/Navigation.jsx` renders `<WorkingContextSwitcher theme={dark?'dark':'light'}/>` as the last child of `nav.erpNavigation`, below the Help Center and Dark navigation footer; `button.wcTrigger` carries the current company 32px rounded-square logo (`span.wcMark` with an `<img>`, initials as the fallback), the organisation name, the branch (or `n branches` when several are selected) and a 16px `IconChevronDown` that rotates while the panel is open. `div.wcMenu` and the `Change / Customize` drawer are portalled to `document.body`, because `.app>aside{height:100dvh;overflow:hidden}` would clip them: the panel is `position:fixed`, placed from the trigger measured box plus 8px and clamped to the viewport, and it flips ABOVE the trigger when less than 360px remains below it, which is the normal case now that the trigger sits at the foot of the window, 248 to 320px wide, `max-height:calc(100dvh - 88px)`, with its own `data-theme`, and the trigger itself sits on a tinted card (`#EEF3FB` light, `#22304A` dark with their 1px lines) so the working context stays visible against the nav in both themes, and it holds an ORGANISATION section and a BRANCH section of `button.wcMenuRow` rows (`role="radio"`, `aria-checked`, a 28px `span.wcMenuMark` tone mark, the name over `type · n branches` or the branch code, and a 20px `IconCircleCheckFilled` on the active row), then `i.wcMenuDivider` and the `button.wcMenuAction` row that opens the drawer. The quick switch selects one organisation or branch; the drawer keeps the multi-selection; both write through `applyWorkingContext` in `src/org-switcher.js`, which owns `wayvida-context-companies`, `wayvida-context-branches`, `wayvida-context-mode`, `wayvida-demo-company`, `wayvida-demo-branch` and the `wayvida-organization-change` / `wayvida-working-context-change` events, so Create Journal Entry, Period Lock and Create Budget can never read a context the switcher did not write. `switchedBranchIds` keeps the branch the user is already on when it belongs to the chosen organisation, and `src/demo-organisations.js` holds the organisation list every reader and every test shares. Row marks may only use blue, violet, cyan and amber: green, red and grey already mean status in the Chart of Accounts. Escape, a pointer outside the trigger or the panel, and choosing a row all close it. Each organisation row shows its 28px logo, the name over its type, and one right-hand slot - `IconCircleCheckFilled` on the current company and a `span.wcMenuBadge` branch count on the others - and `.wcMark img,.wcMenuMark img{object-fit:cover;border-radius:inherit}` is what makes the maroon Viskool square and the white-backed Wayvida glyph both read as app icons. `tests/org-switcher-ui.test.mjs` pins the markup, the portal, the shared writer and the tokens, and `tests/header-context.test.mjs` pins the shell geometry.
63. The register count opens the page heading, not the toolbar: `<h2>Name <span className="registerHeadCount">({n})</span></h2>`, styled once at `.registerHead .registerHeadText .registerHeadCount` on the title tokens (24px/700 `#172033`), exactly as Journal Entries, Items, Budgets and Period Lock already did and as Customers, Sales orders, Sales invoices, Credit Notes and Payment Receipts now do. Count the register and not the rows on screen, so the heading does not move while the reader filters, and keep the numbers unpadded - every count in the app is. Never put a record count back in a register toolbar; two tests pin that the old toolbar counts are gone.
 57. Every table page shares one scroll behaviour, and it is never written per page. `src/register-head.js` and `src/register-head.css` are installed once from `src/main.jsx`: the module finds the page grid - the first table in the scroll port with a `thead`, excluding anything inside a form, dialog, disclosure, detail sheet or document preview - marks it `data-sticky-head`, and publishes the page heading row height as `--register-head-h`, republished as `0px` while that row is hidden; the sheet pins `[data-sticky-head] thead th` at `top:var(--register-head-h,0px)` with `z-index:6`, the `inset 0 -1px 0 #e6ecf6` stand-in for the collapsed border and a `.22s` top transition, so the column heading takes the row place rather than jumping. Never declare a fill in that rule - every shell paints its `th` opaquely and a sticky cell must stay opaque. Sticky positions against the nearest scrolling ancestor, so the module releases a wrapper that is a scroll container to `overflow:visible` through `stickyHeadWrap`, but only while the table fits it horizontally and re-decided on resize: the sales orders table is `min-width:920px`, so it pins at 1440 and keeps its sideways scrolling at 900 and 760. The hide threshold is `HIDE_AFTER=48` plus the row own height, and a `focusin` handler brings the row back for a keyboard-reached action. Every grid page shares one register header row, and it is migrated deliberately. `src/register-head.css` and `src/register-head.js` are imported and installed once from `src/main.jsx` after `ui-quality-polish.css`, and everything in them is scoped to the `registerHead` marker class so an unmigrated page keeps its old header. The marker (`div.ivHeading.registerHead` or its `itemsHeading` equivalent) is one sticky row: a `div.registerHeadText` title and subtitle, then the page toolbar with its card chrome stripped, then the actions - and the search must stay directly beside the Filters disclosure, which is why `margin-left:auto` sits on the search and not on the filter menu. The script keeps one capture-phase document scroll listener (a scroll event does not bubble and the shell scroll port may not exist at import time), hides the row while the reader scrolls down, returns it on the first upward scroll or at the very top, and publishes `--register-head-h`. Migrate one page at a time and verify each: the pages split across the `iv` and `items` register shells with Budgets, Period Lock, Journal Entries and Inventory Adjustments on bespoke headers, and Goods Receipts, Purchases and Debit Notes each carry an inner `ivHeading` inside their create form, so the register heading must be found by hand. Only put the split button on pages that already carry page-level secondary header actions - never invent an Import or Export, because that would add functionality. The one exception is the Journal Entries register, whose menu carries a read-only `Export register` built from the rows already on screen through the Day Book's `csvReport` / `downloadReport` helpers. Journal Entries is the second migrated page and the first bespoke one: `src/journal-heading-fix.css` makes its heading a two-column `!important` grid that loads after the shared sheet, so the merged row restates the grid with a third `auto` column at a higher specificity, and its transparent white heading had to become an opaque `#f6f8fb` bar before it could cover what it sticks over. Budgets, Period Lock and Inventory Adjustments are the remaining bespoke headers and will need the same three moves; the reusable split-button geometry now lives in `src/register-head.css`, and the dropdown it opens wants room: an 8px panel padding, a 212px minimum width and 40px rows with 12px of horizontal padding and `align-items:center`, because a 36px row with 10px of padding reads as cramped. Its entries are labelled `Import` and `Export`. Import on the Journal register is `src/journal-import.js` (a pure parse, group and validate module) over `JournalImportDialog` in `src/JournalEntriesPro.jsx`, styled by `src/journal-import.css`, and it writes `Draft` journals only, through the same store the create page uses, so an import can never post. Watch for one trap: a page that hides a legacy sibling with an adjacent-sibling rule unhides it again the moment anything is rendered between them - the journal dialog did exactly that, and those rules are general-sibling now. That sheet also makes the heading bar the top of the register card, which is what the user meant by a page that "feels like table heading": the bar takes the card border and the `10px 10px 0 0` radius with a hairline beneath it, and the card after it gives up its top border and takes `0 0 10px 10px`, so there is no gap and no seam between the title row and the column header row. Each shell keeps its own gutter inside the bar (16px journal, 22px `iv` / `items`), and the bar never takes a shadow of its own, because a sticky bar paints above the card below it and its shadow would draw a seam across the edge they share.
55. The Chart of Accounts page header is one row, the page scrolls as one page, and the head hides on the way down. `div.am-heading.am-coa-heading` is gone; `AccountFilters` renders `div.am-toolbar.am-filter-toolbar` holding, in order, `div.am-coa-title` (h1 with `span.am-heading-count` and the subtitle `p`), `label.am-search`, `details.am-filter-menu` and `div.am-actions.am-toolbar-actions`. The account-type and status selects live inside the Filters panel beside Created by, Account group, Branch requirement and Balance range, so the panel badge and Reset cover all six, and the summary carries the 17px `IconFilter` funnel and the label `Filters` - never restore the `Advanced filters` label, the `IconAdjustments` summary or the two toolbar selects, and keep `IconAdjustments` imported because the account-detail `toggle` action uses it. Add Account is the split button from the supplied reference: `div.am-split` holds `button.primary.am-split-main` beside `details.am-split-more`, whose 38px `summary` carries the 18px `IconChevronDown` and whose panel holds Import and Export. The primary half keeps `border-top-right-radius:0` / `border-bottom-right-radius:0` and the chevron half carries the outer `7px` radius and a `#6c9bf9` divider, so the two read as one 40px control; the panel uses the canonical menu geometry at `z-index:80` and declares `display:flex` only under `[open]`, because a plain `display` rule on a `<details>` child outranks the user-agent rule that hides closed content. Never put Import or Export back on the row as their own controls. The search box is deliberately narrow at `flex:0 1 180px;min-width:160px` and must stay directly beside the Filters button: `.am-search` carries `margin-left:auto` so the free space opens between the title and the search, and `.am-filter-menu` carries `margin-left:0`, which keeps the search/Filters/Add Account cluster tight and right-aligned. The account list is no longer an accordion: the grid is one flat table whose Account Type column carries what the section headings used to say, `AccountGroup` / `openGroups` / `openList` / `toggleGroup` and the twenty-one `.am-group-row` / `.am-group-toggle` rules are deleted, and only the separate `collapsed` array survives because it still owns the sub-account chevrons. Each type carries one mark in that column - `TYPE_MARKS`: assets indigo `IconHome`, liabilities rose `IconChartPie`, equity blue `IconChartHistogram`, income cyan `IconCoins`, expenses amber `IconShoppingBag`, each on a 20px circle with the glyph reversed out - and because the Status column spends green and grey, no type tone may sit in one of those families. Every row closes with a visible `button.am-row-edit` beside its `details.am-more` menu. That section row survives only in the budget grids: the Budget Account Selection grid (`.budgetAccountGrid .budgetGroupCell`) and the Budget Allocation grid (`.budgetAllocGroup>th>button`) in `src/budget-workspace.css` keep the same 48px height, chip, count pill and accent, and the statement bands (`.budgetAllocBand>th`, `.budgetStatementSection>th`) carry the same accent. The Account Group combobox in the Create/Edit Account drawer sizes its results panel to its own trigger (`.agp-pop{left:0;right:0;width:auto}`), never to a fixed width, so the list can never overhang the field it belongs to. Two traps: `.budgetV3 button` sets `justify-content:center`, so `.budgetAllocGroup>th>button` must state `justify-content:flex-start` or the row centres itself in the wide `colSpan` cell; and the left accent must sit on the button, never on the `th`, because the full-width button covers the cell and would hide it. The account-grid wrapper is not a scroll port: `.am-coa-page .am-table-wrap:has(.am-account-grid)` is `overflow:visible;max-height:none;overscroll-behavior:auto`, `.app>main` is the only scroller, and below 700px only the wrapper regains `overflow-x:auto` while the sticky cells go static. The toolbar keeps `position:sticky;top:0;z-index:9` plus `flex-wrap:wrap` and hides while the reader scrolls down, returning on the first upward scroll or at the top, through `.am-coa-page.am-coa-bar-hidden>.am-filter-toolbar{transform:translateY(calc(-100% - 32px))}`. Because the sticky heading now binds to `main`, it offsets by the measured bar height: the effect in `AccountWorkspace` publishes `bar.offsetHeight` as `--am-coa-bar-h` (65px fallback) and keeps it current with a `ResizeObserver`, so `thead th` is `sticky;top:var(--am-coa-bar-h);z-index:6` and drops by the bar height under `.am-coa-bar-hidden`; never hard-code a bar height or restore the wrapper `max-height`. The row geometry stays `.am-coa-title` as a centred grid at `flex:1 1 340px` with `min-width:170px` and `max-width:min(430px,40vw)`, search `flex:1 1 180px;min-width:150px`, and a `flex:0 0 auto` action group at `gap:8px`, with the widths that matter scoped to `@media(min-width:851px)`. The account grid keeps the shared `.am-table` bones plus a `#f8fbff` row hover, a right-aligned Actions column at `width:1%`, a 48px-padded first header cell lining up with the account names, tabular figures on the code column, and 12px as the smallest text.

56. Create / Edit Vendor is the shared create page, not the purchase document shell. `src/Vendors.jsx` renders `section.itemsPage.vendorsPage` holding `form.itemCreatePage` with one `div.itemDialogHead` (40px `button.itemBack`, `h2#itemDialogTitle`, `small.itemHeadHint`), one `div.itemDialogBody` card and `footer.itemDialogFooter`, and imports `src/items.css` directly; never put it back on `soCreatePage` / `ivCreateForm` / `soActionBar` / `ivFields`. Its three `section.itemSection` blocks are Vendor details (the shared vendor-type radio group over the merged Basic and Contact information fields), Billing address (the always-visible billing grid plus the `label.itemCheck.vendorSameAddress` switch that reveals the shipping grid when it is off) and Tax & payment information (the merged Tax and Payment cards, the GST/TDS `vendorSwitchRow` switches first, then a `vendorSubTitle` hairline opening Payment & bank details). The accountant-only fields stay in one `details.itemAdvanced` disclosure that a failed save opens. `wayvida-vendors-v1` keeps its shape plus the `shippingSameAsBilling` flag: a billing edit mirrors into shipping while it is true and `save` writes `shipping` from `billing` then, so a stored vendor never holds two disagreeing addresses, while a legacy record derives the flag by comparing its two addresses. Never restore the `Copy billing address` button or the `.vendorToggleRow` / `.vendorUpload` / `.vendorControlNote` / `.vendorError` rules.

Before editing, inspect the relevant component, service, tests, and domain document. State the feature's current status. Implement domain rules in service modules rather than JSX. Preserve backward-compatible optional state fields. Add tests for validation, lifecycle, idempotency, reversal, accounting/report impact, and responsive UI contracts. Update the documentation and manifest if architecture, storage keys, module status, or invariants change.

Before declaring any feature complete, follow the mandatory feature checkpoint in `AGENTS.md`: update affected knowledge documents and the manifest, refresh verification metadata, record actual QA, and run `npm run docs:check`, relevant tests, and the production build.

Important current warning: Period Lock is a navigable, styled, tested browser prototype with a task-oriented close-books workflow, five-point closing checklist, recorded closer, approval-based reopen flow and period-control settings. Do not claim it protects accounting data in production until every dated command enforces the policy atomically on an authenticated backend.
 64. The dashboard shell has no working-context row below the application header any more, and that is what the old 46px geometry was. The `div.orgContextBar` rule, `.app>main{margin-top:46px}` and the `top:105px!important` portal offset are gone from `src/ui-quality-polish.css`: `.app>main` is `height:calc(100dvh - 59px)` and the portalled workspaces (`.transactionPortal`, `.auditPortal`, `.helpPortal`, `.settingsPortal`, `.pc`, `.budgetPage`) start at `top:59px`. Do not reintroduce a second shell row - the working context is in the sidebar, and a page that needs its own full-bleed head now sits directly under the application header. Two names moved with it: `src/HeaderOrgSelectors.jsx` is `src/WorkingContextSwitcher.jsx`, `src/header-org-selectors.css` is `src/working-context.css`, and `demoOrganizations` moved to its own `src/demo-organisations.js`, which also removed the import cycle between that component and `src/organisation-context.js`.
 63. The dashboard sidebar has one desktop collapse toggle, and its geometry must stay scoped to `min-width:701px`. `src/App.jsx` keeps the state in `localStorage['finance-erp-nav-collapsed']` (`'1'` means collapsed), renders `className={'app'+(nav?' navCollapsed':'')}`, gives the sidebar `id="appNav"`, and puts `button.navToggle` (with `aria-label`, `aria-expanded`, `aria-controls="appNav"` and the `IconLayoutSidebarLeftCollapse` / `IconLayoutSidebarLeftExpand` glyphs) as the header's first child. `src/navigation.css` owns the block: `.app.navCollapsed:has(>aside .erpNavigation){padding-left:0}`, `.app.navCollapsed>header{left:0}`, `.app.navCollapsed>aside{transform:translateX(-100%);visibility:hidden}` and the button reveal `.app>header>button.navToggle:not(.primary):not(.hamb){display:grid}`. Three details are load-bearing: the `:not()` pair beats `header>button:not(.primary):not(.hamb){display:none}` in `src/styles.css`, which would otherwise hide the toggle below 1050px; the media-query scope is what keeps `aside.open{transform:none}` working for the narrow drawer; and `visibility:hidden` is what takes the slid-out sidebar out of the tab order. The 270px offset is also hardcoded on seven surfaces that are portalled to `document.body` - `.orgContextBar`, `.pc`, `.transactionPortal`, `.settingsPortal` / `.helpPortal` / `.auditPortal`, `.itemCreatePage .itemDialogFooter` and `.guideBackdrop` - so the collapsed state is published as the `body.navCollapsed` class as well, and every one of them is reclaimed with `left:0` from there. Keep those rules beside the toggle, and leave the collapse state a display preference: it changes no route, page name, stored record or accounting rule. `tests/navigation-toggle-ui.test.mjs` pins the markup, the key, the scoping and each reclaimed offset.
 62. The Customer Receipts register is titled Payment Receipts and is the eleventh merged register row. It carries eight columns - Receipt & date, Invoice, Branch & organisation, Payment & type, Reference, Customer & type, Mode & amount and Actions - at 12/9/12/12/14/13/13/15, and the Invoice column prints every invoice the receipt settles, joined by commas, or `Unallocated` when it settles none, with `n invoices` beneath when there is more than one. The Invoice, Reference and payment-mode cells wrap (`white-space:normal;overflow-wrap:anywhere`) rather than being ellipsised, and every row carries one visible `View details` action beside the row menu - which calls the page `open(x)` and opens the receipt detail, so do not restore the menu `View` entry it replaced, keep the trigger at the matching 36px square, and keep that `details` `display:inline-block` so its open 218px panel is never shrunk to the column width. Its widths must be declared as `.receiptWorkspace table.receiptTable th:nth-child(n)` and total exactly 100%: the table also wears `ivInvoiceTable`, whose first seven column rules in `src/invoice-workspace.css` are one class more specific, so a bare `.receiptTable th:nth-child(n)` loses silently and the register falls back to the invoice proportions. The rename is display copy only: `Payment Receipts` is the register title and `Back to payment receipts` is the detail back label, while the route/page name `Payments Received`, the sidebar leaf `Receipts`, and the stored journal source label `Customer Receipt` (which `GeneralLedgerPro.sourcePage` maps back to this page) must not be renamed - they are navigation keys and stored data, not titles. The layout is one card: `div.ivHeading.receiptNoPrint.registerHead` holds `div.registerHeadText`, then `div.ivTools`, then `div.ivActions`, and `div.ivCard.ivRegisterCard` opens AFTER that row; the toolbar must not go back inside the card. Keep the `header(title, description, actions)` helper in `src/Receipts.jsx`, because the New receipt form and the Bank receipt matching screen render through it, and keep `receiptNoPrint` on the merged row, because it is the print stylesheet's display:none hook. The add action is the shared split button over the page `create()`, its caret holding `Bank matching` and `Settings` and closing through `menuRun`, with `IconChevronDown` imported. Status is a field of `div.soFiltersPanel` now: `activeReceiptFilters` counts it with `+(status==='All'?0:1)` and the panel Clear filters row resets it with `setStatus('All')`, so the badge and the reset can never disagree. `tests/receipt-ui.test.mjs` pins all of that and asserts no user-visible `Customer receipts` string survives.\r
 61. The Credit Notes register is the tenth merged register row, and it is one card like the rest: `div.ivHeading.registerHead` holds `div.registerHeadText` (the `Credit Notes` title and the unchanged description), then the `filterBar` (`div.ivTools`, the search directly beside the `details.soFiltersMore` Filters disclosure), then `div.ivActions`; `div.ivCard.ivRegisterCard` opens AFTER that row - the toolbar must not go back inside the card, which is where it used to be, or the row stops merging. The `top(title, sub, actions)` helper stays in `src/CreditNotes.jsx` because the GST transaction register and the Customer statement render through it; only the Credit Notes call was rewritten. The add action is the shared split button over the page `start`, its caret holding `Account mapping` and closing through `menuRun`, and the caret glyph `IconChevronDown` must stay imported. Do not add Import or Export to that menu: the two sibling sales documents had Export removed in the preceding pass and the engine has no credit note import contract. Status, Reason, Customer, From, To and Sort stay inside `div.soFiltersPanel` with the one count badge and the Clear filters row, and the card keeps its own `div.ivActions` pagination row (Previous / Page n of m / Next) and its `ivEmpty` block under the table - never pull either into the heading row. The ten-column table keeps its own width rules in `src/invoice-workspace.css`; only the layout, padding and positioning changed.\r
 59. The Sales orders register row is TWO icons, and the EYE opens that order own preview. `button.soRowIconButton` (36x36, `aria-label`/`title` reading `Preview <number>`) calls `setPrintDoc(x)`, which renders `DocumentPreview kind="Sales Order"` - the only detail screen a sales order has - for EVERY order, whether or not it has an invoice; never restore the disabled `View details` text button, and never restore `.soPreviewButton`. The invoice hand-off moved into the row menu, where it belongs: `SalesOrderActions` takes `onOpenInvoice`, its invoice entry is `disabled={!order.invoice&&order.status!=='Confirmed'}`, and it calls `onOpenInvoice()` when the order already has an invoice and `convert()` only when it does not - so `invoiceIdOf(order)` (which resolves `order.invoice.id` first and then the invoice store by `sourceOrder`, exactly as `convertSalesOrder` does) and the `wayvida-open-invoice` handshake both stay in use. Editing stays in that menu.
 60. Every register renders the same `IconFilter` at `size={17}`, and it must render at that size everywhere: a glyph narrower than it is tall has been shrunk, not swapped. `src/register-head.css` carries the guard - `.registerHead details:has(>summary[aria-label="Open filters"]){flex:0 0 auto}` and `.registerHead summary[aria-label="Open filters"]>svg{flex:0 0 auto;width:17px;height:17px}` - and `src/sales-orders.css` carries the root-cause fix `.soFiltersMore{position:relative;flex:none;margin-left:auto}`, because it was the only filter disclosure in the app without `flex:none` while `.customerFilters`, `.iaFilters` and `.budgetFiltersMore` all had it. The funnel measured 8.5px on the invoices register and 5.3px on the sales orders register before this and 17px on both after, with no horizontal overflow from 700px to 1440px.\r
58. The Sales invoices register is one merged register row, like Vendors, Journal Entries, Budgets, Period Lock, Items, Inventory Adjustments, Customers and Sales Orders before it. `div.ivHeading.registerHead` holds `div.registerHeadText` (the `Sales invoices` title and the unchanged description), then `div.ivTools`, then `div.ivActions`, and `div.ivCard.ivRegisterCard` opens AFTER that row and holds only `div.ivScroll` and `table.ivInvoiceTable` - the toolbar must not go back inside the card or the row stops merging. The card gives up its top border and top corners for `0 0 10px 10px` while the row takes the card border and `10px 10px 0 0`. The Status select is a field of `div.soFiltersPanel` now, not a toolbar control, and the panel Clear filters row resets it together with the nine panel fields while `activeInvoiceFilters` counts it, so the badge and the reset can never disagree. The add action is the shared split button over the page `create()`, and its chevron menu holds `Account mapping`, `Use demo example` and `Export`; the Export is read-only, writing the filtered `visibleInvoices` through the Day Book `csvReport` / `downloadReport` as `wayvida-invoices.csv`. Do not add an Import to that menu: the invoice engine has no import contract and a second write path would bypass the create form validation. Keep the shared `heading(title, sub, actions)` helper in `src/InvoiceWorkspace.jsx` - Payments received, General Ledger, Trial Balance and Financial Statements still render through it. Two shared rules in `src/register-head.css` are load-bearing: `.registerHead.ivHeading .registerHeadText h2` / `p` restate the Journal Entries 24px/700 `#172033` title and 12px `#667085` description because the invoice and vendor shells otherwise take a 23px `.invoiceWorkspace h2`, and `@media(max-width:760px){.registerHead:has(.soFiltersMore[open]),.registerHead:has(.customerFilters[open]){position:static}}` gives the row up while a filter panel opens in the flow at a narrow width, or the sticky row grows taller than the viewport it pins to. The migrated set is now Vendors, Journal Entries, Budgets, Period Lock, Items, Inventory Adjustments, Customers, Sales Orders, Invoices, Credit Notes and Payment Receipts; Purchase Orders, Goods Receipts, Purchase Bills, Debit Notes, Payments Made and Purchase Reports remain.
53. The credit note is a type-aware, refundable adjustment owned by `src/credit-note-service.js`. Keep one vocabulary: `CREDIT_NOTE_TYPES` (Against Invoice default / Without Invoice), `CREDIT_REASONS` with `canonicalReason` for the labels the earlier prototype wrote, `creditMappingRole` (a Tax Correction has a role of its own so it cannot reduce revenue), the derived `CREDIT_NOTE_STATUSES` / `creditNoteStatus` beside the stored lifecycle, and `available()` netting refunds off. A refund posts through the same `journal()` helper, is idempotent by token and may only refund credit that is still available. `calculateCredit` reads the tax from the invoice when there is one and taxes the note's own lines through the same `calculate()` when there is not - never a second tax implementation. The detail page uses the shared `nav.itemDetailTabs.ivDetailTabs` row with Overview | Applications | Accounting | Activity. A credit note never moves stock: `salesReturnId` links a Sales Return document, which is not built yet, and that document owns the inventory movement.
52. The invoices register's row controls must stay identical to the sales order register's: `.invoiceWorkspace .ivRowActions button` restates the row button (36px, `0 9px`, `1px solid #d0d5dd`, 7px radius, white, `#344054`, `#98a2b3` hover, `#84adff` focus ring) and `.invoiceWorkspace .ivRowActions button.soMoreButton` restates the shared trigger at 36x36 with `padding:0;color:#475467`. They exist because `.invoiceWorkspace button` (9px 13px padding, 6px radius, `#dce3ee`, 36px min-height) outranks the shared single-class rules, which made the same component look like a different control on this page. Restate the pair together; when a shared component renders differently on one page, check the cascade before touching the component.
51. The invoices register is EIGHT fixed columns and must not scroll sideways: `.ivInvoiceTable` stays `table-layout:fixed;width:100%;min-width:0` with the percentage widths 13/16/11/12/12/16/11/9 and `overflow:hidden;text-overflow:ellipsis` per cell, and the register card keeps `.ivRegisterCard{overflow:visible}`. The columns are Invoice (number, then `{i.date}` alone - the raw status used to sit under it and would print `Draft` beside a Status column reading `Pending`), Customer, Status, Invoice amount (with the outstanding balance as a small line only while `0 < outstanding < total`), Payment status (the shared `StatusPill` with `PAYMENT_TONES`), Organisation, Branch and Actions. There is NO Order ID column: the order reference is read on the invoice detail page, in the stats row `Order number and status` and the details list `Reference`, and `orderNumberOf(i)` survives only as the register Order ID FILTER - never delete that filter with the column. Organisation and Branch are cells of their own. The Status cell sits AHEAD of the amount by the user request of 2026-09-25 and is `StatusPill status={invoiceDisplayStatus(i)} tone={invoiceDisplayTone(i)}` from `src/invoice-register-display.js`: Cancelled first, then `posted` means Published, then `Pending Approval` means Pending, else Draft - presentation only, never a rewrite of the stored status, so `Paid`, `Sent` and `Overdue` all read Published while a cancelled invoice that once posted stays Cancelled. The row action is `button.ivIconButton` (36x36, `aria-label={'View '+i.number}`) beside the row menu. All eight widths are positional, so any column change means restating them, and the credit-note register overrides all eight for its own table.
50. The invoices register has a row menu per invoice (`src/InvoiceRowActions.jsx`) beside the `View` link, with the six entries the user asked for: Approve and post, Record payment, Make Recurring, Create Credit Note, Duplicate and Cancel Invoice. Keep it on the sales order register's pattern - the 34px `.soMoreButton` trigger, the portalled `.soActionMenu`, the `.soActionDanger` row and `src/sales-order-actions.css` - because the register scrolls inside `.ivScroll` and an absolutely positioned menu is clipped there; keep every entry rendered with its reason sentence while disabled; and keep the handlers on the app's own commands (`run('post')`, `startPayment`, `duplicateInvoice`, the page's cancellation dialog) rather than re-implementing them. Create Credit Note hands off through `wayvida-credit-customer` + `wayvida-credit-invoice` + `QUICK_CREATE_KEY='credit-note'`, and `selectInvoice` in `src/CreditNotes.jsx` must stay a functional `setForm` update - it runs in the same effect as `start()` and reading the closure `form` drops the seeded fields. Make Recurring is a *stored schedule*, not a generator: `RECURRING_FREQUENCIES` and the `recurring` command action live in `src/invoice-engine.js`, only a posted non-cancelled invoice can be scheduled, `off:true` clears it, nothing is posted, and the dialog and the register say so.
49. The action bar on both shared create pages is pinned and right-aligned, and both halves are load-bearing. `.soCreatePage .soActionBar` stays `position:sticky;bottom:0;z-index:6` with the opaque `background:#f6f8fb` and the top hairline - Cancel, Save draft and Save & confirm must never be scrolled out of reach on a long document - and `.soCreatePage .soBarActions` keeps `margin-left:auto`, because the running totals `<dl>` renders only while the document can be calculated and a lone flex child under `justify-content:space-between` sits at the LEFT, which is what parked the buttons under the customer card. The bar stays under the row menus (z-index 80) and the address dialog (z-index 1200), it is sticky rather than fixed so it keeps the page gutter, and it is the form's last element so its natural position is below the last card and nothing is hidden at the end of a scroll. `.soCreatePage .soBillingSummary` must not regain a `border-top` or `padding-top`: that hairline was the line the user asked to remove above the Billing summary. The address dialog's fields are `div.soAddressFields`, so keep `.soAddressDialog>.soAddressFields{padding:16px 18px 0}` - the same gutter as the dialog's heading, note and footer - and do not restore the dead `.soAddressDialog>label` rule. These create pages scroll on `documentElement`, not on `.app>main`, which is what a browser check has to drive.
48. The Edit-customer detour from a sales document is a two-key handshake and must keep working: the page stashes its draft under `wayvida-draft-order` / `wayvida-draft-invoice`, sets `wayvida-edit-customer` and `wayvida-return-to`, and `Customers` sends the operator back on save; the returning page restores the draft in its `useState` initializer and refreshes it through `applyCustomerSnapshot`. Two silent failures are recorded here so they cannot come back: a stored customer that names no `gstTreatment` of its own must not be given a registered treatment it cannot satisfy (the default follows the record's own tax identity - a GSTIN present keeps the registered default, none opens `Unregistered Business`, an individual stays `Consumer`), and the invoice initializer must read the customer list from storage inline, because reading the `customers` const declared below it threw a temporal-dead-zone ReferenceError into the initializer's own `try/catch` and the draft was swallowed. Both create cards are named by their headings alone - the customer caption and the organisation-and-branch caption are deleted - and the customer card shows the shared animated empty state (the `customer` variant of `src/EmptyState.jsx`) until a customer is selected, never the eight placeholder facts.
47. Place of supply is derived, never typed. It is stored as `place` - never rename it, every stored order and invoice and every reader depends on that name - together with the additive optional `placeSource` (`customer_address` / `shipping_address` / `manual`) and `placeCode` (from the existing `GST_STATE_CODES`). `src/customer-tax.js` holds the pure rules: `placeOfSupplySuggestion` prefers a distinct shipping address because goods are supplied where they are delivered, then the customer's registered place, then the state named in the billing text; `findStateInAddress` accepts only a name the app already knows, longest match first, so a city never becomes a state and nothing is ever guessed; `placeOfSupplyStatus` produces the helper line and the reset label; `gstStateCode` inverts the existing table rather than adding a second one. A shipping address that merely copies billing is reported as the customer address, not the shipping one. `src/SalesDocumentFields.jsx` applies them: `chooseCustomer` fills all three fields, `applyPlace(next, force)` recalculates only a derived value, a stored document carrying a place but no source counts as manual so old records are never silently re-taxed, and choosing by hand sets `placeSource:'manual'` and reveals `Reset to shipping state` / `Reset to customer state` - the only path allowed to overwrite a manual value. The control lives at the foot of the Customer card (`div.soPlaceOfSupply`, `#soPlaceOfSupply`, `p.soPlaceHint`, 40px select on the shared tokens, 520px reading width), never in the document-details grid. `convertSalesOrder` carries `place`, `placeSource` and `placeCode` to the invoice draft, and direct invoice creation derives through the same component, so the operator never selects it twice. Do not add GST maths to the UI: `calculate()` decides CGST+SGST vs IGST from the final place of supply, and a fixture pairing an inter-state place with CGST/SGST is refused by the engine.
46. Both shared create pages keep their side padding, and their action bar must never cover the form. The shell gutter is `.soCreatePage>:not(.soCreateHead){margin-left/right:clamp(16px,2.2vw,24px)}` - two classes, `0,2,0` - so a page rule of the same specificity that comes later in the cascade overrides it, and `margin:12px 0 0` on `form.salesOrderForm` / `form.ivCreateForm` set all four sides and silently cancelled it: content sat flush against the sidebar and the right edge, and the action bar's deliberate `-24px` bleed (sized to reach the page edges) then overflowed the document by 24px. Those two rules must set only `margin-top` / `margin-bottom`; never reintroduce a `margin` shorthand there, and prove it by measuring `documentElement.scrollWidth === clientWidth`. `.soCreatePage .soActionBar` is `position:sticky;bottom:0` again - see item 48 - because the in-flow footer it was moved to (a pinned bar had hidden the Billing summary while the form was at the top) left the three decisions a full scroll away; it is opaque, layered under the row menus and the address dialog, and its natural position is below the last card, so nothing is hidden at the end of a scroll. The container's bottom padding is still the normal `28px`, which is enough because the form's own end sits above the bar. The line table keeps `table-layout:fixed` with `min-width:0` at desktop widths - that is what stops the eight-column table scrolling sideways from 1600px down to 980px - but a `@media(max-width:700px)` block sets `min-width:760px` so `.ivScroll` scrolls it at phone width, where the fixed percentage columns left the item search about 35px wide. The same sticky-footer overlap still exists on Create Item and Create Journal Entry (`.itemDialogFooter` / the journal sticky card); it was left alone deliberately.
45. The New sales invoice create page and the Create Sales Order page are the same page, and must stay that way. `src/SalesDocumentFields.jsx` renders one shared layout for both documents - never reintroduce a `kind==='order'?<>` branch, which is what kept the invoice on an older flat card. `div.soOrderTop` opens with the Customer card (shared `SearchSelect` picker, then the read-only GST treatment / GSTIN / PAN facts and the two addresses with their labelled Edit) at 40% beside the document-details card at 60%, whose eight fields are one `soFieldRow4` grid (organisation, branch, number, date, reference, place of supply, payment terms, due date); wording switches through `docWord` / `docTitle`. A page adopts the shell by carrying the `soCreatePage` class, never by copying its rules - `src/InvoiceWorkspace.jsx` renders `invoiceWorkspace soCreatePage ivCreatePage` from `const createOpen=!!form`, so the head bar, gutter, fact list, address editor, Additional details card and sticky action bar come from `src/sales-orders.css` by construction; put any new shared-shell rule there under `.soCreatePage`. Keep the genuine differences: the invoice keeps its editable unit input (its engine refuses a line with no unit) and `Save draft` (it never posts from the create page); the order keeps its read-only unit, its credit-limit notice and its shipment fields. A new invoice seeds the working-context organisation and branch exactly as the order does. Keep the `@media(max-width:560px)` block that restates the `.soOrderTop`-scoped fact-list and field-row folds at the same class depth, because a three-class rule inside one media query beats a two-class rule inside another whatever the source order - media queries add no specificity - and without it every read-only value wraps one character per line at 420px. The invoice deliberately has no document attachments: it has no attachment reader (preview, share or print), so that is a separate feature rather than a styling gap.

After changes, run the relevant Node tests, full test suite, `npm run build`, and `npm run test:sites`; visually inspect the live route at desktop and narrow widths. Report exactly what was verified and what remains prototype-only.

---
Advanced page header bleed (2026-09-25): the advanced header must keep Simple mode full bleed - the shared `.rt-header` side margins of `-24px` with its 24px padding - so the white bar spans the whole content area and touches the page edges on both sides while the form and lines cards stay inset under it. A page scoped rule had set the header 22px INSIDE those cards, which read as the wrong width; that is overridden with `.app main:has(> .je-create:not(.je-business)) .je-create .rt-header{margin-left:-24px;margin-right:-24px}` and `-12px` under 760px. Measured at 1600: Simple header margins -24/-24 and the advanced header margins -24/-24, the advanced bar 1358px wide against its 1310px cards. Do not move it back inside the cards, and do not re-add the earlier translateX shift.

Customer register row action and open defect (2026-09-25): the row View Customer button is icon only - `button.customerView` holds just the eye with `aria-label="View <customer name>"` and `title="View customer"`, sized 34x34 by a page scoped `.customersPage .customerRowActions .customerView` rule; do not put the text label back. Every hook in the `Customers` component must stay ABOVE the `if(selected){...return <section ...customerDetailPage>}` early return - a `useRef` below it made the list render one hook more than the detail and threw "Rendered fewer hooks than expected", crashing the whole app through the start error boundary. That crash is fixed, but the detail still does NOT open on the click (the register keeps rendering with no console error), so verify `openDetail` / `selectedId` against the row ids before claiming the redirect works.



Invoice detail and create actions (2026-09-25): the two screens agree on which verb is primary. On the DETAIL screen a Draft offers `button` + `IconEdit` + `Edit` - text AND icon, because a bare word reads as a link in that action row - beside `button.primary` `Submit for approval`, and the pending step keeps its own `Approve & post` primary. On the CREATE page `div.soBarActions` carries three verbs - `Cancel`, `Save draft`, `Submit for approval` - through ONE saver, `saveInvoice(status)`, which stamps the organisation and branch the way a sales order does. `Save draft` is the form submit button and takes the primary fill ONLY when `invoiceAllowed(role,'submit')` is false, so the page always has exactly one primary action; `Submit for approval` is `type="button"` and primary when the role may submit. If the save succeeds and the SUBMIT is refused, the form stays open bound to the saved record (`setForm({...form,id:saved.id,number:saved.number})`) so a retry edits that invoice rather than creating a second one, and the engine error stays on screen. The create page never posts: posting lives on the detail screen, for the role that holds the duty.
 63. The Sales orders register is EIGHT columns - Order (the SO number over its order date through `fmtDate`), Customer, Organisation, Branch, Amount, Status, Payment status and Actions - at 12/15/17/12/11/10/11/12 percent, and `soOrdersTable` widths are positional so any column change means restating all eight. Organisation and Branch are cells of their own in that order, matching the invoices register, and they replaced the single `Branch & Organisation` cell that printed branch over organisation. There is NO Created by column; Created by survives as a register FILTER, the same choice made for the invoices Order ID, because removing a column is not the same as removing the ability to search by it. `src/sales-orders.css` must keep exactly ONE run of the eight width rules - it used to carry two identical runs, the first entirely overridden by the second, and the dead one has been deleted.


Item detail lifecycle badge (2026-09-25): the Active / Disabled badge sits ON THE NAME LINE, not in the action row. `div.itemDetailNameRow` wraps `<h2>{selected.name}</h2>` and `<span className={'itemStatus '+(selected.active===false?'disabled':'active')}>`, and `.itemDetailNameRow{display:flex;align-items:center;gap:10px;min-width:0}` in `src/items.css` is what keeps them on one line; do not move the badge back into `div.itemDetailActions`.

Sidebar shape (2026-09-25): `src/Navigation.jsx` declares `{...leaf('Items'),icon:IconPackage}` as a TOP-LEVEL destination directly between Accounting and Sales, the `Inventory` group is DELETED, and the `Other` group - now `{label:'Other',children:[leaf('Budgets'),leaf('Period Lock'),leaf('Inventory Adjustments')]}` - is NESTED INSIDE `Settings` rather than a section of its own after Accounting. `IconFolders` left the icon import with the section it belonged to. The renderer is recursive, so a nested group needs no new code, and NO routed page name changed - `Inventory Adjustments` still resolves through `src/App.jsx`; only where the sidebar lists it moved. Do not restore the Inventory group or lift Other back to the top level.

Current printed-sheet geometry invariant (2026-09-26): a document preview is mounted INSIDE a register page, so the host page's table rules reach the printed sheet and must never be allowed to shape it. `src/document-preview-fix.css` restates the sheet's own geometry under `!important` - `.dpPaper table{min-width:0}`, `.dpPaper th{padding:10px 6px;white-space:normal}`, `.dpPaper td{padding:12px 6px}`, `.dpPaper th:first-child,.dpPaper td:first-child{padding-left:6px}`, `.dpPaper th:last-child,.dpPaper td:last-child{padding-right:6px}`, plus `.dpPaper .dpItemsTable th{padding:12px 6px}` and `.dpPaper .dpItemsTable td{padding:14px 6px}` - because `.itemsPage table` in `src/items.css` declares `min-width:800px` and `src/ui-system.css` pads every `th`/`td` with `16px 20px` and a 24px first/last inset, also under `!important`. Measured live on 2026-09-26 that printed the eight-column items table 800px wide inside a 620px paper, 150px past the right page edge, with `HSN / SAC` and `Qty / Unit` wrapped onto two lines. The eight `.dpItemsTable` columns are 4/21/13/12/12/12/10/16 percent and must total 100, so the table always equals the paper's content box at any host width. Never give the sheet table a `min-width`, never let a host rule win, and never add a column without restating every positional width and re-checking both two-word headers.

Current credit note module invariant (2026-09-26): the Credit Note create page is six sections in flow order - **Credit note details** (number generated and READ-ONLY, date, type, customer, reason, reason detail, reference), **Original invoice details** (the picker plus a read-only fact list), **Credit items**, **Tax summary**, **Accounting preview**, and a collapsed **Additional details** (customer note, internal note, attachments). The customer MUST be chosen before the invoice, and the invoice list offers only that customer's posted invoices. The items table loads from the invoice and its rows ARE the invoice items: `['Item','Description','Original quantity','Previously credited','Available quantity','Credit quantity','Unit','Rate ₹','Discount','Tax','Amount','']`, the credit quantity input is capped with `max={avail}` and no per-row invoice-item picker may return. Tax is inherited and has NO control on the page - the Tax summary prints the rate taken from the invoice line. `creditPostingPlan(s,note,totals)` in `src/credit-note-service.js` is the one ordered account plan (reason-driven revenue or adjustment account, one GST reversal per component, round off, then the customer receivable credit) and `issue()` must keep posting through it, so the Accounting Preview and the journal cannot drift; `plan.complete` false is exactly when the posting refuses, and the preview prints the same missing-mapping sentence. `creditNoteStatus` answers **Approved** for an approved but unposted note - never `Issued`, which is what the posting step creates - and `Approved` stays in `CREDIT_NOTE_STATUSES` and `CREDIT_TONES`. The register states Date, Credit note number, Customer, Credit note type, Original invoice, Reason, Amount, Applied amount, Remaining credit, Status, Created By and Actions, and its twelve `.creditNotes .ivInvoiceTable th:nth-child(n)` widths must total exactly 100, with the money columns 7-9 right-aligned and the text columns left-aligned. Do NOT rename the stored lifecycle (Draft, Pending Approval, Approved, Issued, Cancelled): the accounting word "Posted" is the existing Issued state, and a second status vocabulary is forbidden. Nothing on the page may post, tax or move stock on its own, and no amount may be recalculated in the UI. Keep the page on the Create Sales Order / Create Sales Invoice style: every card is an `ivCard soFormSection` with a `soSectionHead` heading, field rows are `ivFields soFieldRow`, and the page opens with the **Customer \*** card built from the shared `CustomerSearch` and the `soCustomerProfile` strip (`soCustomerMeta` Net receivable) - reuse `CustomerSearch` and `customerInitials` from `src/SalesDocumentFields.jsx` rather than writing a second customer block, and keep `chooseCustomer()` seeding the name, addresses, place of supply and GSTIN while `selectInvoice()` overwrites them from the invoice.
