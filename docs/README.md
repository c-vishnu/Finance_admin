# Wayvida Books Knowledge Base

**Last verified:** 2026-09-26

**Day Book checkpoint:** The Day Book is a read-only posted-journal report. It excludes cancelled documents by default, keeps complete balanced vouchers when an account or dimension matches, and exposes source/audit drill-down plus CSV, Excel and print/PDF export. See `docs/DAY-BOOK.md`.

**Latest sidebar utility behavior:** The theme customization is one 34px action aligned at the right of the footer (centered in compact mode), showing the action that will be applied next. Clicking a grouped icon while the 72px rail is compact expands the 250px sidebar and opens that group, so every submenu remains reachable.

**Current sidebar checkpoint:** Expanded desktop navigation is 250px; compact mode remains a 72px icon rail. The accessible Light/Dark icon pill is in the sidebar footer immediately above the always-light organisation/branch switcher. The floating collapse arrow is a high-contrast 26px control at the sidebar edge (`z-index:500`). Branch display codes are organisation-prefixed (`WY-KOC01`, `WY-TVM01`, `WY-BLR01`, `VS-TVM01`, `VS-TSR01`); stable internal branch ids are unchanged.
**Source truth:** `src/`, `tests/`, `package.json`, `vite.config.mjs`, and root domain specifications.

**Current switcher behavior:** The sidebar Company and Branch switcher now uses its dropdown directly for checkbox multi-selection of organisations and branches. The former single-choice quick switch and separate `Change / Customize` drawer are removed. Cancel discards drafts, Apply persists them through the existing working-context writer, the closed trigger tooltip lists every selected organisation and branch, and the dropdown chevron is 20px.

**Current navigation collapse behavior:** On desktop, collapsing navigation keeps a 72px icon rail rather than hiding the sidebar. Top-level destination icons, Help, theme controls and the working-context logo remain available; labels and nested lists fold away. Navigation buttons carry accessible names and hover titles. The toggle is a 28px circular edge control positioned across the sidebar boundary and moves with the rail. Mobile retains its existing drawer.

**Latest sidebar refinement:** Help Center is no longer a sidebar row. The Light/Dark segment is replaced by one 30px theme icon beside the Wayvida Books brand. The collapse control is a translucent 24px left/right arrow. The organisation switcher and its dropdown always use the light treatment, even when navigation is dark, and the dropdown's duplicated selected-context identity card is removed.

**Current theme control:** The single icon has been superseded by a compact 70x32px Light/Dark pill beside the brand, inspired by the supplied reference. Both sun and moon remain visible; the selected mode uses a blue circular thumb and the inactive icon has a separate hover state. It becomes 64px wide in the mobile drawer and is hidden in the 72px compact rail.

This folder is the portable knowledge-transfer package for Wayvida Books. It is designed for engineers and AI coding tools that have no access to earlier conversations.

## Current handover snapshot

- **Ad-hoc invoice lines, receipt role panel and the receipt row menu fixed, 2026-09-26.** A new line - and therefore an ad-hoc item - now starts tax exclusive instead of blank, which was why the engine refused to save an invoice carrying an ad-hoc line (`Choose whether each line rate includes tax.`); the line's own select still switches it to inclusive. The Payments Received role "local simulation" panel is hidden (permissions keep running through `receiptAllowed` with the default role). And the receipt detail's 3-dot menu is now the canonical portalled menu (as the invoice, sales order and credit note registers use) instead of being forced into normal flow, which is what pushed the detail header, tabs and stat cells around when it opened. Evidence: 805 tests / 757 passed / the same 48 pre-existing failures with three assertions repointed, a clean build, and a live pass showing the panel gone, the menu on `document.body` with all seven entries and the page intact. Still open: restyling the **new receipt** form onto the Create Sales Invoice page shell.
- **Credit Note create page: card items with no sideways scroll, ERP field order and a Post action, 2026-09-26.** Credit items are stacked cards (grid-based facts, one editable credit quantity capped at the available quantity, Delete per line) instead of a wide table, so nothing scrolls horizontally at 1280px or below; an invoice-backed note is quantity-only. The details card reads type, original invoice, reason, date, reference, then the auto-generated read-only number, and the footer offers Cancel, Save draft and Post credit note - Post running save -> submit -> approve -> issue through the existing engine. Still to come from the same brief: GST treatment and available credit on the customer card, phone/GSTIN/code search matching, the item-vs-amount credit method, a collapsed accounting preview, the standalone panel and the list-column list. Evidence: 805 tests / 757 passed / the same 48 pre-existing failures, a clean build and a live pass with no horizontal overflow.

- **Credit Note create page: requested field order, typeable id, Account column, 2026-09-26.** The details card reads Credit note id, Reference, Credit note date, the customer's invoice, Reason and type, under the Customer card; the id is auto-suggested in the placeholder and can still be typed. The items table's first column is Item Details with a new Account column showing the income account each line reverses. A new test reproduces the printer example end to end: 2 x 50,000 + 18% = 1,18,000, one unit returned for 59,000, customer balance down to 59,000, the journal Dr Sales Return 50,000 + Dr Output GST 9,000 against Cr Customer Receivable 59,000, revenue and GST reduced, over-crediting and tax edits refused, no stock movement - and it records that issuing clears the customer balance while applying clears the invoice. Evidence: 804 tests / 756 passed / the same 48 pre-existing failures, a clean build.

- **Credit Notes register: nine columns and a four-entry row menu, 2026-09-26.** The register now lists Date, Credit note id, Reference, Customer name, Invoice, Status, Amount and Balance, with one 3-dot menu holding Edit, Preview, Duplicate and Delete. Edit opens a stored draft (disabled otherwise), Preview opens the print preview, Duplicate opens a fresh draft copied from the note, and Delete runs the module's cancellation - reason required, a posted note reversed and its applications released, audit kept. The lifecycle steps and the refund stay on the detail page and the Applications tab, so the row menu loses no capability. Evidence: 803 tests / 755 passed / the same 48 pre-existing failures (`tests/credit-note-ui.test.mjs` 17/17 with a new row-menu test), a clean build, and a live register showing the nine headers with no overflow.

- **Credit Note mapping offers both routes, 2026-09-26.** The Account mapping dialog now carries the shared **Sales Adjustment Account** every reason uses by default *and* the three reason-specific routes - **Sales Return**, **Tax Adjustment**, **Other Adjustment** - each blank meaning "use the shared account", so a dedicated sales-return ledger is one pick away while every other reason follows the shared one. Only Sales Adjustment and Accounts Receivable are required; the inventory and stock accounts became optional, since they matter only when a goods return moves stock (previously those two blanks blocked the dialog from saving). Evidence: 802 tests / 754 passed / the same 48 pre-existing failures (`tests/credit-note.test.mjs` 13/13 and `tests/credit-note-ui.test.mjs` 16/16, one new test each), a clean build and a live dialog showing all eight fields.

- **Credit Note postings no longer require pre-configured mapping, 2026-09-26.** `creditPostingPlan()` now resolves the revenue/adjustment account through the reason's role account, the shared sales-adjustment account, the income account the adjusted invoice line used, then the configured Sales account - so an invoice-backed credit note reverses the revenue the invoice recognised and posts on a fresh install, while a dedicated sales-return ledger stays configurable. The engine still refuses when nothing resolves. The **Account mapping** dialog now opens on those effective accounts (new `creditMappingDefaults()`) instead of empty selects. Evidence: 800 tests / 752 passed / the same 48 pre-existing failures (`tests/credit-note.test.mjs` 12/12 with one new test, `tests/credit-note-ui.test.mjs` 15/15), a clean build, and a live preview resolving `4000 · Sales` with no warning.

- **Credit Note items grid and totals now use the sales-document tokens, 2026-09-26.** The items table is the shared `ivLineTable` with `soItemCell` and `ivQtyCell`, the unit is the read-only `soUnitValue` (it comes from the invoice line), the add action is the shared `soAddItem` menu, and the figures close in the sales page's `soBillingSummary` card - `soSummaryGrid` with the `INR ₹` chip, the `summary(totals)` totals block and a `soPostingNote` saying nothing posts from the page. The twelve required columns are unchanged. Evidence: 799 tests / 751 passed / the same 48 pre-existing failures (`tests/credit-note-ui.test.mjs` 15/15 with one new token test), a clean build and a live pass.

- **Credit Note create page adopts the sales-document style and flow, 2026-09-26.** The page already sat in the shared `soCreatePage` shell with the full-bleed head and pinned action bar, so the inner markup moved onto that design language: every card is an `ivCard soFormSection` with a `soSectionHead` heading, field rows use `ivFields soFieldRow`, and the collapsed tail is a `soFormSection` disclosure. It now opens with the **Customer \*** card exactly as Create Sales Order does - the shared search box plus the create-customer slot, then the `soCustomerProfile` strip with the customer type chip, the billing address line and the right-aligned **Net receivable** - because `CustomerSearch` and `customerInitials` are now exported from `src/SalesDocumentFields.jsx` for reuse. Order: Customer, Credit note details, Original invoice details, Credit items, Tax summary, Accounting preview, Additional details. Evidence: 798 tests / 750 passed / the same 48 pre-existing failures (`tests/credit-note-ui.test.mjs` 14/14 after six repointed assertions and four new ones), a clean build and a live check of the section cards, the customer search in the first card and the heading order.

- **Credit Note module follows invoice-first ERP flow, 2026-09-26.** The module keeps its engine, storage and accounting, and now renders six sections: Credit note details (number generated and read-only), Original invoice details (picker plus a read-only fact list including remaining available credit), Credit items, Tax summary, Accounting preview, and a collapsed Additional details (customer note, internal note, attachments). The customer is chosen before the invoice and only that customer's posted invoices are offered; the items table loads from the invoice with Item, Description, Original/Previously credited/Available/Credit quantity, Unit, Rate, Discount, Tax and Amount, capped at the available quantity; tax is inherited and has no control. A new pure `creditPostingPlan()` in `src/credit-note-service.js` feeds both the Accounting Preview and the posting, and `creditNoteStatus` now reports an approved-but-unposted note as **Approved** instead of **Issued**. The register lists Date, Credit note number, Customer, Credit note type, Original invoice, Reason, Amount, Applied amount, Remaining credit, Status, Created By and Actions. Evidence: 798 tests / 750 passed / the same 48 pre-existing failures (`tests/credit-note.test.mjs` 11/11 unchanged, `tests/credit-note-ui.test.mjs` 14/14 after five repointed assertions and three new tests), a clean build and a live pass reading every new section, the capped quantity, the tax summary and the balanced accounting preview.

- **The printed sheet owns its table geometry, 2026-09-26.** A document preview mounts inside a register page, so the host's table rules were reaching the printed sheet: `.itemsPage table` in `src/items.css` declares `min-width:800px` and `src/ui-system.css` pads every `th`/`td` with `16px 20px !important` plus 24px first- and last-child insets. The eight-column items table measured 800px inside a 620px paper and printed 150px past the page edge, wrapping the `HSN / SAC` and `Qty / Unit` headers. `src/document-preview-fix.css` now restates the sheet's own `min-width:0`, cell padding and edge insets under `!important`, and the eight positional widths in `src/document-preview.css` were rebalanced to `4/21/13/12/12/12/10/16` percent (still 100). No amount, template, storage key or posting path changed. Evidence: 796 tests / 748 passed / the same 48 pre-existing failures with two new passing preview tests, one repointed column assertion, and live measurements in the in-app browser showing the table ending exactly on the paper's content edge with single-line headers for the sales order, invoice and receipt sheets.

- **Accounts Payable can no longer abort the demo bootstrap, 2026-09-26.** `2000 · Accounts Payable` joined `RESERVED_POSTING_ACCOUNTS` in `src/invoice-engine.js`, the one table of system posting ledgers that `account()` provisions or repairs. The only repair for `2000` had been the private `ensureDefaultPayableAccount()` in `src/purchase-service.js`, which covers the purchase-bill and debit-note paths; `createBankingDemo()` in `src/banking-service.js` posts its demo vendor payment through `2000` from `journal()`, which names no expected type, so a stored payable that an operator had deactivated or turned into a heading threw inside `bootstrapDemoData()` before it wrote `wayvida-demo-data-version` - which is why a browser reported **Wayvida Books could not start.** on that load and every load after it. No storage key, posting rule, report balance or screen changed. Evidence: a new test in `tests/demo-data.test.mjs` that is red before the fix with the exact error and the `post` / `createBankingDemo` / `bootstrapDemoData` stack, and green after; `node --test "tests/*.test.mjs"` at 794 tests / 746 passed / the same 48 pre-existing failures; a clean build; a passing docs check; and a live reload that renders the dashboard instead of the banner.
- **Sales order and invoice audit fixes, 2026-09-25.** An audit of both modules drove a control and correctness pass: `command(state, action, payload, ctx)` now enforces `invoiceAllowed(role, action)`, posting requires `Pending Approval`, and an invoice must carry its own organisation and branch; an invoice is dated when it is raised rather than when the order was taken; the customer GSTIN, GST treatment and state are frozen on the document, which also makes the printed invoice carry the recipient GSTIN; conversion requires a Confirmed order and cancelling an order is blocked while a live invoice exists; the invoice and order audit trails record the acting role and the status transition; the next invoice number scans for the first free number; the General Ledger no longer falls back to a fabricated sample ledger or account list and derives its filters from the data; and the unreachable shipment filter is gone. Evidence: full sweep 776 tests / 728 passed / the same 48 pre-existing failures, build clean at 6,982 modules, and direct engine calls proving every gate. Still open: inventory relief and COGS, separate output tax accounts, reverse charge and sales TDS/TCS, incomplete draft saves, a delivery document, ad-hoc returns to stock, a per-organisation GST registration.
- Repository branch: `main`; current committed baseline: `9052841 Rework the Item Details header: merge the tab row into the item panel`, already pushed to `origin/main`.
- The Items multi-organisation master, the register actions, the field-first Inventory Adjustment create page and the merged Item Details header panel remain the committed checkpoints on top of `7b5ae78`. Two shell changes of 2026-09-22 are **not committed yet**: the navigation sidebar toggle (`src/App.jsx`, `src/navigation.css`) and the Company and Branch switcher (`src/HeaderOrgSelectors.jsx`, `src/header-org-selectors.css` and the new `src/org-switcher.js`), with the new `tests/navigation-toggle-ui.test.mjs` and `tests/org-switcher-ui.test.mjs`, so the tree is no longer clean. Inspect `git status` before editing.
- Latest verified behavior: the dashboard shell grew two controls on 2026-09-22. One header button collapses the sidebar (`localStorage['finance-erp-nav-collapsed']`, published as `navCollapsed` on `.app` and `document.body`, scoped to `min-width:701px` so the narrow-screen drawer is untouched). The Company and Branch switcher sits at the foot of the sidebar, below the Help Center and Dark navigation footer, on a light blue-grey card (`#EEF3FB` light, `#22304A` dark): `WorkingContextSwitcher` renders it as the last child of `nav.erpNavigation`, and its panel flips above the trigger because there is no room below it, and its panel and the `Change / Customize` drawer are portalled to `document.body` because the sidebar clips its overflow. It switches between two companies - Wayvida Learning (Kochi, Trivandrum, Bengaluru) and Viskool (Trivandrum, Thrissur) - each shown with its own logo from `src/demo-organisations.js`; the quick switch selects one organisation or branch while the drawer keeps the multi-selection, and `src/org-switcher.js` is the single writer of the five working-context keys and the two change events. The old 46px working-context bar is gone, so `main` starts directly under the 59px header and the portalled workspaces start at 59px. Sections now share one spacing scale (`--wvb-space-*` with the sheet's `#F4F7F9` surface, `#DFE3E6` line and 12px radius), an organisation mark is a fixed-size logo tile carrying its own optical scale, and every register, data grid and list toolbar measures its search the same: 300px beside the Filters button, stated once at the end of `src/register-head.css`, with the narrow-screen full-width search untouched. The sidebar foot is one refined block now - the Help Center leaf, a labelled Light / Dark segment (`role="radiogroup"` over two `role="radio"` options with sun and moon icons, the active one on a white or `#31415C` thumb) and the Company and Branch switcher card, all on the same 14px gutter with a 10px vertical rhythm. The card is a 14px raised surface with a top highlight, a 12px gap beneath it, a chevron chip that is always visible and rotates when the panel is open and a panel whose header strip carries the working-context badge, whose rows are separated by an inset hairline and whose branch list hangs off a 2px tree rule. The panel opens at `z-index:300` with a two-layer shadow so it reads in both themes, and it flips to whichever side has more room with a 240px height floor, so it never opens partly off-screen. Only display data changed when the companies were renamed: the storage ids and organisation codes are keys and did not move. The Items multi-organisation master, the register actions, the field-first Inventory Adjustment create page and the merged Item Details header panel remain the preceding checkpoints.
- Latest validation: `tests/navigation-toggle-ui.test.mjs` passes **2/2**, `tests/org-switcher-ui.test.mjs` **3/3** and `tests/header-context.test.mjs` **3/3**; the full sweep was executed one file per process (`node tests/<file>.test.mjs` for each of the 94 files, because this sandbox refuses the Node test runner's per-file child process with `spawn EPERM`) at **697 tests / 686 passing / the same 11 pre-existing failures** before the switcher change and **700 tests / 689 passing / the same 11 pre-existing failures** after it. The Vite production build passes (6,976 modules; existing large-chunk advisory only), `node scripts/validate-knowledge-docs.mjs` passes over the 15 required files, and both shell states plus the switcher panel, its switch, its reload persistence, its Escape and outside-click dismissal and its 503px behaviour were measured on the painted page in the in-app browser against the dev server on port 4001, with the viewport override reset and the working context restored afterwards.
- Do not treat `.codex-item.patch`, `dist/`, browser storage, screenshots or machine-specific paths as product source. Inspect `git status` before editing.

The fastest clean-context route is: read `AGENTS.md`, then this file, then `TRANSFER-TO-ANOTHER-AI.md`, `WAYVIDA-BOOKS-HANDBOOK.md`, `AI-HANDOFF-PROMPT.md`, `project-manifest.json`, and the source/tests for the requested module.

## Status legend

| Status | Meaning |
|---|---|
| Implemented | Present in source and exercised by code or tests. |
| Prototype only | Interactive browser implementation without production backend guarantees. |
| Partially implemented | Some UI or domain code exists, but routing, integration, tests, or production controls are incomplete. |
| Planned | Product requirement or recommended production architecture; not current behavior. |

## Recommended reading order

1. [Transfer to Another AI](TRANSFER-TO-ANOTHER-AI.md)
2. [Project Overview](PROJECT-OVERVIEW.md)
3. [Technical Architecture](TECHNICAL-ARCHITECTURE.md)
4. [Accounting Domain](ACCOUNTING-DOMAIN.md)
5. [Modules](MODULES.md)
6. [Data Model](DATA-MODEL.md)
7. [Developer Guide](DEVELOPER-GUIDE.md)
8. [Security and Controls](SECURITY-AND-CONTROLS.md)
9. [Testing and QA](TESTING-AND-QA.md)
10. [Known Gaps and Roadmap](KNOWN-GAPS-AND-ROADMAP.md)
11. [UI Design System](UI-DESIGN-SYSTEM.md)
12. [Glossary](GLOSSARY.md)
13. [Period Locking Specification](PERIOD-LOCKING-SPECIFICATION.md)

For a single-file transfer, use [Wayvida Books Handbook](WAYVIDA-BOOKS-HANDBOOK.md). For a new AI session, paste [AI Handoff Prompt](AI-HANDOFF-PROMPT.md) and attach this folder or repository. Machine-readable inventory is in [project-manifest.json](project-manifest.json).

## Governing rules

- Source and tests override prose when they conflict.
- Existing root specifications describe intent but may exceed current implementation.
- Monetary values in accounting engines are integer paise.
- Browser storage is prototype persistence, not a production database.
- Operational documents create journals automatically; normal users do not enter accounting lines.
- Posted records are corrected through reversal or adjustment, not destructive editing.
- Do not claim role selectors or local UI gates provide authentication or authorization.

## Feature checkpoint maintenance

The knowledge package is a required part of every completed feature change. The implementer must update the relevant modular document and `project-manifest.json`, and update the standalone handbook whenever essential cross-project knowledge changes. Record actual test/build results in `TESTING-AND-QA.md` and refresh `Last verified` in materially changed documents.

```text
npm run docs:check
```

This checkpoint validator checks the required file set, strict manifest parsing and sections, verification metadata, and local Markdown links. It detects structural drift; the implementer remains responsible for semantic accuracy.

## Existing supporting specifications

The root documents remain authoritative supporting material for their topics: `AGENTS.md`, `ACCOUNTING-ARCHITECTURE-REVIEW.md`, `COA-EXPERIENCE.md`, `INVOICE-WORKFLOW.md`, `CREDIT-NOTES.md`, `RECEIPTS.md`, `RECEIPT-ENHANCEMENT-REVIEW.md`, `DAYBOOK.md`, and `DOCUMENT-TEMPLATES.md`.


