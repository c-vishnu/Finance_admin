// Supports existing drafts while persisting explicit component rates for new edits.
export function lineTaxes(line,intra){return line.taxes?{...line.taxes}:{cgst:intra?Number(line.tax||0)/2:0,sgst:intra?Number(line.tax||0)/2:0,igst:intra?0:Number(line.tax||0),cess:Number(line.cess||0)}}
export function calculateTax(line,intra,taxable){const rates=lineTaxes(line,intra);for(const k of ['cgst','sgst','igst','cess']){if(rates[k]===''||!Number.isFinite(Number(rates[k]))||Number(rates[k])<0||Number(rates[k])>100)throw Error('Enter valid tax component rates between 0 and 100.');rates[k]=Number(rates[k])}if(intra&&(rates.igst||rates.cgst!==rates.sgst))throw Error('Intra-state supplies require equal CGST and SGST rates, without IGST.');if(!intra&&(rates.cgst||rates.sgst))throw Error('Inter-state supplies require IGST, without CGST or SGST. Review Tax selection.');return {cgst:Math.round(taxable*rates.cgst/100),sgst:Math.round(taxable*rates.sgst/100),igst:Math.round(taxable*rates.igst/100),cessAmount:Math.round(taxable*rates.cess/100)}}
